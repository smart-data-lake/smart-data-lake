connections {
  sparkConnectCon {
    type = SparkConnectConnection
    url = "sc://localhost:15002"
  }
}

dataObjects {
  testTable {
    type = SparkConnectTableDataObject
    connectionId = sparkConnectCon
    table = {
      db = default
      name = sdlb_test_table
    }
  }
}
