{
  "type" : "object",
  "$schema" : "http://json-schema.org/draft-07/schema#",
  "version" : "3.0.0-SNAPSHOT",
  "id" : "sdl-schema-3.0.0-SNAPSHOT.json#",
  "definitions" : {
    "ExecutionMode" : {
      "CustomPartitionMode" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "CustomPartitionMode"
          },
          "className" : {
            "type" : "string",
            "description" : "class name implementing trait`CustomPartitionModeLogic`"
          },
          "alternativeOutputId" : {
            "type" : "string",
            "description" : "optional alternative outputId of DataObject later in the DAG. This replaces the mainOutputId.  \nIt can be used to ensure processing all partitions over multiple actions in case of errors."
          },
          "options" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "Options specified in the configuration for this execution mode"
          }
        },
        "title" : "CustomPartitionMode",
        "required" : [ "type", "className" ],
        "additionalProperties" : false,
        "description" : "Execution mode to create custom partition execution mode logic.  \nImplement trait `CustomPartitionModeLogic` in a class on the classpath and reference it by `className` .  \nThe implementation receives the main input & output DataObject together with the given partition values and\nreturns the partition values to process as `Seq[Map[String,String]]` .  \nUse this if the partition selection of `PartitionDiffMode` is not sufficient, e.g. if you need to reprocess\nthe last n partitions or select partitions by an external trigger table.  \nBoth main input and main output DataObject must support partitions (CanHandlePartitions), otherwise the action fails. Example:\n```\nactions = {\n  copy-airports {\n    type = CopyAction\n    inputId = stg-airports\n    outputId = int-airports\n    executionMode = {\n      type = CustomPartitionMode\n      className = \"com.company.dataPipeline.SelectLastDaysPartitionMode\"\n      options = { nbOfDays = \"3\" }\n    }\n  }\n}\n```\n\n\nSEE: `PartitionDiffMode` for the built-in partition comparison, `CustomMode` for full control over the `ExecutionModeResult`."
      },
      "DataFrameIncrementalMode" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "DataFrameIncrementalMode"
          },
          "compareCol" : {
            "type" : "string",
            "description" : "a comparable column name existing in mainInput and mainOutput used to identify the delta. Column content should be bigger for newer records."
          },
          "alternativeOutputId" : {
            "type" : "string",
            "description" : "optional alternative outputId of DataObject later in the DAG. This replaces the mainOutputId.  \nIt can be used to ensure processing all partitions over multiple actions in case of errors."
          },
          "applyCondition" : {
            "type" : "object",
            "properties" : {
              "expression" : {
                "type" : "string",
                "description" : "Condition formulated as Spark SQL. The attributes available are dependent on the context."
              },
              "description" : {
                "type" : "string",
                "description" : "A textual description of the condition to be shown in error messages."
              }
            },
            "title" : "Condition",
            "required" : [ "expression" ],
            "additionalProperties" : false,
            "description" : "Condition to decide if execution mode should be applied or not. Define a spark sql expression working with attributes of`DefaultExecutionModeExpressionData` returning a boolean.  \nDefault is to apply the execution mode."
          },
          "propagateFilter" : {
            "type" : "boolean",
            "description" : "If true the incremental filter is passed on to following actions, where it is applied to\nevery DataObject having the compareCol column. Set this only if the increment written by\nthis action can still be identified by the filter, as the filter is then assumed to be\napplied already. Default is false, so the filter is only used to read the increment for\nthis action."
          }
        },
        "title" : "DataFrameIncrementalMode",
        "required" : [ "type", "compareCol" ],
        "additionalProperties" : false,
        "description" : "Compares max entry in \"compare column\" between mainOutput and mainInput and incrementally loads the delta.  \nThis mode works only with SparkSubFeeds. By default the filter is not propagated to following actions,\nsee `propagateFilter` .\n\nPick this mode for non-partitioned sources which have a monotonically increasing column, e.g. a technical\ntimestamp or a sequence-based id: the mode reads `max(compareCol)` from the output and adds a filter `compareCol > <lastValue>` on the input. If the output is still empty all records are selected.  \nIf input and output are on the same value, a NoDataToProcessWarning is thrown and following actions are skipped. `compareCol` must exist in both main input and main output and be of a sortable type. Example:\n```\nactions = {\n  copy-airports {\n    type = CopyAction\n    inputId = stg-airports\n    outputId = int-airports\n    executionMode = {\n      type = DataFrameIncrementalMode\n      compareCol = \"dl_ts_captured\"\n      applyCondition = { expression = \"isStartNode\" }\n    }\n  }\n}\n```\n\n\nSEE: `PartitionDiffMode` if the input DataObject is partitioned, `DataObjectStateIncrementalMode` if the\ninput DataObject can remember its own state."
      },
      "DataObjectStateIncrementalMode" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "DataObjectStateIncrementalMode"
          }
        },
        "title" : "DataObjectStateIncrementalMode",
        "required" : [ "type" ],
        "additionalProperties" : false,
        "description" : "An execution mode for incremental processing by remembering DataObjects state from last increment.\n\nIn contrast to `DataFrameIncrementalMode` the state is not derived by comparing input and output, but kept by the\ninput DataObject itself and persisted in the SDLB run state. Choose this mode for sources which can create an\nincrement on their own, e.g. Airbyte, OData, Debezium CDC or JDBC sources with an incremental cursor.  \nIt has no configuration attributes. Example:\n```\nactions = {\n  copy-airports {\n    type = CopyAction\n    inputId = ext-airports\n    outputId = stg-airports\n    executionMode = { type = DataObjectStateIncrementalMode }\n  }\n}\n```\n\n\nNOTE: SmartDataLakeBuilder must be started with a state path (command line option `--state-path` ), otherwise the\nmode fails in the init phase. At least one input DataObject must implement `CanCreateIncrementalOutput` .\n\nSEE: `DataFrameIncrementalMode`, [[PartitionDiffMode]]"
      },
      "FailIfNoPartitionValuesMode" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "FailIfNoPartitionValuesMode"
          }
        },
        "title" : "FailIfNoPartitionValuesMode",
        "required" : [ "type" ],
        "additionalProperties" : false,
        "description" : "An execution mode which just validates that partition values are given.  \nNote: For start nodes of the DAG partition values can be defined by command line, for subsequent nodes partition values are passed on from previous nodes.\n\nUse it as a guard on actions which must never process a full load by accident: if the main input SubFeed has no\npartition values, the action fails with an IllegalStateException instead of reading everything.  \nIt selects no data itself and has no configuration attributes. In dry-run mode the check is skipped. Example:\n```\ndataObjects = {\n  int-departures {\n    type = ParquetFileDataObject\n    path = \"~{env.basedir}/int_departures\"\n    partitions = [dt]\n  }\n}\nactions = {\n  copy-departures {\n    type = CopyAction\n    inputId = int-departures\n    outputId = btl-departures\n    executionMode = { type = FailIfNoPartitionValuesMode }\n  }\n}\n```\n\n\nSEE: `PartitionDiffMode` to select the missing partitions automatically instead of failing."
      },
      "FileIncrementalMoveMode" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "FileIncrementalMoveMode"
          },
          "archivePath" : {
            "type" : "string",
            "description" : "if an archive directory is configured, files are moved into that directory instead of deleted, preserving partition layout. See also`archiveInsidePartition` option.  \nIf this is a relative path, e.g. \"_archive\", it is appended after the path of the DataObject.  \nIf this is an absolute path it replaces the path of the DataObject."
          },
          "archiveInsidePartition" : {
            "type" : "boolean",
            "description" : "By default archiveDir moves files for partitioned DataObjects as according to the following directory layout:\n{{{<dataObject-root>/<archivePath>/<partitionDirs>/<filename>}}}This is good because the partition does no longer exist afterwards.  \nBy setting archiveInsidePartition=true files are moved inside the partition directory as follows:\n{{{<dataObject-root>/<partitionDirs>/<archivePath>/<filename>}}}\nThis is good to keep the file in the original directory structure.  \nNote that archivePath needs to be relative if setting archiveInsidePartition=true."
          }
        },
        "title" : "FileIncrementalMoveMode",
        "required" : [ "type" ],
        "additionalProperties" : false,
        "description" : "Execution mode to incrementally process file-based DataObjects, e.g. FileRefDataObjects and SparkFileDataObjects.  \nFor FileRefDataObjects:\n- All existing files in the input DataObject are processed and removed (deleted or archived) after processing\n- Input partition values are applied to search for files and also used as output partition values\nFor SparkFileDataObjects:\n- Files processed are read from the DataFrames execution plan and removed (deleted or archived) after processing.  \nNote that is only correct if no additional filters are applied in the DataFrame.  \nA better implementation would be to observe files by a custom metric. Unfortunately there is a problem in Spark with that, see also `CollectSetDeterministic` - Partition values preserved.\n\nUse this mode for \"process once and clean up\" file pipelines, e.g. an inbound directory which is emptied by every\nrun. No state needs to be persisted, because the presence of a file is the state. If there is no file to process,\na NoDataToProcessWarning is raised and the following actions are skipped. Example:\n```\nactions = {\n  transfer-airports {\n    type = FileTransferAction\n    inputId = ext-airports\n    outputId = stg-airports\n    executionMode = {\n      type = FileIncrementalMoveMode\n      archivePath = \"_archive\"\n    }\n  }\n}\n```\n\n\nNOTE: Files are deleted after processing unless `archivePath` is set. Make sure the action really succeeded before\nrelying on this mode, as the source files are gone afterwards."
      },
      "KafkaStateIncrementalMode" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "KafkaStateIncrementalMode"
          },
          "delayedMaxTimestampExpr" : {
            "type" : "string",
            "description" : "Optional expression to define a delay to read latest offsets from Kafka. The expression has to return a timestamp which is used to select ending offsets to read from Kafka.  \nDefine a spark sql expression working with the attributes of`DefaultExpressionData` returning a timestamp.  \nDefault is to read latest offsets existing in Kafka."
          }
        },
        "title" : "KafkaStateIncrementalMode",
        "required" : [ "type" ],
        "additionalProperties" : false,
        "description" : "A special incremental execution mode for Kafka Inputs, remembering the state from the last increment through the Kafka Consumer, e.g. committed offsets.\n\nThe state is not kept in the SDLB run state but as committed consumer group offsets in Kafka itself, so no state\nstore is needed and the Action can be run without `--state-path` . Offsets are only committed after the Action\nsucceeded, so a failed run is re-read on the next attempt. Pick this over `io.smartdatalake.workflow.action.executionMode.DataFrameIncrementalMode` whenever the input is a `io.smartdatalake.workflow.dataobject.KafkaTopicDataObject`, as it avoids reading a comparison value from the output. Example:\n```\nactions = {\n  copy-departures {\n    type = CopyAction\n    inputId = ext-departures-kafka\n    outputId = stg-departures\n    executionMode = {\n      type = KafkaStateIncrementalMode\n      delayedMaxTimestampExpr = \"timestamp_seconds(unix_seconds(now()) - 10)\"\n    }\n  }\n}\n```\n\n\nNOTE: At least one input DataObject of the Action must be a `io.smartdatalake.workflow.dataobject.KafkaTopicDataObject`, otherwise initialization fails.  \nThe input KafkaTopicDataObject must set the `groupIdPrefix` option, as it is used as prefix for the Kafka\nconsumer group id in which the offsets are committed."
      },
      "PartitionDiffMode" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "PartitionDiffMode"
          },
          "partitionColNb" : {
            "type" : "integer",
            "description" : "optional number of partition columns to use as a common 'init'."
          },
          "alternativeOutputId" : {
            "type" : "string",
            "description" : "optional alternative outputId of DataObject later in the DAG. This replaces the mainOutputId.  \nIt can be used to ensure processing all partitions over multiple actions in case of errors."
          },
          "nbOfPartitionValuesPerRun" : {
            "type" : "integer",
            "description" : "optional restriction of the number of partition values per run."
          },
          "applyCondition" : {
            "type" : "string",
            "description" : "Condition to decide if execution mode should be applied or not. Define a spark sql expression working with attributes of`DefaultExecutionModeExpressionData` returning a boolean.  \nDefault is to apply the execution mode if given partition values (partition values from command line or passed from previous action) are empty."
          },
          "failCondition" : {
            "type" : "string",
            "description" : "optional single condition to fail application of execution mode if true. Define as spark sql expression working with attributes of[[PartitionDiffModeExpressionData]]returning a boolean.  \nThis is a shortcut for`failConditions`with one entry and without description. It is evaluated in addition to`failConditions` ."
          },
          "failConditions" : {
            "type" : "array",
            "items" : {
              "type" : "object",
              "properties" : {
                "expression" : {
                  "type" : "string",
                  "description" : "Condition formulated as Spark SQL. The attributes available are dependent on the context."
                },
                "description" : {
                  "type" : "string",
                  "description" : "A textual description of the condition to be shown in error messages."
                }
              },
              "title" : "Condition",
              "required" : [ "expression" ],
              "additionalProperties" : false,
              "description" : "Definition of a Spark SQL condition with description.  \nThis is used for example to define failConditions of `PartitionDiffMode`."
            },
            "description" : "List of conditions to fail application of execution mode if true. Define as spark sql expressions working with attributes of`PartitionDiffModeExpressionData` returning a boolean.  \nDefault is that the application of the PartitionDiffMode does not fail the action. If there is no data to process, the following actions are skipped.  \nMultiple conditions are evaluated individually and every condition may fail the execution mode (or-logic)"
          },
          "selectExpression" : {
            "type" : "string",
            "description" : "optional expression to define or refine the list of selected output partitions. Define a spark sql expression working with the attributes of`PartitionDiffModeExpressionData` returning a list<map<string,string>>.  \nDefault is to return the originally selected output partitions found in attribute selectedOutputPartitionValues."
          },
          "applyPartitionValuesTransform" : {
            "type" : "boolean",
            "description" : "If true applies the partition values transform of custom transformations on input partition values before comparison with output partition values.  \nIf enabled input and output partition columns can be different. Default is to disable the transformation of partition values."
          },
          "selectAdditionalInputExpression" : {
            "type" : "string",
            "description" : "optional expression to refine the list of selected input partitions. Note that primarily output partitions are selected by PartitionDiffMode.  \nThe selected output partitions are then transformed back to the input partitions needed to create the selected output partitions. This is one-to-one except if applyPartitionValuesTransform=true.  \nAnd sometimes there is a need for additional input data to create the output partitions, e.g. if you aggregate a window of 7 days for every day.  \nYou can customize selected input partitions by defining a spark sql expression working with the attributes of`PartitionDiffModeExpressionData` returning a list<map<string,string>>.  \nDefault is to return the originally selected input partitions found in attribute selectedInputPartitionValues."
          }
        },
        "title" : "PartitionDiffMode",
        "required" : [ "type" ],
        "additionalProperties" : false,
        "description" : "Partition difference execution mode lists partitions on mainInput & mainOutput DataObject and starts loading all missing partitions.  \nPartition columns to be used for comparison need to be a common 'init' of input and output partition columns.  \nThis mode needs mainInput/Output DataObjects which CanHandlePartitions to list partitions.  \nPartition values are passed to following actions for partition columns which they have in common.\n\nThis is the standard mode for incremental loading of partitioned DataObjects: it needs no state to be persisted,\nas the partitions still missing in the output define the increment. By default it is only applied if no partition\nvalues are given on the command line or by the preceding action. If no partition is missing, a\nNoDataToProcessWarning is raised and the following actions are skipped. Example:\n```\nactions = {\n  copy-departures {\n    type = CopyAction\n    inputId = stg-departures\n    outputId = int-departures\n    executionMode = {\n      type = PartitionDiffMode\n      partitionColNb = 1\n      nbOfPartitionValuesPerRun = 10\n    }\n  }\n}\n```\n\n\nSEE: `DataFrameIncrementalMode` for non-partitioned DataObjects, `CustomPartitionMode` for custom partition\nselection logic."
      },
      "ProcessAllMode" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "ProcessAllMode"
          }
        },
        "title" : "ProcessAllMode",
        "required" : [ "type" ],
        "additionalProperties" : false,
        "description" : "An execution mode which forces processing all data from its inputs.\n\nIt resets the partition values and data filter received from the preceding action, so the action always reads the\nfull content of its inputs. Typical use case is an action which needs the complete history of a predecessor's output\n(e.g. a full aggregation or a deduplication), combined with an `executionCondition` of \"true\" to also run it when\nthe predecessor was skipped because it had no new data. It has no configuration attributes. Example:\n```\nactions = {\n  agg-departures {\n    type = CustomDataFrameAction\n    inputIds = [int-departures]\n    outputIds = [btl-departures-agg]\n    transformers = [{\n      type = SQLDfsTransformer\n      code = {\n        btl-departures-agg = \"select estdepartureairport, count(*) as cnt from %{inputViewName_int-departures} group by estdepartureairport\"\n      }\n    }]\n    executionMode = { type = ProcessAllMode }\n    executionCondition = { expression = \"true\" }\n  }\n}\n```\n"
      },
      "SparkStreamingMode" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "SparkStreamingMode"
          },
          "checkpointLocation" : {
            "type" : "string",
            "description" : "location for checkpoints of streaming query to keep state"
          },
          "triggerType" : {
            "type" : "string",
            "description" : "define execution interval of Spark streaming query. Possible values are Once (default),\nProcessingTime & Continuous. See`Trigger` for details. Note that this is only applied if SDL\nis executed in streaming mode. If SDL is executed in normal mode, TriggerType=Once is used\nalways. If triggerType=Once, the action is repeated with Trigger.Once in SDL streaming mode."
          },
          "triggerTime" : {
            "type" : "string",
            "description" : "Time as String in triggerType = ProcessingTime or Continuous. See`Trigger` for details."
          },
          "inputOptions" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "additional option to apply when reading streaming source. This overwrites options set by the\nDataObjects."
          },
          "outputOptions" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "additional option to apply when writing to streaming sink. This overwrites options set by the\nDataObjects."
          },
          "outputMode" : {
            "type" : "string",
            "description" : "output mode of the streaming query. Possible values are append (default), complete and update.  \nSee`OutputMode` for details. Note that complete and update are only supported by sinks and\nqueries which can handle them, e.g. an aggregation query writing to a Delta Lake table.",
            "enum" : [ "Append", "Complete", "Update" ]
          }
        },
        "title" : "SparkStreamingMode",
        "required" : [ "type", "checkpointLocation" ],
        "additionalProperties" : false,
        "description" : "Spark streaming execution mode uses Spark Structured Streaming to incrementally execute data\nloads and keep track of processed data. This mode needs a DataObject implementing\nCanCreateStreamingDataFrame and works only with SparkSubFeeds. This mode can be executed\nsynchronously in the DAG by using triggerType=Once, or asynchronously as Streaming Query with\ntriggerType = ProcessingTime or Continuous. Example:\n```\nactions = {\n  copy-departures {\n    type = CopyAction\n    inputId = stg-departures\n    outputId = int-departures\n    executionMode = {\n      type = SparkStreamingMode\n      checkpointLocation = \"~{env.basedir}/checkpoints/copy-departures\"\n      triggerType = ProcessingTime\n      triggerTime = \"10 seconds\"\n    }\n  }\n}\n```\n\n\nNOTE: The checkpoint location holds the streaming state. Deleting it makes the query restart from the source's\nconfigured starting position (which for some sources, e.g. Kafka with startingOffsets=latest, skips data\ninstead of reprocessing it); two actions must never share the same location."
      }
    },
    "ValidationRule" : {
      "RowLevelValidationRule" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "RowLevelValidationRule"
          },
          "condition" : {
            "type" : "string",
            "description" : "an SQL expression defining the condition to be tested. The condition should return true if the condition is satisfied."
          },
          "errorMsg" : {
            "type" : "string",
            "description" : "Optional error msg to be create if the condition fails. Default is to use a text representation of the condition."
          }
        },
        "title" : "RowLevelValidationRule",
        "required" : [ "type", "condition" ],
        "additionalProperties" : false,
        "description" : "Definition for a row level data validation rule.  \nThe condition is evaluated for every row on its own, so it can only reference columns of the current\nrecord. Aggregations over the whole DataFrame are not possible, use an Expectation on the output\nDataObject for that. Example:\n```\nactions = {\n  validate-ratings {\n    type = CopyAction\n    inputId = stg-ratings\n    outputId = int-ratings\n    transformers = [{\n      type = DataValidationTransformer\n      rules = [\n        { type = RowLevelValidationRule, condition = \"rating between 1 and 5\", errorMsg = \"rating must be between 1 and 5\" }\n      ]\n    }]\n  }\n}\n```\n"
      }
    },
    "Expectation" : {
      "AvgCountPerPartitionExpectation" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "AvgCountPerPartitionExpectation"
          },
          "name" : {
            "type" : "string",
            "description" : "The name of the expectation"
          },
          "description" : {
            "type" : "string",
            "description" : "Optional detailed description of the expectation"
          },
          "expectation" : {
            "type" : "string",
            "description" : "Optional SQL comparison operator and literal to define expected value for validation, e.g. '> 100000\".  \nIf no expectation is defined, the result value is just recorded in metrics."
          },
          "failedSeverity" : {
            "type" : "string",
            "description" : "Severity if expectation fails - can be Error (default) or Warn.  \nIf set to Error, execution will fail, otherwise there will be just a warning logged.",
            "enum" : [ "Warn", "Error" ]
          }
        },
        "title" : "AvgCountPerPartitionExpectation",
        "required" : [ "type", "name" ],
        "additionalProperties" : false,
        "description" : "Definition of an expectation based on the average number of records per partitions.\n\nNote that the scope for evaluating this expectation is fixed to Job.\n\nThe result is the `count` of the current job divided by the number of partition values processed. Note that it is\nreported as metric `countAvgPerPartition` and not under the name of the expectation. Use it instead of `CountExpectation` when a job may process a varying number of partitions, so that a fixed total count would\nproduce false alarms. Example:\n```\ndataObjects = {\n  int-departures {\n    type = ParquetFileDataObject\n    path = \"~{env.basedir}/int_departures\"\n    partitions = [dt]\n    expectations = [{\n      type = AvgCountPerPartitionExpectation\n      name = \"avgCountPerDay\"\n      expectation = \"> 100000\"\n    }]\n  }\n}\n```\n\n\nNOTE: The expectation can only be evaluated if the job processes partition values. If there are none, a warning\nis logged and the expectation is skipped.\n\nSEE: `CountExpectation`"
      },
      "CountExpectation" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "CountExpectation"
          },
          "name" : {
            "type" : "string",
            "description" : "The name of the expectation"
          },
          "description" : {
            "type" : "string",
            "description" : "Optional detailed description of the expectation"
          },
          "expectation" : {
            "type" : "string",
            "description" : "Optional SQL comparison operator and literal to define expected value for validation, e.g. '> 100000\".  \nIf no expectation is defined, the result value is is just recorded in metrics."
          },
          "scope" : {
            "type" : "string",
            "description" : "The aggregation scope used to evaluate the aggregate expression.  \nDefault is 'Job', which evaluates the records transformed by the current job. This is implemented without big performance impacts on Spark.  \nOther options are 'All' and 'JobPartition', which are implemented by reading the output data again.",
            "enum" : [ "All", "JobPartition", "Job" ]
          },
          "failedSeverity" : {
            "type" : "string",
            "description" : "Severity if expectation fails - can be Error (default) or Warn.  \nIf set to Error, execution will fail, otherwise there will be just a warning logged.",
            "enum" : [ "Warn", "Error" ]
          }
        },
        "title" : "CountExpectation",
        "required" : [ "type" ],
        "additionalProperties" : false,
        "description" : "Definition of an expectation based on the number of records.\n\nThis is the cheapest expectation: for the default `name = \"count\"` and `scope = Job` the record count is measured\nanyway, so no additional aggregation is added. Use it as a basic sanity check that an Action did not produce an empty\nor unexpectedly small result. Define multiple CountExpectations with different names to check several scopes at once,\ne.g. the records of the current job and the total number of records in the table. Example:\n```\ndataObjects = {\n  int-departures {\n    type = ParquetFileDataObject\n    path = \"~{env.basedir}/int_departures\"\n    expectations = [\n      { type = CountExpectation, expectation = \">= 1\" },\n      { type = CountExpectation, name = \"countAll\", expectation = \"> 100000\", scope = All }\n    ]\n  }\n}\n```\n\n\nNOTE: If several CountExpectations are configured on the same DataObject, all but one must be given a distinct `name` ,\nas the name is used as metric name.\n\nSEE: `AvgCountPerPartitionExpectation` if the number of partitions processed varies between runs."
      },
      "SQLExpectation" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "SQLExpectation"
          },
          "name" : {
            "type" : "string",
            "description" : "The name of the expectation"
          },
          "description" : {
            "type" : "string",
            "description" : "Optional detailed description of the expectation"
          },
          "aggExpression" : {
            "type" : "string",
            "description" : "SQL aggregate expression to evaluate on dataset, e.g. count(*)."
          },
          "expectation" : {
            "type" : "string",
            "description" : "Optional SQL comparison operator and literal to define expected value for validation, e.g. '= 0\".  \nTogether with the result of the aggExpression evaluation on the left side, it forms the condition to validate the expectation.  \nIf no expectation is defined, the aggExpression evaluation result is just recorded in metrics."
          },
          "scope" : {
            "type" : "string",
            "description" : "The aggregation scope used to evaluate the aggregate expression.  \nDefault is 'Job', which evaluates the records transformed by the current job. This is implemented without big performance impacts on Spark.  \nOther options are 'All' and 'JobPartition', which are implemented by reading the output data again.",
            "enum" : [ "All", "JobPartition", "Job" ]
          },
          "failedSeverity" : {
            "type" : "string",
            "description" : "Severity if expectation fails - can be Error (default) or Warn.  \nIf set to Error, execution will fail, otherwise there will be just a warning logged.",
            "enum" : [ "Warn", "Error" ]
          }
        },
        "title" : "SQLExpectation",
        "required" : [ "type", "name", "aggExpression" ],
        "additionalProperties" : false,
        "description" : "Definition of expectation based on a SQL aggregate expression to evaluate on dataset-level.\n\nThis is the most generic `Expectation` : any SQL aggregate function supported by the calculation engine can be used.  \nIts result is recorded as a metric named after the expectation, and validated against `expectation` if that is defined.  \nUse it when none of the specialized expectations like `SQLFractionExpectation` or `UniqueKeyExpectation` fits.  \nIf the aggregate cannot be expressed as a single expression, use `SQLQueryExpectation` instead. Example:\n```\ndataObjects = {\n  int-departures {\n    type = ParquetFileDataObject\n    path = \"~{env.basedir}/int_departures\"\n    expectations = [{\n      type = SQLExpectation\n      name = \"avgRatingGt1\"\n      description = \"avg rating should be bigger than 1\"\n      aggExpression = \"avg(rating)\"\n      expectation = \"> 1\"\n    }]\n  }\n}\n```\n\n\nNOTE: The expression is evaluated as a DataFrame observation for the default `scope = Job` , so it must be a valid\naggregate expression over the columns of the DataObject.\n\nSEE: `SQLFractionExpectation`\n\nSEE: `SQLQueryExpectation`"
      },
      "SQLFractionExpectation" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "SQLFractionExpectation"
          },
          "name" : {
            "type" : "string",
            "description" : "The name of the expectation"
          },
          "description" : {
            "type" : "string",
            "description" : "Optional detailed description of the expectation"
          },
          "countConditionExpression" : {
            "type" : "string",
            "description" : "SQL expression returning a boolean to match the rows to count for the fraction."
          },
          "globalConditionExpression" : {
            "type" : "string",
            "description" : "SQL expression returning a boolean used as global filter, e.g. fraction row count and total row count are filtered with global filter before counting."
          },
          "expectation" : {
            "type" : "string",
            "description" : "Optional SQL comparison operator and literal to define expected percentage for validation, e.g. '= 0.9\".  \nIf no expectation is defined, the result value is just recorded in metrics."
          },
          "precision" : {
            "type" : "integer",
            "description" : "Number of digits to keep when calculating fraction. Default is 4."
          },
          "scope" : {
            "type" : "string",
            "description" : "The aggregation scope used to evaluate the aggregate expression.  \nDefault is 'Job', which evaluates the records transformed by the current job. This is implemented without big performance impacts on Spark.  \nOther options are 'All' and 'JobPartition', which are implemented by reading the output data again.",
            "enum" : [ "All", "JobPartition", "Job" ]
          },
          "failedSeverity" : {
            "type" : "string",
            "description" : "Severity if expectation fails - can be Error (default) or Warn.  \nIf set to Error, execution will fail, otherwise there will be just a warning logged.",
            "enum" : [ "Warn", "Error" ]
          }
        },
        "title" : "SQLFractionExpectation",
        "required" : [ "type", "name", "countConditionExpression" ],
        "additionalProperties" : false,
        "description" : "Definition of an expectation based on counting how many rows match an expression vs the number of all rows.  \nThe fraction of these two counts is compared against a given expectation.\n\nUse it for share-based data quality rules, e.g. \"at most 1% of the records may have an unknown status\", where an\nabsolute count would depend on the size of the increment. Optionally restrict both counts to a subset of the data\nwith `globalConditionExpression` , e.g. to ignore records that are known to be incomplete. Example:\n```\ndataObjects = {\n  int-departures {\n    type = ParquetFileDataObject\n    path = \"~{env.basedir}/int_departures\"\n    expectations = [{\n      type = SQLFractionExpectation\n      name = \"pctBob\"\n      countConditionExpression = \"firstname = 'bob'\"\n      expectation = \"= 0\"\n    }]\n  }\n}\n```\n\n\nSEE: `SQLExpectation` for an expectation on an arbitrary aggregate expression."
      },
      "SQLQueryExpectation" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "SQLQueryExpectation"
          },
          "name" : {
            "type" : "string",
            "description" : "The name of the expectation"
          },
          "description" : {
            "type" : "string",
            "description" : "Optional detailed description of the expectation"
          },
          "code" : {
            "type" : "string",
            "description" : "a SQL query returning a single row. All column will be added as metrics.  \nIf there are more than one column, there has to be one column with the same name as this expectation. This column will be used to compare against a potential condition of the expectation.  \nThe special token %{inputViewName} must be used to insert the temporary view name used to provide the DataFrame to the query."
          },
          "expectation" : {
            "type" : "string",
            "description" : "Optional SQL comparison operator and literal to define expected value for validation, e.g. '= 0\".  \nTogether with the result of the aggExpression evaluation on the left side, it forms the condition to validate the expectation.  \nIf no expectation is defined, the aggExpression evaluation result is just recorded in metrics."
          },
          "scope" : {
            "type" : "string",
            "description" : "The aggregation scope used to evaluate the aggregate expression.  \nDefault is 'Job', which evaluates the records transformed by the current job. This is implemented without big performance impacts on Spark.  \nOther options are 'All' and 'JobPartition', which are implemented by reading the output data again.",
            "enum" : [ "All", "JobPartition", "Job" ]
          },
          "failedSeverity" : {
            "type" : "string",
            "description" : "Severity if expectation fails - can be Error (default) or Warn.  \nIf set to Error, execution will fail, otherwise there will be just a warning logged.",
            "enum" : [ "Warn", "Error" ]
          }
        },
        "title" : "SQLQueryExpectation",
        "required" : [ "type", "name", "code" ],
        "additionalProperties" : false,
        "description" : "Definition of an expectation based on a SQL query to be evaluate on dataset-level.  \nThe SQL query will be evaluated in a separate Spark job against the DataFrame.  \nIt supports scope Job and All, but not JobPartition.\n\nUse it when the check cannot be written as a single aggregate expression, e.g. because it needs a subquery,\na `group by ... having` , or a join. Note that this costs an additional Spark job over the data, so prefer `SQLExpectation` whenever a plain aggregate expression is sufficient. Example:\n```\ndataObjects = {\n  int-departures {\n    type = ParquetFileDataObject\n    path = \"~{env.basedir}/int_departures\"\n    expectations = [{\n      type = SQLQueryExpectation\n      name = \"countOfPartitionsWith1Record\"\n      code = \"select count(*) from (select lastname from %{inputViewName} group by lastname having count(*) = 1)\"\n      scope = All\n    }]\n  }\n}\n```\n\n\nNOTE: `scope = JobPartition` is not supported and fails on initialization.\n\nSEE: `SQLExpectation`\n\nSEE: `SQLFractionExpectation`"
      },
      "UniqueKeyExpectation" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "UniqueKeyExpectation"
          },
          "name" : {
            "type" : "string",
            "description" : "The name of the expectation"
          },
          "key" : {
            "type" : "array",
            "items" : {
              "type" : "string"
            },
            "description" : "Optional list of key columns to evaluate uniqueness. If empty primary key definition of DataObject is used if present."
          },
          "expectation" : {
            "type" : "string",
            "description" : "Optional SQL comparison operator and literal to define expected value for validation. Default is '= 1'.  \nTogether with the result of the aggExpression evaluation on the left side, it forms the condition to validate the expectation.  \nIf no expectation is defined, the aggExpression evaluation result is just recorded in metrics."
          },
          "precision" : {
            "type" : "integer",
            "description" : "Number of digits to keep when calculating fraction. Default is 4."
          },
          "approximate" : {
            "type" : "boolean",
            "description" : "If approximate count distinct function should be used for counting distinct.  \nDefault is \"False\", as primary key validation is normally expected to be exact and not approximated.  \nPerformance note for Spark: exact count_distinct is not allowed as DataFrame observation and needs a separate query on the DataFrame,\nbut approx_count_distinct can be calculated as DataFrame observation."
          },
          "approximateRsd" : {
            "type" : "number",
            "description" : "Optional Relative Standard Deviation for approximate count distinct.  \nNote that not all calculation engines support configuring Rsd with approximate count distinct function."
          },
          "scope" : {
            "type" : "string",
            "description" : "The aggregation scope used to evaluate the aggregate expression.  \nDefault is 'Job', which evaluates the records transformed by the current job. This is implemented without big performance impacts on Spark.  \nOther options are 'All' and 'JobPartition', which are implemented by reading the output data again.",
            "enum" : [ "All", "JobPartition", "Job" ]
          },
          "failedSeverity" : {
            "type" : "string",
            "description" : "Severity if expectation fails - can be Error (default) or Warn.  \nIf set to Error, execution will fail, otherwise there will be just a warning logged.",
            "enum" : [ "Warn", "Error" ]
          }
        },
        "title" : "UniqueKeyExpectation",
        "required" : [ "type", "name" ],
        "additionalProperties" : false,
        "description" : "Definition of expectation on uniqueness of a given key.  \nUniqueness is calculated as the fraction of output count distinct on key columns over output count.  \nIt supports scope Job and All, but not JobPartition.\n\nUse it to assert that a primary key or business key has no duplicates. If `key` is left empty, the `table.primaryKey` of the DataObject is used, which requires the DataObject to be a `TableDataObject` with a primary key defined. Set `scope = All` to check uniqueness over the whole table instead of only\nthe records written by the current job. Example:\n```\ndataObjects = {\n  int-airports {\n    type = DeltaLakeTableDataObject\n    path = \"~{env.basedir}/int_airports\"\n    table = { db = \"default\", name = \"int_airports\", primaryKey = [ident] }\n    expectations = [{\n      type = UniqueKeyExpectation\n      name = \"primaryKeyTest\"\n      scope = All\n    }]\n  }\n}\n```\n\n\nNOTE: `scope = JobPartition` is not supported and fails on initialization.  \nThe fraction is rounded down (floor), so any duplicate is detected aggressively."
      }
    },
    "HousekeepingMode" : {
      "PartitionArchiveMode" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "PartitionArchiveMode"
          },
          "archivePartitionExpression" : {
            "type" : "string",
            "description" : "Expression to define the archive partition for a given partition. Define a spark sql expression\nworking with the attributes of`PartitionExpressionData` returning archive partition values\nas Map[String,String]. If return value is the same as input elements, partition is not touched,\notherwise all files of the partition are moved to the returned partition definition. Be aware\nthat the value of the partition columns changes for these files/records.  \nIf not defined, no partition is archived."
          },
          "description" : {
            "type" : "string",
            "description" : "Optional description of this housekeeping mode, e.g. to document the archiving strategy.  \nIt is not interpreted by SDLB."
          }
        },
        "title" : "PartitionArchiveMode",
        "required" : [ "type" ],
        "additionalProperties" : false,
        "description" : "Archive old partitions: Archive partition reduces the number of partitions in the past by moving\nolder partitions into special \"archive partitions\".\n\nUse it to avoid an ever growing number of small partitions for a DataObject that is loaded frequently, while still\nkeeping the historical data. In contrast to `PartitionRetentionMode` no data is deleted, but the partition column\nvalues of the archived records change to the archive partition value. Note that files are relocated as they are and\nnot merged, so the number of files stays the same. Example: archive a table with partition layout run_id=<integer>, moving partitions older than 1000 runs into an\n\"archive partition\" equal to floor(run_id/1000):\n```\ndataObjects = {\n  int-departures {\n    type = ParquetFileDataObject\n    path = \"~{env.basedir}/int_departures\"\n    partitions = [run_id]\n    housekeepingMode = {\n      type = PartitionArchiveMode\n      archivePartitionExpression = \"if( elements['run_id'] < runId - 1000, map('run_id', elements['run_id'] div 1000), elements)\"\n    }\n  }\n}\n```\n\n\nNOTE: Only supported for DataObjects implementing `CanHandlePartitions` ; this is asserted in the prepare phase.  \nIf such a DataObject has no partition column defined, a ConfigurationException is thrown when housekeeping\nruns after the write.\n\nSEE: `PartitionRetentionMode`"
      },
      "PartitionRetentionMode" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "PartitionRetentionMode"
          },
          "retentionCondition" : {
            "type" : "string",
            "description" : "Condition to decide if a partition should be kept. Define a spark sql expression working with\nthe attributes of`PartitionExpressionData` returning a boolean with value true if the\npartition should be kept."
          },
          "description" : {
            "type" : "string",
            "description" : "Optional description of this housekeeping mode, e.g. to document the business rule behind the\nretention condition. It is not interpreted by SDLB."
          }
        },
        "title" : "PartitionRetentionMode",
        "required" : [ "type", "retentionCondition" ],
        "additionalProperties" : false,
        "description" : "Keep partitions while retention condition is fulfilled, delete other partitions.\n\nThe condition is evaluated after every write against all existing partitions of the DataObject, and every\npartition for which it returns false is deleted. Use it to implement a rolling time window on a partitioned\nDataObject. Use `PartitionArchiveMode` instead if old data should be kept but consolidated into fewer\npartitions. Example: cleanup partitions with partition layout dt=<yyyymmdd> after 90 days:\n```\ndataObjects = {\n  int-departures {\n    type = ParquetFileDataObject\n    path = \"~{env.basedir}/int_departures\"\n    partitions = [dt]\n    housekeepingMode = {\n      type = PartitionRetentionMode\n      retentionCondition = \"datediff(now(), to_date(elements['dt'], 'yyyyMMdd')) <= 90\"\n    }\n  }\n}\n```\n\n\nNOTE: Only supported for DataObjects implementing `CanHandlePartitions` ; this is asserted in the prepare phase.  \nIf such a DataObject has no partition column defined, a ConfigurationException is thrown when housekeeping\nruns after the write.\n\nSEE: `PartitionArchiveMode`"
      }
    },
    "Agent" : {
      "AzureRelayAgent" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "AzureRelayAgent"
          },
          "url" : {
            "type" : "string",
            "description" : "Connection URL on how the agent can be reached. See io.smartdatalake.app.SmartDataLakeBuilderAzureRelayAgentIT#azureRelayUrl for an example."
          },
          "connections" : {
            "type" : "object",
            "additionalProperties" : {
              "oneOf" : [ {
                "$ref" : "#/definitions/Connection/BigQueryTableConnection"
              }, {
                "$ref" : "#/definitions/Connection/DebeziumConnection"
              }, {
                "$ref" : "#/definitions/Connection/DeltaLakeTableConnection"
              }, {
                "$ref" : "#/definitions/Connection/HadoopFileConnection"
              }, {
                "$ref" : "#/definitions/Connection/IcebergTableConnection"
              }, {
                "$ref" : "#/definitions/Connection/JdbcTableConnection"
              }, {
                "$ref" : "#/definitions/Connection/KafkaConnection"
              }, {
                "$ref" : "#/definitions/Connection/SFtpFileRefConnection"
              }, {
                "$ref" : "#/definitions/Connection/ScalaConnection"
              }, {
                "$ref" : "#/definitions/Connection/SnowflakeConnection"
              }, {
                "$ref" : "#/definitions/Connection/SparkClassicConnection"
              }, {
                "$ref" : "#/definitions/Connection/SparkConnectConnection"
              } ]
            },
            "description" : "Optional Map of private connections that this agent has access to.\n\nIf an Agent has no own Hocon connection configuration in the remote location, connections can be defined here\nand they will override connections defined in the global connections section for execution on the Agent.  \nThis allows the Agent to use some connections that are only accessible in the Agent's environment.\n\nNote that Agent Servers need to be started with useOnlyLocalConnectionConfig=false to use this feature for security reasons."
          }
        },
        "title" : "AzureRelayAgent",
        "required" : [ "type", "url" ],
        "additionalProperties" : false,
        "description" : "`Agent` that communicates via a Azure Relay Service.  \nSee the class SmartDataLakeBuilderAzureRelayAgentIT for an example.\n\nAn Agent lets a \"main\" SDLB instance delegate the execution of an Action to a remote SDLB instance, e.g. to read\ndata from an on-premise source that the main instance cannot reach. AzureRelayAgent uses an Azure Relay Hybrid\nConnection, so the remote instance does not need an inbound port open: both sides connect outbound to the relay.  \nPrefer this over `JettyAgent` (development only) and `StorageAgent` (file based) when the remote instance\nsits behind a firewall.\n\nActions are delegated to the agent by setting `agentId` on the Action. The `connections` defined on the agent\noverride the connections with the same id on the remote instance, but only if the agent server is started with `useOnlyLocalConnectionConfig=false` . With the default `true` the agent server uses its own local connection\nconfiguration only and the connections defined here are ignored. Example:\n```\nagents = {\n  agent-onprem {\n    type = AzureRelayAgent\n    url = \"Endpoint=sb://my-relay.servicebus.windows.net/;SharedAccessKeyName=my-policy;EntityPath=my-connection;SharedAccessKey=\"${SharedAccessKey}\n    connections {\n      remoteFile { id = remoteFile, type = HadoopFileConnection, pathPrefix = \"/data/remote\" }\n    }\n  }\n}\n```\n\n\nNOTE: Requires the sdl-azure module on the classpath and a running SDLB agent server listening on the same\nAzure Relay hybrid connection.\n\nNOTE: The `connections` block of the example is only taken into account if the agent server is started with `useOnlyLocalConnectionConfig=false` , which is not the default for security reasons."
      },
      "JettyAgent" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "JettyAgent"
          },
          "url" : {
            "type" : "string",
            "description" : "Connection URL on how the agent can be reached, example: \"ws://localhost:4441/ws/\""
          },
          "connections" : {
            "type" : "object",
            "additionalProperties" : {
              "oneOf" : [ {
                "$ref" : "#/definitions/Connection/BigQueryTableConnection"
              }, {
                "$ref" : "#/definitions/Connection/DebeziumConnection"
              }, {
                "$ref" : "#/definitions/Connection/DeltaLakeTableConnection"
              }, {
                "$ref" : "#/definitions/Connection/HadoopFileConnection"
              }, {
                "$ref" : "#/definitions/Connection/IcebergTableConnection"
              }, {
                "$ref" : "#/definitions/Connection/JdbcTableConnection"
              }, {
                "$ref" : "#/definitions/Connection/KafkaConnection"
              }, {
                "$ref" : "#/definitions/Connection/SFtpFileRefConnection"
              }, {
                "$ref" : "#/definitions/Connection/ScalaConnection"
              }, {
                "$ref" : "#/definitions/Connection/SnowflakeConnection"
              }, {
                "$ref" : "#/definitions/Connection/SparkClassicConnection"
              }, {
                "$ref" : "#/definitions/Connection/SparkConnectConnection"
              } ]
            },
            "description" : "Optional Map of private connections that this agent has access to.\n\nIf an Agent has no own Hocon connection configuration in the remote location, connections can be defined here\nand they will override connections defined in the global connections section for execution on the Agent.  \nThis allows the Agent to use some connections that are only accessible in the Agent's environment.\n\nNote that Agent Servers need to be started with useOnlyLocalConnectionConfig=false to use this feature for security reasons."
          }
        },
        "title" : "JettyAgent",
        "required" : [ "type", "url" ],
        "additionalProperties" : false,
        "description" : "Simple, unsecured SDLB Remote `Agent` for development use that communicates via a plain Jetty Websocket.  \nSee the class SmartDataLakeBuilderAgentTest for an example.\n\nAn Agent lets a \"main\" SDLB instance delegate the execution of an Action to a remote SDLB instance, e.g. to read\ndata from a source only the remote instance can reach. Actions are delegated by setting `agentId` on the Action.  \nThe `connections` defined on the agent override the connections with the same id on the remote instance, but only\nif the agent server is started with `useOnlyLocalConnectionConfig=false` . With the default `true` the agent server\nuses its own local connection configuration only and the connections defined here are ignored. Example:\n```\nagents = {\n  jetty-agent1 {\n    type = JettyAgent\n    url = \"ws://localhost:4441/ws/\"\n    connections {\n      remoteFile { id = remoteFile, type = HadoopFileConnection, pathPrefix = \"target/remote\" }\n    }\n  }\n}\n```\n\n\nNOTE: The websocket connection is neither encrypted nor authenticated, so use this only for local development\nand tests. For productive setups use `StorageAgent` or the AzureRelayAgent of the sdl-azure module.\n\nNOTE: The `connections` block of the example is only taken into account if the agent server is started with `useOnlyLocalConnectionConfig=false` , which is not the default for security reasons."
      },
      "StorageAgent" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "StorageAgent"
          },
          "path" : {
            "type" : "string",
            "description" : "Hadoop path where the agent reads instructions from and writes result information to"
          },
          "startTimeoutSec" : {
            "type" : "integer",
            "description" : "maximum time to wait for the start of the processing by the agent in seconds (default: 300s)"
          },
          "execTimeoutSec" : {
            "type" : "integer",
            "description" : "maximum time to wait for the execution result in seconds (default: 300s)"
          },
          "connections" : {
            "type" : "object",
            "additionalProperties" : {
              "oneOf" : [ {
                "$ref" : "#/definitions/Connection/BigQueryTableConnection"
              }, {
                "$ref" : "#/definitions/Connection/DebeziumConnection"
              }, {
                "$ref" : "#/definitions/Connection/DeltaLakeTableConnection"
              }, {
                "$ref" : "#/definitions/Connection/HadoopFileConnection"
              }, {
                "$ref" : "#/definitions/Connection/IcebergTableConnection"
              }, {
                "$ref" : "#/definitions/Connection/JdbcTableConnection"
              }, {
                "$ref" : "#/definitions/Connection/KafkaConnection"
              }, {
                "$ref" : "#/definitions/Connection/SFtpFileRefConnection"
              }, {
                "$ref" : "#/definitions/Connection/ScalaConnection"
              }, {
                "$ref" : "#/definitions/Connection/SnowflakeConnection"
              }, {
                "$ref" : "#/definitions/Connection/SparkClassicConnection"
              }, {
                "$ref" : "#/definitions/Connection/SparkConnectConnection"
              } ]
            },
            "description" : "Optional Map of private connections that this agent has access to.\n\nIf an Agent has no own Hocon connection configuration in the remote location, connections can be defined here\nand they will override connections defined in the global connections section for execution on the Agent.  \nThis allows the Agent to use some connections that are only accessible in the Agent's environment.\n\nNote that Agent Servers need to be started with useOnlyLocalConnectionConfig=false to use this feature for security reasons."
          }
        },
        "title" : "StorageAgent",
        "required" : [ "type", "path" ],
        "additionalProperties" : false,
        "description" : "An SDLB Remote Agent that communicates through Hadoop storage (e.g. HDFS, S3, Azure Blob Storage, ...)\n\nAn Agent lets a \"main\" SDLB instance delegate the execution of an Action to a remote SDLB instance, e.g. to read\ndata from a source only the remote instance can reach. Actions are delegated by setting `agentId` on the Action.  \nInstead of a network connection, instruction and result are exchanged as files under `path` , so this is the right\nchoice if the two instances share a storage location but cannot open a socket between them. The `connections` defined on the agent override the connections with the same id on the remote instance, but only if the agent\nserver is started with `useOnlyLocalConnectionConfig=false` . With the default `true` the agent server uses its\nown local connection configuration only and the connections defined here are ignored. Example:\n```\nagents = {\n  storage-agent1 {\n    type = StorageAgent\n    path = \"~{env.basedir}/storage-agent1\"\n    execTimeoutSec = 900\n    connections {\n      remoteFile { id = remoteFile, type = HadoopFileConnection, pathPrefix = \"/data/remote\" }\n    }\n  }\n}\n```\n\n\nNOTE: Only one instruction is sent at a time per agent instance. If no agent picks up the instruction within `startTimeoutSec` , or does not finish within `execTimeoutSec` , the Action fails with a timeout.\n\nNOTE: The `connections` block of the example is only taken into account if the agent server is started with `useOnlyLocalConnectionConfig=false` , which is not the default for security reasons.\n\nSEE: `JettyAgent`"
      }
    },
    "DataObject" : {
      "AccessTableDataObject" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "AccessTableDataObject"
          },
          "path" : {
            "type" : "string",
            "description" : ": the path to the access database file"
          },
          "schemaMin" : {
            "type" : "string",
            "description" : "An optional, minimal schema that a `DataObject`schema must have to pass schema validation.\n\nThe schema validation semantics are:\n- Schema A is valid in respect to a minimal schema B when B is a subset of A. This means: the whole column set of B is contained in the column set of A.  \n- A column of B is contained in A when A contains a column with equal name and data type.  \n- Column order is ignored.  \n- Column nullability is ignored.  \n- Duplicate columns in terms of name and data type are eliminated (set semantics).\n\nNote: This is mainly used by the functionality defined in `CanCreateDataFrame` and `CanWriteDataFrame`, that is,\nwhen reading or writing Spark data frames from/to the underlying data container. `io.smartdatalake.workflow.action.Action`s that work with files ignore the `schemaMin` attribute\nif it is defined.  \nAdditionally schemaMin can be used to define the schema used if there is no data or table doesn't yet exist."
          },
          "table" : {
            "$ref" : "#/definitions/Others/Table",
            "description" : ": the Access table to be read"
          },
          "metadata" : {
            "$ref" : "#/definitions/Others/DataObjectMetadata",
            "description" : "Additional metadata for the DataObject"
          }
        },
        "title" : "AccessTableDataObject",
        "required" : [ "type", "path", "table" ],
        "additionalProperties" : false,
        "description" : "`DataObject` of type Microsoft Access.  \nCan read a single table from a Microsoft Access database file (.mdb/.accdb) and expose it as a Spark DataFrame.  \nThe database file is read through the Hadoop filesystem layer, copied to a local temporary file and opened with\nthe Jackcess library. Column types are mapped from JDBC types to Spark types, dates/times become Timestamps.  \nUse this to ingest legacy Access files into the data lake; for a real database prefer `JdbcTableDataObject`. Example:\n```\ndataObjects = {\n  ext-orders {\n    type = AccessTableDataObject\n    path = \"~{env.basedir}/ext_orders/northwind.accdb\"\n    table {\n      db = \"northwind\"\n      name = \"Orders\"\n      primaryKey = [OrderID]\n    }\n  }\n}\n```\n\n\nNOTE: read-only: writing and `dropTable` are not implemented. The whole table is materialized on the driver,\nso this is not suited for very large Access tables.\n\nSEE: `JdbcTableDataObject`"
      },
      "ActionsExporterDataObject" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "ActionsExporterDataObject"
          },
          "config" : {
            "type" : "string",
            "description" : "optional location of the config to export, either a configuration file or a directory containing\nconfiguration files. Multiple locations can be given as a comma-separated list.  \nIf not defined, the Actions registered in the current`InstanceRegistry` are exported."
          },
          "metadata" : {
            "$ref" : "#/definitions/Others/DataObjectMetadata",
            "description" : "Additional metadata for the DataObject"
          }
        },
        "title" : "ActionsExporterDataObject",
        "required" : [ "type" ],
        "additionalProperties" : false,
        "description" : "Exports a util `DataFrame` that contains properties and metadata extracted from all `Action`s\nthat are registered in the current `InstanceRegistry`.\n\nAlternatively, it can export the properties and metadata of all `Action`s defined in config files. For this, the\nconfiguration \"config\" has to be set to the location of the config. Example:\n```\ndataObjects = {\n ...  \n actions-exporter {\n   type = ActionsExporterDataObject\n   config = path/to/myconfiguration.conf\n }\n ...  \n}\n```\n\n\nThe config value can point to a configuration file or a directory containing configuration files.\n\nSEE: Refer to `ConfigLoader.loadConfigFromFilesystem()` for details about the configuration loading."
      },
      "AirbyteDataObject" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "AirbyteDataObject"
          },
          "config" : {
            "type" : "string",
            "description" : "Configuration for the source. Its entries are connector specific and are validated against the\nspecification reported by the connector itself in prepare phase.",
            "existingJavaType" : "com.typesafe.config.Config"
          },
          "streamName" : {
            "type" : "string",
            "description" : "The stream name to read. Must match an entry of the catalog of the source."
          },
          "cmd" : {
            "oneOf" : [ {
              "$ref" : "#/definitions/ParsableScriptDef/CmdScript"
            }, {
              "$ref" : "#/definitions/ParsableScriptDef/DockerRunScript"
            } ],
            "description" : "command to launch airbyte connector. Normally this is of type`DockerRunScript`."
          },
          "incrementalCursorFields" : {
            "type" : "array",
            "items" : {
              "type" : "string"
            },
            "description" : "Some sources need a specification of the cursor field for incremental mode"
          },
          "maxRecordsPerPartition" : {
            "type" : "integer",
            "description" : "Maximum number of records to put into one Spark partition.  \nThis helps to limit memory usage, as Spark will offload partitions to disk if memory is scarce."
          },
          "schemaMin" : {
            "type" : "string",
            "description" : "An optional, minimal schema that a `DataObject`schema must have to pass schema validation.\n\nThe schema validation semantics are:\n- Schema A is valid in respect to a minimal schema B when B is a subset of A. This means: the whole column set of B is contained in the column set of A.  \n- A column of B is contained in A when A contains a column with equal name and data type.  \n- Column order is ignored.  \n- Column nullability is ignored.  \n- Duplicate columns in terms of name and data type are eliminated (set semantics).\n\nNote: This is mainly used by the functionality defined in `CanCreateDataFrame` and `CanWriteDataFrame`, that is,\nwhen reading or writing Spark data frames from/to the underlying data container. `io.smartdatalake.workflow.action.Action`s that work with files ignore the `schemaMin` attribute\nif it is defined.  \nAdditionally schemaMin can be used to define the schema used if there is no data or table doesn't yet exist."
          },
          "metadata" : {
            "$ref" : "#/definitions/Others/DataObjectMetadata",
            "description" : "Additional metadata for the DataObject"
          }
        },
        "title" : "AirbyteDataObject",
        "required" : [ "type", "config", "streamName", "cmd" ],
        "additionalProperties" : false,
        "description" : "Uses an Airbyte Connector to read data from a data source.  \nSee https://docs.airbyte.com/integrations/sources/ for the list of available sources.\n\nAn Airbyte Connector is started using shell commands and streams json data into the AirbyteDataObject using stdout pipe.  \nAirbyte Connectors are often written in Python. See https://docs.airbyte.com/connector-development/ for developing a custom connector.\n\nLimitations: Airbyte Connectors can not be distributed to executors. They run on the driver and have only access to locally mounted directories.  \nIn order to avoid memory problems Spark BlockManager is used to create a new Spark partition after every maxRecordsPerPartition number of records.\n\nAlso note that the getDataFrame method is not lazy in Exec-Phase. It will query the Airbyte Connector before creating the DataFrame. Example:\n```\ndataObjects = {\n  ext-users {\n    type = AirbyteDataObject\n    streamName = \"users\"\n    # the entries of config are connector specific, see the documentation of the connector used\n    config {\n      host = \"db.example.com\"\n      port = 5432\n      database = \"postgres\"\n      username = \"sdlb\"\n      password = \"###\"\n    }\n    cmd {\n      type = DockerRunScript\n      image = \"airbyte/source-postgres:latest\"\n    }\n    incrementalCursorFields = [\"updated_at\"]\n  }\n}\n```\n"
      },
      "AvroFileDataObject" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "AvroFileDataObject"
          },
          "path" : {
            "type" : "string",
            "description" : "The root path of the files that are handled by this DataObject.  \nFor most DataObjects this can be a directory or a specific file.\n\nIf it doesn't contain scheme and authority, the connections pathPrefix is applied. If pathPrefix is not\ndefined or doesn't define scheme and authority, default schema and authority is applied.\n\nOptionally defined partitions are appended with hadoop standard partition layout to this path."
          },
          "partitions" : {
            "type" : "array",
            "items" : {
              "type" : "string"
            },
            "description" : "Definition of partition columns\n\nExample: `[dt]`"
          },
          "avroOptions" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "Settings for the underlying `org.apache.spark.sql.DataFrameReader` and `org.apache.spark.sql.DataFrameWriter`."
          },
          "schema" : {
            "type" : "string",
            "description" : "An optional user-defined schema definition for this DataObject.  \nIf defined, any automatic schema inference is avoided.\n\nThe schema corresponds to the schema on write, it must not include optional columns on read, e.g. the filenameColumn for SparkFileDataObjects.  \nIf partition columns are missing, they will be added with type String.\n\nDefine the schema by using one of the schema providers below, default is DDL.  \nThe schema provider and its configuration value must be provided in the format <PROVIDERID>#<VALUE>.\n\nSchema Providers available are (see also `io.smartdatalake.util.misc.SchemaProviderType` ):\n\n- ddl: create the schema from a Spark ddl string, e.g. `ddl#a string, b array<struct<b1: string, b2: long>>, c struct<c1: string, c2: long>` .\n\n- ddlFile: read a Spark ddl definition from a file and create a schema, e.g. `ddlFile#abc/xyz.ddl` .\n\n- caseClass: convert a Scala Case Class to a schema using Spark encoders, e.g. `caseClass#com.sample.XyzClass` .\n\n- javaBean: convert a Java Bean to a schema using Spark encoders, e.g. `javaBean#com.sample.XyzClass` .\n\n- xsdFile: read an Xml Schema Definition file and create a schema, e.g. `xsdFile#abc/xyz.xsd` .  \nThe following parameters allow to customize the behavior: `xsdFile#<path-to-xsd-file>;<row-tag>;<maxRecursion:Int>;<jsonCompatibility:Boolean>` <row-tag>: configure the path of the element to extract from the xsd schema. Leave empty to extract the root.  \n<maxRecursion>: if xsd schema is recursive, this configures the number of levels to create in the schema.  \nDefault is 10 levels.  \n<jsonCompatibility>: In XML array elements are modeled with their own tag named with singular name.  \nIn JSON an array attribute has unnamed array entries, but the array attribute has a plural name.  \nIf true, the singular name of the array element in the XSD is converted to a plural name by adding an 's'\nin order to read corresponding json files.  \nDefault is false.\n\n- jsonSchemaFile: read a Json Schema file and create a schema, e.g. `jsonSchemaFile#abc/xyz.json` .  \nThe following parameters allow to customize the behavior: `jsonSchemaFile#<path-to-json-file>;<row-tag>;<strictTyping:Boolean>;<additionalPropertiesDefault:Boolean>` <row-tag>: configure the path of the element to extract from the json schema. Leave empty to extract the root.  \n<strictTyping>: if true\nunion types (oneOf) are merged if rational, otherwise they are simply mapped to StringType;\nadditional properties are ignored, otherwise the corresponding schema object is mapped to MapType(String,String).  \nDefault is strictTyping=false.  \n<additionalPropertiesDefault>: Set to true or false.  \nThis is used as default value for 'additionalProperties'-field if it is missing in a schema with type='object'.  \nDefault value is additionalPropertiesDefault=true, as this is conform with the specification.\n\n- avroSchemaFile: read an Avro Schema file and create a schema, e.g. `avroSchemaFile#abc/xyz.avsc` .  \nThe following parameters allow to customize the behavior: `avroSchemaFile#<path-to-avsc-file>;<row-tag>` <row-tag>: configure the path of the element to extract from the avro schema. Leave empty to extract the root.\n\n- openApi: read Schema from OpenApi specification operation, e.g. `openApi#https://swagger.myswitzerland.io;getDestinations;opendata.json` .  \nThe following parameters allow to customize the behaviour: `openApi#<baseUrl>;<operationId>;[<apiDocsPath>;][<responseContentType>]`\n\nNote that all schema files are configured as Hadoop path. The custom prefix 'cp' can be used to read schema files\nfrom the classpath, e.g. `xsdFile#cp:/xyz.xsd` ."
          },
          "schemaMin" : {
            "type" : "string",
            "description" : "An optional, minimal schema that a `DataObject`schema must have to pass schema validation.\n\nThe schema validation semantics are:\n- Schema A is valid in respect to a minimal schema B when B is a subset of A. This means: the whole column set of B is contained in the column set of A.  \n- A column of B is contained in A when A contains a column with equal name and data type.  \n- Column order is ignored.  \n- Column nullability is ignored.  \n- Duplicate columns in terms of name and data type are eliminated (set semantics).\n\nNote: This is mainly used by the functionality defined in `CanCreateDataFrame` and `CanWriteDataFrame`, that is,\nwhen reading or writing Spark data frames from/to the underlying data container. `io.smartdatalake.workflow.action.Action`s that work with files ignore the `schemaMin` attribute\nif it is defined.  \nAdditionally schemaMin can be used to define the schema used if there is no data or table doesn't yet exist."
          },
          "saveMode" : {
            "type" : "string",
            "description" : "Overwrite or Append new data.  \nWhen writing partitioned data, this applies only to partitions concerned.",
            "enum" : [ "Merge", "OverwriteOptimized", "OverwritePreserveDirectories", "Ignore", "ErrorIfExists", "Append", "Overwrite" ]
          },
          "sparkRepartition" : {
            "type" : "object",
            "properties" : {
              "numberOfTasksPerPartition" : {
                "type" : "integer",
                "description" : "Number of Spark tasks to create per partition before writing to DataObject by repartitioning the DataFrame.  \nThis controls how many files are created in each Hadoop partition."
              },
              "keyCols" : {
                "type" : "array",
                "items" : {
                  "type" : "string"
                },
                "description" : "Optional key columns to distribute records over Spark tasks inside a Hadoop partition.  \nIf DataObject has Hadoop partitions defined, keyCols must be defined."
              },
              "sortCols" : {
                "type" : "array",
                "items" : {
                  "type" : "string"
                },
                "description" : "Optional columns to sort records inside files created."
              },
              "filename" : {
                "type" : "string",
                "description" : "Option filename to rename target file(s). If numberOfTasksPerPartition is greater than 1,\nmultiple files can exist in a directory and a number is inserted into the filename after the first '.'.  \nExample: filename=data.csv → files created are data.1.csv, data.2.csv, ..."
              }
            },
            "title" : "SparkRepartitionDef",
            "required" : [ "numberOfTasksPerPartition" ],
            "additionalProperties" : false,
            "description" : "Definition of repartition operation before writing DataFrame with Spark to Hadoop."
          },
          "connectionId" : {
            "type" : "string",
            "description" : "Return the connection id.\n\nConnection defines path prefix (scheme, authority, base path) and ACL's in central location."
          },
          "filenameColumn" : {
            "type" : "string",
            "description" : "The name of the (optional) additional column containing the source filename"
          },
          "expectedPartitionsCondition" : {
            "type" : "string",
            "description" : "Definition of partitions that are expected to exists.  \nThis is used to validate that partitions being read exists and don't return no data.  \nDefine a Spark SQL expression that is evaluated against a `PartitionValues` instance and returns true or false\n\nExample: \"elements['yourColName'] > 2017\"\n\nIf empty (default) all partition are expected to exists.\n\nOTHERTAG: true if partition is expected to exist."
          },
          "housekeepingMode" : {
            "oneOf" : [ {
              "$ref" : "#/definitions/HousekeepingMode/PartitionArchiveMode"
            }, {
              "$ref" : "#/definitions/HousekeepingMode/PartitionRetentionMode"
            } ],
            "description" : "Configure a housekeeping mode to e.g cleanup, archive and compact partitions.\n\nDefault is None."
          },
          "constraints" : {
            "type" : "array",
            "items" : {
              "type" : "object",
              "properties" : {
                "name" : {
                  "type" : "string",
                  "description" : "name of the constraint"
                },
                "description" : {
                  "type" : "string",
                  "description" : "optional detailed description of the constraint"
                },
                "expression" : {
                  "type" : "string",
                  "description" : "SQL expression to evaluate on every row. The expressions return value should be a boolean.  \nIf it evaluates to true the constraint is validated successfully, otherwise it will throw an exception."
                },
                "errorMsgCols" : {
                  "type" : "array",
                  "items" : {
                    "type" : "string"
                  },
                  "description" : "Optional list of column names to add to error message.  \nNote that primary key columns are always included.  \nIf there is no primary key defined, by default all columns with simple datatype are included in the error message."
                }
              },
              "title" : "Constraint",
              "required" : [ "name", "expression" ],
              "additionalProperties" : false,
              "description" : "Definition of row-level constraint to validate."
            },
            "description" : "List of constraint definitions to validate on write, see `Constraint` for details.  \nConstraints are expressions defined on row-level and validated during evaluation of the DataFrame.  \nIf validation fails an exception is thrown and further processing is stopped.  \nNote that this is done while evaluating the DataFrame when writing to the DataObject. It doesn't need a separate action on the DataFrame.  \nIf a constraint validation for a row fails, it will throw an exception and abort writing to the DataObject."
          },
          "expectations" : {
            "type" : "array",
            "items" : {
              "oneOf" : [ {
                "$ref" : "#/definitions/Expectation/AvgCountPerPartitionExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/CountExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/SQLExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/SQLFractionExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/SQLQueryExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/UniqueKeyExpectation"
              } ]
            },
            "description" : "List expectation definitions to evaluate on write, see `Expectation` for details.  \nExpectations are aggregation expressions defined on dataset-level and evaluated on every write.  \nBy default their result is logged with level info (ok) and error (failed), but this can be customized to be logged as warning.  \nIn case of failed expectations logged as error, an exceptions is thrown and further processing is stopped.  \nNote that the exception is thrown after writing to the DataObject is finished.\n\nThe following expectations names are reserved to create default metrics and should not be used:\n- count"
          },
          "metadata" : {
            "$ref" : "#/definitions/Others/DataObjectMetadata",
            "description" : "Additional metadata for the DataObject"
          }
        },
        "title" : "AvroFileDataObject",
        "required" : [ "type", "path" ],
        "additionalProperties" : false,
        "description" : "A `io.smartdatalake.workflow.dataobject.DataObject` backed by an Avro data source.\n\nIt manages read and write access and configurations required for `io.smartdatalake.workflow.action.Action`s to\nwork on Avro formatted files.\n\nReading and writing details are delegated to Apache Spark `org.apache.spark.sql.DataFrameReader` and `org.apache.spark.sql.DataFrameWriter` respectively. The reader and writer implementations are provided by\nthe [databricks spark-avro](https://github.com/databricks/spark-avro) project.\n\nAvro files carry their own schema. If the `schema` attribute is defined, it is not pushed to the reader but applied\nafterwards to select and cast the content to the target schema. Example:\n```\ndataObjects = {\n  stg-airports {\n    type = AvroFileDataObject\n    path = \"~{env.basedir}/stg_airports\"\n    partitions = [dt]\n    saveMode = Append\n    avroOptions {\n      compression = \"snappy\"\n    }\n  }\n}\n```\n\n\nSEE: `org.apache.spark.sql.DataFrameReader`\n\nSEE: [[org.apache.spark.sql.DataFrameWriter]]"
      },
      "BigQueryTableDataObject" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "BigQueryTableDataObject"
          },
          "table" : {
            "$ref" : "#/definitions/Others/Table",
            "description" : "BigQuery table to by written by this output. BigQuery requires a table name and a dataset."
          },
          "viewsEnabled" : {
            "type" : "boolean",
            "description" : "Required if the table is being read with a query, as BigQuery will create a temporary table in the background."
          },
          "materializationDataset" : {
            "type" : "string",
            "description" : "Required if the table is being read with a query, as BigQuery will create a temporary table in the background.  \nThis argument defines on which dataset the view will be created. Defaults to the same dataset as the table."
          },
          "writeMethod" : {
            "type" : "string",
            "description" : "Can be 'direct' or 'indirect'. The indirect mode stores the data in a Bucket or a path first and load it to BigQuery in a second step.  \nIf 'indirect' mode is used, one of the options 'temporaryGcsBucket', 'persistentGscBucket' or 'persistentGscPath' must be set.  \nDefaults to 'direct'"
          },
          "temporaryGcsBucket" : {
            "type" : "string",
            "description" : "Temporary Bucket to store data if saving with \"indirect\" mode."
          },
          "persistentGcsBucket" : {
            "type" : "string",
            "description" : "Persistent Bucket to store data if saving with \"indirect\" mode."
          },
          "persistentGcsPath" : {
            "type" : "string",
            "description" : "Temporary GSC Path to store data if saving with \"indirect\" mode."
          },
          "project" : {
            "type" : "string",
            "description" : "Defaults to the project of the project Id of the service account being used"
          },
          "options" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "Additional Spark options provided by the connector.  \nPlease seehttps://github.com/GoogleCloudDataproc/spark-bigquery-connector for more information."
          },
          "schemaMin" : {
            "type" : "string",
            "description" : "An optional, minimal schema that this DataObject must have to pass schema validation on reading and writing.  \nDefine schema by using a DDL-formatted string, which is a comma separated list of field definitions, e.g., a INT, b STRING."
          },
          "constraints" : {
            "type" : "array",
            "items" : {
              "type" : "object",
              "properties" : {
                "name" : {
                  "type" : "string",
                  "description" : "name of the constraint"
                },
                "description" : {
                  "type" : "string",
                  "description" : "optional detailed description of the constraint"
                },
                "expression" : {
                  "type" : "string",
                  "description" : "SQL expression to evaluate on every row. The expressions return value should be a boolean.  \nIf it evaluates to true the constraint is validated successfully, otherwise it will throw an exception."
                },
                "errorMsgCols" : {
                  "type" : "array",
                  "items" : {
                    "type" : "string"
                  },
                  "description" : "Optional list of column names to add to error message.  \nNote that primary key columns are always included.  \nIf there is no primary key defined, by default all columns with simple datatype are included in the error message."
                }
              },
              "title" : "Constraint",
              "required" : [ "name", "expression" ],
              "additionalProperties" : false,
              "description" : "Definition of row-level constraint to validate."
            },
            "description" : "List of row-level`Constraint`s to enforce when writing to this data object."
          },
          "expectations" : {
            "type" : "array",
            "items" : {
              "oneOf" : [ {
                "$ref" : "#/definitions/Expectation/AvgCountPerPartitionExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/CountExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/SQLExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/SQLFractionExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/SQLQueryExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/UniqueKeyExpectation"
              } ]
            },
            "description" : "List of`Expectation`s to enforce when writing to this data object. Expectations are checks based on aggregates over all rows of a dataset."
          },
          "saveMode" : {
            "type" : "string",
            "description" : "`SDLSaveMode` to use when writing files, default is \"append\". Overwrite, Append are supported for now.",
            "enum" : [ "Merge", "OverwriteOptimized", "OverwritePreserveDirectories", "Ignore", "ErrorIfExists", "Append", "Overwrite" ]
          },
          "connectionId" : {
            "type" : "string",
            "description" : "Id of`io.smartdatalake.workflow.connection.BigQueryTableConnection`"
          },
          "metadata" : {
            "$ref" : "#/definitions/Others/DataObjectMetadata",
            "description" : "Metadata of the Object in form Key-Value form."
          }
        },
        "title" : "BigQueryTableDataObject",
        "required" : [ "type", "table", "connectionId" ],
        "additionalProperties" : false,
        "description" : "`DataObject` of type BigQueryTableDataObject.  \nProvides details to access BigQuery Tables to an Action.  \nThe object uses the official Spark-connector by Google see: https://github.com/GoogleCloudDataproc/spark-bigquery-connector.  \nAs of now, the only authentication mode supported is a Service Account Key.\n\nA `io.smartdatalake.workflow.connection.BigQueryTableConnection` is required, as it holds the service account key\nused to authenticate. The table can either be read directly (table.name/table.db) or through a SQL query\n(table.query), in which case viewsEnabled must be true and BigQuery materializes a temporary table. Example:\n```\nconnections = {\n  google-playground {\n    type = BigQueryTableConnection\n    parentProject = \"playground-223717\"\n    authMode = { type = GCPCredentialsKeyAuth, serviceAccountKeyFile = \"/secrets/playground-sa-key.json\" }\n  }\n}\ndataObjects = {\n  int-test-table {\n    type = BigQueryTableDataObject\n    connectionId = google-playground\n    project = \"playground-223717\"\n    table = { db = \"sdl_playground\", name = \"test_table\" }\n    writeMethod = \"indirect\"\n    temporaryGcsBucket = \"sdl-playground-tmp\"\n  }\n}\n```\n"
      },
      "CsvFileDataObject" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "CsvFileDataObject"
          },
          "path" : {
            "type" : "string",
            "description" : "The root path of the files that are handled by this DataObject.  \nFor most DataObjects this can be a directory or a specific file.\n\nIf it doesn't contain scheme and authority, the connections pathPrefix is applied. If pathPrefix is not\ndefined or doesn't define scheme and authority, default schema and authority is applied.\n\nOptionally defined partitions are appended with hadoop standard partition layout to this path."
          },
          "csvOptions" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "Settings for the underlying`org.apache.spark.sql.DataFrameReader]]and[[org.apache.spark.sql.DataFrameWriter`."
          },
          "partitions" : {
            "type" : "array",
            "items" : {
              "type" : "string"
            },
            "description" : "Definition of partition columns\n\nExample: `[dt]`"
          },
          "schema" : {
            "type" : "string",
            "description" : "An optional user-defined schema definition for this DataObject.  \nIf defined, any automatic schema inference is avoided.\n\nThe schema corresponds to the schema on write, it must not include optional columns on read, e.g. the filenameColumn for SparkFileDataObjects.  \nIf partition columns are missing, they will be added with type String.\n\nDefine the schema by using one of the schema providers below, default is DDL.  \nThe schema provider and its configuration value must be provided in the format <PROVIDERID>#<VALUE>.\n\nSchema Providers available are (see also `io.smartdatalake.util.misc.SchemaProviderType` ):\n\n- ddl: create the schema from a Spark ddl string, e.g. `ddl#a string, b array<struct<b1: string, b2: long>>, c struct<c1: string, c2: long>` .\n\n- ddlFile: read a Spark ddl definition from a file and create a schema, e.g. `ddlFile#abc/xyz.ddl` .\n\n- caseClass: convert a Scala Case Class to a schema using Spark encoders, e.g. `caseClass#com.sample.XyzClass` .\n\n- javaBean: convert a Java Bean to a schema using Spark encoders, e.g. `javaBean#com.sample.XyzClass` .\n\n- xsdFile: read an Xml Schema Definition file and create a schema, e.g. `xsdFile#abc/xyz.xsd` .  \nThe following parameters allow to customize the behavior: `xsdFile#<path-to-xsd-file>;<row-tag>;<maxRecursion:Int>;<jsonCompatibility:Boolean>` <row-tag>: configure the path of the element to extract from the xsd schema. Leave empty to extract the root.  \n<maxRecursion>: if xsd schema is recursive, this configures the number of levels to create in the schema.  \nDefault is 10 levels.  \n<jsonCompatibility>: In XML array elements are modeled with their own tag named with singular name.  \nIn JSON an array attribute has unnamed array entries, but the array attribute has a plural name.  \nIf true, the singular name of the array element in the XSD is converted to a plural name by adding an 's'\nin order to read corresponding json files.  \nDefault is false.\n\n- jsonSchemaFile: read a Json Schema file and create a schema, e.g. `jsonSchemaFile#abc/xyz.json` .  \nThe following parameters allow to customize the behavior: `jsonSchemaFile#<path-to-json-file>;<row-tag>;<strictTyping:Boolean>;<additionalPropertiesDefault:Boolean>` <row-tag>: configure the path of the element to extract from the json schema. Leave empty to extract the root.  \n<strictTyping>: if true\nunion types (oneOf) are merged if rational, otherwise they are simply mapped to StringType;\nadditional properties are ignored, otherwise the corresponding schema object is mapped to MapType(String,String).  \nDefault is strictTyping=false.  \n<additionalPropertiesDefault>: Set to true or false.  \nThis is used as default value for 'additionalProperties'-field if it is missing in a schema with type='object'.  \nDefault value is additionalPropertiesDefault=true, as this is conform with the specification.\n\n- avroSchemaFile: read an Avro Schema file and create a schema, e.g. `avroSchemaFile#abc/xyz.avsc` .  \nThe following parameters allow to customize the behavior: `avroSchemaFile#<path-to-avsc-file>;<row-tag>` <row-tag>: configure the path of the element to extract from the avro schema. Leave empty to extract the root.\n\n- openApi: read Schema from OpenApi specification operation, e.g. `openApi#https://swagger.myswitzerland.io;getDestinations;opendata.json` .  \nThe following parameters allow to customize the behaviour: `openApi#<baseUrl>;<operationId>;[<apiDocsPath>;][<responseContentType>]`\n\nNote that all schema files are configured as Hadoop path. The custom prefix 'cp' can be used to read schema files\nfrom the classpath, e.g. `xsdFile#cp:/xyz.xsd` ."
          },
          "schemaMin" : {
            "type" : "string",
            "description" : "An optional, minimal schema that a `DataObject`schema must have to pass schema validation.\n\nThe schema validation semantics are:\n- Schema A is valid in respect to a minimal schema B when B is a subset of A. This means: the whole column set of B is contained in the column set of A.  \n- A column of B is contained in A when A contains a column with equal name and data type.  \n- Column order is ignored.  \n- Column nullability is ignored.  \n- Duplicate columns in terms of name and data type are eliminated (set semantics).\n\nNote: This is mainly used by the functionality defined in `CanCreateDataFrame` and `CanWriteDataFrame`, that is,\nwhen reading or writing Spark data frames from/to the underlying data container. `io.smartdatalake.workflow.action.Action`s that work with files ignore the `schemaMin` attribute\nif it is defined.  \nAdditionally schemaMin can be used to define the schema used if there is no data or table doesn't yet exist."
          },
          "dateColumnType" : {
            "type" : "string",
            "description" : "how to convert columns of Spark type date before writing (default: date). With`date`they are\ncast to timestamp, with`string`they are cast to string. See`io.smartdatalake.definitions.DateColumnType`.",
            "enum" : [ "Default", "String", "Date" ]
          },
          "saveMode" : {
            "type" : "string",
            "description" : "Overwrite or Append new data.  \nWhen writing partitioned data, this applies only to partitions concerned.",
            "enum" : [ "Merge", "OverwriteOptimized", "OverwritePreserveDirectories", "Ignore", "ErrorIfExists", "Append", "Overwrite" ]
          },
          "sparkRepartition" : {
            "type" : "object",
            "properties" : {
              "numberOfTasksPerPartition" : {
                "type" : "integer",
                "description" : "Number of Spark tasks to create per partition before writing to DataObject by repartitioning the DataFrame.  \nThis controls how many files are created in each Hadoop partition."
              },
              "keyCols" : {
                "type" : "array",
                "items" : {
                  "type" : "string"
                },
                "description" : "Optional key columns to distribute records over Spark tasks inside a Hadoop partition.  \nIf DataObject has Hadoop partitions defined, keyCols must be defined."
              },
              "sortCols" : {
                "type" : "array",
                "items" : {
                  "type" : "string"
                },
                "description" : "Optional columns to sort records inside files created."
              },
              "filename" : {
                "type" : "string",
                "description" : "Option filename to rename target file(s). If numberOfTasksPerPartition is greater than 1,\nmultiple files can exist in a directory and a number is inserted into the filename after the first '.'.  \nExample: filename=data.csv → files created are data.1.csv, data.2.csv, ..."
              }
            },
            "title" : "SparkRepartitionDef",
            "required" : [ "numberOfTasksPerPartition" ],
            "additionalProperties" : false,
            "description" : "Definition of repartition operation before writing DataFrame with Spark to Hadoop."
          },
          "connectionId" : {
            "type" : "string",
            "description" : "Return the connection id.\n\nConnection defines path prefix (scheme, authority, base path) and ACL's in central location."
          },
          "filenameColumn" : {
            "type" : "string",
            "description" : "The name of the (optional) additional column containing the source filename"
          },
          "expectedPartitionsCondition" : {
            "type" : "string",
            "description" : "Definition of partitions that are expected to exists.  \nThis is used to validate that partitions being read exists and don't return no data.  \nDefine a Spark SQL expression that is evaluated against a `PartitionValues` instance and returns true or false\n\nExample: \"elements['yourColName'] > 2017\"\n\nIf empty (default) all partition are expected to exists.\n\nOTHERTAG: true if partition is expected to exist."
          },
          "housekeepingMode" : {
            "oneOf" : [ {
              "$ref" : "#/definitions/HousekeepingMode/PartitionArchiveMode"
            }, {
              "$ref" : "#/definitions/HousekeepingMode/PartitionRetentionMode"
            } ],
            "description" : "Configure a housekeeping mode to e.g cleanup, archive and compact partitions.\n\nDefault is None."
          },
          "constraints" : {
            "type" : "array",
            "items" : {
              "type" : "object",
              "properties" : {
                "name" : {
                  "type" : "string",
                  "description" : "name of the constraint"
                },
                "description" : {
                  "type" : "string",
                  "description" : "optional detailed description of the constraint"
                },
                "expression" : {
                  "type" : "string",
                  "description" : "SQL expression to evaluate on every row. The expressions return value should be a boolean.  \nIf it evaluates to true the constraint is validated successfully, otherwise it will throw an exception."
                },
                "errorMsgCols" : {
                  "type" : "array",
                  "items" : {
                    "type" : "string"
                  },
                  "description" : "Optional list of column names to add to error message.  \nNote that primary key columns are always included.  \nIf there is no primary key defined, by default all columns with simple datatype are included in the error message."
                }
              },
              "title" : "Constraint",
              "required" : [ "name", "expression" ],
              "additionalProperties" : false,
              "description" : "Definition of row-level constraint to validate."
            },
            "description" : "List of constraint definitions to validate on write, see `Constraint` for details.  \nConstraints are expressions defined on row-level and validated during evaluation of the DataFrame.  \nIf validation fails an exception is thrown and further processing is stopped.  \nNote that this is done while evaluating the DataFrame when writing to the DataObject. It doesn't need a separate action on the DataFrame.  \nIf a constraint validation for a row fails, it will throw an exception and abort writing to the DataObject."
          },
          "expectations" : {
            "type" : "array",
            "items" : {
              "oneOf" : [ {
                "$ref" : "#/definitions/Expectation/AvgCountPerPartitionExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/CountExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/SQLExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/SQLFractionExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/SQLQueryExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/UniqueKeyExpectation"
              } ]
            },
            "description" : "List expectation definitions to evaluate on write, see `Expectation` for details.  \nExpectations are aggregation expressions defined on dataset-level and evaluated on every write.  \nBy default their result is logged with level info (ok) and error (failed), but this can be customized to be logged as warning.  \nIn case of failed expectations logged as error, an exceptions is thrown and further processing is stopped.  \nNote that the exception is thrown after writing to the DataObject is finished.\n\nThe following expectations names are reserved to create default metrics and should not be used:\n- count"
          },
          "metadata" : {
            "$ref" : "#/definitions/Others/DataObjectMetadata",
            "description" : "Additional metadata for the DataObject"
          }
        },
        "title" : "CsvFileDataObject",
        "required" : [ "type", "path" ],
        "additionalProperties" : false,
        "description" : "A `DataObject` backed by a comma-separated value (CSV) data source.\n\nIt manages read and write access and configurations required for `io.smartdatalake.workflow.action.Action`s to work on CSV formatted files.\n\nCSV reading and writing details are delegated to Apache Spark `org.apache.spark.sql.DataFrameReader` and `org.apache.spark.sql.DataFrameWriter` respectively.\n\nRead Schema specifications:\n\nIf a data object schema is not defined via the `schema` attribute (default) and `inferSchema` option is disabled (default) in `csvOptions` , then all column types are set to String and the\nfirst row of the CSV file is read to determine the column names and the number of fields.\n\nIf the `header` option is disabled in `csvOptions` (it is enabled by default, see note below),\nthen the header is defined as \"_c#\" for each column where \"#\" is the column index. Otherwise the\nfirst row of the CSV file is not included in the DataFrame content and its entries are used as\nthe column names for the schema.\n\nIf a data object schema is not defined via the `schema` attribute and `inferSchema` is enabled in `csvOptions` , then the `samplingRatio` (default: 1.0) option in `csvOptions` is used to extract a\nsample from the CSV file in order to determine the input schema automatically. Example:\n```\ndataObjects = {\n  stg-airports {\n    type = CsvFileDataObject\n    path = \"~{env.basedir}/stg_airports\"\n    csvOptions {\n      delimiter = \";\"\n      inferSchema = true\n    }\n    partitions = [dt]\n    saveMode = Append\n  }\n}\n```\n\n\nNOTE: This data object sets the following default values for `csvOptions` : header = true,\ninferSchema = false, delimiter = \",\", and quote = null. All other `csvOption` default to the\nvalues defined by Apache Spark. Note that either `header` or `inferSchema` must be enabled in `csvOptions` , or an explicit `schema` must be defined.\n\nSEE: `org.apache.spark.sql.DataFrameReader`\n\nSEE: [[org.apache.spark.sql.DataFrameWriter]]"
      },
      "DataObjectsExporterDataObject" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "DataObjectsExporterDataObject"
          },
          "config" : {
            "type" : "string",
            "description" : "optional location of the config to export, either a configuration file or a directory containing\nconfiguration files. Multiple locations can be given as a comma-separated list. If not defined,\nthe DataObjects registered in the current`InstanceRegistry` are exported."
          },
          "metadata" : {
            "$ref" : "#/definitions/Others/DataObjectMetadata",
            "description" : "Additional metadata for the DataObject"
          }
        },
        "title" : "DataObjectsExporterDataObject",
        "required" : [ "type" ],
        "additionalProperties" : false,
        "description" : "Exports a util `DataFrame` that contains properties and metadata extracted from all `DataObject`s that are registered in the current `InstanceRegistry`.\n\nAlternatively, it can export the properties and metadata of all `DataObject`s defined in config\nfiles. For this, the configuration \"config\" has to be set to the location of the config. Example:\n```\ndataObjects = {\n ...  \n dataobject-exporter {\n   type = DataObjectsExporterDataObject\n   config = path/to/myconfiguration.conf\n }\n ...  \n}\n```\n\n\nThe config value can point to a configuration file or a directory containing configuration files.\n\nSEE: Refer to `ConfigLoader.loadConfigFromFilesystem()` for details about the configuration\nloading."
      },
      "DebeziumCdcDataObject" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "DebeziumCdcDataObject"
          },
          "connectionId" : {
            "type" : "string",
            "description" : "optional id of`io.smartdatalake.workflow.connection.DebeziumConnection`"
          },
          "table" : {
            "$ref" : "#/definitions/Others/Table",
            "description" : "Source table to get change data from"
          },
          "schemaMin" : {
            "type" : "string",
            "description" : "An optional, minimal schema that a `DataObject`schema must have to pass schema validation.\n\nThe schema validation semantics are:\n- Schema A is valid in respect to a minimal schema B when B is a subset of A. This means: the whole column set of B is contained in the column set of A.  \n- A column of B is contained in A when A contains a column with equal name and data type.  \n- Column order is ignored.  \n- Column nullability is ignored.  \n- Duplicate columns in terms of name and data type are eliminated (set semantics).\n\nNote: This is mainly used by the functionality defined in `CanCreateDataFrame` and `CanWriteDataFrame`, that is,\nwhen reading or writing Spark data frames from/to the underlying data container. `io.smartdatalake.workflow.action.Action`s that work with files ignore the `schemaMin` attribute\nif it is defined.  \nAdditionally schemaMin can be used to define the schema used if there is no data or table doesn't yet exist."
          },
          "debeziumProperties" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "Properties for the specific Debezium connector"
          },
          "maxWaitTimeAfterLastBatchMilliSeconds" : {
            "type" : "integer",
            "description" : "(optional) Waiting time interval for debezium to finish (when a batch arrived, the engine waits the defined interval for completion), default = 10 seconds"
          },
          "metadata" : {
            "$ref" : "#/definitions/Others/DataObjectMetadata",
            "description" : "(optional) data object metadata"
          }
        },
        "title" : "DebeziumCdcDataObject",
        "required" : [ "type", "connectionId", "table" ],
        "additionalProperties" : false,
        "description" : "`DataObject` of type DebeziumCdcDataObject.  \nProvides details to access Change data over Debezium Engine.  \nNote that this implementation is not yet tested on production, and must therefore be seen as experimental.\n\nThe DataObject runs an embedded Debezium engine to read the change events of one database table. The returned\nDataFrame contains the columns of the changed row, followed by three SDLB metadata columns: `_change_type` (insert, update_preimage, update_postimage, delete or read), `_commit_timestamp` and `_change_ordinal` .  \nThe naming of the metadata columns follows the change data feed of Delta Lake and Iceberg, see `CdcChangeType`.  \nThe result can be historized without further configuration by using HistorizeAction, see its documentation.  \nDebezium's own envelope\nfields (before, after, source, op, ...) are not part of the result. Read offsets are stored in SDLBs state, so it\nsupports incremental output and should be used together with DataObjectStateIncrementalMode. Reading stops once no\nmore events arrive within `maxWaitTimeAfterLastBatchMilliSeconds` . If no event arrives at all, `schemaMin` is used\nas fallback schema for the empty DataFrame. Example:\n```\nconnections = {\n  dbz-postgres {\n    type = DebeziumConnection\n    dbEngine = \"postgresql\"\n    hostname = \"localhost\"\n    port = 5432\n    db = \"demo\"\n    authMode = { type = BasicAuthMode, user = \"###ENV#PG_USER###\", password = \"###ENV#PG_PWD###\" }\n  }\n}\ndataObjects = {\n  src-test {\n    type = DebeziumCdcDataObject\n    connectionId = dbz-postgres\n    table = { db = \"demo\", name = \"test\" }\n    debeziumProperties = {\n      \"plugin.name\" = \"pgoutput\"\n      \"schema.history.internal\" = \"io.debezium.storage.file.history.FileSchemaHistory\"\n      \"schema.history.internal.file.filename\" = \"/tmp/schemahistory.dat\"\n    }\n  }\n}\n```\n"
      },
      "DeltaLakeTableDataObject" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "DeltaLakeTableDataObject"
          },
          "path" : {
            "type" : "string",
            "description" : "Optional hadoop directory for this table. If path is not defined, table is handled as a managed table.  \nIf it doesn't contain scheme and authority, the connections pathPrefix is applied.  \nIf pathPrefix is not defined or doesn't define scheme and authority, default schema and authority is applied."
          },
          "partitions" : {
            "type" : "array",
            "items" : {
              "type" : "string"
            },
            "description" : "partition columns for this data object"
          },
          "options" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "Options for Delta Lake tables see:https://docs.delta.io/latest/delta-batch.html]]and[[org.apache.spark.sql.delta.DeltaOptions"
          },
          "schemaMin" : {
            "type" : "string",
            "description" : "An optional, minimal schema that this DataObject must have to pass schema validation on reading and writing.  \nDefine schema by using a DDL-formatted string, which is a comma separated list of field definitions, e.g., a INT, b STRING."
          },
          "table" : {
            "$ref" : "#/definitions/Others/Table",
            "description" : "DeltaLake table to be written by this output"
          },
          "constraints" : {
            "type" : "array",
            "items" : {
              "type" : "object",
              "properties" : {
                "name" : {
                  "type" : "string",
                  "description" : "name of the constraint"
                },
                "description" : {
                  "type" : "string",
                  "description" : "optional detailed description of the constraint"
                },
                "expression" : {
                  "type" : "string",
                  "description" : "SQL expression to evaluate on every row. The expressions return value should be a boolean.  \nIf it evaluates to true the constraint is validated successfully, otherwise it will throw an exception."
                },
                "errorMsgCols" : {
                  "type" : "array",
                  "items" : {
                    "type" : "string"
                  },
                  "description" : "Optional list of column names to add to error message.  \nNote that primary key columns are always included.  \nIf there is no primary key defined, by default all columns with simple datatype are included in the error message."
                }
              },
              "title" : "Constraint",
              "required" : [ "name", "expression" ],
              "additionalProperties" : false,
              "description" : "Definition of row-level constraint to validate."
            },
            "description" : "List of row-level`Constraint`s to enforce when writing to this data object."
          },
          "expectations" : {
            "type" : "array",
            "items" : {
              "oneOf" : [ {
                "$ref" : "#/definitions/Expectation/AvgCountPerPartitionExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/CountExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/SQLExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/SQLFractionExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/SQLQueryExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/UniqueKeyExpectation"
              } ]
            },
            "description" : "List of`Expectation`s to enforce when writing to this data object. Expectations are checks based on aggregates over all rows of a dataset."
          },
          "preReadSql" : {
            "type" : "string",
            "description" : "SQL-statement to be executed in exec phase before reading input table. If the catalog and/or schema are not\nexplicitly defined, the ones present in the configured \"table\" object are used."
          },
          "postReadSql" : {
            "type" : "string",
            "description" : "SQL-statement to be executed in exec phase after reading input table and before action is finished. If the catalog and/or schema are not\nexplicitly defined, the ones present in the configured \"table\" object are used."
          },
          "preWriteSql" : {
            "type" : "string",
            "description" : "SQL-statement to be executed in exec phase before writing output table. If the catalog and/or schema are not\nexplicitly defined, the ones present in the configured \"table\" object are used."
          },
          "postWriteSql" : {
            "type" : "string",
            "description" : "SQL-statement to be executed in exec phase after writing output table. If the catalog and/or schema are not\nexplicitly defined, the ones present in the configured \"table\" object are used."
          },
          "saveMode" : {
            "type" : "string",
            "description" : "`SDLSaveMode` to use when writing files, default is \"Overwrite\". Overwrite, Append and Merge are supported for now.",
            "enum" : [ "Merge", "OverwriteOptimized", "OverwritePreserveDirectories", "Ignore", "ErrorIfExists", "Append", "Overwrite" ]
          },
          "allowSchemaEvolution" : {
            "type" : "boolean",
            "description" : "If set to true schema evolution will automatically occur when writing to this DataObject with different schema, otherwise SDL will stop with error."
          },
          "retentionPeriod" : {
            "type" : "integer",
            "description" : "Optional delta lake retention threshold in hours. Files required by the table for reading versions younger than retentionPeriod will be preserved and the rest of them will be deleted."
          },
          "minVacuumInterval" : {
            "type" : "string",
            "description" : "Optional String to determine the minimum time interval between two vacuum operations. If the parameter is set,\nSDLB will look at the last vacuum-execution time in the table and compare it to the current time. If the parameter is not set or if a vacuum has never happened, it will vacuum the table.  \nThe interval must be provided as a String in ISO 8601 Duration format (e.g. \"P4DT12H\" for \"four days and twelve hours\")"
          },
          "connectionId" : {
            "type" : "string",
            "description" : "optional id of`io.smartdatalake.workflow.connection.HiveTableConnection`"
          },
          "expectedPartitionsCondition" : {
            "type" : "string",
            "description" : "Optional definition of partitions expected to exist.  \nDefine a Spark SQL expression that is evaluated against a`PartitionValues` instance and returns true or false\nDefault is to expect all partitions to exist."
          },
          "housekeepingMode" : {
            "oneOf" : [ {
              "$ref" : "#/definitions/HousekeepingMode/PartitionArchiveMode"
            }, {
              "$ref" : "#/definitions/HousekeepingMode/PartitionRetentionMode"
            } ],
            "description" : "Optional definition of a housekeeping mode applied after every write.  \nE.g. it can be used to clean up, archive and compact partitions.  \nSee HousekeepingMode for available implementations. Default is None."
          },
          "metadata" : {
            "$ref" : "#/definitions/Others/DataObjectMetadata",
            "description" : "metadata of the table. metadata.description is applied as table comment in the catalog\nby the DataObjectSchemaExporter, see also `io.smartdatalake.workflow.dataobject.generic.CanHandleCatalogMetadata`."
          }
        },
        "title" : "DeltaLakeTableDataObject",
        "required" : [ "type", "table" ],
        "additionalProperties" : false,
        "description" : "`DataObject` of type DeltaLakeTableDataObject.  \nProvides details to access Tables in delta format to an Action.\n\nDelta format maintains a transaction log in a separate _delta_log subfolder.  \nThe schema is registered in Metastore by DeltaLakeTableDataObject.\n\nThe following anomalies might occur:\n- table is registered in metastore but path does not exist → table is dropped from metastore\n- table is registered in metastore but path is empty → error is thrown. Delete the path to clean up\n- table is registered and path contains parquet files, but _delta_log subfolder is missing → path is converted to delta format\n- table is not registered but path contains parquet files and _delta_log subfolder → Table is registered\n- table is not registered but path contains parquet files without _delta_log subfolder → path is converted to delta format and table is registered\n- table is not registered and path does not exist → table is created on write\n\nDeltaLakeTableDataObject implements\n- `CanMergeDataFrame` by using DeltaTable.merge API.  \n- `CanEvolveSchema` by using mergeSchema option.  \n- Overwriting partitions is implemented by replaceWhere option in one transaction.\n\nUse this DataObject instead of a plain file DataObject if you need ACID transactions, merge (upsert) or schema\nevolution on a table. Example:\n```\ndataObjects = {\n  int-airports {\n    type = DeltaLakeTableDataObject\n    path = \"~{env.basedir}/int_airports\"\n    table = { db = \"default\", name = \"int_airports\", primaryKey = [ident] }\n    saveMode = Merge\n    allowSchemaEvolution = true\n    retentionPeriod = 168\n  }\n}\n```\n\n\nNOTE: DeltaLake needs the spark properties spark.sql.extensions=io.delta.sql.DeltaSparkSessionExtension and\nspark.sql.catalog.spark_catalog=org.apache.spark.sql.delta.catalog.DeltaCatalog. They are added automatically\nby DeltaLakeModulePlugin when SDLB creates the SparkSession (except on Databricks, where they are preset),\nso normally no manual configuration is needed.\n\nSEE: `io.smartdatalake.workflow.connection.DeltaLakeTableConnection` to share catalog, db and path prefix between\nmultiple DeltaLake DataObjects."
      },
      "ExcelFileDataObject" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "ExcelFileDataObject"
          },
          "path" : {
            "type" : "string",
            "description" : "The root path of the files that are handled by this DataObject.  \nFor most DataObjects this can be a directory or a specific file.\n\nIf it doesn't contain scheme and authority, the connections pathPrefix is applied. If pathPrefix is not\ndefined or doesn't define scheme and authority, default schema and authority is applied.\n\nOptionally defined partitions are appended with hadoop standard partition layout to this path."
          },
          "excelOptions" : {
            "type" : "object",
            "properties" : {
              "sheetName" : {
                "type" : "string",
                "description" : "Optional name of the Excel Sheet to read from/write to."
              },
              "numLinesToSkip" : {
                "type" : "integer",
                "description" : "Optional number of rows in the excel spreadsheet to skip before any data is read.  \nThis option must not be set for writing."
              },
              "startColumn" : {
                "type" : "string",
                "description" : "Optional first column in the specified Excel Sheet to read from (as string, e.g B).  \nThis option must not be set for writing."
              },
              "endColumn" : {
                "type" : "string",
                "description" : "Optional last column in the specified Excel Sheet to read from (as string, e.g. F)."
              },
              "rowLimit" : {
                "type" : "integer",
                "description" : "Optional limit of the number of rows being returned on read.  \nThis is applied after`numLinesToSkip` ."
              },
              "useHeader" : {
                "type" : "boolean",
                "description" : "If`true` , the first row of the excel sheet specifies the column names (default: true)."
              },
              "treatEmptyValuesAsNulls" : {
                "type" : "boolean",
                "description" : "Empty cells are parsed as`null` values (default: true)."
              },
              "inferSchema" : {
                "type" : "boolean",
                "description" : "Infer the schema of the excel sheet automatically (default: true)."
              },
              "timestampFormat" : {
                "type" : "string",
                "description" : "A format string specifying the format to use when writing timestamps (default: dd-MM-yyyy HH:mm:ss)."
              },
              "dateFormat" : {
                "type" : "string",
                "description" : "A format string specifying the format to use when writing dates."
              },
              "maxRowsInMemory" : {
                "type" : "integer",
                "description" : "The number of rows that are stored in memory.  \nIf set, a streaming reader is used which can help with big files."
              },
              "excerptSize" : {
                "type" : "integer",
                "description" : "Sample size for schema inference."
              }
            },
            "title" : "ExcelOptions",
            "additionalProperties" : false,
            "description" : "Settings for the underlying`org.apache.spark.sql.DataFrameReader]]and[[org.apache.spark.sql.DataFrameWriter`."
          },
          "partitions" : {
            "type" : "array",
            "items" : {
              "type" : "string"
            },
            "description" : "Definition of partition columns\n\nExample: `[dt]`"
          },
          "schema" : {
            "type" : "string",
            "description" : "An optional user-defined schema definition for this DataObject.  \nIf defined, any automatic schema inference is avoided.\n\nThe schema corresponds to the schema on write, it must not include optional columns on read, e.g. the filenameColumn for SparkFileDataObjects.  \nIf partition columns are missing, they will be added with type String.\n\nDefine the schema by using one of the schema providers below, default is DDL.  \nThe schema provider and its configuration value must be provided in the format <PROVIDERID>#<VALUE>.\n\nSchema Providers available are (see also `io.smartdatalake.util.misc.SchemaProviderType` ):\n\n- ddl: create the schema from a Spark ddl string, e.g. `ddl#a string, b array<struct<b1: string, b2: long>>, c struct<c1: string, c2: long>` .\n\n- ddlFile: read a Spark ddl definition from a file and create a schema, e.g. `ddlFile#abc/xyz.ddl` .\n\n- caseClass: convert a Scala Case Class to a schema using Spark encoders, e.g. `caseClass#com.sample.XyzClass` .\n\n- javaBean: convert a Java Bean to a schema using Spark encoders, e.g. `javaBean#com.sample.XyzClass` .\n\n- xsdFile: read an Xml Schema Definition file and create a schema, e.g. `xsdFile#abc/xyz.xsd` .  \nThe following parameters allow to customize the behavior: `xsdFile#<path-to-xsd-file>;<row-tag>;<maxRecursion:Int>;<jsonCompatibility:Boolean>` <row-tag>: configure the path of the element to extract from the xsd schema. Leave empty to extract the root.  \n<maxRecursion>: if xsd schema is recursive, this configures the number of levels to create in the schema.  \nDefault is 10 levels.  \n<jsonCompatibility>: In XML array elements are modeled with their own tag named with singular name.  \nIn JSON an array attribute has unnamed array entries, but the array attribute has a plural name.  \nIf true, the singular name of the array element in the XSD is converted to a plural name by adding an 's'\nin order to read corresponding json files.  \nDefault is false.\n\n- jsonSchemaFile: read a Json Schema file and create a schema, e.g. `jsonSchemaFile#abc/xyz.json` .  \nThe following parameters allow to customize the behavior: `jsonSchemaFile#<path-to-json-file>;<row-tag>;<strictTyping:Boolean>;<additionalPropertiesDefault:Boolean>` <row-tag>: configure the path of the element to extract from the json schema. Leave empty to extract the root.  \n<strictTyping>: if true\nunion types (oneOf) are merged if rational, otherwise they are simply mapped to StringType;\nadditional properties are ignored, otherwise the corresponding schema object is mapped to MapType(String,String).  \nDefault is strictTyping=false.  \n<additionalPropertiesDefault>: Set to true or false.  \nThis is used as default value for 'additionalProperties'-field if it is missing in a schema with type='object'.  \nDefault value is additionalPropertiesDefault=true, as this is conform with the specification.\n\n- avroSchemaFile: read an Avro Schema file and create a schema, e.g. `avroSchemaFile#abc/xyz.avsc` .  \nThe following parameters allow to customize the behavior: `avroSchemaFile#<path-to-avsc-file>;<row-tag>` <row-tag>: configure the path of the element to extract from the avro schema. Leave empty to extract the root.\n\n- openApi: read Schema from OpenApi specification operation, e.g. `openApi#https://swagger.myswitzerland.io;getDestinations;opendata.json` .  \nThe following parameters allow to customize the behaviour: `openApi#<baseUrl>;<operationId>;[<apiDocsPath>;][<responseContentType>]`\n\nNote that all schema files are configured as Hadoop path. The custom prefix 'cp' can be used to read schema files\nfrom the classpath, e.g. `xsdFile#cp:/xyz.xsd` ."
          },
          "schemaMin" : {
            "type" : "string",
            "description" : "An optional, minimal schema that a `DataObject`schema must have to pass schema validation.\n\nThe schema validation semantics are:\n- Schema A is valid in respect to a minimal schema B when B is a subset of A. This means: the whole column set of B is contained in the column set of A.  \n- A column of B is contained in A when A contains a column with equal name and data type.  \n- Column order is ignored.  \n- Column nullability is ignored.  \n- Duplicate columns in terms of name and data type are eliminated (set semantics).\n\nNote: This is mainly used by the functionality defined in `CanCreateDataFrame` and `CanWriteDataFrame`, that is,\nwhen reading or writing Spark data frames from/to the underlying data container. `io.smartdatalake.workflow.action.Action`s that work with files ignore the `schemaMin` attribute\nif it is defined.  \nAdditionally schemaMin can be used to define the schema used if there is no data or table doesn't yet exist."
          },
          "saveMode" : {
            "type" : "string",
            "description" : "Overwrite or Append new data.  \nWhen writing partitioned data, this applies only to partitions concerned.",
            "enum" : [ "Merge", "OverwriteOptimized", "OverwritePreserveDirectories", "Ignore", "ErrorIfExists", "Append", "Overwrite" ]
          },
          "sparkRepartition" : {
            "type" : "object",
            "properties" : {
              "numberOfTasksPerPartition" : {
                "type" : "integer",
                "description" : "Number of Spark tasks to create per partition before writing to DataObject by repartitioning the DataFrame.  \nThis controls how many files are created in each Hadoop partition."
              },
              "keyCols" : {
                "type" : "array",
                "items" : {
                  "type" : "string"
                },
                "description" : "Optional key columns to distribute records over Spark tasks inside a Hadoop partition.  \nIf DataObject has Hadoop partitions defined, keyCols must be defined."
              },
              "sortCols" : {
                "type" : "array",
                "items" : {
                  "type" : "string"
                },
                "description" : "Optional columns to sort records inside files created."
              },
              "filename" : {
                "type" : "string",
                "description" : "Option filename to rename target file(s). If numberOfTasksPerPartition is greater than 1,\nmultiple files can exist in a directory and a number is inserted into the filename after the first '.'.  \nExample: filename=data.csv → files created are data.1.csv, data.2.csv, ..."
              }
            },
            "title" : "SparkRepartitionDef",
            "required" : [ "numberOfTasksPerPartition" ],
            "additionalProperties" : false,
            "description" : "Definition of repartition operation before writing DataFrame with Spark to Hadoop."
          },
          "connectionId" : {
            "type" : "string",
            "description" : "Return the connection id.\n\nConnection defines path prefix (scheme, authority, base path) and ACL's in central location."
          },
          "filenameColumn" : {
            "type" : "string",
            "description" : "The name of the (optional) additional column containing the source filename"
          },
          "expectedPartitionsCondition" : {
            "type" : "string",
            "description" : "Definition of partitions that are expected to exists.  \nThis is used to validate that partitions being read exists and don't return no data.  \nDefine a Spark SQL expression that is evaluated against a `PartitionValues` instance and returns true or false\n\nExample: \"elements['yourColName'] > 2017\"\n\nIf empty (default) all partition are expected to exists.\n\nOTHERTAG: true if partition is expected to exist."
          },
          "housekeepingMode" : {
            "oneOf" : [ {
              "$ref" : "#/definitions/HousekeepingMode/PartitionArchiveMode"
            }, {
              "$ref" : "#/definitions/HousekeepingMode/PartitionRetentionMode"
            } ],
            "description" : "Configure a housekeeping mode to e.g cleanup, archive and compact partitions.\n\nDefault is None."
          },
          "constraints" : {
            "type" : "array",
            "items" : {
              "type" : "object",
              "properties" : {
                "name" : {
                  "type" : "string",
                  "description" : "name of the constraint"
                },
                "description" : {
                  "type" : "string",
                  "description" : "optional detailed description of the constraint"
                },
                "expression" : {
                  "type" : "string",
                  "description" : "SQL expression to evaluate on every row. The expressions return value should be a boolean.  \nIf it evaluates to true the constraint is validated successfully, otherwise it will throw an exception."
                },
                "errorMsgCols" : {
                  "type" : "array",
                  "items" : {
                    "type" : "string"
                  },
                  "description" : "Optional list of column names to add to error message.  \nNote that primary key columns are always included.  \nIf there is no primary key defined, by default all columns with simple datatype are included in the error message."
                }
              },
              "title" : "Constraint",
              "required" : [ "name", "expression" ],
              "additionalProperties" : false,
              "description" : "Definition of row-level constraint to validate."
            },
            "description" : "List of constraint definitions to validate on write, see `Constraint` for details.  \nConstraints are expressions defined on row-level and validated during evaluation of the DataFrame.  \nIf validation fails an exception is thrown and further processing is stopped.  \nNote that this is done while evaluating the DataFrame when writing to the DataObject. It doesn't need a separate action on the DataFrame.  \nIf a constraint validation for a row fails, it will throw an exception and abort writing to the DataObject."
          },
          "expectations" : {
            "type" : "array",
            "items" : {
              "oneOf" : [ {
                "$ref" : "#/definitions/Expectation/AvgCountPerPartitionExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/CountExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/SQLExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/SQLFractionExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/SQLQueryExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/UniqueKeyExpectation"
              } ]
            },
            "description" : "List expectation definitions to evaluate on write, see `Expectation` for details.  \nExpectations are aggregation expressions defined on dataset-level and evaluated on every write.  \nBy default their result is logged with level info (ok) and error (failed), but this can be customized to be logged as warning.  \nIn case of failed expectations logged as error, an exceptions is thrown and further processing is stopped.  \nNote that the exception is thrown after writing to the DataObject is finished.\n\nThe following expectations names are reserved to create default metrics and should not be used:\n- count"
          },
          "metadata" : {
            "$ref" : "#/definitions/Others/DataObjectMetadata",
            "description" : "Additional metadata for the DataObject"
          }
        },
        "title" : "ExcelFileDataObject",
        "required" : [ "type", "path" ],
        "additionalProperties" : false,
        "description" : "A `DataObject` backed by an Microsoft Excel data source.\n\nIt manages read and write access and configurations required for `io.smartdatalake.workflow.action.Action`s to\nwork on Microsoft Excel (.xslx) formatted files.\n\nReading and writing details are delegated to Apache Spark `org.apache.spark.sql.DataFrameReader` and `org.apache.spark.sql.DataFrameWriter` respectively. The reader and writer implementation is provided by the [Crealytics spark-excel](https://github.com/crealytics/spark-excel) project.\n\nRead Schema:\n\nWhen `useHeader` is set to true (default), the reader will use the first row of the Excel sheet as column names for\nthe schema and not include the first row as data values. Otherwise the column names are taken from the schema.  \nIf the schema is not provided or inferred, then each column name is defined as \"_c#\" where \"#\" is the column index.\n\nWhen a data object schema is provided, it is used as the schema for the DataFrame. Otherwise if `inferSchema` is\nenabled (default), then the data types of the columns are inferred based on the first `excerptSize` rows\n(excluding the first).  \nWhen no schema is provided and `inferSchema` is disabled, all columns are assumed to be of string type.\n\nColumn names read from the header are cleaned up on read: blanks, dashes and dots become underscores, all other\nnon-alphanumeric characters are dropped and camel case is converted to lower case with underscores. Example:\n```\ndataObjects = {\n  ext-airports {\n    type = ExcelFileDataObject\n    path = \"~{env.basedir}/ext_airports\"\n    excelOptions {\n      sheetName = \"airports\"\n      maxRowsInMemory = 20000\n    }\n  }\n}\n```\n\n\nNOTE: The Excel data source cannot read a whole directory at once, so files are read one by one. `numLinesToSkip` and `startColumn` are read-only options and must not be set when writing."
      },
      "IcebergTableDataObject" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "IcebergTableDataObject"
          },
          "path" : {
            "type" : "string",
            "description" : "hadoop directory for this table. If it doesn't contain scheme and authority, the connections pathPrefix is applied.  \nIf pathPrefix is not defined or doesn't define scheme and authority, default schema and authority is applied.  \nIf Iceberg table is defined on a hadoop catalog, path must be None as it is defined through the catalog directory structure."
          },
          "partitions" : {
            "type" : "array",
            "items" : {
              "type" : "string"
            },
            "description" : "Definition of partition columns\n\nExample: `[dt]`"
          },
          "options" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "Options for Iceberg tables see:https://iceberg.apache.org/docs/latest/configuration/"
          },
          "schemaMin" : {
            "type" : "string",
            "description" : "An optional, minimal schema that a `DataObject`schema must have to pass schema validation.\n\nThe schema validation semantics are:\n- Schema A is valid in respect to a minimal schema B when B is a subset of A. This means: the whole column set of B is contained in the column set of A.  \n- A column of B is contained in A when A contains a column with equal name and data type.  \n- Column order is ignored.  \n- Column nullability is ignored.  \n- Duplicate columns in terms of name and data type are eliminated (set semantics).\n\nNote: This is mainly used by the functionality defined in `CanCreateDataFrame` and `CanWriteDataFrame`, that is,\nwhen reading or writing Spark data frames from/to the underlying data container. `io.smartdatalake.workflow.action.Action`s that work with files ignore the `schemaMin` attribute\nif it is defined.  \nAdditionally schemaMin can be used to define the schema used if there is no data or table doesn't yet exist."
          },
          "table" : {
            "$ref" : "#/definitions/Others/Table",
            "description" : "Iceberg table to be written by this output"
          },
          "constraints" : {
            "type" : "array",
            "items" : {
              "type" : "object",
              "properties" : {
                "name" : {
                  "type" : "string",
                  "description" : "name of the constraint"
                },
                "description" : {
                  "type" : "string",
                  "description" : "optional detailed description of the constraint"
                },
                "expression" : {
                  "type" : "string",
                  "description" : "SQL expression to evaluate on every row. The expressions return value should be a boolean.  \nIf it evaluates to true the constraint is validated successfully, otherwise it will throw an exception."
                },
                "errorMsgCols" : {
                  "type" : "array",
                  "items" : {
                    "type" : "string"
                  },
                  "description" : "Optional list of column names to add to error message.  \nNote that primary key columns are always included.  \nIf there is no primary key defined, by default all columns with simple datatype are included in the error message."
                }
              },
              "title" : "Constraint",
              "required" : [ "name", "expression" ],
              "additionalProperties" : false,
              "description" : "Definition of row-level constraint to validate."
            },
            "description" : "List of constraint definitions to validate on write, see `Constraint` for details.  \nConstraints are expressions defined on row-level and validated during evaluation of the DataFrame.  \nIf validation fails an exception is thrown and further processing is stopped.  \nNote that this is done while evaluating the DataFrame when writing to the DataObject. It doesn't need a separate action on the DataFrame.  \nIf a constraint validation for a row fails, it will throw an exception and abort writing to the DataObject."
          },
          "expectations" : {
            "type" : "array",
            "items" : {
              "oneOf" : [ {
                "$ref" : "#/definitions/Expectation/AvgCountPerPartitionExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/CountExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/SQLExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/SQLFractionExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/SQLQueryExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/UniqueKeyExpectation"
              } ]
            },
            "description" : "List expectation definitions to evaluate on write, see `Expectation` for details.  \nExpectations are aggregation expressions defined on dataset-level and evaluated on every write.  \nBy default their result is logged with level info (ok) and error (failed), but this can be customized to be logged as warning.  \nIn case of failed expectations logged as error, an exceptions is thrown and further processing is stopped.  \nNote that the exception is thrown after writing to the DataObject is finished.\n\nThe following expectations names are reserved to create default metrics and should not be used:\n- count"
          },
          "saveMode" : {
            "type" : "string",
            "description" : "`SDLSaveMode` to use when writing files, default is \"Overwrite\". Overwrite, Append and Merge are supported for now.",
            "enum" : [ "Merge", "OverwriteOptimized", "OverwritePreserveDirectories", "Ignore", "ErrorIfExists", "Append", "Overwrite" ]
          },
          "allowSchemaEvolution" : {
            "type" : "boolean",
            "description" : "If set to true schema evolution will automatically occur when writing to this DataObject with different schema, otherwise SDL will stop with error."
          },
          "historyRetentionPeriod" : {
            "type" : "integer",
            "description" : "Optional Iceberg retention threshold in hours. Files required by the table for reading versions younger than retentionPeriod will be preserved and the rest of them will be deleted."
          },
          "connectionId" : {
            "type" : "string",
            "description" : "optional id of`IcebergTableConnection`"
          },
          "expectedPartitionsCondition" : {
            "type" : "string",
            "description" : "Definition of partitions that are expected to exists.  \nThis is used to validate that partitions being read exists and don't return no data.  \nDefine a Spark SQL expression that is evaluated against a `PartitionValues` instance and returns true or false\n\nExample: \"elements['yourColName'] > 2017\"\n\nIf empty (default) all partition are expected to exists.\n\nOTHERTAG: true if partition is expected to exist."
          },
          "housekeepingMode" : {
            "oneOf" : [ {
              "$ref" : "#/definitions/HousekeepingMode/PartitionArchiveMode"
            }, {
              "$ref" : "#/definitions/HousekeepingMode/PartitionRetentionMode"
            } ],
            "description" : "Configure a housekeeping mode to e.g cleanup, archive and compact partitions.\n\nDefault is None."
          },
          "metadata" : {
            "$ref" : "#/definitions/Others/DataObjectMetadata",
            "description" : "metadata"
          },
          "preReadSql" : {
            "type" : "string",
            "description" : "SQL-statement to be executed in exec phase before reading input table. If the catalog and/or schema are not\nexplicitly defined, the ones present in the configured \"table\" object are used."
          },
          "postReadSql" : {
            "type" : "string",
            "description" : "SQL-statement to be executed in exec phase after reading input table and before action is finished. If the catalog and/or schema are not\nexplicitly defined, the ones present in the configured \"table\" object are used."
          },
          "preWriteSql" : {
            "type" : "string",
            "description" : "SQL-statement to be executed in exec phase before writing output table. If the catalog and/or schema are not\nexplicitly defined, the ones present in the configured \"table\" object are used."
          },
          "postWriteSql" : {
            "type" : "string",
            "description" : "SQL-statement to be executed in exec phase after writing output table. If the catalog and/or schema are not\nexplicitly defined, the ones present in the configured \"table\" object are used."
          }
        },
        "title" : "IcebergTableDataObject",
        "required" : [ "type", "table" ],
        "additionalProperties" : false,
        "description" : "`DataObject` of type IcebergTableDataObject.  \nProvides details to access Tables in Iceberg format to an Action.\n\nIceberg format maintains a transaction log in a separate metadata subfolder.  \nThe schema is registered in Metastore by IcebergTableDataObject.  \nFor this either the default spark catalog must be wrapped in an IcebergSessionCatalog,\nor an additional IcebergCatalog has to be configured. See also https://iceberg.apache.org/docs/latest/getting-started/.\n\nThe following anomalies between metastore and filesystem might occur:\n- table is registered in metastore but path does not exist → table is dropped from metastore\n- table is registered in metastore but path is empty → error is thrown. Delete the path manually to clean up.  \n- table is registered and path contains parquet files, but metadata subfolder is missing → path is converted to Iceberg format\n- table is not registered but path contains parquet files and metadata subfolder → Table is registered in catalog\n- table is not registered but path contains parquet files without metadata subfolder → path is converted to Iceberg format and table is registered in catalog\n- table is not registered and path does not exist → table is created on write\n\nIcebergTableDataObject implements\n- `CanMergeDataFrame` by writing a temp table and using one SQL merge statement.  \n- `CanEvolveSchema` by using internal Iceberg API.  \n- Overwriting partitions is implemented by using DataFrameWriterV2.overwrite(condition) API in one transaction.\n\nPick this DataObject over a plain file DataObject if you need ACID transactions, merge (upsert), schema evolution\nor snapshot expiration on a table, and over DeltaLakeTableDataObject if your platform standardizes on Iceberg.  \nNote that `partitions` are always mapped to Iceberg identity transforms; other Iceberg partition transforms\n(bucket, truncate, days, ...) cannot be configured from SDLB. Example:\n```\ndataObjects = {\n  int-airports {\n    type = IcebergTableDataObject\n    path = \"~{env.basedir}/int_airports\"\n    table = { db = \"default\", name = \"int_airports\", primaryKey = [ident] }\n    partitions = [country]\n    saveMode = Merge\n    allowSchemaEvolution = true\n  }\n}\n```\n\n\nNOTE: If the Iceberg table is defined on a hadoop catalog, `path` must not be set as it is derived from the catalog\ndirectory structure.\n\nNOTE: The engine specific implementation is provided by module sdl-iceberg (classic Spark) or sdl-sparkconnect\n(Spark Connect), see `IcebergTableEngine`.\n\nSEE: `IcebergTableConnection` to share catalog, db and path prefix between multiple Iceberg DataObjects."
      },
      "JdbcTableDataObject" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "JdbcTableDataObject"
          },
          "createSql" : {
            "type" : "string",
            "description" : "DDL-statement to be executed in prepare phase, using output jdbc connection.  \nNote that it is also possible to let Spark create the table in Init-phase. See jdbcOptions to customize column data types for auto-created DDL-statement."
          },
          "preReadSql" : {
            "type" : "string",
            "description" : "SQL-statement to be executed in exec phase before reading input table, using input jdbc connection.  \nUse tokens with syntax %{<spark sql expression>} to substitute with values from`DefaultExpressionData`."
          },
          "postReadSql" : {
            "type" : "string",
            "description" : "SQL-statement to be executed in exec phase after reading input table and before action is finished, using input jdbc connection\nUse tokens with syntax %{<spark sql expression>} to substitute with values from`DefaultExpressionData`."
          },
          "preWriteSql" : {
            "type" : "string",
            "description" : "SQL-statement to be executed in exec phase before writing output table, using output jdbc connection\nUse tokens with syntax %{<spark sql expression>} to substitute with values from`DefaultExpressionData`."
          },
          "postWriteSql" : {
            "type" : "string",
            "description" : "SQL-statement to be executed in exec phase after writing output table, using output jdbc connection\nUse tokens with syntax %{<spark sql expression>} to substitute with values from`DefaultExpressionData`."
          },
          "schemaMin" : {
            "type" : "string",
            "description" : "An optional, minimal schema that this DataObject must have to pass schema validation on reading and writing.  \nDefine schema by using a DDL-formatted string, which is a comma separated list of field definitions, e.g., a INT, b STRING."
          },
          "table" : {
            "$ref" : "#/definitions/Others/Table",
            "description" : "The jdbc table to be read"
          },
          "constraints" : {
            "type" : "array",
            "items" : {
              "type" : "object",
              "properties" : {
                "name" : {
                  "type" : "string",
                  "description" : "name of the constraint"
                },
                "description" : {
                  "type" : "string",
                  "description" : "optional detailed description of the constraint"
                },
                "expression" : {
                  "type" : "string",
                  "description" : "SQL expression to evaluate on every row. The expressions return value should be a boolean.  \nIf it evaluates to true the constraint is validated successfully, otherwise it will throw an exception."
                },
                "errorMsgCols" : {
                  "type" : "array",
                  "items" : {
                    "type" : "string"
                  },
                  "description" : "Optional list of column names to add to error message.  \nNote that primary key columns are always included.  \nIf there is no primary key defined, by default all columns with simple datatype are included in the error message."
                }
              },
              "title" : "Constraint",
              "required" : [ "name", "expression" ],
              "additionalProperties" : false,
              "description" : "Definition of row-level constraint to validate."
            },
            "description" : "List of row-level`Constraint`s to enforce when writing to this data object."
          },
          "expectations" : {
            "type" : "array",
            "items" : {
              "oneOf" : [ {
                "$ref" : "#/definitions/Expectation/AvgCountPerPartitionExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/CountExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/SQLExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/SQLFractionExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/SQLQueryExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/UniqueKeyExpectation"
              } ]
            },
            "description" : "List of`Expectation`s to enforce when writing to this data object. Expectations are checks based on aggregates over all rows of a dataset."
          },
          "jdbcFetchSize" : {
            "type" : "integer",
            "description" : "Number of rows to be fetched together by the Jdbc driver"
          },
          "saveMode" : {
            "type" : "string",
            "description" : "[[SDLSaveMode]]to use when writing table, default is \"Overwrite\". Only \"Append\", \"Overwrite\" and \"Merge\" are supported.  \n\"Merge\" requires a primary key to be defined on`table` .",
            "enum" : [ "Merge", "OverwriteOptimized", "OverwritePreserveDirectories", "Ignore", "ErrorIfExists", "Append", "Overwrite" ]
          },
          "allowSchemaEvolution" : {
            "type" : "boolean",
            "description" : "If set to true schema evolution will automatically occur when writing to this DataObject with different schema, otherwise SDL will stop with error."
          },
          "connectionId" : {
            "type" : "string",
            "description" : "Id of JdbcConnection configuration"
          },
          "jdbcOptions" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "Any jdbc options according tohttps://spark.apache.org/docs/latest/sql-data-sources-jdbc.html.  \nNote that some options above set and override some of these options explicitly.  \nUse \"createTableOptions\" and \"createTableColumnTypes\" to control automatic creating of database tables."
          },
          "virtualPartitions" : {
            "type" : "array",
            "items" : {
              "type" : "string"
            },
            "description" : "Virtual partition columns. Note that this doesn't need to be the same as the database partition\ncolumns for this table. But it is important that there is an index on these columns to efficiently\nlist existing \"partitions\"."
          },
          "expectedPartitionsCondition" : {
            "type" : "string",
            "description" : "Optional definition of partitions expected to exist.  \nDefine a Spark SQL expression that is evaluated against a`PartitionValues` instance and returns true or false\nDefault is to expect all partitions to exist."
          },
          "incrementalOutputExpr" : {
            "type" : "string",
            "description" : "Optional expression to use for creating incremental output with DataObjectStateIncrementalMode.  \nThe expression is used to get the high-water-mark for the incremental update state.  \nNormally this can be just a column name, e.g. an id or updated timestamp which is continually increasing."
          },
          "housekeepingMode" : {
            "oneOf" : [ {
              "$ref" : "#/definitions/HousekeepingMode/PartitionArchiveMode"
            }, {
              "$ref" : "#/definitions/HousekeepingMode/PartitionRetentionMode"
            } ],
            "description" : "Optional definition of a housekeeping mode applied after every write.  \nE.g. it can be used to clean up or archive virtual partitions, see[[HousekeepingMode]].  \nNote that housekeeping works on`virtualPartitions` , so these need to be defined.  \nDefault is None."
          },
          "metadata" : {
            "$ref" : "#/definitions/Others/DataObjectMetadata",
            "description" : "Additional metadata for the DataObject"
          }
        },
        "title" : "JdbcTableDataObject",
        "required" : [ "type", "table", "connectionId" ],
        "additionalProperties" : false,
        "description" : "`DataObject` of type JDBC.  \nProvides details for an action to read and write tables in a database through JDBC.\n\nNote that Sparks distributed processing can not directly write to a JDBC table in one transaction.  \nJdbcTableDataObject implements this in one transaction by writing to a temporary-table with Spark,\nthen using a separate \"insert into ... select\" SQL statement to copy data into the final table.\n\nJdbcTableDataObject implements\n- `CanMergeDataFrame` by writing a temp table and using one SQL merge statement.  \n- `CanEvolveSchema` by generating corresponding alter table DDL statements.  \n- Overwriting partitions is implemented by using SQL delete and insert statement embedded in one transaction.\n\nRequires a `JdbcTableConnection` referenced by `connectionId` , which holds url, driver and credentials. Example:\n```\nconnections = {\n  jdbc-dwh {\n    type = JdbcTableConnection\n    url = \"jdbc:postgresql://dwh:5432/mydb\"\n    driver = org.postgresql.Driver\n  }\n}\ndataObjects = {\n  int-airports {\n    type = JdbcTableDataObject\n    connectionId = jdbc-dwh\n    table = { db = public, name = airports, primaryKey = [ident] }\n    saveMode = Merge\n  }\n}\n```\n"
      },
      "JsonFileDataObject" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "JsonFileDataObject"
          },
          "path" : {
            "type" : "string",
            "description" : "The root path of the files that are handled by this DataObject.  \nFor most DataObjects this can be a directory or a specific file.\n\nIf it doesn't contain scheme and authority, the connections pathPrefix is applied. If pathPrefix is not\ndefined or doesn't define scheme and authority, default schema and authority is applied.\n\nOptionally defined partitions are appended with hadoop standard partition layout to this path."
          },
          "jsonOptions" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "Settings for the underlying `org.apache.spark.sql.DataFrameReader` and `org.apache.spark.sql.DataFrameWriter`."
          },
          "partitions" : {
            "type" : "array",
            "items" : {
              "type" : "string"
            },
            "description" : "Definition of partition columns\n\nExample: `[dt]`"
          },
          "schema" : {
            "type" : "string",
            "description" : "An optional user-defined schema definition for this DataObject.  \nIf defined, any automatic schema inference is avoided.\n\nThe schema corresponds to the schema on write, it must not include optional columns on read, e.g. the filenameColumn for SparkFileDataObjects.  \nIf partition columns are missing, they will be added with type String.\n\nDefine the schema by using one of the schema providers below, default is DDL.  \nThe schema provider and its configuration value must be provided in the format <PROVIDERID>#<VALUE>.\n\nSchema Providers available are (see also `io.smartdatalake.util.misc.SchemaProviderType` ):\n\n- ddl: create the schema from a Spark ddl string, e.g. `ddl#a string, b array<struct<b1: string, b2: long>>, c struct<c1: string, c2: long>` .\n\n- ddlFile: read a Spark ddl definition from a file and create a schema, e.g. `ddlFile#abc/xyz.ddl` .\n\n- caseClass: convert a Scala Case Class to a schema using Spark encoders, e.g. `caseClass#com.sample.XyzClass` .\n\n- javaBean: convert a Java Bean to a schema using Spark encoders, e.g. `javaBean#com.sample.XyzClass` .\n\n- xsdFile: read an Xml Schema Definition file and create a schema, e.g. `xsdFile#abc/xyz.xsd` .  \nThe following parameters allow to customize the behavior: `xsdFile#<path-to-xsd-file>;<row-tag>;<maxRecursion:Int>;<jsonCompatibility:Boolean>` <row-tag>: configure the path of the element to extract from the xsd schema. Leave empty to extract the root.  \n<maxRecursion>: if xsd schema is recursive, this configures the number of levels to create in the schema.  \nDefault is 10 levels.  \n<jsonCompatibility>: In XML array elements are modeled with their own tag named with singular name.  \nIn JSON an array attribute has unnamed array entries, but the array attribute has a plural name.  \nIf true, the singular name of the array element in the XSD is converted to a plural name by adding an 's'\nin order to read corresponding json files.  \nDefault is false.\n\n- jsonSchemaFile: read a Json Schema file and create a schema, e.g. `jsonSchemaFile#abc/xyz.json` .  \nThe following parameters allow to customize the behavior: `jsonSchemaFile#<path-to-json-file>;<row-tag>;<strictTyping:Boolean>;<additionalPropertiesDefault:Boolean>` <row-tag>: configure the path of the element to extract from the json schema. Leave empty to extract the root.  \n<strictTyping>: if true\nunion types (oneOf) are merged if rational, otherwise they are simply mapped to StringType;\nadditional properties are ignored, otherwise the corresponding schema object is mapped to MapType(String,String).  \nDefault is strictTyping=false.  \n<additionalPropertiesDefault>: Set to true or false.  \nThis is used as default value for 'additionalProperties'-field if it is missing in a schema with type='object'.  \nDefault value is additionalPropertiesDefault=true, as this is conform with the specification.\n\n- avroSchemaFile: read an Avro Schema file and create a schema, e.g. `avroSchemaFile#abc/xyz.avsc` .  \nThe following parameters allow to customize the behavior: `avroSchemaFile#<path-to-avsc-file>;<row-tag>` <row-tag>: configure the path of the element to extract from the avro schema. Leave empty to extract the root.\n\n- openApi: read Schema from OpenApi specification operation, e.g. `openApi#https://swagger.myswitzerland.io;getDestinations;opendata.json` .  \nThe following parameters allow to customize the behaviour: `openApi#<baseUrl>;<operationId>;[<apiDocsPath>;][<responseContentType>]`\n\nNote that all schema files are configured as Hadoop path. The custom prefix 'cp' can be used to read schema files\nfrom the classpath, e.g. `xsdFile#cp:/xyz.xsd` ."
          },
          "schemaMin" : {
            "type" : "string",
            "description" : "An optional, minimal schema that a `DataObject`schema must have to pass schema validation.\n\nThe schema validation semantics are:\n- Schema A is valid in respect to a minimal schema B when B is a subset of A. This means: the whole column set of B is contained in the column set of A.  \n- A column of B is contained in A when A contains a column with equal name and data type.  \n- Column order is ignored.  \n- Column nullability is ignored.  \n- Duplicate columns in terms of name and data type are eliminated (set semantics).\n\nNote: This is mainly used by the functionality defined in `CanCreateDataFrame` and `CanWriteDataFrame`, that is,\nwhen reading or writing Spark data frames from/to the underlying data container. `io.smartdatalake.workflow.action.Action`s that work with files ignore the `schemaMin` attribute\nif it is defined.  \nAdditionally schemaMin can be used to define the schema used if there is no data or table doesn't yet exist."
          },
          "saveMode" : {
            "type" : "string",
            "description" : "Overwrite or Append new data.  \nWhen writing partitioned data, this applies only to partitions concerned.",
            "enum" : [ "Merge", "OverwriteOptimized", "OverwritePreserveDirectories", "Ignore", "ErrorIfExists", "Append", "Overwrite" ]
          },
          "sparkRepartition" : {
            "type" : "object",
            "properties" : {
              "numberOfTasksPerPartition" : {
                "type" : "integer",
                "description" : "Number of Spark tasks to create per partition before writing to DataObject by repartitioning the DataFrame.  \nThis controls how many files are created in each Hadoop partition."
              },
              "keyCols" : {
                "type" : "array",
                "items" : {
                  "type" : "string"
                },
                "description" : "Optional key columns to distribute records over Spark tasks inside a Hadoop partition.  \nIf DataObject has Hadoop partitions defined, keyCols must be defined."
              },
              "sortCols" : {
                "type" : "array",
                "items" : {
                  "type" : "string"
                },
                "description" : "Optional columns to sort records inside files created."
              },
              "filename" : {
                "type" : "string",
                "description" : "Option filename to rename target file(s). If numberOfTasksPerPartition is greater than 1,\nmultiple files can exist in a directory and a number is inserted into the filename after the first '.'.  \nExample: filename=data.csv → files created are data.1.csv, data.2.csv, ..."
              }
            },
            "title" : "SparkRepartitionDef",
            "required" : [ "numberOfTasksPerPartition" ],
            "additionalProperties" : false,
            "description" : "Definition of repartition operation before writing DataFrame with Spark to Hadoop."
          },
          "stringify" : {
            "type" : "boolean",
            "description" : "Set the data type for all values to string. Use action/transformers instead.",
            "deprecated" : true
          },
          "connectionId" : {
            "type" : "string",
            "description" : "Return the connection id.\n\nConnection defines path prefix (scheme, authority, base path) and ACL's in central location."
          },
          "filenameColumn" : {
            "type" : "string",
            "description" : "The name of the (optional) additional column containing the source filename"
          },
          "expectedPartitionsCondition" : {
            "type" : "string",
            "description" : "Definition of partitions that are expected to exists.  \nThis is used to validate that partitions being read exists and don't return no data.  \nDefine a Spark SQL expression that is evaluated against a `PartitionValues` instance and returns true or false\n\nExample: \"elements['yourColName'] > 2017\"\n\nIf empty (default) all partition are expected to exists.\n\nOTHERTAG: true if partition is expected to exist."
          },
          "housekeepingMode" : {
            "oneOf" : [ {
              "$ref" : "#/definitions/HousekeepingMode/PartitionArchiveMode"
            }, {
              "$ref" : "#/definitions/HousekeepingMode/PartitionRetentionMode"
            } ],
            "description" : "Configure a housekeeping mode to e.g cleanup, archive and compact partitions.\n\nDefault is None."
          },
          "constraints" : {
            "type" : "array",
            "items" : {
              "type" : "object",
              "properties" : {
                "name" : {
                  "type" : "string",
                  "description" : "name of the constraint"
                },
                "description" : {
                  "type" : "string",
                  "description" : "optional detailed description of the constraint"
                },
                "expression" : {
                  "type" : "string",
                  "description" : "SQL expression to evaluate on every row. The expressions return value should be a boolean.  \nIf it evaluates to true the constraint is validated successfully, otherwise it will throw an exception."
                },
                "errorMsgCols" : {
                  "type" : "array",
                  "items" : {
                    "type" : "string"
                  },
                  "description" : "Optional list of column names to add to error message.  \nNote that primary key columns are always included.  \nIf there is no primary key defined, by default all columns with simple datatype are included in the error message."
                }
              },
              "title" : "Constraint",
              "required" : [ "name", "expression" ],
              "additionalProperties" : false,
              "description" : "Definition of row-level constraint to validate."
            },
            "description" : "List of constraint definitions to validate on write, see `Constraint` for details.  \nConstraints are expressions defined on row-level and validated during evaluation of the DataFrame.  \nIf validation fails an exception is thrown and further processing is stopped.  \nNote that this is done while evaluating the DataFrame when writing to the DataObject. It doesn't need a separate action on the DataFrame.  \nIf a constraint validation for a row fails, it will throw an exception and abort writing to the DataObject."
          },
          "expectations" : {
            "type" : "array",
            "items" : {
              "oneOf" : [ {
                "$ref" : "#/definitions/Expectation/AvgCountPerPartitionExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/CountExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/SQLExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/SQLFractionExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/SQLQueryExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/UniqueKeyExpectation"
              } ]
            },
            "description" : "List expectation definitions to evaluate on write, see `Expectation` for details.  \nExpectations are aggregation expressions defined on dataset-level and evaluated on every write.  \nBy default their result is logged with level info (ok) and error (failed), but this can be customized to be logged as warning.  \nIn case of failed expectations logged as error, an exceptions is thrown and further processing is stopped.  \nNote that the exception is thrown after writing to the DataObject is finished.\n\nThe following expectations names are reserved to create default metrics and should not be used:\n- count"
          },
          "metadata" : {
            "$ref" : "#/definitions/Others/DataObjectMetadata",
            "description" : "Additional metadata for the DataObject"
          }
        },
        "title" : "JsonFileDataObject",
        "required" : [ "type", "path" ],
        "additionalProperties" : false,
        "description" : "A `io.smartdatalake.workflow.dataobject.DataObject` backed by a JSON data source.\n\nIt manages read and write access and configurations required for `io.smartdatalake.workflow.action.Action`s to\nwork on JSON formatted files.\n\nReading and writing details are delegated to Apache Spark `org.apache.spark.sql.DataFrameReader` and `org.apache.spark.sql.DataFrameWriter` respectively.\n\nUse this DataObject for semi-structured, nested data. As JSON carries no fixed schema, Spark infers it from the data\nunless an explicit `schema` is configured - configuring a schema is recommended for stable pipelines. Example:\n```\ndataObjects = {\n  stg-airports {\n    type = JsonFileDataObject\n    path = \"~{env.basedir}/stg_airports\"\n    jsonOptions { multiLine = false }\n    saveMode = Overwrite\n  }\n}\n```\n\n\nNOTE: By default, the JSON option `multiline` is enabled.\n\nSEE: `org.apache.spark.sql.DataFrameReader`\n\nSEE: [[org.apache.spark.sql.DataFrameWriter]]"
      },
      "KafkaTopicDataObject" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "KafkaTopicDataObject"
          },
          "topicName" : {
            "type" : "string",
            "description" : "The name of the topic to read"
          },
          "connectionId" : {
            "type" : "string",
            "description" : "id of the`io.smartdatalake.workflow.connection.KafkaConnection` defining brokers, schema registry and authentication.  \nA schemaRegistry must be configured in the connection if keyType or valueType is set to Json/AvroSchemaRegistry."
          },
          "keyType" : {
            "type" : "string",
            "description" : "Optional type the key column should be converted to. If none is given it will be interpreted as string.",
            "enum" : [ "String", "Binary", "Json", "Avro", "JsonSchemaRegistry", "AvroSchemaRegistry" ]
          },
          "keySchema" : {
            "type" : "string",
            "description" : "An optional schema for parsing the key column. This can be used if keyType = JSON or Avro to parse the corresponding content.  \nDefine the schema by using one of the schema providers DDL, jsonSchemaFile, avroSchemaFile, xsdFile or caseClassName.  \nThe schema provider and its configuration value must be provided in the format <PROVIDERID>#<VALUE>.  \nA DDL-formatted string is a comma separated list of field definitions, e.g., a INT, b STRING."
          },
          "valueType" : {
            "type" : "string",
            "description" : "Optional type the value column should be converted to. If none is given it will be interpreted as string.",
            "enum" : [ "String", "Binary", "Json", "Avro", "JsonSchemaRegistry", "AvroSchemaRegistry" ]
          },
          "valueSchema" : {
            "type" : "string",
            "description" : "An optional schema for parsing the value column. This has to be specified if valueType = JSON or Avro to parse the corresponding content.  \nDefine the schema by using one of the schema providers DDL, jsonSchemaFile, avroSchemaFile, xsdFile or caseClassName.  \nThe schema provider and its configuration value must be provided in the format <PROVIDERID>#<VALUE>.  \nA DDL-formatted string is a comma separated list of field definitions, e.g., a INT, b STRING."
          },
          "allowSchemaEvolution" : {
            "type" : "boolean",
            "description" : "If set to true schema evolution within schema registry will automatically occur\nwhen writing to this DataObject with different key or value schema, otherwise SDL will stop with error.  \nThis only applies if keyType or valueType is set to Json/AvroSchemaRegistry.  \nKafka Schema Evolution implementation will update schema if existing records with old schema can be read with new schema (backward compatible).  \nOtherwise an IncompatibleSchemaException is thrown."
          },
          "selectCols" : {
            "type" : "array",
            "items" : {
              "type" : "string"
            },
            "description" : "Columns to be selected when reading the DataFrame. Available columns are key, value, topic,\npartition, offset, timestamp, timestampType. If key/valueType is AvroSchemaRegistry the key/value column are\nconvert to a complex type according to the avro schema. To expand it select \"value.*\".  \nDefault is to select key and value."
          },
          "datePartitionCol" : {
            "type" : "object",
            "properties" : {
              "colName" : {
                "type" : "string",
                "description" : "date partition column name to extract time into column on batch read"
              },
              "timeFormat" : {
                "type" : "string",
                "description" : "time format for timestamp in date partition column, definition according to java DateTimeFormatter. Default is \"yyyyMMdd\"."
              },
              "timeUnit" : {
                "type" : "string",
                "description" : "time unit for timestamp in date partition column, definition according to java ChronoUnit. Default is \"days\"."
              },
              "timeZone" : {
                "type" : "string",
                "description" : "time zone used for date logic. If not specified, java system default is used."
              },
              "includeCurrentPartition" : {
                "type" : "boolean",
                "description" : "If the current partition should be included. Default is to list only completed partitions.  \nAttention: including the current partition might result in data loss if there is more data arriving.  \nBut it might be useful to export all data before a scheduled maintenance."
              }
            },
            "title" : "DatePartitionColumnDef",
            "required" : [ "colName" ],
            "additionalProperties" : false,
            "description" : "definition of date partition column to extract formatted timestamp into column.  \nThis is used to list existing partition and is added as additional column on batch read."
          },
          "batchReadConsecutivePartitionsAsRanges" : {
            "type" : "boolean",
            "description" : "Set to true if consecutive partitions should be combined as one range of offsets when batch reading from topic. This results in less tasks but can be a performance problem when reading many partitions. (default=false)"
          },
          "batchReadMaxOffsetsPerTask" : {
            "type" : "integer",
            "description" : "Set number of offsets per Spark task when batch reading from topic."
          },
          "options" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "Options for the Kafka stream reader (see https://spark.apache.org/docs/latest/structured-streaming-kafka-integration.html).  \nThese options override connection.options."
          },
          "metadata" : {
            "$ref" : "#/definitions/Others/DataObjectMetadata",
            "description" : "Additional metadata for the DataObject"
          }
        },
        "title" : "KafkaTopicDataObject",
        "required" : [ "type", "topicName", "connectionId" ],
        "additionalProperties" : false,
        "description" : "`DataObject` of type KafkaTopic.  \nProvides details to an action to read from Kafka Topics using either `org.apache.spark.sql.DataFrameReader` or [[org.apache.spark.sql.streaming.DataStreamReader]]\n\nKey & value schema can be automatically read from and written to confluent schema registry for JSON and Avro.  \nJSON and Avro can also be parsed with a fixed schema.\n\nCan interpret record timestamp as SDLB partition values by setting datePartitionCol attribute. This allows to use this DataObject as input for PartitionDiffMode.  \nThe DataObject does not support writing with SDLB partition values, as timestamp is autogenerated by Kafka using current time.\n\nSupport incremental output and use with DataObjectStateIncrementalMode. Example:\n```\nconnections = {\n  kafka-con {\n    type = KafkaConnection\n    brokers = \"localhost:9092\"\n    schemaRegistry = \"http://localhost:8081\"\n  }\n}\ndataObjects = {\n  stg-departures {\n    type = KafkaTopicDataObject\n    topicName = \"departures\"\n    connectionId = kafka-con\n    valueType = AvroSchemaRegistry\n    selectCols = [\"value.*\", \"timestamp\"]\n  }\n}\n```\n"
      },
      "ODataDataObject" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "ODataDataObject"
          },
          "schema" : {
            "type" : "string",
            "description" : "Schema of the expected output. It can be provided as a string in the form of <<array< struct<\ncolumnA:string, columnB: integer ... >>, as ddl-File, caseClass, java Bean, XSD-File,\nJSON-Schema-File or Avro-Schema File. For more information, please see the SDLB-documentation\non specifying schemas (https://smartdatalake.ch/docs/reference/schema#specifying-schema)."
          },
          "baseUrl" : {
            "type" : "string",
            "description" : "Base URL of the OData Service like https://xxx.crm4.dynamics.com/api/data/v9.2/"
          },
          "tableName" : {
            "type" : "string",
            "description" : "Name of the table which needs to be accessed"
          },
          "sourceFilters" : {
            "type" : "string",
            "description" : "Optional. OData filter string which will be applied to the access operation like\n\"objecttypecode eq 'task' and createdon ge 2024-01-01T00:00:00.000Z\""
          },
          "timeouts" : {
            "type" : "object",
            "properties" : {
              "connectionTimeoutMs" : {
                "type" : "integer"
              },
              "readTimeoutMs" : {
                "type" : "integer"
              }
            },
            "title" : "HttpTimeoutConfig",
            "required" : [ "connectionTimeoutMs", "readTimeoutMs" ],
            "additionalProperties" : false,
            "description" : "Optional. Timeout settings of type`HttpTimeoutConfig`"
          },
          "proxy" : {
            "type" : "object",
            "properties" : {
              "host" : {
                "type" : "string",
                "description" : "proxy host"
              },
              "port" : {
                "type" : "integer",
                "description" : "proxy port"
              },
              "user" : {
                "type" : "string",
                "description" : "The value can be provided in plaintext or with the id of a secret provider and the\nname of the secret in the format\n```\n###<PROVIDERID>#<SECRETNAME>###\n```\n ,\ne.g.  \n```\n###ENV#<ENV_VARIABLE_NAME>###\n```\n\n\nto get a secret from an environment variable."
              },
              "password" : {
                "type" : "string",
                "description" : "The value can be provided in plaintext or with the id of a secret provider and the\nname of the secret in the format\n```\n###<PROVIDERID>#<SECRETNAME>###\n```\n ,\ne.g.  \n```\n###ENV#<ENV_VARIABLE_NAME>###\n```\n\n\nto get a secret from an environment variable."
              }
            },
            "title" : "HttpProxyConfig",
            "required" : [ "host", "port" ],
            "additionalProperties" : false,
            "description" : "Optional. HTTP proxy configuration of type`HttpProxyConfig` used to make the HTTP-connection."
          },
          "authMode" : {
            "oneOf" : [ {
              "$ref" : "#/definitions/HttpAuthMode/AWSUserPwdAuthMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/AuthHeaderMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/AzureADClientGrantAuthMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/BasicAuthMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/CustomHttpAuthMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/OAuthMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/TokenAuthMode"
            } ],
            "description" : "Optional configuration of webservice authentication. Supported`AuthMode` s are all\nHttpAuthModes, e.g. BasicAuthMode, OAuthMode, CustomHttpAuthMode. CustomHttpAuthMode can be\nused to implement a custom authentication protocol, e.g. AzureADClientGrantAuthMode in\nsdl-azure module."
          },
          "followRedirects" : {
            "type" : "boolean",
            "description" : "Optional. If HTTP redirects should be followed when creating the HTTP-connection, default = false because of\nsecurity concerns."
          },
          "incrementalOutputExpr" : {
            "type" : "string",
            "description" : ":\nOptional. Name of the column which will be used to read incrementally (like \"modifiedon\"). The\ncolumn must be part of the schema. If this column is originally of datatype Timestamp in the\nsource, it should be marked as a string in the schema to prevent casting problems."
          },
          "nRetry" : {
            "type" : "integer",
            "description" : ":\nOptional. Number of retries after a failed attempt, default = 1"
          },
          "responseBufferSetup" : {
            "type" : "object",
            "properties" : {
              "tempFileDirectoryPath" : {
                "type" : "string",
                "description" : ": Path to the temporary directory in which the temporary files can be stored"
              },
              "memoryToFileSwitchThresholdNumOfChars" : {
                "type" : "integer",
                "description" : ": Max number of character which can be stored in memory before switching to file buffering. (If set to 0 then no swichting will occur)"
              }
            },
            "title" : "ODataResponseBufferSetup",
            "additionalProperties" : false,
            "description" : ":\nOptional. Setup for response buffers of type`ODataResponseBufferSetup`"
          },
          "maxRecordCount" : {
            "type" : "integer",
            "description" : ":\nOptional. Maximum number of records to be extracted."
          },
          "metadata" : {
            "$ref" : "#/definitions/Others/DataObjectMetadata",
            "description" : "Additional metadata for the DataObject"
          }
        },
        "title" : "ODataDataObject",
        "required" : [ "type", "baseUrl", "tableName" ],
        "additionalProperties" : false,
        "description" : "`DataObject` of type OData.\n\nReads one entity set (table) of an OData v4 REST service, e.g. Microsoft Dynamics 365 / Dataverse, into a Spark\nDataFrame. The requested columns are derived from the configured `schema` , which is mandatory - the service is\nqueried with a corresponding OData `select` system query option, optionally narrowed by `sourceFilters` . This\nDataObject is read-only (there is no write support) and requests are executed on the driver, not distributed to\nexecutors. Responses are collected in a memory buffer that spills to temporary files once it grows too large, see `responseBufferSetup` . Example:\n```\ndataObjects = {\n  ext-tasks {\n    type = ODataDataObject\n    baseUrl = \"https://myorg.crm4.dynamics.com/api/data/v9.2/\"\n    tableName = \"tasks\"\n    schema = \"activityid string, subject string, modifiedon string\"\n    sourceFilters = \"statecode eq 0\"\n    incrementalOutputExpr = \"modifiedon\"\n    authMode = {\n      type = AzureADClientGrantAuthMode\n      authority = \"https://login.microsoftonline.com/{tenant-guid}/\"\n      applicationId = \"###ENV#AZURE_CLIENT_ID###\"\n      clientSecret = \"###ENV#AZURE_CLIENT_SECRET###\"\n      scope = \"https://myorg.crm4.dynamics.com/.default\"\n    }\n  }\n}\n```\n"
      },
      "OpenApiDataObject" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "OpenApiDataObject"
          },
          "baseUrl" : {
            "type" : "string",
            "description" : "the server url to use for querying the OpenApi specification and content"
          },
          "operationId" : {
            "type" : "string",
            "description" : "the operationId from the OpenApi specification to use to get data for this DataObject\nIf OpenApi specification has no operationId defined,`<sub-path>:<operation>`is used, e.g.`/test/abc:get` ."
          },
          "apiDocsUrl" : {
            "type" : "string",
            "description" : "The url to load the OpenApi specification from.  \nIf it is a relative Url it is appended to baseUrl.  \nDefault is \"v3/api-docs\", which will be concatenated to`<baseUrl>/v3/api-docs` .  \nAlternatively this can also be a hadoop file or classpath resource.  \nAn url starting with protocol \"cp:\" will be resolved as classpath resource.  \nAll protocols different from http/https/cp will be resolved as Hadoop path.  \nTo use a relative Hadoop path start with \"./\"."
          },
          "useFirstOpenApiSpecServer" : {
            "type" : "boolean",
            "description" : "if true content is queried using first server definition from OpenApi specification, instead of using baseUrl.  \nDefault is false."
          },
          "responseContentType" : {
            "type" : "string",
            "description" : "Content-type of OpenApi response to use. Default is application/json.  \nNote that additional attributes such as`; charset=utf-8` are ignored if not found in the first try."
          },
          "authMode" : {
            "oneOf" : [ {
              "$ref" : "#/definitions/HttpAuthMode/AWSUserPwdAuthMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/AuthHeaderMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/AzureADClientGrantAuthMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/BasicAuthMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/CustomHttpAuthMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/OAuthMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/TokenAuthMode"
            } ],
            "description" : "Optional configuration of webservice authentication. Supported`AuthMode` s are all HttpAuthModes, e.g. BasicAuthMode, OAuthMode, CustomHttpAuthMode.  \nCustomHttpAuthMode can be used to implement a custom authentication protocol, e.g. AzureADClientGrantAuthMode in sdl-azure module."
          },
          "proxy" : {
            "type" : "object",
            "properties" : {
              "host" : {
                "type" : "string",
                "description" : "proxy host"
              },
              "port" : {
                "type" : "integer",
                "description" : "proxy port"
              },
              "user" : {
                "type" : "string",
                "description" : "The value can be provided in plaintext or with the id of a secret provider and the\nname of the secret in the format\n```\n###<PROVIDERID>#<SECRETNAME>###\n```\n ,\ne.g.  \n```\n###ENV#<ENV_VARIABLE_NAME>###\n```\n\n\nto get a secret from an environment variable."
              },
              "password" : {
                "type" : "string",
                "description" : "The value can be provided in plaintext or with the id of a secret provider and the\nname of the secret in the format\n```\n###<PROVIDERID>#<SECRETNAME>###\n```\n ,\ne.g.  \n```\n###ENV#<ENV_VARIABLE_NAME>###\n```\n\n\nto get a secret from an environment variable."
              }
            },
            "title" : "HttpProxyConfig",
            "required" : [ "host", "port" ],
            "additionalProperties" : false,
            "description" : "optional Proxy configuration used to make HTTP-connection."
          },
          "followRedirects" : {
            "type" : "boolean",
            "description" : "if redirects should be followed when creating HTTP-connection. Default is false because of security concerns."
          },
          "retries" : {
            "type" : "integer",
            "description" : "number of retries if http request fails. Default is 0 retries."
          },
          "urlParameters" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "Additional Url parameters to pass with the http request to get data"
          },
          "additionalHeaders" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "Additional headers to pass with the http request to get spec and data"
          },
          "timeouts" : {
            "type" : "object",
            "properties" : {
              "connectionTimeoutMs" : {
                "type" : "integer"
              },
              "readTimeoutMs" : {
                "type" : "integer"
              }
            },
            "title" : "HttpTimeoutConfig",
            "required" : [ "connectionTimeoutMs", "readTimeoutMs" ],
            "additionalProperties" : false,
            "description" : "optional configuration of HTTP timeouts"
          },
          "pagingLinkJsonPath" : {
            "type" : "string",
            "description" : "If selected operation implements paging and returns content-type application/json, configure a JsonPath expression to extract the link of the next page to query.  \nThe JsonPath expression should return a String or a list of Strings as result.  \nIf a link for the next page is not found, it is assumed that it is the last page and no further queries will be made.  \nExample JsonPath expression: \"$._links[?("
          },
          "schemaMatchJsonPath" : {
            "type" : "string",
            "description" : "The response schema of an operation in the OpenApi specification should describe the structure of the entire response body.  \nIf that's not the case, define schemaMatchJsonPath to extract the response part that matches the schema.  \nExample JsonPath expression: \"$.data\""
          },
          "maxPagesPerPartition" : {
            "type" : "integer",
            "description" : "Only relevant together pagingLinkRegex.  \nSets maximum number of pages to put into one Spark partition.  \nDefault is 10. Remember that a page normally includes multiple records and can already be quite large.  \nThis helps to limit memory usage, as Spark will offload partitions to disk if memory is scarce."
          },
          "metadata" : {
            "$ref" : "#/definitions/Others/DataObjectMetadata",
            "description" : "Additional metadata for the DataObject"
          }
        },
        "title" : "OpenApiDataObject",
        "required" : [ "type", "baseUrl", "operationId" ],
        "additionalProperties" : false,
        "description" : "Reads data from an OpenApi compliant WebService operation.  \nThe operation must return JSON and have its schema defined in the OpenApi specification.\n\nLimitations:\n- Only GET method implemented for now\n- No dynamic URL parameters supported for now\n- Only responseContentType=application/json will be parsed into schema.  \nOtherwise schema will be a single column with name 'content' of type String or Binary.  \n- Only token based pagination is supported. Use pagingLinkJsonPath to extract Url for the next page.  \n- OpenApi Webservice requests can not be parallelized and distributed to executors. They run on the driver.  \nIn order to avoid memory problems Spark BlockManager is used to create\na new Spark partition after every maxRecordsPerPartition number of records.\n\nAlso note that the getDataFrame method is not lazy in Exec-Phase.  \nIt will query the WebService before creating the DataFrame. Example:\n```\ndataObjects = {\n  ext-datasets {\n    type = OpenApiDataObject\n    baseUrl = \"https://data.sbb.ch/api/explore/v2.1\"\n    apiDocsUrl = \"swagger.json\"\n    operationId = getDatasets\n    urlParameters = { include_links = \"true\" }\n    pagingLinkJsonPath = \"$._links[?(\n```\n"
      },
      "PKViolatorsDataObject" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "PKViolatorsDataObject"
          },
          "config" : {
            "type" : "string",
            "description" : ": The config value can point to a configuration file or a directory containing configuration files.  \nMultiple locations can be given as comma separated list. If not defined, the DataObjects of the\ncurrent run's`InstanceRegistry` are checked."
          },
          "flattenOutput" : {
            "type" : "boolean",
            "description" : ": if true, the key and data column are cast from their default type `array<struct<name,value>>` to string (default: false)."
          },
          "metadata" : {
            "$ref" : "#/definitions/Others/DataObjectMetadata",
            "description" : "Additional metadata for the DataObject"
          }
        },
        "title" : "PKViolatorsDataObject",
        "required" : [ "type" ],
        "additionalProperties" : false,
        "description" : "Checks for Primary Key violations for all `DataObject`s with Primary Keys defined that are registered in the current `InstanceRegistry`.  \nReturns the DataFrame of Primary Key violations.\n\nAlternatively, it can check for Primary Key violations of all `DataObject`s defined in config files. For this, the\nconfiguration \"config\" has to be set to the location of the config. Example:\n```\ndataObjects = {\n  primarykey-violations {\n    type = PKViolatorsDataObject\n    config = \"path/to/myconfiguration.conf\"\n    flattenOutput = true\n  }\n}\n```\n\n\nSEE: Refer to `ConfigLoader.loadConfigFromFilesystem()` for details about the configuration loading."
      },
      "ParquetFileDataObject" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "ParquetFileDataObject"
          },
          "path" : {
            "type" : "string",
            "description" : "The root path of the files that are handled by this DataObject.  \nFor most DataObjects this can be a directory or a specific file.\n\nIf it doesn't contain scheme and authority, the connections pathPrefix is applied. If pathPrefix is not\ndefined or doesn't define scheme and authority, default schema and authority is applied.\n\nOptionally defined partitions are appended with hadoop standard partition layout to this path."
          },
          "partitions" : {
            "type" : "array",
            "items" : {
              "type" : "string"
            },
            "description" : "Definition of partition columns\n\nExample: `[dt]`"
          },
          "parquetOptions" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "Settings for the underlying`org.apache.spark.sql.DataFrameReader]]and[[org.apache.spark.sql.DataFrameWriter`."
          },
          "schema" : {
            "type" : "string",
            "description" : "An optional user-defined schema definition for this DataObject.  \nIf defined, any automatic schema inference is avoided.\n\nThe schema corresponds to the schema on write, it must not include optional columns on read, e.g. the filenameColumn for SparkFileDataObjects.  \nIf partition columns are missing, they will be added with type String.\n\nDefine the schema by using one of the schema providers below, default is DDL.  \nThe schema provider and its configuration value must be provided in the format <PROVIDERID>#<VALUE>.\n\nSchema Providers available are (see also `io.smartdatalake.util.misc.SchemaProviderType` ):\n\n- ddl: create the schema from a Spark ddl string, e.g. `ddl#a string, b array<struct<b1: string, b2: long>>, c struct<c1: string, c2: long>` .\n\n- ddlFile: read a Spark ddl definition from a file and create a schema, e.g. `ddlFile#abc/xyz.ddl` .\n\n- caseClass: convert a Scala Case Class to a schema using Spark encoders, e.g. `caseClass#com.sample.XyzClass` .\n\n- javaBean: convert a Java Bean to a schema using Spark encoders, e.g. `javaBean#com.sample.XyzClass` .\n\n- xsdFile: read an Xml Schema Definition file and create a schema, e.g. `xsdFile#abc/xyz.xsd` .  \nThe following parameters allow to customize the behavior: `xsdFile#<path-to-xsd-file>;<row-tag>;<maxRecursion:Int>;<jsonCompatibility:Boolean>` <row-tag>: configure the path of the element to extract from the xsd schema. Leave empty to extract the root.  \n<maxRecursion>: if xsd schema is recursive, this configures the number of levels to create in the schema.  \nDefault is 10 levels.  \n<jsonCompatibility>: In XML array elements are modeled with their own tag named with singular name.  \nIn JSON an array attribute has unnamed array entries, but the array attribute has a plural name.  \nIf true, the singular name of the array element in the XSD is converted to a plural name by adding an 's'\nin order to read corresponding json files.  \nDefault is false.\n\n- jsonSchemaFile: read a Json Schema file and create a schema, e.g. `jsonSchemaFile#abc/xyz.json` .  \nThe following parameters allow to customize the behavior: `jsonSchemaFile#<path-to-json-file>;<row-tag>;<strictTyping:Boolean>;<additionalPropertiesDefault:Boolean>` <row-tag>: configure the path of the element to extract from the json schema. Leave empty to extract the root.  \n<strictTyping>: if true\nunion types (oneOf) are merged if rational, otherwise they are simply mapped to StringType;\nadditional properties are ignored, otherwise the corresponding schema object is mapped to MapType(String,String).  \nDefault is strictTyping=false.  \n<additionalPropertiesDefault>: Set to true or false.  \nThis is used as default value for 'additionalProperties'-field if it is missing in a schema with type='object'.  \nDefault value is additionalPropertiesDefault=true, as this is conform with the specification.\n\n- avroSchemaFile: read an Avro Schema file and create a schema, e.g. `avroSchemaFile#abc/xyz.avsc` .  \nThe following parameters allow to customize the behavior: `avroSchemaFile#<path-to-avsc-file>;<row-tag>` <row-tag>: configure the path of the element to extract from the avro schema. Leave empty to extract the root.\n\n- openApi: read Schema from OpenApi specification operation, e.g. `openApi#https://swagger.myswitzerland.io;getDestinations;opendata.json` .  \nThe following parameters allow to customize the behaviour: `openApi#<baseUrl>;<operationId>;[<apiDocsPath>;][<responseContentType>]`\n\nNote that all schema files are configured as Hadoop path. The custom prefix 'cp' can be used to read schema files\nfrom the classpath, e.g. `xsdFile#cp:/xyz.xsd` ."
          },
          "schemaMin" : {
            "type" : "string",
            "description" : "An optional, minimal schema that a `DataObject`schema must have to pass schema validation.\n\nThe schema validation semantics are:\n- Schema A is valid in respect to a minimal schema B when B is a subset of A. This means: the whole column set of B is contained in the column set of A.  \n- A column of B is contained in A when A contains a column with equal name and data type.  \n- Column order is ignored.  \n- Column nullability is ignored.  \n- Duplicate columns in terms of name and data type are eliminated (set semantics).\n\nNote: This is mainly used by the functionality defined in `CanCreateDataFrame` and `CanWriteDataFrame`, that is,\nwhen reading or writing Spark data frames from/to the underlying data container. `io.smartdatalake.workflow.action.Action`s that work with files ignore the `schemaMin` attribute\nif it is defined.  \nAdditionally schemaMin can be used to define the schema used if there is no data or table doesn't yet exist."
          },
          "saveMode" : {
            "type" : "string",
            "description" : "Overwrite or Append new data.  \nWhen writing partitioned data, this applies only to partitions concerned.",
            "enum" : [ "Merge", "OverwriteOptimized", "OverwritePreserveDirectories", "Ignore", "ErrorIfExists", "Append", "Overwrite" ]
          },
          "sparkRepartition" : {
            "type" : "object",
            "properties" : {
              "numberOfTasksPerPartition" : {
                "type" : "integer",
                "description" : "Number of Spark tasks to create per partition before writing to DataObject by repartitioning the DataFrame.  \nThis controls how many files are created in each Hadoop partition."
              },
              "keyCols" : {
                "type" : "array",
                "items" : {
                  "type" : "string"
                },
                "description" : "Optional key columns to distribute records over Spark tasks inside a Hadoop partition.  \nIf DataObject has Hadoop partitions defined, keyCols must be defined."
              },
              "sortCols" : {
                "type" : "array",
                "items" : {
                  "type" : "string"
                },
                "description" : "Optional columns to sort records inside files created."
              },
              "filename" : {
                "type" : "string",
                "description" : "Option filename to rename target file(s). If numberOfTasksPerPartition is greater than 1,\nmultiple files can exist in a directory and a number is inserted into the filename after the first '.'.  \nExample: filename=data.csv → files created are data.1.csv, data.2.csv, ..."
              }
            },
            "title" : "SparkRepartitionDef",
            "required" : [ "numberOfTasksPerPartition" ],
            "additionalProperties" : false,
            "description" : "Definition of repartition operation before writing DataFrame with Spark to Hadoop."
          },
          "connectionId" : {
            "type" : "string",
            "description" : "Return the connection id.\n\nConnection defines path prefix (scheme, authority, base path) and ACL's in central location."
          },
          "filenameColumn" : {
            "type" : "string",
            "description" : "The name of the (optional) additional column containing the source filename"
          },
          "expectedPartitionsCondition" : {
            "type" : "string",
            "description" : "Definition of partitions that are expected to exists.  \nThis is used to validate that partitions being read exists and don't return no data.  \nDefine a Spark SQL expression that is evaluated against a `PartitionValues` instance and returns true or false\n\nExample: \"elements['yourColName'] > 2017\"\n\nIf empty (default) all partition are expected to exists.\n\nOTHERTAG: true if partition is expected to exist."
          },
          "housekeepingMode" : {
            "oneOf" : [ {
              "$ref" : "#/definitions/HousekeepingMode/PartitionArchiveMode"
            }, {
              "$ref" : "#/definitions/HousekeepingMode/PartitionRetentionMode"
            } ],
            "description" : "Configure a housekeeping mode to e.g cleanup, archive and compact partitions.\n\nDefault is None."
          },
          "constraints" : {
            "type" : "array",
            "items" : {
              "type" : "object",
              "properties" : {
                "name" : {
                  "type" : "string",
                  "description" : "name of the constraint"
                },
                "description" : {
                  "type" : "string",
                  "description" : "optional detailed description of the constraint"
                },
                "expression" : {
                  "type" : "string",
                  "description" : "SQL expression to evaluate on every row. The expressions return value should be a boolean.  \nIf it evaluates to true the constraint is validated successfully, otherwise it will throw an exception."
                },
                "errorMsgCols" : {
                  "type" : "array",
                  "items" : {
                    "type" : "string"
                  },
                  "description" : "Optional list of column names to add to error message.  \nNote that primary key columns are always included.  \nIf there is no primary key defined, by default all columns with simple datatype are included in the error message."
                }
              },
              "title" : "Constraint",
              "required" : [ "name", "expression" ],
              "additionalProperties" : false,
              "description" : "Definition of row-level constraint to validate."
            },
            "description" : "List of constraint definitions to validate on write, see `Constraint` for details.  \nConstraints are expressions defined on row-level and validated during evaluation of the DataFrame.  \nIf validation fails an exception is thrown and further processing is stopped.  \nNote that this is done while evaluating the DataFrame when writing to the DataObject. It doesn't need a separate action on the DataFrame.  \nIf a constraint validation for a row fails, it will throw an exception and abort writing to the DataObject."
          },
          "expectations" : {
            "type" : "array",
            "items" : {
              "oneOf" : [ {
                "$ref" : "#/definitions/Expectation/AvgCountPerPartitionExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/CountExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/SQLExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/SQLFractionExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/SQLQueryExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/UniqueKeyExpectation"
              } ]
            },
            "description" : "List expectation definitions to evaluate on write, see `Expectation` for details.  \nExpectations are aggregation expressions defined on dataset-level and evaluated on every write.  \nBy default their result is logged with level info (ok) and error (failed), but this can be customized to be logged as warning.  \nIn case of failed expectations logged as error, an exceptions is thrown and further processing is stopped.  \nNote that the exception is thrown after writing to the DataObject is finished.\n\nThe following expectations names are reserved to create default metrics and should not be used:\n- count"
          },
          "metadata" : {
            "$ref" : "#/definitions/Others/DataObjectMetadata",
            "description" : "Additional metadata for the DataObject"
          }
        },
        "title" : "ParquetFileDataObject",
        "required" : [ "type", "path" ],
        "additionalProperties" : false,
        "description" : "A `io.smartdatalake.workflow.dataobject.DataObject` backed by an Apache Parquet data source.\n\nIt manages read and write access and configurations required for `io.smartdatalake.workflow.action.Action`s to\nwork on Parquet formatted files.\n\nReading and writing details are delegated to Apache Spark `org.apache.spark.sql.DataFrameReader` and `org.apache.spark.sql.DataFrameWriter` respectively.\n\nThis is the default choice for storing intermediate and integration layer data in a data lake: it is columnar,\ncompressed and carries its schema, so no `schema` needs to be configured for reading. If a `schema` is configured\nit is applied after reading to select and cast the given columns, see also `NestedColumnUtil`. Example:\n```\ndataObjects = {\n  int-airports {\n    type = ParquetFileDataObject\n    path = \"~{env.basedir}/int_airports\"\n    partitions = [year, month]\n    saveMode = Overwrite\n  }\n}\n```\n\n\nSEE: `org.apache.spark.sql.DataFrameReader`\n\nSEE: `org.apache.spark.sql.DataFrameWriter`"
      },
      "RawFileDataObject" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "RawFileDataObject"
          },
          "path" : {
            "type" : "string",
            "description" : "The root path of the files that are handled by this DataObject.  \nFor most DataObjects this can be a directory or a specific file.\n\nIf it doesn't contain scheme and authority, the connections pathPrefix is applied. If pathPrefix is not\ndefined or doesn't define scheme and authority, default schema and authority is applied.\n\nOptionally defined partitions are appended with hadoop standard partition layout to this path."
          },
          "customFormat" : {
            "type" : "string",
            "description" : "Custom Spark data source format, e.g. binaryFile or text.  \nOnly needed if you want to read/write this DataObject with Spark."
          },
          "options" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "Options for custom Spark data source format.  \nOnly of use if you want to read/write this DataObject with Spark."
          },
          "fileName" : {
            "type" : "string",
            "description" : "Definition of fileName. Default is an asterix to match everything.  \nThis is concatenated with the partition layout to search for files."
          },
          "partitions" : {
            "type" : "array",
            "items" : {
              "type" : "string"
            },
            "description" : "Definition of partition columns\n\nExample: `[dt]`"
          },
          "customPartitionLayout" : {
            "type" : "string",
            "description" : "Define a different partition layout than the default Hadoop directory partitioning.  \nNote that this works with file based Actions like FileTransferAction, but not Spark (getSparkDataFrame method). Spark does not support reading data with a different partition layout.  \nPartition layout defines how partition values can be extracted from the path.  \nUse \"%<colname>%\" as token to extract the value for a partition column.  \nAs partition layout extracts partition from the path of individual files, it can also be used to extract partitions from the file name.  \nWith \"%<colname:regex>%\" a regex can be given to limit search. This is especially useful\nif there is no char to delimit the last token from the rest of the path or also between two tokens.  \nBe careful that for directory based partition values extraction, the final path separator must be part\nof the partition layout to extract the last token correctly, e.g. \"%year%/\" for partitioning with yearly directories."
          },
          "schema" : {
            "type" : "string",
            "description" : "An optional user-defined schema definition for this DataObject.  \nIf defined, any automatic schema inference is avoided.\n\nThe schema corresponds to the schema on write, it must not include optional columns on read, e.g. the filenameColumn for SparkFileDataObjects.  \nIf partition columns are missing, they will be added with type String.\n\nDefine the schema by using one of the schema providers below, default is DDL.  \nThe schema provider and its configuration value must be provided in the format <PROVIDERID>#<VALUE>.\n\nSchema Providers available are (see also `io.smartdatalake.util.misc.SchemaProviderType` ):\n\n- ddl: create the schema from a Spark ddl string, e.g. `ddl#a string, b array<struct<b1: string, b2: long>>, c struct<c1: string, c2: long>` .\n\n- ddlFile: read a Spark ddl definition from a file and create a schema, e.g. `ddlFile#abc/xyz.ddl` .\n\n- caseClass: convert a Scala Case Class to a schema using Spark encoders, e.g. `caseClass#com.sample.XyzClass` .\n\n- javaBean: convert a Java Bean to a schema using Spark encoders, e.g. `javaBean#com.sample.XyzClass` .\n\n- xsdFile: read an Xml Schema Definition file and create a schema, e.g. `xsdFile#abc/xyz.xsd` .  \nThe following parameters allow to customize the behavior: `xsdFile#<path-to-xsd-file>;<row-tag>;<maxRecursion:Int>;<jsonCompatibility:Boolean>` <row-tag>: configure the path of the element to extract from the xsd schema. Leave empty to extract the root.  \n<maxRecursion>: if xsd schema is recursive, this configures the number of levels to create in the schema.  \nDefault is 10 levels.  \n<jsonCompatibility>: In XML array elements are modeled with their own tag named with singular name.  \nIn JSON an array attribute has unnamed array entries, but the array attribute has a plural name.  \nIf true, the singular name of the array element in the XSD is converted to a plural name by adding an 's'\nin order to read corresponding json files.  \nDefault is false.\n\n- jsonSchemaFile: read a Json Schema file and create a schema, e.g. `jsonSchemaFile#abc/xyz.json` .  \nThe following parameters allow to customize the behavior: `jsonSchemaFile#<path-to-json-file>;<row-tag>;<strictTyping:Boolean>;<additionalPropertiesDefault:Boolean>` <row-tag>: configure the path of the element to extract from the json schema. Leave empty to extract the root.  \n<strictTyping>: if true\nunion types (oneOf) are merged if rational, otherwise they are simply mapped to StringType;\nadditional properties are ignored, otherwise the corresponding schema object is mapped to MapType(String,String).  \nDefault is strictTyping=false.  \n<additionalPropertiesDefault>: Set to true or false.  \nThis is used as default value for 'additionalProperties'-field if it is missing in a schema with type='object'.  \nDefault value is additionalPropertiesDefault=true, as this is conform with the specification.\n\n- avroSchemaFile: read an Avro Schema file and create a schema, e.g. `avroSchemaFile#abc/xyz.avsc` .  \nThe following parameters allow to customize the behavior: `avroSchemaFile#<path-to-avsc-file>;<row-tag>` <row-tag>: configure the path of the element to extract from the avro schema. Leave empty to extract the root.\n\n- openApi: read Schema from OpenApi specification operation, e.g. `openApi#https://swagger.myswitzerland.io;getDestinations;opendata.json` .  \nThe following parameters allow to customize the behaviour: `openApi#<baseUrl>;<operationId>;[<apiDocsPath>;][<responseContentType>]`\n\nNote that all schema files are configured as Hadoop path. The custom prefix 'cp' can be used to read schema files\nfrom the classpath, e.g. `xsdFile#cp:/xyz.xsd` ."
          },
          "schemaMin" : {
            "type" : "string",
            "description" : "An optional, minimal schema that a `DataObject`schema must have to pass schema validation.\n\nThe schema validation semantics are:\n- Schema A is valid in respect to a minimal schema B when B is a subset of A. This means: the whole column set of B is contained in the column set of A.  \n- A column of B is contained in A when A contains a column with equal name and data type.  \n- Column order is ignored.  \n- Column nullability is ignored.  \n- Duplicate columns in terms of name and data type are eliminated (set semantics).\n\nNote: This is mainly used by the functionality defined in `CanCreateDataFrame` and `CanWriteDataFrame`, that is,\nwhen reading or writing Spark data frames from/to the underlying data container. `io.smartdatalake.workflow.action.Action`s that work with files ignore the `schemaMin` attribute\nif it is defined.  \nAdditionally schemaMin can be used to define the schema used if there is no data or table doesn't yet exist."
          },
          "saveMode" : {
            "type" : "string",
            "description" : "Overwrite or Append new data.  \nWhen writing partitioned data, this applies only to partitions concerned.",
            "enum" : [ "Merge", "OverwriteOptimized", "OverwritePreserveDirectories", "Ignore", "ErrorIfExists", "Append", "Overwrite" ]
          },
          "sparkRepartition" : {
            "type" : "object",
            "properties" : {
              "numberOfTasksPerPartition" : {
                "type" : "integer",
                "description" : "Number of Spark tasks to create per partition before writing to DataObject by repartitioning the DataFrame.  \nThis controls how many files are created in each Hadoop partition."
              },
              "keyCols" : {
                "type" : "array",
                "items" : {
                  "type" : "string"
                },
                "description" : "Optional key columns to distribute records over Spark tasks inside a Hadoop partition.  \nIf DataObject has Hadoop partitions defined, keyCols must be defined."
              },
              "sortCols" : {
                "type" : "array",
                "items" : {
                  "type" : "string"
                },
                "description" : "Optional columns to sort records inside files created."
              },
              "filename" : {
                "type" : "string",
                "description" : "Option filename to rename target file(s). If numberOfTasksPerPartition is greater than 1,\nmultiple files can exist in a directory and a number is inserted into the filename after the first '.'.  \nExample: filename=data.csv → files created are data.1.csv, data.2.csv, ..."
              }
            },
            "title" : "SparkRepartitionDef",
            "required" : [ "numberOfTasksPerPartition" ],
            "additionalProperties" : false,
            "description" : "Definition of repartition operation before writing DataFrame with Spark to Hadoop."
          },
          "connectionId" : {
            "type" : "string",
            "description" : "Return the connection id.\n\nConnection defines path prefix (scheme, authority, base path) and ACL's in central location."
          },
          "filenameColumn" : {
            "type" : "string",
            "description" : "The name of the (optional) additional column containing the source filename"
          },
          "expectedPartitionsCondition" : {
            "type" : "string",
            "description" : "Definition of partitions that are expected to exists.  \nThis is used to validate that partitions being read exists and don't return no data.  \nDefine a Spark SQL expression that is evaluated against a `PartitionValues` instance and returns true or false\n\nExample: \"elements['yourColName'] > 2017\"\n\nIf empty (default) all partition are expected to exists.\n\nOTHERTAG: true if partition is expected to exist."
          },
          "housekeepingMode" : {
            "oneOf" : [ {
              "$ref" : "#/definitions/HousekeepingMode/PartitionArchiveMode"
            }, {
              "$ref" : "#/definitions/HousekeepingMode/PartitionRetentionMode"
            } ],
            "description" : "Configure a housekeeping mode to e.g cleanup, archive and compact partitions.\n\nDefault is None."
          },
          "constraints" : {
            "type" : "array",
            "items" : {
              "type" : "object",
              "properties" : {
                "name" : {
                  "type" : "string",
                  "description" : "name of the constraint"
                },
                "description" : {
                  "type" : "string",
                  "description" : "optional detailed description of the constraint"
                },
                "expression" : {
                  "type" : "string",
                  "description" : "SQL expression to evaluate on every row. The expressions return value should be a boolean.  \nIf it evaluates to true the constraint is validated successfully, otherwise it will throw an exception."
                },
                "errorMsgCols" : {
                  "type" : "array",
                  "items" : {
                    "type" : "string"
                  },
                  "description" : "Optional list of column names to add to error message.  \nNote that primary key columns are always included.  \nIf there is no primary key defined, by default all columns with simple datatype are included in the error message."
                }
              },
              "title" : "Constraint",
              "required" : [ "name", "expression" ],
              "additionalProperties" : false,
              "description" : "Definition of row-level constraint to validate."
            },
            "description" : "List of constraint definitions to validate on write, see `Constraint` for details.  \nConstraints are expressions defined on row-level and validated during evaluation of the DataFrame.  \nIf validation fails an exception is thrown and further processing is stopped.  \nNote that this is done while evaluating the DataFrame when writing to the DataObject. It doesn't need a separate action on the DataFrame.  \nIf a constraint validation for a row fails, it will throw an exception and abort writing to the DataObject."
          },
          "expectations" : {
            "type" : "array",
            "items" : {
              "oneOf" : [ {
                "$ref" : "#/definitions/Expectation/AvgCountPerPartitionExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/CountExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/SQLExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/SQLFractionExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/SQLQueryExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/UniqueKeyExpectation"
              } ]
            },
            "description" : "List expectation definitions to evaluate on write, see `Expectation` for details.  \nExpectations are aggregation expressions defined on dataset-level and evaluated on every write.  \nBy default their result is logged with level info (ok) and error (failed), but this can be customized to be logged as warning.  \nIn case of failed expectations logged as error, an exceptions is thrown and further processing is stopped.  \nNote that the exception is thrown after writing to the DataObject is finished.\n\nThe following expectations names are reserved to create default metrics and should not be used:\n- count"
          },
          "metadata" : {
            "$ref" : "#/definitions/Others/DataObjectMetadata",
            "description" : "Additional metadata for the DataObject"
          }
        },
        "title" : "RawFileDataObject",
        "required" : [ "type", "path" ],
        "additionalProperties" : false,
        "description" : "DataObject of type raw for files with unknown content.  \nProvides details to an Action to access raw files.\n\nBy specifying customFormat, binary or text files can read with Spark.\n\nUse this DataObject to move files around without interpreting their content, e.g. with a FileTransferAction, or to\ningest formats SDLB has no dedicated DataObject for. Without customFormat this DataObject can only be used by\nfile based Actions; reading or writing it with Spark then fails with a configuration exception. Example:\n```\ndataObjects = {\n  stg-documents {\n    type = RawFileDataObject\n    path = \"~{env.basedir}/stg_documents\"\n    customFormat = binaryFile\n    partitions = [year]\n  }\n}\n```\n Note that customPartitionLayout is only supported by file based Actions, not by Spark:\n```\ndataObjects = {\n  stg-documents-files {\n    type = RawFileDataObject\n    path = \"~{env.basedir}/stg_documents\"\n    fileName = \"*.pdf\"\n    partitions = [year]\n    customPartitionLayout = \"%year%/\"\n  }\n}\n```\n"
      },
      "RelaxedCsvFileDataObject" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "RelaxedCsvFileDataObject"
          },
          "path" : {
            "type" : "string",
            "description" : "The root path of the files that are handled by this DataObject.  \nFor most DataObjects this can be a directory or a specific file.\n\nIf it doesn't contain scheme and authority, the connections pathPrefix is applied. If pathPrefix is not\ndefined or doesn't define scheme and authority, default schema and authority is applied.\n\nOptionally defined partitions are appended with hadoop standard partition layout to this path."
          },
          "csvOptions" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "Settings for the underlying`org.apache.spark.sql.DataFrameReader]]and[[org.apache.spark.sql.DataFrameWriter`."
          },
          "partitions" : {
            "type" : "array",
            "items" : {
              "type" : "string"
            },
            "description" : "Definition of partition columns\n\nExample: `[dt]`"
          },
          "schema" : {
            "type" : "string",
            "description" : "An optional user-defined schema definition for this DataObject.  \nIf defined, any automatic schema inference is avoided.\n\nThe schema corresponds to the schema on write, it must not include optional columns on read, e.g. the filenameColumn for SparkFileDataObjects.  \nIf partition columns are missing, they will be added with type String.\n\nDefine the schema by using one of the schema providers below, default is DDL.  \nThe schema provider and its configuration value must be provided in the format <PROVIDERID>#<VALUE>.\n\nSchema Providers available are (see also `io.smartdatalake.util.misc.SchemaProviderType` ):\n\n- ddl: create the schema from a Spark ddl string, e.g. `ddl#a string, b array<struct<b1: string, b2: long>>, c struct<c1: string, c2: long>` .\n\n- ddlFile: read a Spark ddl definition from a file and create a schema, e.g. `ddlFile#abc/xyz.ddl` .\n\n- caseClass: convert a Scala Case Class to a schema using Spark encoders, e.g. `caseClass#com.sample.XyzClass` .\n\n- javaBean: convert a Java Bean to a schema using Spark encoders, e.g. `javaBean#com.sample.XyzClass` .\n\n- xsdFile: read an Xml Schema Definition file and create a schema, e.g. `xsdFile#abc/xyz.xsd` .  \nThe following parameters allow to customize the behavior: `xsdFile#<path-to-xsd-file>;<row-tag>;<maxRecursion:Int>;<jsonCompatibility:Boolean>` <row-tag>: configure the path of the element to extract from the xsd schema. Leave empty to extract the root.  \n<maxRecursion>: if xsd schema is recursive, this configures the number of levels to create in the schema.  \nDefault is 10 levels.  \n<jsonCompatibility>: In XML array elements are modeled with their own tag named with singular name.  \nIn JSON an array attribute has unnamed array entries, but the array attribute has a plural name.  \nIf true, the singular name of the array element in the XSD is converted to a plural name by adding an 's'\nin order to read corresponding json files.  \nDefault is false.\n\n- jsonSchemaFile: read a Json Schema file and create a schema, e.g. `jsonSchemaFile#abc/xyz.json` .  \nThe following parameters allow to customize the behavior: `jsonSchemaFile#<path-to-json-file>;<row-tag>;<strictTyping:Boolean>;<additionalPropertiesDefault:Boolean>` <row-tag>: configure the path of the element to extract from the json schema. Leave empty to extract the root.  \n<strictTyping>: if true\nunion types (oneOf) are merged if rational, otherwise they are simply mapped to StringType;\nadditional properties are ignored, otherwise the corresponding schema object is mapped to MapType(String,String).  \nDefault is strictTyping=false.  \n<additionalPropertiesDefault>: Set to true or false.  \nThis is used as default value for 'additionalProperties'-field if it is missing in a schema with type='object'.  \nDefault value is additionalPropertiesDefault=true, as this is conform with the specification.\n\n- avroSchemaFile: read an Avro Schema file and create a schema, e.g. `avroSchemaFile#abc/xyz.avsc` .  \nThe following parameters allow to customize the behavior: `avroSchemaFile#<path-to-avsc-file>;<row-tag>` <row-tag>: configure the path of the element to extract from the avro schema. Leave empty to extract the root.\n\n- openApi: read Schema from OpenApi specification operation, e.g. `openApi#https://swagger.myswitzerland.io;getDestinations;opendata.json` .  \nThe following parameters allow to customize the behaviour: `openApi#<baseUrl>;<operationId>;[<apiDocsPath>;][<responseContentType>]`\n\nNote that all schema files are configured as Hadoop path. The custom prefix 'cp' can be used to read schema files\nfrom the classpath, e.g. `xsdFile#cp:/xyz.xsd` ."
          },
          "schemaMin" : {
            "type" : "string",
            "description" : "An optional, minimal schema that a `DataObject`schema must have to pass schema validation.\n\nThe schema validation semantics are:\n- Schema A is valid in respect to a minimal schema B when B is a subset of A. This means: the whole column set of B is contained in the column set of A.  \n- A column of B is contained in A when A contains a column with equal name and data type.  \n- Column order is ignored.  \n- Column nullability is ignored.  \n- Duplicate columns in terms of name and data type are eliminated (set semantics).\n\nNote: This is mainly used by the functionality defined in `CanCreateDataFrame` and `CanWriteDataFrame`, that is,\nwhen reading or writing Spark data frames from/to the underlying data container. `io.smartdatalake.workflow.action.Action`s that work with files ignore the `schemaMin` attribute\nif it is defined.  \nAdditionally schemaMin can be used to define the schema used if there is no data or table doesn't yet exist."
          },
          "dateColumnType" : {
            "type" : "string",
            "description" : "how to convert columns of Spark type date before writing (default: date). With`date`they are\ncast to timestamp, with`string`they are cast to string.  \nSee`io.smartdatalake.definitions.DateColumnType`.",
            "enum" : [ "Default", "String", "Date" ]
          },
          "treatMissingColumnsAsCorrupt" : {
            "type" : "boolean",
            "description" : "If set to true records from files with missing columns in its header are treated as corrupt (default=false).  \nCorrupt records are handled according to options.mode (default=permissive)."
          },
          "treatSuperfluousColumnsAsCorrupt" : {
            "type" : "boolean",
            "description" : "If set to true records from files with superfluous columns in its header are treated as corrupt (default=false).  \nCorrupt records are handled according to options.mode (default=permissive)."
          },
          "saveMode" : {
            "type" : "string",
            "description" : "Overwrite or Append new data.  \nWhen writing partitioned data, this applies only to partitions concerned.",
            "enum" : [ "Merge", "OverwriteOptimized", "OverwritePreserveDirectories", "Ignore", "ErrorIfExists", "Append", "Overwrite" ]
          },
          "sparkRepartition" : {
            "type" : "object",
            "properties" : {
              "numberOfTasksPerPartition" : {
                "type" : "integer",
                "description" : "Number of Spark tasks to create per partition before writing to DataObject by repartitioning the DataFrame.  \nThis controls how many files are created in each Hadoop partition."
              },
              "keyCols" : {
                "type" : "array",
                "items" : {
                  "type" : "string"
                },
                "description" : "Optional key columns to distribute records over Spark tasks inside a Hadoop partition.  \nIf DataObject has Hadoop partitions defined, keyCols must be defined."
              },
              "sortCols" : {
                "type" : "array",
                "items" : {
                  "type" : "string"
                },
                "description" : "Optional columns to sort records inside files created."
              },
              "filename" : {
                "type" : "string",
                "description" : "Option filename to rename target file(s). If numberOfTasksPerPartition is greater than 1,\nmultiple files can exist in a directory and a number is inserted into the filename after the first '.'.  \nExample: filename=data.csv → files created are data.1.csv, data.2.csv, ..."
              }
            },
            "title" : "SparkRepartitionDef",
            "required" : [ "numberOfTasksPerPartition" ],
            "additionalProperties" : false,
            "description" : "Definition of repartition operation before writing DataFrame with Spark to Hadoop."
          },
          "connectionId" : {
            "type" : "string",
            "description" : "Return the connection id.\n\nConnection defines path prefix (scheme, authority, base path) and ACL's in central location."
          },
          "filenameColumn" : {
            "type" : "string",
            "description" : "The name of the (optional) additional column containing the source filename"
          },
          "expectedPartitionsCondition" : {
            "type" : "string",
            "description" : "Definition of partitions that are expected to exists.  \nThis is used to validate that partitions being read exists and don't return no data.  \nDefine a Spark SQL expression that is evaluated against a `PartitionValues` instance and returns true or false\n\nExample: \"elements['yourColName'] > 2017\"\n\nIf empty (default) all partition are expected to exists.\n\nOTHERTAG: true if partition is expected to exist."
          },
          "housekeepingMode" : {
            "oneOf" : [ {
              "$ref" : "#/definitions/HousekeepingMode/PartitionArchiveMode"
            }, {
              "$ref" : "#/definitions/HousekeepingMode/PartitionRetentionMode"
            } ],
            "description" : "Configure a housekeeping mode to e.g cleanup, archive and compact partitions.\n\nDefault is None."
          },
          "constraints" : {
            "type" : "array",
            "items" : {
              "type" : "object",
              "properties" : {
                "name" : {
                  "type" : "string",
                  "description" : "name of the constraint"
                },
                "description" : {
                  "type" : "string",
                  "description" : "optional detailed description of the constraint"
                },
                "expression" : {
                  "type" : "string",
                  "description" : "SQL expression to evaluate on every row. The expressions return value should be a boolean.  \nIf it evaluates to true the constraint is validated successfully, otherwise it will throw an exception."
                },
                "errorMsgCols" : {
                  "type" : "array",
                  "items" : {
                    "type" : "string"
                  },
                  "description" : "Optional list of column names to add to error message.  \nNote that primary key columns are always included.  \nIf there is no primary key defined, by default all columns with simple datatype are included in the error message."
                }
              },
              "title" : "Constraint",
              "required" : [ "name", "expression" ],
              "additionalProperties" : false,
              "description" : "Definition of row-level constraint to validate."
            },
            "description" : "List of constraint definitions to validate on write, see `Constraint` for details.  \nConstraints are expressions defined on row-level and validated during evaluation of the DataFrame.  \nIf validation fails an exception is thrown and further processing is stopped.  \nNote that this is done while evaluating the DataFrame when writing to the DataObject. It doesn't need a separate action on the DataFrame.  \nIf a constraint validation for a row fails, it will throw an exception and abort writing to the DataObject."
          },
          "expectations" : {
            "type" : "array",
            "items" : {
              "oneOf" : [ {
                "$ref" : "#/definitions/Expectation/AvgCountPerPartitionExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/CountExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/SQLExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/SQLFractionExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/SQLQueryExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/UniqueKeyExpectation"
              } ]
            },
            "description" : "List expectation definitions to evaluate on write, see `Expectation` for details.  \nExpectations are aggregation expressions defined on dataset-level and evaluated on every write.  \nBy default their result is logged with level info (ok) and error (failed), but this can be customized to be logged as warning.  \nIn case of failed expectations logged as error, an exceptions is thrown and further processing is stopped.  \nNote that the exception is thrown after writing to the DataObject is finished.\n\nThe following expectations names are reserved to create default metrics and should not be used:\n- count"
          },
          "metadata" : {
            "$ref" : "#/definitions/Others/DataObjectMetadata",
            "description" : "Additional metadata for the DataObject"
          }
        },
        "title" : "RelaxedCsvFileDataObject",
        "required" : [ "type", "path" ],
        "additionalProperties" : false,
        "description" : "A `DataObject` which allows for more flexible CSV parsing.  \nThe standard CsvFileDataObject does not support reading multiple CSV-Files with different column order, missing columns\nor additional columns.  \nRelaxCsvFileDataObject works more like reading JSON-Files. You need to define a schema, then it tries to read every file\nwith that schema independently of the column order, adding missing columns and removing superfluous ones.\n\nCSV files are read by Spark as whole text files and then parsed manually with Sparks CSV parser class. You can therefore use the\nnormal CSV options of spark, but some properties are fixed, e.g. header=true, inferSchema=false, enforceSchema (ignored). Example:\n```\ndataObjects = {\n  stg-airports {\n    type = RelaxedCsvFileDataObject\n    path = \"~{env.basedir}/stg_airports\"\n    schema = \"ident string, name string, elevation_ft integer, _corrupt_record string, _corrupt_record_msg string\"\n    csvOptions { delimiter = \";\" }\n    treatMissingColumnsAsCorrupt = true\n    treatSuperfluousColumnsAsCorrupt = true\n  }\n}\n```\n\n\nNOTE: This data object sets the following default values for `csvOptions` : delimiter = \",\", quote = null\nAll other `csvOption` default to the values defined by Apache Spark.\n\nSEE: `org.apache.spark.sql.DataFrameReader`\n\nSEE: [[org.apache.spark.sql.DataFrameWriter]]\n\nIf mode is permissive you can retrieve the corrupt input record by adding <options.columnNameOfCorruptRecord> as field to the schema.  \nRelaxCsvFileDataObject also supports getting an error msg by adding \"<options.columnNameOfCorruptRecord>_msg\" as field to the schema."
      },
      "SFtpFileRefDataObject" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "SFtpFileRefDataObject"
          },
          "path" : {
            "type" : "string",
            "description" : "The root path of the files that are handled by this DataObject.  \nFor most DataObjects this can be a directory or a specific file.\n\nIf it doesn't contain scheme and authority, the connections pathPrefix is applied. If pathPrefix is not\ndefined or doesn't define scheme and authority, default schema and authority is applied.\n\nOptionally defined partitions are appended with hadoop standard partition layout to this path."
          },
          "connectionId" : {
            "type" : "string",
            "description" : "id of the`SFtpFileRefConnection` defining host, port and authentication mode"
          },
          "partitions" : {
            "type" : "array",
            "items" : {
              "type" : "string"
            },
            "description" : "Definition of partition columns\n\nExample: `[dt]`"
          },
          "partitionLayout" : {
            "type" : "string",
            "description" : "partition layout defines how partition values can be extracted from the path.  \nUse \"%<colname>%\" as token to extract the value for a partition column.  \nAs partition layout extracts partition from the path of individual files, it can also be used to extract partitions from the file name.  \nWith \"%<colname:regex>%\" a regex can be given to limit search. This is especially useful\nif there is no char to delimit the last token from the rest of the path or also between\ntwo tokens.  \nBe careful that for directory based partition values extraction, the final path separator must be part\nof the partition layout to extract the last token correctly, e.g. \"%year%/\" for partitioning with yearly directories."
          },
          "saveMode" : {
            "type" : "string",
            "description" : "Overwrite or Append new data.",
            "enum" : [ "Merge", "OverwriteOptimized", "OverwritePreserveDirectories", "Ignore", "ErrorIfExists", "Append", "Overwrite" ]
          },
          "expectedPartitionsCondition" : {
            "type" : "string",
            "description" : "Optional definition of partitions expected to exist.  \nDefine a Spark SQL expression that is evaluated against a`PartitionValues` instance and returns true or false\nDefault is to expect all partitions to exist."
          },
          "metadata" : {
            "$ref" : "#/definitions/Others/DataObjectMetadata",
            "description" : "Additional metadata for the DataObject"
          }
        },
        "title" : "SFtpFileRefDataObject",
        "required" : [ "type", "path", "connectionId" ],
        "additionalProperties" : false,
        "description" : "Connects to SFtp files\nNeeds java library \"com.hieronymus % sshj % 0.21.1\"\nThe following authentication mechanisms are supported\n→ public/private-key: private key must be saved in ~/.ssh, public key must be registered on server.  \n→ user/pwd authentication: user and password is taken from two variables set as parameters.  \nThese variables could come from clear text (CLEAR), a file (FILE) or an environment variable (ENV)\n\nHost, port and credentials are not configured here but in the referenced `SFtpFileRefConnection`, so that they can\nbe shared between multiple DataObjects. Files are only referenced, not parsed - use a FileTransferAction to copy\nthem to/from another FileRefDataObject. Example:\n```\nconnections = {\n  sftp-src {\n    type = SFtpFileRefConnection\n    host = \"sftp.example.com\"\n    authMode = { type = BasicAuthMode, user = \"###ENV#SFTP_USER###\", password = \"###ENV#SFTP_PWD###\" }\n  }\n}\ndataObjects = {\n  ext-airports {\n    type = SFtpFileRefDataObject\n    path = \"/data/airports\"\n    connectionId = sftp-src\n    partitions = [year]\n    partitionLayout = \"%year%/\"\n  }\n}\n```\n"
      },
      "SnowflakeTableDataObject" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "SnowflakeTableDataObject"
          },
          "table" : {
            "$ref" : "#/definitions/Others/Table",
            "description" : "Snowflake table to be written by this output"
          },
          "schemaMin" : {
            "type" : "string",
            "description" : "An optional, minimal schema that a `DataObject`schema must have to pass schema validation.\n\nThe schema validation semantics are:\n- Schema A is valid in respect to a minimal schema B when B is a subset of A. This means: the whole column set of B is contained in the column set of A.  \n- A column of B is contained in A when A contains a column with equal name and data type.  \n- Column order is ignored.  \n- Column nullability is ignored.  \n- Duplicate columns in terms of name and data type are eliminated (set semantics).\n\nNote: This is mainly used by the functionality defined in `CanCreateDataFrame` and `CanWriteDataFrame`, that is,\nwhen reading or writing Spark data frames from/to the underlying data container. `io.smartdatalake.workflow.action.Action`s that work with files ignore the `schemaMin` attribute\nif it is defined.  \nAdditionally schemaMin can be used to define the schema used if there is no data or table doesn't yet exist."
          },
          "constraints" : {
            "type" : "array",
            "items" : {
              "type" : "object",
              "properties" : {
                "name" : {
                  "type" : "string",
                  "description" : "name of the constraint"
                },
                "description" : {
                  "type" : "string",
                  "description" : "optional detailed description of the constraint"
                },
                "expression" : {
                  "type" : "string",
                  "description" : "SQL expression to evaluate on every row. The expressions return value should be a boolean.  \nIf it evaluates to true the constraint is validated successfully, otherwise it will throw an exception."
                },
                "errorMsgCols" : {
                  "type" : "array",
                  "items" : {
                    "type" : "string"
                  },
                  "description" : "Optional list of column names to add to error message.  \nNote that primary key columns are always included.  \nIf there is no primary key defined, by default all columns with simple datatype are included in the error message."
                }
              },
              "title" : "Constraint",
              "required" : [ "name", "expression" ],
              "additionalProperties" : false,
              "description" : "Definition of row-level constraint to validate."
            },
            "description" : "List of row-level`Constraint`s to enforce when writing to this data object."
          },
          "expectations" : {
            "type" : "array",
            "items" : {
              "oneOf" : [ {
                "$ref" : "#/definitions/Expectation/AvgCountPerPartitionExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/CountExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/SQLExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/SQLFractionExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/SQLQueryExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/UniqueKeyExpectation"
              } ]
            },
            "description" : "List of`Expectation`s to enforce when writing to this data object. Expectations are checks based on aggregates over all rows of a dataset."
          },
          "preReadSql" : {
            "type" : "string",
            "description" : "SQL-statement to be executed in exec phase before reading input table. It uses the SnowflakeConnection for the target database."
          },
          "postReadSql" : {
            "type" : "string",
            "description" : "SQL-statement to be executed in exec phase after reading input table and before action is finished. It uses the SnowflakeConnection for the target database."
          },
          "preWriteSql" : {
            "type" : "string",
            "description" : "SQL-statement to be executed in exec phase before writing output table. It uses the SnowflakeConnection for the target database."
          },
          "postWriteSql" : {
            "type" : "string",
            "description" : "SQL-statement to be executed in exec phase after writing output table. It uses the SnowflakeConnection for the target database."
          },
          "saveMode" : {
            "type" : "string",
            "description" : "spark`SDLSaveMode` to use when writing files, default is \"Overwrite\"",
            "enum" : [ "Merge", "OverwriteOptimized", "OverwritePreserveDirectories", "Ignore", "ErrorIfExists", "Append", "Overwrite" ]
          },
          "connectionId" : {
            "type" : "string",
            "description" : "The SnowflakeTableConnection to use for the table"
          },
          "sparkOptions" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "Options for the Snowflake Spark Connector, see https://docs.snowflake.com/en/user-guide/spark-connector-use#additional-options.  \nThese options override connection.options."
          },
          "virtualPartitions" : {
            "type" : "array",
            "items" : {
              "type" : "string"
            },
            "description" : "Virtual partition columns. Note that Snowflake has no partition concept, and SDLB is emulating partitions on its own."
          },
          "readTransformer" : {
            "oneOf" : [ {
              "$ref" : "#/definitions/GenericDfTransformer/BlacklistTransformer"
            }, {
              "$ref" : "#/definitions/GenericDfTransformer/ColumnsTransformer"
            }, {
              "$ref" : "#/definitions/GenericDfTransformer/ConvertNullValuesTransformer"
            }, {
              "$ref" : "#/definitions/GenericDfTransformer/DataValidationTransformer"
            }, {
              "$ref" : "#/definitions/GenericDfTransformer/DebugTransformer"
            }, {
              "$ref" : "#/definitions/GenericDfTransformer/DecryptColumnsTransformer"
            }, {
              "$ref" : "#/definitions/GenericDfTransformer/DeduplicateTransformer"
            }, {
              "$ref" : "#/definitions/GenericDfTransformer/EncryptColumnsTransformer"
            }, {
              "$ref" : "#/definitions/GenericDfTransformer/FilterTransformer"
            }, {
              "$ref" : "#/definitions/GenericDfTransformer/PythonCodeDfTransformer"
            }, {
              "$ref" : "#/definitions/GenericDfTransformer/SQLDfTransformer"
            }, {
              "$ref" : "#/definitions/GenericDfTransformer/ScalaClassGenericDfTransformer"
            }, {
              "$ref" : "#/definitions/GenericDfTransformer/ScalaClassSnowparkDfTransformer"
            }, {
              "$ref" : "#/definitions/GenericDfTransformer/ScalaClassSparkDfTransformer"
            }, {
              "$ref" : "#/definitions/GenericDfTransformer/ScalaCodeSparkDfTransformer"
            }, {
              "$ref" : "#/definitions/GenericDfTransformer/ScalaNotebookSparkDfTransformer"
            }, {
              "$ref" : "#/definitions/GenericDfTransformer/SparkFlattenDfTransformer"
            }, {
              "$ref" : "#/definitions/GenericDfTransformer/SparkRepartitionTransformer"
            }, {
              "$ref" : "#/definitions/GenericDfTransformer/StandardizeColNamesTransformer"
            }, {
              "$ref" : "#/definitions/GenericDfTransformer/StandardizeSparkDatatypesTransformer"
            }, {
              "$ref" : "#/definitions/GenericDfTransformer/WhitelistTransformer"
            } ],
            "description" : "An optional transformer that is applied on read. This is often used to adapt Snowflakes Decimal datatype to more accurate IntegralTypes like Long, Integer, Byte."
          },
          "expectedPartitionsCondition" : {
            "type" : "string",
            "description" : "Optional definition of partitions expected to exist.  \nDefine a Spark SQL expression that is evaluated against a`PartitionValues` instance and returns true or false\nDefault is to expect all partitions to exist."
          },
          "metadata" : {
            "$ref" : "#/definitions/Others/DataObjectMetadata",
            "description" : "meta data"
          }
        },
        "title" : "SnowflakeTableDataObject",
        "required" : [ "type", "table", "connectionId" ],
        "additionalProperties" : false,
        "description" : "`DataObject` of type SnowflakeTableDataObject.  \nProvides details to access Snowflake tables via an action\nCan be used both for interacting with Snowflake through Spark with JDBC,\nas well as for actions written in the Snowpark API that run directly on Snowflake\n\nNote 1: Snowflake does not support partitioning.  \nBut SDLB emulates partitions through the `virtualPartitions` attribute.\n\nNote 2: Snowflake is tricky with Datatypes.  \nAs Integral types like Long, Int, Short don't exist in Snowflake, they all become a Decimal(38,0) by default.  \nSDLB improves the default by automatically converting Integral types to Decimals with accurate precision on write, e.g. Int => Decimal(10,0).  \nFor adapting types on read, use the `readTransformer` attribute.\n\nNote 3: case-insensitive column names are all uppercase in Snowflake tables. This is opposite from Spark when Spark is in case-insensitive mode (see also `Environment.caseSensitive` )\nIf `Environment.caseSensitive=false` then SDLB converts all case-insensitive column names to lowercase when reading from Snowflake with Spark Connector. Example:\n```\ndataObjects = {\n  tgt-airports {\n    type = SnowflakeTableDataObject\n    connectionId = sf-con\n    table {\n      db = \"test_schema\"\n      name = \"airports\"\n    }\n    virtualPartitions = [dt]\n    saveMode = Append\n  }\n}\n```\n"
      },
      "SparkConnectTableDataObject" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "SparkConnectTableDataObject"
          },
          "table" : {
            "$ref" : "#/definitions/Others/Table",
            "description" : "table to be read/written by this data object"
          },
          "schemaMin" : {
            "type" : "string",
            "description" : "An optional, minimal schema that this DataObject must have to pass schema validation on reading and writing."
          },
          "partitions" : {
            "type" : "array",
            "items" : {
              "type" : "string"
            },
            "description" : "partition columns for this data object"
          },
          "constraints" : {
            "type" : "array",
            "items" : {
              "type" : "object",
              "properties" : {
                "name" : {
                  "type" : "string",
                  "description" : "name of the constraint"
                },
                "description" : {
                  "type" : "string",
                  "description" : "optional detailed description of the constraint"
                },
                "expression" : {
                  "type" : "string",
                  "description" : "SQL expression to evaluate on every row. The expressions return value should be a boolean.  \nIf it evaluates to true the constraint is validated successfully, otherwise it will throw an exception."
                },
                "errorMsgCols" : {
                  "type" : "array",
                  "items" : {
                    "type" : "string"
                  },
                  "description" : "Optional list of column names to add to error message.  \nNote that primary key columns are always included.  \nIf there is no primary key defined, by default all columns with simple datatype are included in the error message."
                }
              },
              "title" : "Constraint",
              "required" : [ "name", "expression" ],
              "additionalProperties" : false,
              "description" : "Definition of row-level constraint to validate."
            },
            "description" : "List of row-level`Constraint`s to enforce when writing to this data object."
          },
          "expectations" : {
            "type" : "array",
            "items" : {
              "oneOf" : [ {
                "$ref" : "#/definitions/Expectation/AvgCountPerPartitionExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/CountExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/SQLExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/SQLFractionExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/SQLQueryExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/UniqueKeyExpectation"
              } ]
            },
            "description" : "List of`Expectation`s to enforce when writing to this data object. Expectations are checks based on aggregates over all rows of a dataset."
          },
          "saveMode" : {
            "type" : "string",
            "description" : "`SDLSaveMode` to use when writing the table, default is \"Overwrite\"",
            "enum" : [ "Merge", "OverwriteOptimized", "OverwritePreserveDirectories", "Ignore", "ErrorIfExists", "Append", "Overwrite" ]
          },
          "format" : {
            "type" : "string",
            "description" : "Optional table format used when creating the table, e.g. parquet or delta. Default is the servers default table format."
          },
          "allowSchemaEvolution" : {
            "type" : "boolean",
            "description" : "If set to true schema evolution will automatically occur when writing to this DataObject with different schema.  \nNote that this needs a table format supporting schema evolution on the server side, e.g. delta."
          },
          "options" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "Options for the Spark DataFrameReader/DataFrameWriter, e.g. format specific options."
          },
          "connectionId" : {
            "type" : "string",
            "description" : "The SparkConnectConnection to use. If not defined, the default engine connection is used."
          },
          "preReadSql" : {
            "type" : "string",
            "description" : "SQL-statement to be executed in exec phase before reading input table."
          },
          "postReadSql" : {
            "type" : "string",
            "description" : "SQL-statement to be executed in exec phase after reading input table and before action is finished."
          },
          "preWriteSql" : {
            "type" : "string",
            "description" : "SQL-statement to be executed in exec phase before writing output table."
          },
          "postWriteSql" : {
            "type" : "string",
            "description" : "SQL-statement to be executed in exec phase after writing output table."
          },
          "expectedPartitionsCondition" : {
            "type" : "string",
            "description" : "Optional definition of partitions expected to exist.  \nDefine a Spark SQL expression that is evaluated against a`PartitionValues` instance and returns true or false\nDefault is to expect all partitions to exist."
          },
          "metadata" : {
            "$ref" : "#/definitions/Others/DataObjectMetadata",
            "description" : "meta data"
          }
        },
        "title" : "SparkConnectTableDataObject",
        "required" : [ "type", "table", "connectionId" ],
        "additionalProperties" : false,
        "description" : "`DataObject` of type SparkConnectTableDataObject.  \nProvides access to tables of the catalog of a remote Spark Connect server through the normal Spark Table API,\ne.g. spark.read.table and DataFrame.write.saveAsTable.\n\nNote that data is only accessed through the Spark Connect session - there is no Hadoop FileSystem access\nto the underlying data from the SDLB client.\n\nNote that SDLSaveMode.Merge needs a table format supporting row-level operations on the server side, e.g. delta or iceberg."
      },
      "WebserviceFileDataObject" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "WebserviceFileDataObject"
          },
          "url" : {
            "type" : "string",
            "description" : "URL of the webservice"
          },
          "additionalHeaders" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "Additional headers to pass with the http request"
          },
          "timeouts" : {
            "type" : "object",
            "properties" : {
              "connectionTimeoutMs" : {
                "type" : "integer"
              },
              "readTimeoutMs" : {
                "type" : "integer"
              }
            },
            "title" : "HttpTimeoutConfig",
            "required" : [ "connectionTimeoutMs", "readTimeoutMs" ],
            "additionalProperties" : false,
            "description" : "optional configuration of HTTP timeouts"
          },
          "authMode" : {
            "oneOf" : [ {
              "$ref" : "#/definitions/HttpAuthMode/AWSUserPwdAuthMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/AuthHeaderMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/AzureADClientGrantAuthMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/BasicAuthMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/CustomHttpAuthMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/OAuthMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/TokenAuthMode"
            } ],
            "description" : "Optional configuration of webservice authentication. Supported`AuthMode` s are BasicAuthMode and CustomHttpAuthMode.  \nCustomHttpAuthMode can be used to implement a custom authentication protocol, e.g. AzureADClientGrantAuthMode in sdl-azure module."
          },
          "mimeType" : {
            "type" : "string",
            "description" : "Optionally specify mime-type of Webservice response. If not specified SDLB tries to guess the type."
          },
          "writeMethod" : {
            "type" : "string",
            "description" : "HTTP method used when uploading data to a webservice.  \nDefault method is POST.",
            "enum" : [ "Delete", "Put", "Post", "Get" ]
          },
          "proxy" : {
            "type" : "object",
            "properties" : {
              "host" : {
                "type" : "string",
                "description" : "proxy host"
              },
              "port" : {
                "type" : "integer",
                "description" : "proxy port"
              },
              "user" : {
                "type" : "string",
                "description" : "The value can be provided in plaintext or with the id of a secret provider and the\nname of the secret in the format\n```\n###<PROVIDERID>#<SECRETNAME>###\n```\n ,\ne.g.  \n```\n###ENV#<ENV_VARIABLE_NAME>###\n```\n\n\nto get a secret from an environment variable."
              },
              "password" : {
                "type" : "string",
                "description" : "The value can be provided in plaintext or with the id of a secret provider and the\nname of the secret in the format\n```\n###<PROVIDERID>#<SECRETNAME>###\n```\n ,\ne.g.  \n```\n###ENV#<ENV_VARIABLE_NAME>###\n```\n\n\nto get a secret from an environment variable."
              }
            },
            "title" : "HttpProxyConfig",
            "required" : [ "host", "port" ],
            "additionalProperties" : false,
            "description" : "optional Proxy configuration used to make HTTP-connection."
          },
          "followRedirects" : {
            "type" : "boolean",
            "description" : "if redirects should be followed when creating HTTP-connection. Default is false because of security concerns."
          },
          "retries" : {
            "type" : "integer",
            "description" : "number of retries if http request fails. Default is 0 retries."
          },
          "partitionDefs" : {
            "type" : "array",
            "items" : {
              "type" : "object",
              "properties" : {
                "name" : {
                  "type" : "string",
                  "description" : "partition name"
                },
                "values" : {
                  "type" : "array",
                  "items" : {
                    "type" : "string"
                  },
                  "description" : "possible values for this partition"
                }
              },
              "title" : "WebservicePartitionDefinition",
              "required" : [ "name", "values" ],
              "additionalProperties" : false,
              "description" : "List of partitions possible values for this partition"
            },
            "description" : "Optional list of partitions and possible partition values.  \nPartitions and their values are mapped as query parameter in webservice requests.  \nMultiple values are translated into multiple requests. Each request handles one combination of partition values."
          },
          "partitionLayout" : {
            "type" : "string",
            "description" : "definition of partitions in query string. Use %<partitionColName>% as placeholder for partition column value in layout."
          },
          "pagingLinkRegex" : {
            "type" : "string",
            "description" : "if Webservice implements paging, configure a regular expression to extract a link for the next page.  \nExample: \"\\slink=(\\S*)\""
          },
          "metadata" : {
            "$ref" : "#/definitions/Others/DataObjectMetadata",
            "description" : "Additional metadata for the DataObject"
          }
        },
        "title" : "WebserviceFileDataObject",
        "required" : [ "type", "url" ],
        "additionalProperties" : false,
        "description" : "`DataObject` to call webservice and return response as InputStream, or upload data as OutputStream to webservice.\n\nThe corresponding Action to process the response or upload data should be a FileTransferAction.  \nThis is implemented as FileRefDataObject because the response / upload data is treated as some file content.\n\nFileRefDataObjects support partitioned data. For a WebserviceFileDataObject partitions are mapped as query parameters to create query string.\n\nQuery parameter (partitions) and possible values can be configured through `partitionDefs` attribute.  \nIf no values are given for a query parameter, the values are taken from the partition values of the input SubFeed, e.g. the command line if it is the first Action in the DAG. Example:\n```\ndataObjects = {\n  ext-departures {\n    type = WebserviceFileDataObject\n    url = \"https://opensky-network.org/api/flights/departure\"\n    authMode = { type = BasicAuthMode, user = \"###ENV#API_USER###\", password = \"###ENV#API_PWD###\" }\n    followRedirects = true\n    retries = 3\n    partitionDefs = [\n      { name = airport, values = [LSZB, EDDF] }\n    ]\n    partitionLayout = \"?airport=%airport%\"\n  }\n}\n```\n"
      },
      "XmlFileDataObject" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "XmlFileDataObject"
          },
          "path" : {
            "type" : "string",
            "description" : "The root path of the files that are handled by this DataObject.  \nFor most DataObjects this can be a directory or a specific file.\n\nIf it doesn't contain scheme and authority, the connections pathPrefix is applied. If pathPrefix is not\ndefined or doesn't define scheme and authority, default schema and authority is applied.\n\nOptionally defined partitions are appended with hadoop standard partition layout to this path."
          },
          "xmlOptions" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "Settings for the underlying`org.apache.spark.sql.DataFrameReader]]and[[org.apache.spark.sql.DataFrameWriter`."
          },
          "partitions" : {
            "type" : "array",
            "items" : {
              "type" : "string"
            },
            "description" : "Definition of partition columns\n\nExample: `[dt]`"
          },
          "schema" : {
            "type" : "string",
            "description" : "An optional user-defined schema definition for this DataObject.  \nIf defined, any automatic schema inference is avoided.\n\nThe schema corresponds to the schema on write, it must not include optional columns on read, e.g. the filenameColumn for SparkFileDataObjects.  \nIf partition columns are missing, they will be added with type String.\n\nDefine the schema by using one of the schema providers below, default is DDL.  \nThe schema provider and its configuration value must be provided in the format <PROVIDERID>#<VALUE>.\n\nSchema Providers available are (see also `io.smartdatalake.util.misc.SchemaProviderType` ):\n\n- ddl: create the schema from a Spark ddl string, e.g. `ddl#a string, b array<struct<b1: string, b2: long>>, c struct<c1: string, c2: long>` .\n\n- ddlFile: read a Spark ddl definition from a file and create a schema, e.g. `ddlFile#abc/xyz.ddl` .\n\n- caseClass: convert a Scala Case Class to a schema using Spark encoders, e.g. `caseClass#com.sample.XyzClass` .\n\n- javaBean: convert a Java Bean to a schema using Spark encoders, e.g. `javaBean#com.sample.XyzClass` .\n\n- xsdFile: read an Xml Schema Definition file and create a schema, e.g. `xsdFile#abc/xyz.xsd` .  \nThe following parameters allow to customize the behavior: `xsdFile#<path-to-xsd-file>;<row-tag>;<maxRecursion:Int>;<jsonCompatibility:Boolean>` <row-tag>: configure the path of the element to extract from the xsd schema. Leave empty to extract the root.  \n<maxRecursion>: if xsd schema is recursive, this configures the number of levels to create in the schema.  \nDefault is 10 levels.  \n<jsonCompatibility>: In XML array elements are modeled with their own tag named with singular name.  \nIn JSON an array attribute has unnamed array entries, but the array attribute has a plural name.  \nIf true, the singular name of the array element in the XSD is converted to a plural name by adding an 's'\nin order to read corresponding json files.  \nDefault is false.\n\n- jsonSchemaFile: read a Json Schema file and create a schema, e.g. `jsonSchemaFile#abc/xyz.json` .  \nThe following parameters allow to customize the behavior: `jsonSchemaFile#<path-to-json-file>;<row-tag>;<strictTyping:Boolean>;<additionalPropertiesDefault:Boolean>` <row-tag>: configure the path of the element to extract from the json schema. Leave empty to extract the root.  \n<strictTyping>: if true\nunion types (oneOf) are merged if rational, otherwise they are simply mapped to StringType;\nadditional properties are ignored, otherwise the corresponding schema object is mapped to MapType(String,String).  \nDefault is strictTyping=false.  \n<additionalPropertiesDefault>: Set to true or false.  \nThis is used as default value for 'additionalProperties'-field if it is missing in a schema with type='object'.  \nDefault value is additionalPropertiesDefault=true, as this is conform with the specification.\n\n- avroSchemaFile: read an Avro Schema file and create a schema, e.g. `avroSchemaFile#abc/xyz.avsc` .  \nThe following parameters allow to customize the behavior: `avroSchemaFile#<path-to-avsc-file>;<row-tag>` <row-tag>: configure the path of the element to extract from the avro schema. Leave empty to extract the root.\n\n- openApi: read Schema from OpenApi specification operation, e.g. `openApi#https://swagger.myswitzerland.io;getDestinations;opendata.json` .  \nThe following parameters allow to customize the behaviour: `openApi#<baseUrl>;<operationId>;[<apiDocsPath>;][<responseContentType>]`\n\nNote that all schema files are configured as Hadoop path. The custom prefix 'cp' can be used to read schema files\nfrom the classpath, e.g. `xsdFile#cp:/xyz.xsd` ."
          },
          "schemaMin" : {
            "type" : "string",
            "description" : "An optional, minimal schema that a `DataObject`schema must have to pass schema validation.\n\nThe schema validation semantics are:\n- Schema A is valid in respect to a minimal schema B when B is a subset of A. This means: the whole column set of B is contained in the column set of A.  \n- A column of B is contained in A when A contains a column with equal name and data type.  \n- Column order is ignored.  \n- Column nullability is ignored.  \n- Duplicate columns in terms of name and data type are eliminated (set semantics).\n\nNote: This is mainly used by the functionality defined in `CanCreateDataFrame` and `CanWriteDataFrame`, that is,\nwhen reading or writing Spark data frames from/to the underlying data container. `io.smartdatalake.workflow.action.Action`s that work with files ignore the `schemaMin` attribute\nif it is defined.  \nAdditionally schemaMin can be used to define the schema used if there is no data or table doesn't yet exist."
          },
          "saveMode" : {
            "type" : "string",
            "description" : "Overwrite or Append new data.  \nWhen writing partitioned data, this applies only to partitions concerned.",
            "enum" : [ "Merge", "OverwriteOptimized", "OverwritePreserveDirectories", "Ignore", "ErrorIfExists", "Append", "Overwrite" ]
          },
          "sparkRepartition" : {
            "type" : "object",
            "properties" : {
              "numberOfTasksPerPartition" : {
                "type" : "integer",
                "description" : "Number of Spark tasks to create per partition before writing to DataObject by repartitioning the DataFrame.  \nThis controls how many files are created in each Hadoop partition."
              },
              "keyCols" : {
                "type" : "array",
                "items" : {
                  "type" : "string"
                },
                "description" : "Optional key columns to distribute records over Spark tasks inside a Hadoop partition.  \nIf DataObject has Hadoop partitions defined, keyCols must be defined."
              },
              "sortCols" : {
                "type" : "array",
                "items" : {
                  "type" : "string"
                },
                "description" : "Optional columns to sort records inside files created."
              },
              "filename" : {
                "type" : "string",
                "description" : "Option filename to rename target file(s). If numberOfTasksPerPartition is greater than 1,\nmultiple files can exist in a directory and a number is inserted into the filename after the first '.'.  \nExample: filename=data.csv → files created are data.1.csv, data.2.csv, ..."
              }
            },
            "title" : "SparkRepartitionDef",
            "required" : [ "numberOfTasksPerPartition" ],
            "additionalProperties" : false,
            "description" : "Definition of repartition operation before writing DataFrame with Spark to Hadoop."
          },
          "connectionId" : {
            "type" : "string",
            "description" : "Return the connection id.\n\nConnection defines path prefix (scheme, authority, base path) and ACL's in central location."
          },
          "filenameColumn" : {
            "type" : "string",
            "description" : "The name of the (optional) additional column containing the source filename"
          },
          "expectedPartitionsCondition" : {
            "type" : "string",
            "description" : "Definition of partitions that are expected to exists.  \nThis is used to validate that partitions being read exists and don't return no data.  \nDefine a Spark SQL expression that is evaluated against a `PartitionValues` instance and returns true or false\n\nExample: \"elements['yourColName'] > 2017\"\n\nIf empty (default) all partition are expected to exists.\n\nOTHERTAG: true if partition is expected to exist."
          },
          "housekeepingMode" : {
            "oneOf" : [ {
              "$ref" : "#/definitions/HousekeepingMode/PartitionArchiveMode"
            }, {
              "$ref" : "#/definitions/HousekeepingMode/PartitionRetentionMode"
            } ],
            "description" : "Configure a housekeeping mode to e.g cleanup, archive and compact partitions.\n\nDefault is None."
          },
          "constraints" : {
            "type" : "array",
            "items" : {
              "type" : "object",
              "properties" : {
                "name" : {
                  "type" : "string",
                  "description" : "name of the constraint"
                },
                "description" : {
                  "type" : "string",
                  "description" : "optional detailed description of the constraint"
                },
                "expression" : {
                  "type" : "string",
                  "description" : "SQL expression to evaluate on every row. The expressions return value should be a boolean.  \nIf it evaluates to true the constraint is validated successfully, otherwise it will throw an exception."
                },
                "errorMsgCols" : {
                  "type" : "array",
                  "items" : {
                    "type" : "string"
                  },
                  "description" : "Optional list of column names to add to error message.  \nNote that primary key columns are always included.  \nIf there is no primary key defined, by default all columns with simple datatype are included in the error message."
                }
              },
              "title" : "Constraint",
              "required" : [ "name", "expression" ],
              "additionalProperties" : false,
              "description" : "Definition of row-level constraint to validate."
            },
            "description" : "List of constraint definitions to validate on write, see `Constraint` for details.  \nConstraints are expressions defined on row-level and validated during evaluation of the DataFrame.  \nIf validation fails an exception is thrown and further processing is stopped.  \nNote that this is done while evaluating the DataFrame when writing to the DataObject. It doesn't need a separate action on the DataFrame.  \nIf a constraint validation for a row fails, it will throw an exception and abort writing to the DataObject."
          },
          "expectations" : {
            "type" : "array",
            "items" : {
              "oneOf" : [ {
                "$ref" : "#/definitions/Expectation/AvgCountPerPartitionExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/CountExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/SQLExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/SQLFractionExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/SQLQueryExpectation"
              }, {
                "$ref" : "#/definitions/Expectation/UniqueKeyExpectation"
              } ]
            },
            "description" : "List expectation definitions to evaluate on write, see `Expectation` for details.  \nExpectations are aggregation expressions defined on dataset-level and evaluated on every write.  \nBy default their result is logged with level info (ok) and error (failed), but this can be customized to be logged as warning.  \nIn case of failed expectations logged as error, an exceptions is thrown and further processing is stopped.  \nNote that the exception is thrown after writing to the DataObject is finished.\n\nThe following expectations names are reserved to create default metrics and should not be used:\n- count"
          },
          "metadata" : {
            "$ref" : "#/definitions/Others/DataObjectMetadata",
            "description" : "Additional metadata for the DataObject"
          }
        },
        "title" : "XmlFileDataObject",
        "required" : [ "type", "path" ],
        "additionalProperties" : false,
        "description" : "A `DataObject` backed by an XML data source.\n\nIt manages read and write access and configurations required for `Action`s to\nwork on XML formatted files.\n\nReading and writing details are delegated to Apache Spark `org.apache.spark.sql.DataFrameReader` and `org.apache.spark.sql.DataFrameWriter` respectively. The reader and writer implementations are provided by\nSparks built-in XML data source, see https://spark.apache.org/docs/latest/sql-data-sources-xml.html.\n\nThe most important entry in `xmlOptions` is `rowTag` , which selects the XML element that is mapped to one row of the\nresulting DataFrame. Nested elements become structs and arrays, so a transformer is normally needed to flatten them.\n\nRows read can be validated against an XSD schema by setting the `rowValidationXSDPath` option. The XSD file is read\nthrough the Hadoop filesystem, so a full path can be given, e.g. `dbfs:/demos/xml/invoice.xsd` .  \nNote that this is independent of deriving the schema of this DataObject from an XSD file, which is done with the `xsdfile` schema provider, see also `schema` attribute. Example:\n```\ndataObjects = {\n  stg-airports {\n    type = XmlFileDataObject\n    path = \"~{env.basedir}/stg_airports\"\n    xmlOptions { rowTag = \"entry\", dateFormat = \"yyyy-MM-dd\" }\n    schema = \"ident string, name string, elevation_ft integer\"\n  }\n}\n```\n"
      }
    },
    "Action" : {
      "CopyAction" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "CopyAction"
          },
          "inputId" : {
            "type" : "string",
            "description" : "inputs DataObject"
          },
          "outputId" : {
            "type" : "string",
            "description" : "output DataObject"
          },
          "deleteDataAfterRead" : {
            "type" : "boolean",
            "description" : "a flag to enable deletion of input partitions after copying."
          },
          "transformers" : {
            "type" : "array",
            "items" : {
              "oneOf" : [ {
                "$ref" : "#/definitions/GenericDfTransformer/BlacklistTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfTransformer/ColumnsTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfTransformer/ConvertNullValuesTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfTransformer/DataValidationTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfTransformer/DebugTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfTransformer/DecryptColumnsTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfTransformer/DeduplicateTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfTransformer/EncryptColumnsTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfTransformer/FilterTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfTransformer/PythonCodeDfTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfTransformer/SQLDfTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfTransformer/ScalaClassGenericDfTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfTransformer/ScalaClassSnowparkDfTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfTransformer/ScalaClassSparkDfTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfTransformer/ScalaCodeSparkDfTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfTransformer/ScalaNotebookSparkDfTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfTransformer/SparkFlattenDfTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfTransformer/SparkRepartitionTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfTransformer/StandardizeColNamesTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfTransformer/StandardizeSparkDatatypesTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfTransformer/WhitelistTransformer"
              } ]
            },
            "description" : "optional list of transformations to apply. See`spark.transformer` for a list of included\nTransformers. The transformations are applied according to the lists ordering."
          },
          "cacheOutput" : {
            "type" : "boolean",
            "description" : "Cache the output DataFrame of this Action, so that subsequent Actions can reuse it instead of reading the output\nDataObject again. This saves reading the data again, at the cost of materializing it in the engine.\n\nBy default the DataFrame is not propagated to subsequent Actions, they get a fresh DataFrame from the output\nDataObject according to the SubFeed's partition values.\n\nNote that on Snowflake this materializes a temporary table which is only released when the session is closed."
          },
          "cacheInput" : {
            "type" : "boolean",
            "description" : "Force materializing input DataFrame's.  \nThis improves performance if the input DataFrame is used multiple times in the transformation of this Action\nand can serve as a recovery point in case a task gets lost."
          },
          "executionMode" : {
            "oneOf" : [ {
              "$ref" : "#/definitions/ExecutionMode/CustomPartitionMode"
            }, {
              "$ref" : "#/definitions/ExecutionMode/DataFrameIncrementalMode"
            }, {
              "$ref" : "#/definitions/ExecutionMode/DataObjectStateIncrementalMode"
            }, {
              "$ref" : "#/definitions/ExecutionMode/FailIfNoPartitionValuesMode"
            }, {
              "$ref" : "#/definitions/ExecutionMode/FileIncrementalMoveMode"
            }, {
              "$ref" : "#/definitions/ExecutionMode/KafkaStateIncrementalMode"
            }, {
              "$ref" : "#/definitions/ExecutionMode/PartitionDiffMode"
            }, {
              "$ref" : "#/definitions/ExecutionMode/ProcessAllMode"
            }, {
              "$ref" : "#/definitions/ExecutionMode/SparkStreamingMode"
            } ],
            "description" : "Optional execution mode for this action."
          },
          "executionCondition" : {
            "type" : "object",
            "properties" : {
              "expression" : {
                "type" : "string",
                "description" : "Condition formulated as Spark SQL. The attributes available are dependent on the context."
              },
              "description" : {
                "type" : "string",
                "description" : "A textual description of the condition to be shown in error messages."
              }
            },
            "title" : "Condition",
            "required" : [ "expression" ],
            "additionalProperties" : false,
            "description" : "Optional execution condition for this action.  \nBy default, an Action is executed if all inputs are available, e.g. no input from a previous Action is skipped.  \nOverride the default behaviour by specifying an executionCondition in Spark SQL expression syntax.  \nIt is evaluated against the properties available in `SubFeedsExpressionData`.  \nIf true, the Action is executed, otherwise it is skipped. Example:\n```\nexecutionCondition = {\n  description = \"execute if input stg-src1 is not skipped\"\n  expression = \"!inputSubFeeds.stg-src1.isSkipped\"\n}\n```\n\n\nBesides the input SubFeeds, the runtime information of all Actions this Action transitively depends on is\navailable through `SubFeedsExpressionData.predecessorActions`, see `ActionExpressionData`. Example:\n```\nexecutionCondition = {\n  description = \"execute if the previous action loaded data\"\n  expression = \"predecessorActions['loadAction'].metrics['stg-src1']['records_written'] > 0\"\n}\n```\n"
          },
          "metricsFailCondition" : {
            "type" : "string",
            "description" : "Optional Spark SQL condition evaluated as where-clause against dataframe of SubFeed metrics. Available columns are dataObjectId, key, value.  \nIf there are any rows passing the where clause, a MetricCheckFailed exception is thrown.\n\nTo check for skipped SubFeeds, an additional row with key='skipped' and value=true|false is created per output DataObject. Example - fail an action writing to output int-tgt in case there are no records written:\n```\ndataObjectId = 'int-tgt' and key = 'no_data' and value = true\"\n```\n"
          },
          "expectations" : {
            "type" : "array",
            "items" : {
              "oneOf" : [ {
                "type" : "object",
                "properties" : {
                  "type" : {
                    "const" : "CompletenessExpectation"
                  },
                  "name" : {
                    "type" : "string",
                    "description" : "The name of the expectation"
                  },
                  "expectation" : {
                    "type" : "string",
                    "description" : "Optional SQL comparison operator and literal to define expected value for validation. Default is '= 1\".  \nTogether with the result of the aggExpression evaluation on the left side, it forms the condition to validate the expectation.  \nIf no expectation is defined, the aggExpression evaluation result is just recorded in metrics."
                  },
                  "precision" : {
                    "type" : "integer",
                    "description" : "Number of digits to keep when calculating fraction. Default is 4."
                  },
                  "failedSeverity" : {
                    "type" : "string",
                    "description" : "Severity if expectation fails - can be Error (default) or Warn.  \nIf set to Error, execution will fail, otherwise there will be just a warning logged.",
                    "enum" : [ "Warn", "Error" ]
                  }
                },
                "title" : "CompletenessExpectation",
                "required" : [ "type" ],
                "additionalProperties" : false,
                "description" : "Definition of expectation on comparing count all records of input and output table.  \nCompleteness is calculated as the fraction of main output count-all over main input count-all.\n\nUse it to detect unintended record loss in an Action, e.g. rows dropped by a join or a filter.  \nAs it needs a main input and a main output to compare, it can only be configured as `expectations` of an Action (see `ActionExpectation` ), not on a DataObject. The scope is fixed to the whole\ntable, and the fraction is rounded down, so an incomplete result is detected aggressively. Example:\n```\nactions = {\n  join-departures-airports {\n    type = CustomDataFrameAction\n    inputIds = [stg-departures, int-airports]\n    outputIds = [btl-departures-arrivals-airports]\n    mainInputId = stg-departures\n    expectations = [{\n      type = CompletenessExpectation\n      expectation = \"> 0.95\"\n    }]\n  }\n}\n```\n"
              }, {
                "type" : "object",
                "properties" : {
                  "type" : {
                    "const" : "TransferRateExpectation"
                  },
                  "name" : {
                    "type" : "string",
                    "description" : "The name of the expectation"
                  },
                  "expectation" : {
                    "type" : "string",
                    "description" : "Optional SQL comparison operator and literal to define expected value for validation. Default is '= 1\".  \nTogether with the result of the aggExpression evaluation on the left side, it forms the condition to validate the expectation.  \nIf no expectation is defined, the aggExpression evaluation result is just recorded in metrics."
                  },
                  "precision" : {
                    "type" : "integer",
                    "description" : "Number of digits to keep when calculating fraction. Default is 4."
                  },
                  "failedSeverity" : {
                    "type" : "string",
                    "description" : "Severity if expectation fails - can be Error (default) or Warn.  \nIf set to Error, execution will fail, otherwise there will be just a warning logged.",
                    "enum" : [ "Warn", "Error" ]
                  }
                },
                "title" : "TransferRateExpectation",
                "required" : [ "type" ],
                "additionalProperties" : false,
                "description" : "Definition of expectation on transfer rate.  \nTransfer rate is calculated as the fraction of main output count over main input count.\n\nUse this Action level expectation to detect records unintentionally lost (or multiplied) by the transformation of an Action,\ne.g. by a join that does not match, or a filter that is too restrictive. In contrast to `CompletenessExpectation`,\nwhich always compares the count of the whole table (scope `All` ), the transfer rate compares the number of records\nprocessed by the current job, e.g. only the incrementally loaded records. Example:\n```\nactions = {\n  copy-airports {\n    type = CopyAction\n    inputId = stg-airports\n    outputId = int-airports\n    expectations = [\n      { type = TransferRateExpectation, expectation = \"> 0.95\", failedSeverity = Warn }\n    ]\n  }\n}\n```\n\n\nNOTE: The metric is only available for Actions with a main input and a main output, e.g. `io.smartdatalake.workflow.action.CopyAction` or `io.smartdatalake.workflow.action.CustomDataFrameAction`.\n\nSEE: `CompletenessExpectation`"
              } ]
            },
            "description" : "List of expectation definitions to evaluate when executing this Action, see `Expectation` for details.\n\nNote: Expectations defined on DataObjects measure data quality and are evaluated against the output only.  \nExpectations defined on Actions measure quality of the transformation process and can measure and compare metrics between all input DataObjects and the main output DataObject.\n\nExpectations defined at Action level are executed together with the expectations of the main output DataObject."
          },
          "saveModeOptions" : {
            "oneOf" : [ {
              "$ref" : "#/definitions/SaveModeOptions/SaveModeGenericOptions"
            }, {
              "$ref" : "#/definitions/SaveModeOptions/SaveModeMergeOptions"
            } ],
            "description" : "Optionally override and parametrize saveMode in output DataObject configurations when writing to DataObjects."
          },
          "metadata" : {
            "$ref" : "#/definitions/Others/ActionMetadata",
            "description" : "Additional metadata for the Action"
          },
          "agentId" : {
            "type" : "string"
          },
          "engineConnectionId" : {
            "type" : "string",
            "description" : "Actions needing an engine to execute, e.g. Spark, can override this method to provide the connection to the engine."
          }
        },
        "title" : "CopyAction",
        "required" : [ "type", "inputId", "outputId" ],
        "additionalProperties" : false,
        "description" : "This `Action` copies data between an input and output DataObject using DataFrames. The input\nDataObject reads the data and converts it to a DataFrame according to its definition. The\nDataFrame might be transformed using SQL or DataFrame transformations. Then the output\nDataObjects writes the DataFrame to the output according to its definition.\n\nCopyAction is the standard 1:1 Action. Use it whenever exactly one input should be written to\nexactly one output. Choose `CustomDataFrameAction` instead if you need multiple inputs or\noutputs, and `FileTransferAction` if the data should be moved byte-by-byte without being read\ninto a DataFrame. Example:\n```\nactions = {\n  copy-airports {\n    type = CopyAction\n    inputId = stg-airports\n    outputId = int-airports\n    transformers = [{\n      type = SQLDfTransformer\n      code = \"select ident, name, latitude_deg, longitude_deg from %{inputViewName}\"\n    }]\n  }\n}\n```\n"
      },
      "CustomDataFrameAction" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "CustomDataFrameAction"
          },
          "inputIds" : {
            "type" : "array",
            "items" : {
              "type" : "string"
            },
            "description" : "input DataObject's"
          },
          "outputIds" : {
            "type" : "array",
            "items" : {
              "type" : "string"
            },
            "description" : "output DataObject's"
          },
          "transformers" : {
            "type" : "array",
            "items" : {
              "oneOf" : [ {
                "$ref" : "#/definitions/GenericDfsTransformer/DfTransformerWrapperDfsTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfsTransformer/PythonCodeDfsTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfsTransformer/SQLDfsTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfsTransformer/ScalaClassGenericDfsTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfsTransformer/ScalaClassSnowparkDfsTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfsTransformer/ScalaClassSparkDfsTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfsTransformer/ScalaCodeSparkDfsTransformer"
              } ]
            },
            "description" : "list of transformations to apply. See`spark.transformer` for a list of included Transformers.  \nThe transformations are applied according to the ordering of the list.  \nNote that all outputs of previous transformers are kept as input for next transformer,\nbut in the end only outputs of the last transformer are mapped to output DataObjects."
          },
          "cacheOutput" : {
            "type" : "boolean",
            "description" : "Cache the output DataFrame of this Action, so that subsequent Actions can reuse it instead of reading the output\nDataObject again. This saves reading the data again, at the cost of materializing it in the engine.\n\nBy default the DataFrame is not propagated to subsequent Actions, they get a fresh DataFrame from the output\nDataObject according to the SubFeed's partition values.\n\nNote that on Snowflake this materializes a temporary table which is only released when the session is closed."
          },
          "cacheInput" : {
            "type" : "boolean",
            "description" : "Force materializing input DataFrame's.  \nThis improves performance if the input DataFrame is used multiple times in the transformation of this Action\nand can serve as a recovery point in case a task gets lost."
          },
          "mainInputId" : {
            "type" : "string",
            "description" : "optional selection of main inputId used for execution mode and partition values propagation. Only needed if there are multiple input DataObject's."
          },
          "mainOutputId" : {
            "type" : "string",
            "description" : "optional selection of main outputId used for execution mode and partition values propagation. Only needed if there are multiple output DataObject's."
          },
          "executionMode" : {
            "oneOf" : [ {
              "$ref" : "#/definitions/ExecutionMode/CustomPartitionMode"
            }, {
              "$ref" : "#/definitions/ExecutionMode/DataFrameIncrementalMode"
            }, {
              "$ref" : "#/definitions/ExecutionMode/DataObjectStateIncrementalMode"
            }, {
              "$ref" : "#/definitions/ExecutionMode/FailIfNoPartitionValuesMode"
            }, {
              "$ref" : "#/definitions/ExecutionMode/FileIncrementalMoveMode"
            }, {
              "$ref" : "#/definitions/ExecutionMode/KafkaStateIncrementalMode"
            }, {
              "$ref" : "#/definitions/ExecutionMode/PartitionDiffMode"
            }, {
              "$ref" : "#/definitions/ExecutionMode/ProcessAllMode"
            }, {
              "$ref" : "#/definitions/ExecutionMode/SparkStreamingMode"
            } ],
            "description" : "Optional execution mode for this action."
          },
          "executionCondition" : {
            "type" : "object",
            "properties" : {
              "expression" : {
                "type" : "string",
                "description" : "Condition formulated as Spark SQL. The attributes available are dependent on the context."
              },
              "description" : {
                "type" : "string",
                "description" : "A textual description of the condition to be shown in error messages."
              }
            },
            "title" : "Condition",
            "required" : [ "expression" ],
            "additionalProperties" : false,
            "description" : "Optional execution condition for this action.  \nBy default, an Action is executed if all inputs are available, e.g. no input from a previous Action is skipped.  \nOverride the default behaviour by specifying an executionCondition in Spark SQL expression syntax.  \nIt is evaluated against the properties available in `SubFeedsExpressionData`.  \nIf true, the Action is executed, otherwise it is skipped. Example:\n```\nexecutionCondition = {\n  description = \"execute if input stg-src1 is not skipped\"\n  expression = \"!inputSubFeeds.stg-src1.isSkipped\"\n}\n```\n\n\nBesides the input SubFeeds, the runtime information of all Actions this Action transitively depends on is\navailable through `SubFeedsExpressionData.predecessorActions`, see `ActionExpressionData`. Example:\n```\nexecutionCondition = {\n  description = \"execute if the previous action loaded data\"\n  expression = \"predecessorActions['loadAction'].metrics['stg-src1']['records_written'] > 0\"\n}\n```\n"
          },
          "metricsFailCondition" : {
            "type" : "string",
            "description" : "Optional Spark SQL condition evaluated as where-clause against dataframe of SubFeed metrics. Available columns are dataObjectId, key, value.  \nIf there are any rows passing the where clause, a MetricCheckFailed exception is thrown.\n\nTo check for skipped SubFeeds, an additional row with key='skipped' and value=true|false is created per output DataObject. Example - fail an action writing to output int-tgt in case there are no records written:\n```\ndataObjectId = 'int-tgt' and key = 'no_data' and value = true\"\n```\n"
          },
          "expectations" : {
            "type" : "array",
            "items" : {
              "oneOf" : [ {
                "type" : "object",
                "properties" : {
                  "type" : {
                    "const" : "CompletenessExpectation"
                  },
                  "name" : {
                    "type" : "string",
                    "description" : "The name of the expectation"
                  },
                  "expectation" : {
                    "type" : "string",
                    "description" : "Optional SQL comparison operator and literal to define expected value for validation. Default is '= 1\".  \nTogether with the result of the aggExpression evaluation on the left side, it forms the condition to validate the expectation.  \nIf no expectation is defined, the aggExpression evaluation result is just recorded in metrics."
                  },
                  "precision" : {
                    "type" : "integer",
                    "description" : "Number of digits to keep when calculating fraction. Default is 4."
                  },
                  "failedSeverity" : {
                    "type" : "string",
                    "description" : "Severity if expectation fails - can be Error (default) or Warn.  \nIf set to Error, execution will fail, otherwise there will be just a warning logged.",
                    "enum" : [ "Warn", "Error" ]
                  }
                },
                "title" : "CompletenessExpectation",
                "required" : [ "type" ],
                "additionalProperties" : false,
                "description" : "Definition of expectation on comparing count all records of input and output table.  \nCompleteness is calculated as the fraction of main output count-all over main input count-all.\n\nUse it to detect unintended record loss in an Action, e.g. rows dropped by a join or a filter.  \nAs it needs a main input and a main output to compare, it can only be configured as `expectations` of an Action (see `ActionExpectation` ), not on a DataObject. The scope is fixed to the whole\ntable, and the fraction is rounded down, so an incomplete result is detected aggressively. Example:\n```\nactions = {\n  join-departures-airports {\n    type = CustomDataFrameAction\n    inputIds = [stg-departures, int-airports]\n    outputIds = [btl-departures-arrivals-airports]\n    mainInputId = stg-departures\n    expectations = [{\n      type = CompletenessExpectation\n      expectation = \"> 0.95\"\n    }]\n  }\n}\n```\n"
              }, {
                "type" : "object",
                "properties" : {
                  "type" : {
                    "const" : "TransferRateExpectation"
                  },
                  "name" : {
                    "type" : "string",
                    "description" : "The name of the expectation"
                  },
                  "expectation" : {
                    "type" : "string",
                    "description" : "Optional SQL comparison operator and literal to define expected value for validation. Default is '= 1\".  \nTogether with the result of the aggExpression evaluation on the left side, it forms the condition to validate the expectation.  \nIf no expectation is defined, the aggExpression evaluation result is just recorded in metrics."
                  },
                  "precision" : {
                    "type" : "integer",
                    "description" : "Number of digits to keep when calculating fraction. Default is 4."
                  },
                  "failedSeverity" : {
                    "type" : "string",
                    "description" : "Severity if expectation fails - can be Error (default) or Warn.  \nIf set to Error, execution will fail, otherwise there will be just a warning logged.",
                    "enum" : [ "Warn", "Error" ]
                  }
                },
                "title" : "TransferRateExpectation",
                "required" : [ "type" ],
                "additionalProperties" : false,
                "description" : "Definition of expectation on transfer rate.  \nTransfer rate is calculated as the fraction of main output count over main input count.\n\nUse this Action level expectation to detect records unintentionally lost (or multiplied) by the transformation of an Action,\ne.g. by a join that does not match, or a filter that is too restrictive. In contrast to `CompletenessExpectation`,\nwhich always compares the count of the whole table (scope `All` ), the transfer rate compares the number of records\nprocessed by the current job, e.g. only the incrementally loaded records. Example:\n```\nactions = {\n  copy-airports {\n    type = CopyAction\n    inputId = stg-airports\n    outputId = int-airports\n    expectations = [\n      { type = TransferRateExpectation, expectation = \"> 0.95\", failedSeverity = Warn }\n    ]\n  }\n}\n```\n\n\nNOTE: The metric is only available for Actions with a main input and a main output, e.g. `io.smartdatalake.workflow.action.CopyAction` or `io.smartdatalake.workflow.action.CustomDataFrameAction`.\n\nSEE: `CompletenessExpectation`"
              } ]
            },
            "description" : "List of expectation definitions to evaluate when executing this Action, see `Expectation` for details.\n\nNote: Expectations defined on DataObjects measure data quality and are evaluated against the output only.  \nExpectations defined on Actions measure quality of the transformation process and can measure and compare metrics between all input DataObjects and the main output DataObject.\n\nExpectations defined at Action level are executed together with the expectations of the main output DataObject."
          },
          "metadata" : {
            "$ref" : "#/definitions/Others/ActionMetadata",
            "description" : "Additional metadata for the Action"
          },
          "recursiveInputIds" : {
            "type" : "array",
            "items" : {
              "type" : "string"
            },
            "description" : "output of action that are used as input in the same action"
          },
          "inputIdsToIgnoreFilter" : {
            "type" : "array",
            "items" : {
              "type" : "string"
            },
            "description" : "optional list of input ids to ignore filter (partition values & column filters)"
          },
          "engineConnectionId" : {
            "type" : "string",
            "description" : "Actions needing an engine to execute, e.g. Spark, can override this method to provide the connection to the engine."
          }
        },
        "title" : "CustomDataFrameAction",
        "required" : [ "type", "inputIds", "outputIds" ],
        "additionalProperties" : false,
        "description" : "This `Action` transforms data between many input and output DataObjects using DataFrames.  \nCustomDataFrameAction allows to define transformations between n input DataObjects and m output DataObjects,\nbut is is recommended to implement n:1 or 1:m transformations, as otherwise dependencies between DataObjects might not be accurate anymore.  \nThe input DataFrames might be transformed using SQL or DataFrame transformations.  \nWhen chaining multiple transformers, output DataFrames of previous transformers are available as input DataFrames for later transformers by their corresponding name.\n\nUse CustomDataFrameAction for joins, unions and fan-outs. For simple 1:1 copies prefer `CopyAction`,\nwhich derives more accurate lineage. Note that a transformer must return a result for every outputId,\nkeyed by the output DataObject id, otherwise the Action fails with a configuration error. Example:\n```\nactions = {\n  join-departures-airports {\n    type = CustomDataFrameAction\n    inputIds = [stg-departures, int-airports]\n    outputIds = [btl-departures-arrivals-airports]\n    mainInputId = stg-departures\n    transformers = [{\n      type = SQLDfsTransformer\n      code = {\n        btl-departures-arrivals-airports = \"\"\"select d.*, a.name from %{inputViewName_stg-departures} d\n          join %{inputViewName_int-airports} a on d.estdepartureairport = a.ident\"\"\"\n      }\n    }]\n  }\n}\n```\n"
      },
      "CustomFileAction" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "CustomFileAction"
          },
          "inputId" : {
            "type" : "string",
            "description" : "inputs DataObject"
          },
          "outputId" : {
            "type" : "string",
            "description" : "output DataObject"
          },
          "transformer" : {
            "type" : "object",
            "properties" : {
              "className" : {
                "type" : "string",
                "description" : "Optional class name to load transformer code from"
              },
              "scalaFile" : {
                "type" : "string",
                "description" : "Optional file where Scala code for transformation is loaded from"
              },
              "scalaCode" : {
                "type" : "string",
                "description" : "Optional Scala code for transformation"
              },
              "options" : {
                "type" : "object",
                "additionalProperties" : {
                  "type" : "string"
                },
                "description" : "Options to pass to the transformation"
              }
            },
            "title" : "CustomFileTransformerConfig",
            "additionalProperties" : false,
            "description" : "a custom file transformer, which reads a file from HadoopFileDataObject and writes it back to another HadoopFileDataObject"
          },
          "filesPerPartition" : {
            "type" : "integer",
            "description" : "number of files per Spark partition"
          },
          "breakFileRefLineage" : {
            "type" : "boolean",
            "description" : "If set to true, file references passed on from previous action are ignored by this action. and instead get new FileRefs from DataObject according to the SubFeed's partitionValue.  \nThis is needed to reprocess all files of a path/partition instead of the FileRef's passed from the previous Action.  \nDefault is false."
          },
          "executionMode" : {
            "oneOf" : [ {
              "$ref" : "#/definitions/ExecutionMode/CustomPartitionMode"
            }, {
              "$ref" : "#/definitions/ExecutionMode/DataFrameIncrementalMode"
            }, {
              "$ref" : "#/definitions/ExecutionMode/DataObjectStateIncrementalMode"
            }, {
              "$ref" : "#/definitions/ExecutionMode/FailIfNoPartitionValuesMode"
            }, {
              "$ref" : "#/definitions/ExecutionMode/FileIncrementalMoveMode"
            }, {
              "$ref" : "#/definitions/ExecutionMode/KafkaStateIncrementalMode"
            }, {
              "$ref" : "#/definitions/ExecutionMode/PartitionDiffMode"
            }, {
              "$ref" : "#/definitions/ExecutionMode/ProcessAllMode"
            }, {
              "$ref" : "#/definitions/ExecutionMode/SparkStreamingMode"
            } ],
            "description" : "Optional execution mode for this action."
          },
          "executionCondition" : {
            "type" : "object",
            "properties" : {
              "expression" : {
                "type" : "string",
                "description" : "Condition formulated as Spark SQL. The attributes available are dependent on the context."
              },
              "description" : {
                "type" : "string",
                "description" : "A textual description of the condition to be shown in error messages."
              }
            },
            "title" : "Condition",
            "required" : [ "expression" ],
            "additionalProperties" : false,
            "description" : "Optional execution condition for this action.  \nBy default, an Action is executed if all inputs are available, e.g. no input from a previous Action is skipped.  \nOverride the default behaviour by specifying an executionCondition in Spark SQL expression syntax.  \nIt is evaluated against the properties available in `SubFeedsExpressionData`.  \nIf true, the Action is executed, otherwise it is skipped. Example:\n```\nexecutionCondition = {\n  description = \"execute if input stg-src1 is not skipped\"\n  expression = \"!inputSubFeeds.stg-src1.isSkipped\"\n}\n```\n\n\nBesides the input SubFeeds, the runtime information of all Actions this Action transitively depends on is\navailable through `SubFeedsExpressionData.predecessorActions`, see `ActionExpressionData`. Example:\n```\nexecutionCondition = {\n  description = \"execute if the previous action loaded data\"\n  expression = \"predecessorActions['loadAction'].metrics['stg-src1']['records_written'] > 0\"\n}\n```\n"
          },
          "metricsFailCondition" : {
            "type" : "string",
            "description" : "Optional Spark SQL condition evaluated as where-clause against dataframe of SubFeed metrics. Available columns are dataObjectId, key, value.  \nIf there are any rows passing the where clause, a MetricCheckFailed exception is thrown.\n\nTo check for skipped SubFeeds, an additional row with key='skipped' and value=true|false is created per output DataObject. Example - fail an action writing to output int-tgt in case there are no records written:\n```\ndataObjectId = 'int-tgt' and key = 'no_data' and value = true\"\n```\n"
          },
          "metadata" : {
            "$ref" : "#/definitions/Others/ActionMetadata",
            "description" : "Additional metadata for the Action"
          },
          "engineConnectionId" : {
            "type" : "string",
            "description" : "Actions needing an engine to execute, e.g. Spark, can override this method to provide the connection to the engine."
          }
        },
        "title" : "CustomFileAction",
        "required" : [ "type", "inputId", "outputId", "transformer" ],
        "additionalProperties" : false,
        "description" : "`Action` to transform files between two Hadoop Data Objects.  \nThe transformation is executed in distributed mode on Spark executors.  \nA custom file transformer must be given, which reads a file from Hadoop and writes it back to Hadoop.\n\nUse this Action if files must be processed as files (byte or line streams), e.g. to unzip, decrypt or repair a file\nformat Spark can not read. The list of files to transfer is created on the driver and then distributed to the\nexecutors, where the transformer gets the input stream and the output stream of one file at a time. If the content\ncan be read as a DataFrame, prefer `CopyAction`, if the files only need to be moved unchanged prefer `FileTransferAction`. Example:\n```\nactions = {\n  transform-airports {\n    type = CustomFileAction\n    inputId = stg-airports\n    outputId = int-airports\n    transformer = {\n      className = com.company.transformer.CutColumnsFileTransformer\n      options = { delimiter = \",\" }\n    }\n    filesPerPartition = 5\n  }\n}\n```\n\n\nNOTE: inputId must be a HadoopFileDataObject and outputId a SparkFileDataObject, and the transformer code must be\nserializable as it is shipped to the Spark executors."
      },
      "CustomScriptAction" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "CustomScriptAction"
          },
          "inputIds" : {
            "type" : "array",
            "items" : {
              "type" : "string"
            },
            "description" : "input DataObject's"
          },
          "outputIds" : {
            "type" : "array",
            "items" : {
              "type" : "string"
            },
            "description" : "output DataObject's"
          },
          "scripts" : {
            "type" : "array",
            "items" : {
              "oneOf" : [ {
                "$ref" : "#/definitions/ParsableScriptDef/CmdScript"
              }, {
                "$ref" : "#/definitions/ParsableScriptDef/DockerRunScript"
              } ]
            },
            "description" : "definition of scripts to execute"
          },
          "executionCondition" : {
            "type" : "object",
            "properties" : {
              "expression" : {
                "type" : "string",
                "description" : "Condition formulated as Spark SQL. The attributes available are dependent on the context."
              },
              "description" : {
                "type" : "string",
                "description" : "A textual description of the condition to be shown in error messages."
              }
            },
            "title" : "Condition",
            "required" : [ "expression" ],
            "additionalProperties" : false,
            "description" : "Optional execution condition for this action.  \nBy default, an Action is executed if all inputs are available, e.g. no input from a previous Action is skipped.  \nOverride the default behaviour by specifying an executionCondition in Spark SQL expression syntax.  \nIt is evaluated against the properties available in `SubFeedsExpressionData`.  \nIf true, the Action is executed, otherwise it is skipped. Example:\n```\nexecutionCondition = {\n  description = \"execute if input stg-src1 is not skipped\"\n  expression = \"!inputSubFeeds.stg-src1.isSkipped\"\n}\n```\n\n\nBesides the input SubFeeds, the runtime information of all Actions this Action transitively depends on is\navailable through `SubFeedsExpressionData.predecessorActions`, see `ActionExpressionData`. Example:\n```\nexecutionCondition = {\n  description = \"execute if the previous action loaded data\"\n  expression = \"predecessorActions['loadAction'].metrics['stg-src1']['records_written'] > 0\"\n}\n```\n"
          },
          "metadata" : {
            "$ref" : "#/definitions/Others/ActionMetadata",
            "description" : "Additional metadata for the Action"
          }
        },
        "title" : "CustomScriptAction",
        "required" : [ "type", "inputIds", "outputIds" ],
        "additionalProperties" : false,
        "description" : "`Action` execute script after multiple input DataObjects are ready, notifying multiple output DataObjects when script succeeded.\n\nNote that this action can also be used to give your data pipeline additional structure, e.g. adding a decision point after several actions have been executed.\n\nNo data is read or written by this Action: the input DataObjects are only used as dependencies to order the DAG,\nand the output DataObjects just receive a notification once all scripts succeeded. The last line of a script's\nstandard output is parsed as `key=value` pairs and passed on as parameters to the next script and output SubFeeds. Example:\n```\nactions = {\n  refresh-dashboard {\n    type = CustomScriptAction\n    inputIds = [int-airports]\n    outputIds = [ext-dashboard]\n    scripts = [{\n      type = CmdScript\n      linuxCmd = \"./scripts/refresh-dashboard.sh\"\n      winCmd = \"scripts\\\\refresh-dashboard.cmd\"\n    }]\n  }\n}\n```\n\n\nNOTE: Output DataObjects must implement `io.smartdatalake.workflow.dataobject.script.CanReceiveScriptNotification` to be able to receive the notification. No DataObject shipped with SDLB implements it yet, so `outputIds` normally references a custom DataObject implementation. Otherwise the Action fails with a\nConfigurationException when the configuration is parsed."
      },
      "DeduplicateAction" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "DeduplicateAction"
          },
          "inputId" : {
            "type" : "string",
            "description" : "inputs DataObject"
          },
          "outputId" : {
            "type" : "string",
            "description" : "output DataObject"
          },
          "transformers" : {
            "type" : "array",
            "items" : {
              "oneOf" : [ {
                "$ref" : "#/definitions/GenericDfTransformer/BlacklistTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfTransformer/ColumnsTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfTransformer/ConvertNullValuesTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfTransformer/DataValidationTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfTransformer/DebugTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfTransformer/DecryptColumnsTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfTransformer/DeduplicateTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfTransformer/EncryptColumnsTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfTransformer/FilterTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfTransformer/PythonCodeDfTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfTransformer/SQLDfTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfTransformer/ScalaClassGenericDfTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfTransformer/ScalaClassSnowparkDfTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfTransformer/ScalaClassSparkDfTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfTransformer/ScalaCodeSparkDfTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfTransformer/ScalaNotebookSparkDfTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfTransformer/SparkFlattenDfTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfTransformer/SparkRepartitionTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfTransformer/StandardizeColNamesTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfTransformer/StandardizeSparkDatatypesTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfTransformer/WhitelistTransformer"
              } ]
            },
            "description" : "optional list of transformations to apply before deduplication. See`sparktransformer` for a\nlist of included Transformers. The transformations are applied according to the lists ordering."
          },
          "ignoreOldDeletedColumns" : {
            "type" : "boolean",
            "description" : "if true, remove no longer existing columns in Schema Evolution"
          },
          "ignoreOldDeletedNestedColumns" : {
            "type" : "boolean",
            "description" : "if true, remove no longer existing columns from nested data types in Schema Evolution. Keeping\ndeleted columns in complex data types has performance impact as all new data in the future has\nto be converted by a complex function."
          },
          "updateCapturedColumnOnlyWhenChanged" : {
            "type" : "boolean",
            "description" : "Set to true to enable update Column`Environment.capturedColumnName` only if Record has\nchanged in the source, instead of updating it with every execution (default=false). This\nresults in much less records updated with saveMode.Merge.  \nNote that this is normally not needed if sourceTimestampColumn is set, see there."
          },
          "sourceTimestampColumn" : {
            "type" : "string",
            "description" : "Optional column holding the timestamp of the last change of the record in the source system. If set, it is used\nas value for Column[[Environment.capturedColumnName]]instead of the runs reference timestamp. The column must\nexist in the data to deduplicate and be of type timestamp. Records where it is null fall back to the runs\nreference timestamp.  \nNote that the column itself is not written to the output DataObject, as its value is kept in[[Environment.capturedColumnName]]. Copy it to a column with another name in a transformer if you want to keep it.  \nRecords arriving late, e.g. having a source timestamp older than the record already stored, are not applied, so\nthat[[Environment.capturedColumnName]]always holds the latest version according to the source system.  \nSetting updateCapturedColumnOnlyWhenChanged = true is normally not needed together with sourceTimestampColumn:`Environment.capturedColumnName` is moved forward only if the source system changed the record anyway, no\nmatter how it is set. What it changes is that existing records are updated only if the source timestamp\nincreased, instead of comparing all columns. This avoids rewriting records which the source system delivers\nagain with an unchanged timestamp, but a change which the source system did not timestamp is not applied."
          },
          "mergeModeAdditionalJoinPredicate" : {
            "type" : "string",
            "description" : "To optimize performance it might be interesting to limit the records read from the existing\ntable data, e.g. it might be sufficient to use only the last 7 days. Specify a condition to\nselect existing data to be used in transformation as Spark SQL expression. Use table alias\n'existing' to reference columns of the existing table data."
          },
          "cacheOutput" : {
            "type" : "boolean",
            "description" : "Cache the output DataFrame of this Action, so that subsequent Actions can reuse it instead of reading the output\nDataObject again. This saves reading the data again, at the cost of materializing it in the engine.\n\nBy default the DataFrame is not propagated to subsequent Actions, they get a fresh DataFrame from the output\nDataObject according to the SubFeed's partition values.\n\nNote that on Snowflake this materializes a temporary table which is only released when the session is closed."
          },
          "cacheInput" : {
            "type" : "boolean",
            "description" : "Force materializing input DataFrame's.  \nThis improves performance if the input DataFrame is used multiple times in the transformation of this Action\nand can serve as a recovery point in case a task gets lost."
          },
          "executionMode" : {
            "oneOf" : [ {
              "$ref" : "#/definitions/ExecutionMode/CustomPartitionMode"
            }, {
              "$ref" : "#/definitions/ExecutionMode/DataFrameIncrementalMode"
            }, {
              "$ref" : "#/definitions/ExecutionMode/DataObjectStateIncrementalMode"
            }, {
              "$ref" : "#/definitions/ExecutionMode/FailIfNoPartitionValuesMode"
            }, {
              "$ref" : "#/definitions/ExecutionMode/FileIncrementalMoveMode"
            }, {
              "$ref" : "#/definitions/ExecutionMode/KafkaStateIncrementalMode"
            }, {
              "$ref" : "#/definitions/ExecutionMode/PartitionDiffMode"
            }, {
              "$ref" : "#/definitions/ExecutionMode/ProcessAllMode"
            }, {
              "$ref" : "#/definitions/ExecutionMode/SparkStreamingMode"
            } ],
            "description" : "Optional execution mode for this action."
          },
          "executionCondition" : {
            "type" : "object",
            "properties" : {
              "expression" : {
                "type" : "string",
                "description" : "Condition formulated as Spark SQL. The attributes available are dependent on the context."
              },
              "description" : {
                "type" : "string",
                "description" : "A textual description of the condition to be shown in error messages."
              }
            },
            "title" : "Condition",
            "required" : [ "expression" ],
            "additionalProperties" : false,
            "description" : "Optional execution condition for this action.  \nBy default, an Action is executed if all inputs are available, e.g. no input from a previous Action is skipped.  \nOverride the default behaviour by specifying an executionCondition in Spark SQL expression syntax.  \nIt is evaluated against the properties available in `SubFeedsExpressionData`.  \nIf true, the Action is executed, otherwise it is skipped. Example:\n```\nexecutionCondition = {\n  description = \"execute if input stg-src1 is not skipped\"\n  expression = \"!inputSubFeeds.stg-src1.isSkipped\"\n}\n```\n\n\nBesides the input SubFeeds, the runtime information of all Actions this Action transitively depends on is\navailable through `SubFeedsExpressionData.predecessorActions`, see `ActionExpressionData`. Example:\n```\nexecutionCondition = {\n  description = \"execute if the previous action loaded data\"\n  expression = \"predecessorActions['loadAction'].metrics['stg-src1']['records_written'] > 0\"\n}\n```\n"
          },
          "metricsFailCondition" : {
            "type" : "string",
            "description" : "Optional Spark SQL condition evaluated as where-clause against dataframe of SubFeed metrics. Available columns are dataObjectId, key, value.  \nIf there are any rows passing the where clause, a MetricCheckFailed exception is thrown.\n\nTo check for skipped SubFeeds, an additional row with key='skipped' and value=true|false is created per output DataObject. Example - fail an action writing to output int-tgt in case there are no records written:\n```\ndataObjectId = 'int-tgt' and key = 'no_data' and value = true\"\n```\n"
          },
          "metadata" : {
            "$ref" : "#/definitions/Others/ActionMetadata",
            "description" : "Additional metadata for the Action"
          },
          "engineConnectionId" : {
            "type" : "string",
            "description" : "Actions needing an engine to execute, e.g. Spark, can override this method to provide the connection to the engine."
          }
        },
        "title" : "DeduplicateAction",
        "required" : [ "type", "inputId", "outputId" ],
        "additionalProperties" : false,
        "description" : "This `Action` copies and deduplicates data between an input and output DataObject using\nDataFrames. Deduplication keeps the last record for every key, also after it has been deleted in\nthe source. The DataFrame might be transformed using SQL or DataFrame transformations. These\ntransformations are applied before the deduplication.\n\nDeduplicateAction adds an additional Column `Environment.capturedColumnName`. It contains the\ntimestamp of the last occurrence of the record in the source. This creates lots of updates.  \nEspecially when using saveMode.Merge it is better to set `Environment.capturedColumnName` to\nthe last change of the record in the source. Use updateCapturedColumnOnlyWhenChanged = true to\nenable this optimization.\n\nIf the input contains the timestamp of the last change of the record in the source system, it can be used as `Environment.capturedColumnName` by setting sourceTimestampColumn, so that the output reflects the time axis of\nthe source system instead of the schedule of the pipeline. updateCapturedColumnOnlyWhenChanged is normally not\nneeded then, as the source timestamp is only moved forward if the source system changed the record anyway.\n\nDeduplicateAction needs a transactional table (e.g. `TransactionalTableDataObject` ) as output\nwith defined primary keys. If output implements `CanMergeDataFrame`, saveMode.\n\nDeduplicateAction's input data must be unique across the primary key, otherwise the merge\nstatement creates errors like `DeltaUnsupportedOperationException: [DELTA_MULTIPLE_SOURCE_ROW_MATCHING_TARGET_ROW_IN_MERGE] Cannot perform Merge as multiple source rows matched and attempted to modify the same` .  \nThis can be achieved through adding a DeduplicateTransformer to transformers. Note that this is\nnot included by default in DeduplicateAction, as it is a performance intensive operation. Example:\n```\nactions = {\n  dedup-airports {\n    type = DeduplicateAction\n    inputId = stg-airports\n    outputId = int-airports\n    updateCapturedColumnOnlyWhenChanged = true\n    mergeModeAdditionalJoinPredicate = \"existing.dl_ts_captured > current_date - interval 7 days\"\n  }\n}\n```\n"
      },
      "FileTransferAction" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "FileTransferAction"
          },
          "inputId" : {
            "type" : "string",
            "description" : "inputs DataObject"
          },
          "outputId" : {
            "type" : "string",
            "description" : "output DataObject"
          },
          "overwrite" : {
            "type" : "boolean",
            "description" : "Allow existing output file to be overwritten. If false the action will fail if a file to be created already exists. Default is true."
          },
          "maxParallelism" : {
            "type" : "integer",
            "description" : "Set maximum of files to be transferred in parallel.  \nNote that this information can also be set on DataObjects like SFtpFileRefDataObject, resp. its SFtpFileRefConnection.  \nThe FileTransferAction will then take the minimum parallelism of input, output and this attribute.  \nIf parallelism is not specified on input, output and this attribute, it is set to 1."
          },
          "filenameExtractorRegex" : {
            "type" : "string",
            "description" : "A regex to extract a part of the filename to keep in the translated FileRef.  \nIf the regex contains group definitions, the first group is taken, otherwise the whole regex match.  \nDefault is None which keeps the whole filename (without path)."
          },
          "createFileRefLineage" : {
            "type" : "boolean",
            "description" : "If set to false, this action does not propagate output FileRefs to further actions.  \nThis helps to avoid performance and memory problems with too many FileRefs.  \nDefault is true."
          },
          "breakFileRefLineage" : {
            "type" : "boolean",
            "description" : "If set to true, file references passed on from previous action are ignored by this action. and instead get new FileRefs from DataObject according to the SubFeed's partitionValue.  \nThis is needed to reprocess all files of a path/partition instead of the FileRef's passed from the previous Action.  \nDefault is false."
          },
          "executionMode" : {
            "oneOf" : [ {
              "$ref" : "#/definitions/ExecutionMode/CustomPartitionMode"
            }, {
              "$ref" : "#/definitions/ExecutionMode/DataFrameIncrementalMode"
            }, {
              "$ref" : "#/definitions/ExecutionMode/DataObjectStateIncrementalMode"
            }, {
              "$ref" : "#/definitions/ExecutionMode/FailIfNoPartitionValuesMode"
            }, {
              "$ref" : "#/definitions/ExecutionMode/FileIncrementalMoveMode"
            }, {
              "$ref" : "#/definitions/ExecutionMode/KafkaStateIncrementalMode"
            }, {
              "$ref" : "#/definitions/ExecutionMode/PartitionDiffMode"
            }, {
              "$ref" : "#/definitions/ExecutionMode/ProcessAllMode"
            }, {
              "$ref" : "#/definitions/ExecutionMode/SparkStreamingMode"
            } ],
            "description" : "Optional execution mode for this action."
          },
          "executionCondition" : {
            "type" : "object",
            "properties" : {
              "expression" : {
                "type" : "string",
                "description" : "Condition formulated as Spark SQL. The attributes available are dependent on the context."
              },
              "description" : {
                "type" : "string",
                "description" : "A textual description of the condition to be shown in error messages."
              }
            },
            "title" : "Condition",
            "required" : [ "expression" ],
            "additionalProperties" : false,
            "description" : "Optional execution condition for this action.  \nBy default, an Action is executed if all inputs are available, e.g. no input from a previous Action is skipped.  \nOverride the default behaviour by specifying an executionCondition in Spark SQL expression syntax.  \nIt is evaluated against the properties available in `SubFeedsExpressionData`.  \nIf true, the Action is executed, otherwise it is skipped. Example:\n```\nexecutionCondition = {\n  description = \"execute if input stg-src1 is not skipped\"\n  expression = \"!inputSubFeeds.stg-src1.isSkipped\"\n}\n```\n\n\nBesides the input SubFeeds, the runtime information of all Actions this Action transitively depends on is\navailable through `SubFeedsExpressionData.predecessorActions`, see `ActionExpressionData`. Example:\n```\nexecutionCondition = {\n  description = \"execute if the previous action loaded data\"\n  expression = \"predecessorActions['loadAction'].metrics['stg-src1']['records_written'] > 0\"\n}\n```\n"
          },
          "metricsFailCondition" : {
            "type" : "string",
            "description" : "Optional Spark SQL condition evaluated as where-clause against dataframe of SubFeed metrics. Available columns are dataObjectId, key, value.  \nIf there are any rows passing the where clause, a MetricCheckFailed exception is thrown.\n\nTo check for skipped SubFeeds, an additional row with key='skipped' and value=true|false is created per output DataObject. Example - fail an action writing to output int-tgt in case there are no records written:\n```\ndataObjectId = 'int-tgt' and key = 'no_data' and value = true\"\n```\n"
          },
          "metadata" : {
            "$ref" : "#/definitions/Others/ActionMetadata",
            "description" : "Additional metadata for the Action"
          }
        },
        "title" : "FileTransferAction",
        "required" : [ "type", "inputId", "outputId" ],
        "additionalProperties" : false,
        "description" : "`Action` to transfer files between SFtp, Hadoop, local Filesystem and a Webservice. Note that the Input DataObject and Output DataObject are not interpreted by this Action: the Data is just transferred as is.  \nAs data is transferred as is, matching the data format between the Input DataObject (e.g. CSV from WebserviceFileDataObject) and Output DataObject (e.g. CsvFileDataObject) is in the responsibility of the developer/user.  \nIf you want to convert or transform data formats between input and output, use the CopyAction instead. CopyAction will read the data from the Input DataObject into a DataFrame, and write that DataFrame to the Output DataObject. In this case the DataObjects are responsible to convert the data into a DataFrame and back. Example:\n```\nactions = {\n  download-airports {\n    type = FileTransferAction\n    inputId = ext-airports\n    outputId = stg-airports\n    overwrite = false\n    maxParallelism = 4\n  }\n}\n```\n"
      },
      "HistorizeAction" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "HistorizeAction"
          },
          "inputId" : {
            "type" : "string",
            "description" : "inputs DataObject"
          },
          "outputId" : {
            "type" : "string",
            "description" : "output DataObject"
          },
          "transformers" : {
            "type" : "array",
            "items" : {
              "oneOf" : [ {
                "$ref" : "#/definitions/GenericDfTransformer/BlacklistTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfTransformer/ColumnsTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfTransformer/ConvertNullValuesTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfTransformer/DataValidationTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfTransformer/DebugTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfTransformer/DecryptColumnsTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfTransformer/DeduplicateTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfTransformer/EncryptColumnsTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfTransformer/FilterTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfTransformer/PythonCodeDfTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfTransformer/SQLDfTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfTransformer/ScalaClassGenericDfTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfTransformer/ScalaClassSnowparkDfTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfTransformer/ScalaClassSparkDfTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfTransformer/ScalaCodeSparkDfTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfTransformer/ScalaNotebookSparkDfTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfTransformer/SparkFlattenDfTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfTransformer/SparkRepartitionTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfTransformer/StandardizeColNamesTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfTransformer/StandardizeSparkDatatypesTransformer"
              }, {
                "$ref" : "#/definitions/GenericDfTransformer/WhitelistTransformer"
              } ]
            },
            "description" : "optional list of transformations to apply before historization. The transformations are applied\naccording to the lists ordering."
          },
          "historizeBlacklist" : {
            "type" : "array",
            "items" : {
              "type" : "string"
            },
            "description" : "optional list of columns to ignore when comparing two records in historization. Can not be used\ntogether with`historizeWhitelist`."
          },
          "historizeWhitelist" : {
            "type" : "array",
            "items" : {
              "type" : "string"
            },
            "description" : "optional final list of columns to use when comparing two records in historization. Can not be\nused together with`historizeBlacklist`."
          },
          "ignoreOldDeletedColumns" : {
            "type" : "boolean",
            "description" : "if true, remove no longer existing columns in Schema Evolution"
          },
          "ignoreOldDeletedNestedColumns" : {
            "type" : "boolean",
            "description" : "if true, remove no longer existing columns from nested data types in Schema Evolution. Keeping\ndeleted columns in complex data types has performance impact as all new data in the future has\nto be converted by a complex function."
          },
          "mergeModeAdditionalJoinPredicate" : {
            "type" : "string",
            "description" : "To optimize performance it might be interesting to limit the records read from the existing\ntable data, e.g. it might be sufficient to use only the last 7 days. Specify a condition to\nselect existing data to be used in transformation as Spark SQL expression. Use table alias\n'existing' to reference columns of the existing table data."
          },
          "mergeModeCDCColumn" : {
            "type" : "string",
            "description" : "Optional column holding the CDC operation to replay to enable mergeModeCDC. If CDC information\nis available from the source incremental historization can be further optimized, as the join\nwith existing data can be omitted. Note that this should be enabled only, if input data\ncontains just inserted, updated and deleted records. HistorizeAction in mergeModeCDC will make\nno* change detection on its own, and create a new version for every inserted/updated record it\nreceives! You will also need to specify parameter mergeModeCDCDeletedValue to use this.  \nIncrement CDC historization will add a column \"dl_dummy\" to the target table, which is used to\nwork around limitations of SQL merge statement, but \"dl_hash\" column from mergeMode is no\nlonger needed."
          },
          "mergeModeCDCDeletedValue" : {
            "type" : "string",
            "description" : "Optional value of mergeModeCDCColumn that marks a record as deleted. It defaults to`delete` if SDLBs standard\nCDC columns are used, see mergeModeCDCAutoDetect."
          },
          "mergeModeCDCAutoDetect" : {
            "type" : "boolean",
            "description" : "If true (default), mergeModeCDC is enabled automatically if the input has a column`_change_type`, e.g. it\ndelivers change events using SDLBs standard CDC columns (see[[io.smartdatalake.definitions.CdcChangeType]]), as DebeziumCdcDataObject does. No further configuration is\nneeded in that case: records marked as`delete`close the existing version, records of type`update_preimage`are ignored, and if several change events exist for the same primary key only the last one is historized. The\norder of the change events is defined by column`_change_ordinal`, or`_commit_timestamp` if it is missing.  \nThe CDC columns themselves are not written to the output DataObject."
          },
          "mergeModeCDCTimestampAutoDetect" : {
            "type" : "boolean",
            "description" : "If true (default), column`_commit_timestamp` is used as sourceTimestampColumn, if the input delivers change\nevents using SDLBs standard CDC columns (see mergeModeCDCAutoDetect) and sourceTimestampColumn is not set. Set\nto false to start the validity of new versions at the runs reference timestamp instead. Note that there is no\nauto detection of a source timestamp column outside of CDC historization."
          },
          "sourceTimestampColumn" : {
            "type" : "string",
            "description" : "Optional column holding the timestamp of the last change of the record in the source system. If set, the\nvalidity of a new version starts at this timestamp instead of the runs reference timestamp, e.g. the history\nreflects the time axis of the source system. The column must exist in the input and be of type timestamp.  \nNote that the column itself is not historized: it is excluded from change detection and not written to the\noutput DataObject. Copy it to a column with another name in a transformer if you want to keep it.  \nRecords arriving late, e.g. having a source timestamp older than the version they replace, are delayed to the\nnext tick on the time axis after that version was captured, in order to avoid negative validity intervals."
          },
          "checkInputUnique" : {
            "type" : "boolean",
            "description" : "If true, validates that input records have unique primary keys according to output DataObject\nprimary key before historization. This is a fail-fast mechanism to detect data quality issues\nearly and prevent incorrect historization. If duplicate keys are found, the job will fail with\ndetails about the duplicate records. Default is false to maintain backward compatibility. In\nthat case dropDuplicate(primary key columns) is applied on input DataFrame."
          },
          "timeAxisUnit" : {
            "type" : "string",
            "description" : "Time between ticks on the time axis. Used to create valid to timestamp for existing/old\nrecords. Set to 0 to create a history with half-open intervals (e.g. valid to timestamp is\nexclusive). Format is`x(ns|us|ms|s|m|h|d)` , e.g. 1d. Default is 1ms.",
            "existingJavaType" : "java.time.Duration"
          },
          "cacheOutput" : {
            "type" : "boolean",
            "description" : "Cache the output DataFrame of this Action, so that subsequent Actions can reuse it instead of reading the output\nDataObject again. This saves reading the data again, at the cost of materializing it in the engine.\n\nBy default the DataFrame is not propagated to subsequent Actions, they get a fresh DataFrame from the output\nDataObject according to the SubFeed's partition values.\n\nNote that on Snowflake this materializes a temporary table which is only released when the session is closed."
          },
          "cacheInput" : {
            "type" : "boolean",
            "description" : "Force materializing input DataFrame's.  \nThis improves performance if the input DataFrame is used multiple times in the transformation of this Action\nand can serve as a recovery point in case a task gets lost."
          },
          "executionMode" : {
            "oneOf" : [ {
              "$ref" : "#/definitions/ExecutionMode/CustomPartitionMode"
            }, {
              "$ref" : "#/definitions/ExecutionMode/DataFrameIncrementalMode"
            }, {
              "$ref" : "#/definitions/ExecutionMode/DataObjectStateIncrementalMode"
            }, {
              "$ref" : "#/definitions/ExecutionMode/FailIfNoPartitionValuesMode"
            }, {
              "$ref" : "#/definitions/ExecutionMode/FileIncrementalMoveMode"
            }, {
              "$ref" : "#/definitions/ExecutionMode/KafkaStateIncrementalMode"
            }, {
              "$ref" : "#/definitions/ExecutionMode/PartitionDiffMode"
            }, {
              "$ref" : "#/definitions/ExecutionMode/ProcessAllMode"
            }, {
              "$ref" : "#/definitions/ExecutionMode/SparkStreamingMode"
            } ],
            "description" : "Optional execution mode for this action."
          },
          "executionCondition" : {
            "type" : "object",
            "properties" : {
              "expression" : {
                "type" : "string",
                "description" : "Condition formulated as Spark SQL. The attributes available are dependent on the context."
              },
              "description" : {
                "type" : "string",
                "description" : "A textual description of the condition to be shown in error messages."
              }
            },
            "title" : "Condition",
            "required" : [ "expression" ],
            "additionalProperties" : false,
            "description" : "Optional execution condition for this action.  \nBy default, an Action is executed if all inputs are available, e.g. no input from a previous Action is skipped.  \nOverride the default behaviour by specifying an executionCondition in Spark SQL expression syntax.  \nIt is evaluated against the properties available in `SubFeedsExpressionData`.  \nIf true, the Action is executed, otherwise it is skipped. Example:\n```\nexecutionCondition = {\n  description = \"execute if input stg-src1 is not skipped\"\n  expression = \"!inputSubFeeds.stg-src1.isSkipped\"\n}\n```\n\n\nBesides the input SubFeeds, the runtime information of all Actions this Action transitively depends on is\navailable through `SubFeedsExpressionData.predecessorActions`, see `ActionExpressionData`. Example:\n```\nexecutionCondition = {\n  description = \"execute if the previous action loaded data\"\n  expression = \"predecessorActions['loadAction'].metrics['stg-src1']['records_written'] > 0\"\n}\n```\n"
          },
          "metricsFailCondition" : {
            "type" : "string",
            "description" : "Optional Spark SQL condition evaluated as where-clause against dataframe of SubFeed metrics. Available columns are dataObjectId, key, value.  \nIf there are any rows passing the where clause, a MetricCheckFailed exception is thrown.\n\nTo check for skipped SubFeeds, an additional row with key='skipped' and value=true|false is created per output DataObject. Example - fail an action writing to output int-tgt in case there are no records written:\n```\ndataObjectId = 'int-tgt' and key = 'no_data' and value = true\"\n```\n"
          },
          "metadata" : {
            "$ref" : "#/definitions/Others/ActionMetadata",
            "description" : "Additional metadata for the Action"
          },
          "engineConnectionId" : {
            "type" : "string",
            "description" : "Actions needing an engine to execute, e.g. Spark, can override this method to provide the connection to the engine."
          }
        },
        "title" : "HistorizeAction",
        "required" : [ "type", "inputId", "outputId" ],
        "additionalProperties" : false,
        "description" : "This `Action` historizes data between an input and output DataObject using DataFrames.  \nHistorization creates a technical history of data by creating valid-from/to columns. The\nDataFrame might be transformed using SQL or DataFrame transformations. These transformations are\napplied before the deduplication.\n\nBy default, a history with closed intervals is created, e.g. valid-from and valid-to is\ninclusive. The time axis unit can be set by configuration attribute `timeAxisUnit` . It is used as\nthe offset between valid-to of the previous record and valid-from of the current record. A\nhistory with half-open intervals can be created by setting timeAxisUnit=0. In a half-open\ninterval valid-from is inclusive and valid-to is exclusive.\n\nHistorizeAction needs a transactional table (e.g. implementation of `TransactionalTableDataObject` ) as output with defined primary keys.\n\nSince SDLB version 3.0 incremental historization is used per default, and full historization is removed.  \nIncremental historization does not rewrite all data in output table.  \nIt still needs to join new data with all existing data, but uses hash values to minimize data\ntransfer. If you have change-data-capture (CDC) information available to identify deleted\nrecords, you can set mergeModeCDCColumn and mergeModeCDCDeletedValue to even avoid the join\nbetween new and existing data. This is optimal from a performance perspective.\n\nIf the input delivers change events using SDLBs standard CDC columns `_change_type` , `_commit_timestamp` and `_change_ordinal` (as DebeziumCdcDataObject does), CDC historization is used without any further configuration,\nsee mergeModeCDCAutoDetect.\n\nIf you still have a legacy full historization table, migration to incremental historization should happen\nautomatically. The missing hash column is detected and added to existing data.\n\nBy default the validity of a new version starts at the reference timestamp of the run. If the input contains the\ntimestamp of the last change in the source system, it can be used instead by setting sourceTimestampColumn, so\nthat the history reflects the time axis of the source system. Example:\n```\nactions = {\n  historize-airports {\n    type = HistorizeAction\n    inputId = stg-airports\n    outputId = int-airports\n    timeAxisUnit = 1d\n    historizeBlacklist = [last_updated]\n  }\n}\n```\n"
      }
    },
    "HttpAuthMode" : {
      "AWSUserPwdAuthMode" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "AWSUserPwdAuthMode"
          },
          "region" : {
            "type" : "string",
            "description" : "AWS region"
          },
          "userPool" : {
            "type" : "string",
            "description" : "Cognito domain prefix of the AWS Cognito user pool, e.g. \"sdlb-ui\" (not the user pool name and\nnot the user pool id). It is used to build the token endpoint\nhttps://{userPool}.auth.{region}.amazoncognito.com/oauth2/token used to refresh tokens."
          },
          "clientId" : {
            "type" : "string",
            "description" : "client id of the AWS application\n\nThe value can be provided in plaintext or with the id of a secret provider and the\nname of the secret in the format\n```\n###<PROVIDERID>#<SECRETNAME>###\n```\n ,\ne.g.  \n```\n###ENV#<ENV_VARIABLE_NAME>###\n```\n\n\nto get a secret from an environment variable."
          },
          "user" : {
            "type" : "string",
            "description" : "user to login\n\nThe value can be provided in plaintext or with the id of a secret provider and the\nname of the secret in the format\n```\n###<PROVIDERID>#<SECRETNAME>###\n```\n ,\ne.g.  \n```\n###ENV#<ENV_VARIABLE_NAME>###\n```\n\n\nto get a secret from an environment variable."
          },
          "password" : {
            "type" : "string",
            "description" : "password to use for login.\n\nThe value can be provided in plaintext or with the id of a secret provider and the\nname of the secret in the format\n```\n###<PROVIDERID>#<SECRETNAME>###\n```\n ,\ne.g.  \n```\n###ENV#<ENV_VARIABLE_NAME>###\n```\n\n\nto get a secret from an environment variable."
          },
          "useIdToken" : {
            "type" : "boolean",
            "description" : "If true, id_token is used for Http Authorization header, otherwise access_token. Default is false."
          },
          "timeouts" : {
            "type" : "object",
            "properties" : {
              "connectionTimeoutMs" : {
                "type" : "integer"
              },
              "readTimeoutMs" : {
                "type" : "integer"
              }
            },
            "title" : "HttpTimeoutConfig",
            "required" : [ "connectionTimeoutMs", "readTimeoutMs" ],
            "additionalProperties" : false,
            "description" : "configuration of HTTP timeouts. Default is connectionTimeout=500ms, readTimeout=1s."
          },
          "proxy" : {
            "type" : "object",
            "properties" : {
              "host" : {
                "type" : "string",
                "description" : "proxy host"
              },
              "port" : {
                "type" : "integer",
                "description" : "proxy port"
              },
              "user" : {
                "type" : "string",
                "description" : "The value can be provided in plaintext or with the id of a secret provider and the\nname of the secret in the format\n```\n###<PROVIDERID>#<SECRETNAME>###\n```\n ,\ne.g.  \n```\n###ENV#<ENV_VARIABLE_NAME>###\n```\n\n\nto get a secret from an environment variable."
              },
              "password" : {
                "type" : "string",
                "description" : "The value can be provided in plaintext or with the id of a secret provider and the\nname of the secret in the format\n```\n###<PROVIDERID>#<SECRETNAME>###\n```\n ,\ne.g.  \n```\n###ENV#<ENV_VARIABLE_NAME>###\n```\n\n\nto get a secret from an environment variable."
              }
            },
            "title" : "HttpProxyConfig",
            "required" : [ "host", "port" ],
            "additionalProperties" : false,
            "description" : "optional configuration of an HTTP proxy used for the Cognito and token requests."
          }
        },
        "title" : "AWSUserPwdAuthMode",
        "required" : [ "type", "region", "userPool", "clientId", "user", "password" ],
        "additionalProperties" : false,
        "description" : "AWS User/Pwd Authentication.\n\nThis first calls AWS Cognito InitiateAuth endpoint from the selected region to authenticate the user.  \nThe tokens returned can then be refreshed with the corresponding token endpoint of the user pool.\n\nUse this for HTTP endpoints protected by an AWS Cognito user pool, e.g. the SDLB UI backend.  \nThe authentication flow used is USER_PASSWORD_AUTH, so the app client of the user pool must have it enabled. Example:\n```\nglobal {\n  uiBackend {\n    baseUrl = \"https://xxxxxxxxxx.execute-api.eu-central-1.amazonaws.com/DEV/api/v1\"\n    repo = my-project\n    authMode = {\n      type = AWSUserPwdAuthMode\n      region = eu-central-1\n      userPool = sdlb-ui\n      clientId = \"###FILE#ui-auth;clientId###\"\n      user = \"###FILE#ui-auth;user###\"\n      password = \"###FILE#ui-auth;pwd###\"\n      useIdToken = true\n    }\n  }\n}\n```\n"
      },
      "AuthHeaderMode" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "AuthHeaderMode"
          },
          "headerName" : {
            "type" : "string",
            "description" : "name of the HTTP header to set, e.g. \"X-API-KEY\""
          },
          "secret" : {
            "type" : "string",
            "description" : "value to set as header content (supports secret providers)\n\nThe value can be provided in plaintext or with the id of a secret provider and the\nname of the secret in the format\n```\n###<PROVIDERID>#<SECRETNAME>###\n```\n ,\ne.g.  \n```\n###ENV#<ENV_VARIABLE_NAME>###\n```\n\n\nto get a secret from an environment variable."
          }
        },
        "title" : "AuthHeaderMode",
        "required" : [ "type", "headerName", "secret" ],
        "additionalProperties" : false,
        "description" : "Authenticate using a custom HTTP header.\n\nUse this when the webservice expects its credential in a proprietary header like `X-API-KEY` instead of\nthe standard `Authorization` header. The secret is sent verbatim as header value, so a scheme prefix such\nas \"Basic \" or \"Bearer \" must be part of the secret itself. Only usable for HTTP based connections and\nDataObjects. Example:\n```\ndataObjects = {\n  ext-departures {\n    type = WebserviceFileDataObject\n    url = \"https://opensky-network.org/api/flights/departure\"\n    authMode = {\n      type = AuthHeaderMode\n      headerName = \"X-API-KEY\"\n      secret = \"###ENV#API_KEY###\"\n    }\n  }\n}\n```\n\n\nSEE: `BasicAuthMode` and `TokenAuthMode` for the standard `Authorization` header variants."
      },
      "AzureADClientGrantAuthMode" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "AzureADClientGrantAuthMode"
          },
          "authority" : {
            "type" : "string",
            "description" : "URL of the Azure AD authority to request the token from, including the tenant,\ne.g. \"https://login.microsoftonline.com/{tenant-guid}/\" (supports secret providers)\n\nThe value can be provided in plaintext or with the id of a secret provider and the\nname of the secret in the format\n```\n###<PROVIDERID>#<SECRETNAME>###\n```\n ,\ne.g.  \n```\n###ENV#<ENV_VARIABLE_NAME>###\n```\n\n\nto get a secret from an environment variable."
          },
          "applicationId" : {
            "type" : "string",
            "description" : "application (client) id of the registered Azure AD application (supports secret providers)\n\nThe value can be provided in plaintext or with the id of a secret provider and the\nname of the secret in the format\n```\n###<PROVIDERID>#<SECRETNAME>###\n```\n ,\ne.g.  \n```\n###ENV#<ENV_VARIABLE_NAME>###\n```\n\n\nto get a secret from an environment variable."
          },
          "clientSecret" : {
            "type" : "string",
            "description" : "client secret of the registered Azure AD application (supports secret providers)\n\nThe value can be provided in plaintext or with the id of a secret provider and the\nname of the secret in the format\n```\n###<PROVIDERID>#<SECRETNAME>###\n```\n ,\ne.g.  \n```\n###ENV#<ENV_VARIABLE_NAME>###\n```\n\n\nto get a secret from an environment variable."
          },
          "scope" : {
            "type" : "string",
            "description" : "OAuth2 scope to request the token for, normally \"<resource>/.default\" for the client\ncredentials grant (supports secret providers)\n\nThe value can be provided in plaintext or with the id of a secret provider and the\nname of the secret in the format\n```\n###<PROVIDERID>#<SECRETNAME>###\n```\n ,\ne.g.  \n```\n###ENV#<ENV_VARIABLE_NAME>###\n```\n\n\nto get a secret from an environment variable."
          }
        },
        "title" : "AzureADClientGrantAuthMode",
        "required" : [ "type", "authority", "applicationId", "clientSecret", "scope" ],
        "additionalProperties" : false,
        "description" : "Authenticate against an HTTP service with an Azure AD (Microsoft Entra ID) access token, using the\nOAuth2 client credentials grant.\n\nA token is requested from the given Azure AD `authority` for the registered application (service principal)\nand passed on as `Authorization: Bearer <token>` header. Use this instead of the generic `io.smartdatalake.workflow.connection.authMode.OAuthMode` if the token should be acquired through the\nMicrosoft MSAL library, e.g. because the authority handles tenant discovery and token caching.  \nAll parameters support secret providers, so no credential has to be stored in clear text. Example:\n```\ndataObjects = {\n  ext-airports {\n    type = WebserviceFileDataObject\n    url = \"https://my-api.azurewebsites.net/airports\"\n    authMode = {\n      type = AzureADClientGrantAuthMode\n      authority = \"https://login.microsoftonline.com/{tenant-guid}/\"\n      applicationId = \"###ENV#AZURE_CLIENT_ID###\"\n      clientSecret = \"###ENV#AZURE_CLIENT_SECRET###\"\n      scope = \"api://my-api/.default\"\n    }\n  }\n}\n```\n\n\nNOTE: This authentication mode lives in the sdl-azure module, so that module has to be on the classpath."
      },
      "BasicAuthMode" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "BasicAuthMode"
          },
          "user" : {
            "type" : "string",
            "description" : "user to login with (supports secret providers, e.g. \"###ENV#SFTP_USER###\")\n\nThe value can be provided in plaintext or with the id of a secret provider and the\nname of the secret in the format\n```\n###<PROVIDERID>#<SECRETNAME>###\n```\n ,\ne.g.  \n```\n###ENV#<ENV_VARIABLE_NAME>###\n```\n\n\nto get a secret from an environment variable."
          },
          "password" : {
            "type" : "string",
            "description" : "password to login with (supports secret providers)\n\nThe value can be provided in plaintext or with the id of a secret provider and the\nname of the secret in the format\n```\n###<PROVIDERID>#<SECRETNAME>###\n```\n ,\ne.g.  \n```\n###ENV#<ENV_VARIABLE_NAME>###\n```\n\n\nto get a secret from an environment variable."
          }
        },
        "title" : "BasicAuthMode",
        "required" : [ "type", "user", "password" ],
        "additionalProperties" : false,
        "description" : "Authenticate using basic user/pwd authentication.\n\nFor http connection this will create a basic authentication header.\n\nThis is the most commonly used authentication mode. It is the only AuthMode supported by JdbcTableConnection and\nDebeziumConnection, and one of the supported modes of SFtpFileRefConnection, SnowflakeConnection and HTTP based\nDataObjects. Note that no connection falls back to it implicitly - authMode has to be configured explicitly.  \nBoth user and password support secret providers, so credentials do not have to be stored in clear text in the\nconfiguration. Example:\n```\nconnections = {\n  sftp-src {\n    type = SFtpFileRefConnection\n    host = \"sftp.example.com\"\n    authMode = {\n      type = BasicAuthMode\n      user = \"###ENV#SFTP_USER###\"\n      password = \"###ENV#SFTP_PWD###\"\n    }\n  }\n}\n```\n"
      },
      "CustomHttpAuthMode" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "CustomHttpAuthMode"
          },
          "className" : {
            "type" : "string",
            "description" : "class name implementing trait`CustomHttpAuthModeLogic`"
          },
          "options" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string",
              "description" : "The value can be provided in plaintext or with the id of a secret provider and the\nname of the secret in the format\n```\n###<PROVIDERID>#<SECRETNAME>###\n```\n ,\ne.g.  \n```\n###ENV#<ENV_VARIABLE_NAME>###\n```\n\n\nto get a secret from an environment variable."
            },
            "description" : "Options to pass to the custom auth mode logic in prepare function.\n\nThe value can be provided in plaintext or with the id of a secret provider and the\nname of the secret in the format\n```\n###<PROVIDERID>#<SECRETNAME>###\n```\n ,\ne.g.  \n```\n###ENV#<ENV_VARIABLE_NAME>###\n```\n\n\nto get a secret from an environment variable."
          }
        },
        "title" : "CustomHttpAuthMode",
        "required" : [ "type", "className", "options" ],
        "additionalProperties" : false,
        "description" : "Connect with custom HTTP-header based authentication\n\nThe configured class implements `CustomHttpAuthModeLogic` and returns the HTTP headers to add to every\nrequest; `options` are handed over once in the prepare phase, e.g. to acquire a token.\n\nDeprecated since 2.7.1: implement `HttpAuthMode` (or `AuthMode` ) directly instead, which gives access\nto typed configuration attributes rather than an untyped option map. Example:\n```\ndataObjects = {\n  ext-departures {\n    type = WebserviceFileDataObject\n    url = \"https://opensky-network.org/api/flights/departure\"\n    authMode = {\n      type = CustomHttpAuthMode\n      className = \"com.example.auth.MyCustomHttpAuthMode\"\n      options = { apiKey = \"###ENV#API_KEY###\" }\n    }\n  }\n}\n```\n"
      },
      "OAuthMode" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "OAuthMode"
          },
          "oauthUrl" : {
            "type" : "string",
            "description" : "URL to the OAuth2 authorization instance like \"https://login.microsoftonline.com/{tenant-guid}/oauth2/v2.0/token\" (supports secret providers)\n\nThe value can be provided in plaintext or with the id of a secret provider and the\nname of the secret in the format\n```\n###<PROVIDERID>#<SECRETNAME>###\n```\n ,\ne.g.  \n```\n###ENV#<ENV_VARIABLE_NAME>###\n```\n\n\nto get a secret from an environment variable."
          },
          "clientId" : {
            "type" : "string",
            "description" : "Name of the user (supports secrets providers)\n\nThe value can be provided in plaintext or with the id of a secret provider and the\nname of the secret in the format\n```\n###<PROVIDERID>#<SECRETNAME>###\n```\n ,\ne.g.  \n```\n###ENV#<ENV_VARIABLE_NAME>###\n```\n\n\nto get a secret from an environment variable."
          },
          "clientSecret" : {
            "type" : "string",
            "description" : "Password of the user (supports secret providers)\n\nThe value can be provided in plaintext or with the id of a secret provider and the\nname of the secret in the format\n```\n###<PROVIDERID>#<SECRETNAME>###\n```\n ,\ne.g.  \n```\n###ENV#<ENV_VARIABLE_NAME>###\n```\n\n\nto get a secret from an environment variable."
          },
          "oauthScope" : {
            "type" : "string",
            "description" : "OAuth authorization scope (like https://xxx.crm4.dynamics.com/.default) (supports secret providers)\n\nThe value can be provided in plaintext or with the id of a secret provider and the\nname of the secret in the format\n```\n###<PROVIDERID>#<SECRETNAME>###\n```\n ,\ne.g.  \n```\n###ENV#<ENV_VARIABLE_NAME>###\n```\n\n\nto get a secret from an environment variable."
          },
          "useIdToken" : {
            "type" : "boolean",
            "description" : "If true, id_token is used for Http Authorization header, otherwise access_token. Default is false."
          },
          "proxy" : {
            "type" : "object",
            "properties" : {
              "host" : {
                "type" : "string",
                "description" : "proxy host"
              },
              "port" : {
                "type" : "integer",
                "description" : "proxy port"
              },
              "user" : {
                "type" : "string",
                "description" : "The value can be provided in plaintext or with the id of a secret provider and the\nname of the secret in the format\n```\n###<PROVIDERID>#<SECRETNAME>###\n```\n ,\ne.g.  \n```\n###ENV#<ENV_VARIABLE_NAME>###\n```\n\n\nto get a secret from an environment variable."
              },
              "password" : {
                "type" : "string",
                "description" : "The value can be provided in plaintext or with the id of a secret provider and the\nname of the secret in the format\n```\n###<PROVIDERID>#<SECRETNAME>###\n```\n ,\ne.g.  \n```\n###ENV#<ENV_VARIABLE_NAME>###\n```\n\n\nto get a secret from an environment variable."
              }
            },
            "title" : "HttpProxyConfig",
            "required" : [ "host", "port" ],
            "additionalProperties" : false,
            "description" : "optional Proxy configuration used to make HTTP-connection."
          },
          "timeouts" : {
            "type" : "object",
            "properties" : {
              "connectionTimeoutMs" : {
                "type" : "integer"
              },
              "readTimeoutMs" : {
                "type" : "integer"
              }
            },
            "title" : "HttpTimeoutConfig",
            "required" : [ "connectionTimeoutMs", "readTimeoutMs" ],
            "additionalProperties" : false,
            "description" : "optional configuration of HTTP timeouts"
          },
          "followRedirects" : {
            "type" : "boolean",
            "description" : "if redirects should be followed when creating HTTP-connection. Default is false because of security concerns."
          },
          "retries" : {
            "type" : "integer",
            "description" : "number of retries if http request fails. Default is 1 retry."
          }
        },
        "title" : "OAuthMode",
        "required" : [ "type", "oauthUrl", "clientId", "clientSecret", "oauthScope" ],
        "additionalProperties" : false,
        "description" : "`OAuthMode` contains the coordinates and credentials to gain access to the OData DataSource\n\nAuthenticates with the OAuth2 client_credentials flow: an access token is fetched from `oauthUrl` for the\ngiven `oauthScope` and cached until it expires, then refreshed automatically. Pick this over `TokenAuthMode` whenever tokens are short-lived and must be renewed during a run, e.g. for Microsoft\nDynamics 365 / Dataverse, other OData or REST services, and SnowflakeConnection. Example:\n```\ndataObjects = {\n  ext-tasks {\n    type = ODataDataObject\n    baseUrl = \"https://myorg.crm4.dynamics.com/api/data/v9.2/\"\n    tableName = \"tasks\"\n    schema = \"activityid string, subject string, modifiedon string\"\n    authMode = {\n      type = OAuthMode\n      oauthUrl = \"https://login.microsoftonline.com/{tenant-guid}/oauth2/v2.0/token\"\n      clientId = \"###ENV#CRM_CLIENT_ID###\"\n      clientSecret = \"###ENV#CRM_CLIENT_SECRET###\"\n      oauthScope = \"https://myorg.crm4.dynamics.com/.default\"\n    }\n  }\n}\n```\n"
      },
      "TokenAuthMode" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "TokenAuthMode"
          },
          "tokenType" : {
            "type" : "string",
            "description" : "token type to use in HTTP Authorization header. Default is \"Bearer\"."
          },
          "token" : {
            "type" : "string",
            "description" : "token to authenticate with (supports secret providers). Although declared optional for configuration\nparsing, it must be defined, otherwise a ConfigurationException is thrown.\n\nThe value can be provided in plaintext or with the id of a secret provider and the\nname of the secret in the format\n```\n###<PROVIDERID>#<SECRETNAME>###\n```\n ,\ne.g.  \n```\n###ENV#<ENV_VARIABLE_NAME>###\n```\n\n\nto get a secret from an environment variable."
          }
        },
        "title" : "TokenAuthMode",
        "required" : [ "type" ],
        "additionalProperties" : false,
        "description" : "Authenticate using a predefined token.\n\nFor HTTP Connections the token is used as Authorization header.\n\nThe header is built as \"tokenType token\", e.g. \"Authorization: Bearer eyJ...\". Use this for services\naccessed with a long-living personal access token or API key. If the token has to be requested and\nrefreshed during the run, use `OAuthMode` instead; if the credential goes into a non-standard header,\nuse `AuthHeaderMode`. Example:\n```\ndataObjects = {\n  ext-departures {\n    type = WebserviceFileDataObject\n    url = \"https://opensky-network.org/api/flights/departure\"\n    authMode = {\n      type = TokenAuthMode\n      token = \"###ENV#API_TOKEN###\"\n    }\n  }\n}\n```\n"
      }
    },
    "Connection" : {
      "BigQueryTableConnection" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "BigQueryTableConnection"
          },
          "authMode" : {
            "oneOf" : [ {
              "$ref" : "#/definitions/AuthMode/GCPCredentialsKeyAuth"
            }, {
              "$ref" : "#/definitions/AuthMode/PublicKeyAuthMode"
            }, {
              "$ref" : "#/definitions/AuthMode/SASLSCRAMAuthMode"
            }, {
              "$ref" : "#/definitions/AuthMode/SSLCertsAuthMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/AWSUserPwdAuthMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/AuthHeaderMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/AzureADClientGrantAuthMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/BasicAuthMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/CustomHttpAuthMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/OAuthMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/TokenAuthMode"
            } ],
            "description" : "Credentials to be used for this connection using`GCPCredentialsKeyAuth` As of now, only Service Account keys (either as encoded Strings or as json-files)\nare supported as an authentication method."
          },
          "parentProject" : {
            "type" : "string",
            "description" : "GCP Project ID to be billed for the BigQuery jobs of this connection. It is passed to the\nBigQuery Spark connector as`parentProject` option and is mandatory, e.g. it is not\nderived from the Service Account of the credentials."
          },
          "metadata" : {
            "$ref" : "#/definitions/Others/ConnectionMetadata",
            "description" : "Additional metadata for the Connection"
          }
        },
        "title" : "BigQueryTableConnection",
        "required" : [ "type", "authMode", "parentProject" ],
        "additionalProperties" : false,
        "description" : "Connection information for GCP BigQuery Tables.\n\nIt holds the GCP project used for billing/API access and the service account credentials shared by all\nBigQueryTableDataObjects referencing it through `connectionId` . The credentials are also used to build a\nBigQuery client for metadata operations (e.g. checking table existence), so a connection is required to read\nor write BigQuery tables. Example:\n```\nconnections {\n  google-playground {\n    type = BigQueryTableConnection\n    parentProject = \"playground-223717\"\n    authMode = {\n      type = GCPCredentialsKeyAuth\n      serviceAccountKeyFile = \"~{env.basedir}/playground-223717-key.json\"\n    }\n  }\n}\n```\n\n\nNOTE: authentication is only supported with `GCPCredentialsKeyAuth` ; any other AuthMode is rejected on\ninstantiation."
      },
      "DebeziumConnection" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "DebeziumConnection"
          },
          "dbEngine" : {
            "type" : "string",
            "description" : "what database engine to use, currently supported engines: mysql, postgresql, oracle, mariadb, sqlserver, db2."
          },
          "hostname" : {
            "type" : "string",
            "description" : "database server"
          },
          "db" : {
            "type" : "string",
            "description" : "database to read data from. Optional, as some engines (e.g. mysql) do not need it."
          },
          "port" : {
            "type" : "integer",
            "description" : "port the database server listens on, e.g. 5432 for postgresql"
          },
          "authMode" : {
            "oneOf" : [ {
              "$ref" : "#/definitions/AuthMode/GCPCredentialsKeyAuth"
            }, {
              "$ref" : "#/definitions/AuthMode/PublicKeyAuthMode"
            }, {
              "$ref" : "#/definitions/AuthMode/SASLSCRAMAuthMode"
            }, {
              "$ref" : "#/definitions/AuthMode/SSLCertsAuthMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/AWSUserPwdAuthMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/AuthHeaderMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/AzureADClientGrantAuthMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/BasicAuthMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/CustomHttpAuthMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/OAuthMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/TokenAuthMode"
            } ],
            "description" : "authentication information: for now BasicAuthMode is supported."
          },
          "metadata" : {
            "$ref" : "#/definitions/Others/ConnectionMetadata",
            "description" : "Additional metadata for the Connection"
          }
        },
        "title" : "DebeziumConnection",
        "required" : [ "type", "dbEngine", "hostname", "port", "authMode" ],
        "additionalProperties" : false,
        "description" : "Connection information for a Debezium change-data-capture (CDC) source database.\n\nIt describes the source database server which the embedded Debezium engine connects to in order to read its\ntransaction log. Use it together with `DebeziumCdcDataObject` to stream inserts/updates/deletes out of an\noperational database instead of repeatedly reading full tables over JDBC. The Debezium connector class is\nderived from `dbEngine` when the connector properties are assembled, e.g. when a `DebeziumCdcDataObject` starts reading. The connector must then be on the classpath, otherwise a `ClassNotFoundException` is thrown -\nadd the corresponding optional maven dependency of the sdl-debezium module (e.g. `io.debezium:debezium-connector-postgres` for postgresql, `io.smartdatalake:debezium-connector-mysql-shaded` for mysql). Example:\n```\nconnections {\n  dbzCon {\n    type = DebeziumConnection\n    dbEngine = \"postgresql\"\n    hostname = \"localhost\"\n    port = 5432\n    db = \"test\"\n    authMode = {\n      type = BasicAuthMode\n      user = \"###ENV#POSTGRES_USER###\"\n      password = \"###ENV#POSTGRES_PW###\"\n    }\n  }\n}\n```\n"
      },
      "DeltaLakeTableConnection" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "DeltaLakeTableConnection"
          },
          "catalog" : {
            "type" : "string",
            "description" : "optional catalog to be used for this connection"
          },
          "db" : {
            "type" : "string",
            "description" : "hive db"
          },
          "pathPrefix" : {
            "type" : "string",
            "description" : "schema, authority and base path for tables directory on hadoop"
          },
          "checkDeltaLakeSparkOptions" : {
            "type" : "boolean",
            "description" : "if true (default) it is verified on prepare that the Spark session registers`io.delta.sql.DeltaSparkSessionExtension`in`spark.sql.extensions` .  \nSet to false to skip this check. The check is skipped automatically on Databricks."
          },
          "metadata" : {
            "$ref" : "#/definitions/Others/ConnectionMetadata",
            "description" : "Additional metadata for the Connection"
          }
        },
        "title" : "DeltaLakeTableConnection",
        "required" : [ "type", "db", "pathPrefix" ],
        "additionalProperties" : false,
        "description" : "Connection information for DeltaLake tables.\n\nIt centralizes catalog, database and the base directory of the table files, so that DeltaLakeTableDataObjects\nreferencing it through `connectionId` do not need to repeat them. Catalog and db of the DataObjects `table` are\ntaken from this connection, and the DataObjects `path` is resolved relative to `pathPrefix` . This keeps\nenvironment specific storage locations out of the DataObject definitions. Example:\n```\nconnections {\n  deltalake-int {\n    type = DeltaLakeTableConnection\n    db = \"integration\"\n    pathPrefix = \"~{env.basedir}/deltalake\"\n  }\n}\n```\n\n\nNOTE: the database given in `db` must already exist; SDLB does not create it."
      },
      "HadoopFileConnection" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "HadoopFileConnection"
          },
          "pathPrefix" : {
            "type" : "string",
            "description" : "schema, authority and base path for accessing files on hadoop"
          },
          "sparkConnectionId" : {
            "type" : "string",
            "description" : "optional id of a Spark engine connection (e.g.`SparkClassicConnection`).  \nNote that this attribute is currently not evaluated by SDLB. The engine connection\nused is selected per Action through its`engineConnectionId`, which falls back to`Environment.defaultEngineConnectionId` ."
          },
          "metadata" : {
            "$ref" : "#/definitions/Others/ConnectionMetadata",
            "description" : "Additional metadata for the Connection"
          }
        },
        "title" : "HadoopFileConnection",
        "required" : [ "type", "pathPrefix" ],
        "additionalProperties" : false,
        "description" : "Connection information for files on hadoop.\n\nIt centralizes the schema, authority and base directory ( `pathPrefix` ) of a hadoop compatible filesystem\n(local filesystem, HDFS, ADLS, S3, ...). File based DataObjects referencing this connection through `connectionId` only configure a relative `path` , which is resolved against `pathPrefix` . Use it to keep\nenvironment specific storage locations out of the DataObject definitions, so the same DataObjects can be\ndeployed to different environments by exchanging the connection only. Example:\n```\nconnections {\n  cloud-staging {\n    type = HadoopFileConnection\n    pathPrefix = \"abfs://staging\n```\n"
      },
      "IcebergTableConnection" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "IcebergTableConnection"
          },
          "catalog" : {
            "type" : "string",
            "description" : "optional catalog to be used for this connection"
          },
          "db" : {
            "type" : "string",
            "description" : "database to be used for this connection"
          },
          "pathPrefix" : {
            "type" : "string",
            "description" : "schema, authority and base path for tables directory on hadoop"
          },
          "checkIcebergSparkOptions" : {
            "type" : "boolean",
            "description" : "if true check if IcebergSparkSessionExtensions is registered through spark.sql.extensions property.  \nDefault is true."
          },
          "addFilesParallelism" : {
            "type" : "integer",
            "description" : "Number of thread to use for file reading when migrating table from parquet to iceberg using procedure 'system.add_files'.  \nIcberg Default value is 1, but should be increased for acceptable performance with larger tables."
          },
          "metadata" : {
            "$ref" : "#/definitions/Others/ConnectionMetadata",
            "description" : "Additional metadata for the Connection"
          }
        },
        "title" : "IcebergTableConnection",
        "required" : [ "type", "db", "pathPrefix" ],
        "additionalProperties" : false,
        "description" : "Connection information for Iceberg tables.\n\nIt centralizes catalog, database and the base directory of the table files, so that IcebergTableDataObjects\nreferencing it through `connectionId` do not need to repeat them. Catalog and db of the DataObjects `table` are\ntaken from this connection, and the DataObjects `path` is resolved relative to `pathPrefix` . This keeps\nenvironment specific storage locations out of the DataObject definitions. Example:\n```\nconnections {\n  iceberg-int {\n    type = IcebergTableConnection\n    db = \"integration\"\n    pathPrefix = \"~{env.basedir}/iceberg\"\n    addFilesParallelism = 8\n  }\n}\n```\n\n\nNOTE: the database given in `db` must already exist; SDLB does not create it."
      },
      "JdbcTableConnection" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "JdbcTableConnection"
          },
          "url" : {
            "type" : "string",
            "description" : "jdbc connection url"
          },
          "driver" : {
            "type" : "string",
            "description" : "class name of jdbc driver"
          },
          "authMode" : {
            "oneOf" : [ {
              "$ref" : "#/definitions/AuthMode/GCPCredentialsKeyAuth"
            }, {
              "$ref" : "#/definitions/AuthMode/PublicKeyAuthMode"
            }, {
              "$ref" : "#/definitions/AuthMode/SASLSCRAMAuthMode"
            }, {
              "$ref" : "#/definitions/AuthMode/SSLCertsAuthMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/AWSUserPwdAuthMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/AuthHeaderMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/AzureADClientGrantAuthMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/BasicAuthMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/CustomHttpAuthMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/OAuthMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/TokenAuthMode"
            } ],
            "description" : "optional authentication information: for now BasicAuthMode is supported."
          },
          "db" : {
            "type" : "string",
            "description" : "optional jdbc database to be used by tables having this connection assigned."
          },
          "maxParallelConnections" : {
            "type" : "integer",
            "description" : "max number of parallel jdbc connections created by an instance of this connection, default is 3\nNote that Spark manages JDBC Connections on its own. This setting only applies to JDBC\nconnection used by SDL for validating metadata or pre/postSQL."
          },
          "connectionInitSql" : {
            "type" : "string",
            "description" : "SQL statement to be executed every time a new connection is created, for example to set session\nparameters"
          },
          "directTableOverwrite" : {
            "type" : "boolean",
            "description" : "flag to enable overwriting target tables directly without creating temporary table. Background:\nSpark uses multiple JDBC connections from different workers, this is done using multiple\ntransactions. For SaveMode.Append this is ok, but it is problematic with SaveMode.Overwrite,\nwhere the table is truncated in a first transaction. Default is directTableWrite=false, this\nwill write data first into a temporary table, and then use a \"DELETE\" + \"INSERT INTO SELECT\"\nstatement to overwrite data in the target table within one transaction. Also note that\nSDLSaveMode.Merge always creates a temporary table."
          },
          "connectionPool" : {
            "type" : "object",
            "properties" : {
              "maxIdleTimeSec" : {
                "type" : "integer",
                "description" : "timeout to close unused connections in the pool. Default is 3 seconds."
              },
              "maxWaitTimeSec" : {
                "type" : "integer",
                "description" : "timeout when waiting for connection in pool to become available. Default is 600 seconds (10 minutes)."
              },
              "testOnBorrow" : {
                "type" : "boolean",
                "description" : "flag to set the GenericObjectPool's`testOnBorrow` . If true the connection pool will validate the connection before it is lent. Default is false."
              },
              "testOnCreate" : {
                "type" : "boolean",
                "description" : "flag to set the GenericObjectPool's`testOnCreate` . If true the connection pool will validate the connection when it is created. Default is false."
              },
              "testOnReturn" : {
                "type" : "boolean",
                "description" : "flag to set the GenericObjectPool's`testOnReturn` . If true the connection pool will validate the connection after it is returned. Default is false."
              },
              "testWhileIdle" : {
                "type" : "boolean",
                "description" : "flag to set the GenericObjectPool's`testWhileIdle` . If true the connection pool will validate the connection periodically while it is idle. Default is false."
              },
              "testTimeoutSec" : {
                "type" : "integer",
                "description" : "Timeout in seconds for connection.isValid call."
              }
            },
            "title" : "ConnectionPoolConfig",
            "additionalProperties" : false,
            "description" : "fine tuning of the JDBC connection pool used by SDLB, see[[ConnectionPoolConfig]], e.g. idle timeout and\nconnection validation. Default is`ConnectionPoolConfig` with its default values."
          },
          "metadata" : {
            "$ref" : "#/definitions/Others/ConnectionMetadata",
            "description" : "Additional metadata for the Connection"
          }
        },
        "title" : "JdbcTableConnection",
        "required" : [ "type", "url", "driver" ],
        "additionalProperties" : false,
        "description" : "Connection information for JDBC tables. If authentication is needed, user and password must be\nprovided.\n\nIt holds url, driver and credentials shared by all JdbcTableDataObjects referencing it through `connectionId` ,\nand maintains a small JDBC connection pool which SDLB uses for metadata and DDL statements (checking table\nexistence, creating tables, pre/postSQL, constraints). Note that Spark opens its own connections for reading\nand writing data, so `maxParallelConnections` limits SDLB's own connections only. A database specific `JdbcCatalog` is derived from `driver` to run catalog queries in the correct dialect. Example:\n```\nconnections {\n  jdbc-dwh {\n    type = JdbcTableConnection\n    url = \"jdbc:postgresql://dwh.example.com:5432/dwh\"\n    driver = \"org.postgresql.Driver\"\n    db = \"public\"\n    authMode = {\n      type = BasicAuthMode\n      user = \"###ENV#JDBC_USER###\"\n      password = \"###ENV#JDBC_PASSWORD###\"\n    }\n  }\n}\n```\n\n\nNOTE: the JDBC driver named in `driver` must be on the classpath; only BasicAuthMode is supported as authMode."
      },
      "KafkaConnection" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "KafkaConnection"
          },
          "brokers" : {
            "type" : "string",
            "description" : "comma separated list of kafka bootstrap server incl. port, e.g. \"host1:9092,host2:9092:"
          },
          "schemaRegistry" : {
            "type" : "string",
            "description" : "url of schema registry service, e.g. \"https://host2\""
          },
          "options" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "Options for the Kafka stream reader (see\nhttps://spark.apache.org/docs/latest/structured-streaming-kafka-integration.html)"
          },
          "authMode" : {
            "oneOf" : [ {
              "$ref" : "#/definitions/AuthMode/GCPCredentialsKeyAuth"
            }, {
              "$ref" : "#/definitions/AuthMode/PublicKeyAuthMode"
            }, {
              "$ref" : "#/definitions/AuthMode/SASLSCRAMAuthMode"
            }, {
              "$ref" : "#/definitions/AuthMode/SSLCertsAuthMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/AWSUserPwdAuthMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/AuthHeaderMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/AzureADClientGrantAuthMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/BasicAuthMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/CustomHttpAuthMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/OAuthMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/TokenAuthMode"
            } ],
            "description" : "optional authentication information. Only SASLSCRAMAuthMode and SSLCertsAuthMode are supported; if not set,\nan unauthenticated PLAINTEXT connection is used."
          },
          "metadata" : {
            "$ref" : "#/definitions/Others/ConnectionMetadata",
            "description" : "Additional metadata for the Connection"
          }
        },
        "title" : "KafkaConnection",
        "required" : [ "type", "brokers" ],
        "additionalProperties" : false,
        "description" : "Connection information for kafka.\n\nIt holds the bootstrap servers, the optional Confluent schema registry url and the authentication settings\nshared by all KafkaTopicDataObjects referencing it through `connectionId` . The authentication settings are used\nboth for the Spark Kafka data source (options prefixed with `kafka.` ) and for the Kafka AdminClient which SDLB\nuses to validate that a topic exists. Configure `schemaRegistry` if topics use Confluent Avro/Json\nschema-registry serialization. Example:\n```\nconnections {\n  kafka-int {\n    type = KafkaConnection\n    brokers = \"broker1.example.com:9092,broker2.example.com:9092\"\n    schemaRegistry = \"https://schemaregistry.example.com\"\n    authMode = {\n      type = SASLSCRAMAuthMode\n      username = \"###ENV#KAFKA_USER###\"\n      password = \"###ENV#KAFKA_PASSWORD###\"\n      sslMechanism = \"SCRAM-SHA-512\"\n    }\n  }\n}\n```\n"
      },
      "SFtpFileRefConnection" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "SFtpFileRefConnection"
          },
          "host" : {
            "type" : "string",
            "description" : "sftp host"
          },
          "port" : {
            "type" : "integer",
            "description" : "port of sftp service, default is 22"
          },
          "authMode" : {
            "oneOf" : [ {
              "$ref" : "#/definitions/AuthMode/GCPCredentialsKeyAuth"
            }, {
              "$ref" : "#/definitions/AuthMode/PublicKeyAuthMode"
            }, {
              "$ref" : "#/definitions/AuthMode/SASLSCRAMAuthMode"
            }, {
              "$ref" : "#/definitions/AuthMode/SSLCertsAuthMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/AWSUserPwdAuthMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/AuthHeaderMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/AzureADClientGrantAuthMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/BasicAuthMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/CustomHttpAuthMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/OAuthMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/TokenAuthMode"
            } ],
            "description" : "authentication information: for now BasicAuthMode and PublicKeyAuthMode are supported."
          },
          "proxy" : {
            "type" : "object",
            "properties" : {
              "host" : {
                "type" : "string",
                "description" : "proxy host"
              },
              "port" : {
                "type" : "integer",
                "description" : "proxy port"
              },
              "proxyType" : {
                "type" : "string",
                "description" : "Type of proxy: HTTP or SOCKS. Default is HTTP.",
                "enum" : [ "DIRECT", "HTTP", "SOCKS" ]
              }
            },
            "title" : "JavaNetProxyConfig",
            "required" : [ "host", "port" ],
            "additionalProperties" : false,
            "description" : "optional proxy configuration"
          },
          "ignoreHostKeyVerification" : {
            "type" : "boolean",
            "description" : "do not validate host key if true, default is false"
          },
          "maxParallelConnections" : {
            "type" : "integer",
            "description" : "number of parallel sftp connections created by an instance of this connection"
          },
          "connectionPoolMaxIdleTimeSec" : {
            "type" : "integer",
            "description" : "timeout to close unused connections in the pool"
          },
          "metadata" : {
            "$ref" : "#/definitions/Others/ConnectionMetadata",
            "description" : "Additional metadata for the Connection"
          }
        },
        "title" : "SFtpFileRefConnection",
        "required" : [ "type", "host", "authMode" ],
        "additionalProperties" : false,
        "description" : "SFTP Connection information.\n\nIt holds host, port and credentials of an SFTP server and manages a pool of SSH connections which is shared\nby all DataObjects referencing this connection. Use it together with `SFtpFileRefDataObject` to list and\ntransfer remote files, e.g. with a FileTransferAction. Only `BasicAuthMode` (user/password) and `PublicKeyAuthMode` (user, key taken from the local ssh agent/key store) are supported. Example:\n```\nconnections {\n  sftp-partner {\n    type = SFtpFileRefConnection\n    host = \"sftp.partner.example.com\"\n    port = 22\n    authMode = {\n      type = BasicAuthMode\n      user = \"###ENV#SFTP_USER###\"\n      password = \"###ENV#SFTP_PASSWORD###\"\n    }\n    maxParallelConnections = 4\n  }\n}\n```\n\n\nNOTE: the host key of the server must be known (e.g. present in `~/.ssh/known_hosts` ), otherwise the connection\nfails unless `ignoreHostKeyVerification` is enabled."
      },
      "ScalaConnection" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "ScalaConnection"
          },
          "metadata" : {
            "$ref" : "#/definitions/Others/ConnectionMetadata",
            "description" : "additional metadata for this connection (name, description, layer, ...)"
          }
        },
        "title" : "ScalaConnection",
        "required" : [ "type" ],
        "additionalProperties" : false,
        "description" : "Engine connection for the plain Scala execution engine.\n\nAn `EngineConnection` declares which DataFrame engine an Action uses; this one selects the lightweight,\nSpark-free engine working on `ScalaSubFeed` . Pick it over `SparkClassicConnection` for small pipelines and\nunit tests where starting a Spark session is unnecessary overhead. Actions select an engine connection with\ntheir `engineConnectionId` attribute; if none is given the connection with id `default-engine` is used\n(configurable through the SDLB parameter `defaultEngineConnectionId` ). Example:\n```\nconnections {\n  default-engine {\n    type = ScalaConnection\n  }\n}\n```\n"
      },
      "SnowflakeConnection" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "SnowflakeConnection"
          },
          "url" : {
            "type" : "string",
            "description" : "Snowflake connection url"
          },
          "warehouse" : {
            "type" : "string",
            "description" : "Snowflake namespace"
          },
          "database" : {
            "type" : "string",
            "description" : "Snowflake database"
          },
          "role" : {
            "type" : "string",
            "description" : "Snowflake role"
          },
          "authMode" : {
            "oneOf" : [ {
              "$ref" : "#/definitions/AuthMode/GCPCredentialsKeyAuth"
            }, {
              "$ref" : "#/definitions/AuthMode/PublicKeyAuthMode"
            }, {
              "$ref" : "#/definitions/AuthMode/SASLSCRAMAuthMode"
            }, {
              "$ref" : "#/definitions/AuthMode/SSLCertsAuthMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/AWSUserPwdAuthMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/AuthHeaderMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/AzureADClientGrantAuthMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/BasicAuthMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/CustomHttpAuthMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/OAuthMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/TokenAuthMode"
            } ],
            "description" : "optional authentication information: for now BasicAuthMode is supported."
          },
          "proxy" : {
            "type" : "object",
            "properties" : {
              "host" : {
                "type" : "string",
                "description" : "proxy host"
              },
              "port" : {
                "type" : "integer",
                "description" : "proxy port"
              },
              "user" : {
                "type" : "string",
                "description" : "The value can be provided in plaintext or with the id of a secret provider and the\nname of the secret in the format\n```\n###<PROVIDERID>#<SECRETNAME>###\n```\n ,\ne.g.  \n```\n###ENV#<ENV_VARIABLE_NAME>###\n```\n\n\nto get a secret from an environment variable."
              },
              "password" : {
                "type" : "string",
                "description" : "The value can be provided in plaintext or with the id of a secret provider and the\nname of the secret in the format\n```\n###<PROVIDERID>#<SECRETNAME>###\n```\n ,\ne.g.  \n```\n###ENV#<ENV_VARIABLE_NAME>###\n```\n\n\nto get a secret from an environment variable."
              }
            },
            "title" : "HttpProxyConfig",
            "required" : [ "host", "port" ],
            "additionalProperties" : false,
            "description" : "optional HTTP Proxy for Snowflake connection (Jdbc & Snowpark)"
          },
          "sparkOptions" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "Options for the Snowflake Spark Connector, see https://docs.snowflake.com/en/user-guide/spark-connector-use#additional-options."
          },
          "metadata" : {
            "$ref" : "#/definitions/Others/ConnectionMetadata",
            "description" : "Additional metadata for the Connection"
          }
        },
        "title" : "SnowflakeConnection",
        "required" : [ "type", "url", "warehouse", "database", "role", "authMode" ],
        "additionalProperties" : false,
        "description" : "Connection information for Snowflake databases.  \nThe connection can be used for SnowflakeTableDataObjects\nIf multiple SnowflakeTableDataObjects share a connection, they share the same Snowpark session\n\nBeside the Snowpark session it also maintains a JDBC connection pool used for metadata and DDL queries\n(e.g. checking table existence, pre/postSQL). The schema is not part of the connection - it is taken from `table.db` of the referencing SnowflakeTableDataObject, so one connection can serve multiple schemas.  \nAuthentication is supported with BasicAuthMode (user/password) or OAuthMode; any other AuthMode is rejected\non instantiation. Example:\n```\nconnections {\n  sfCon {\n    type = SnowflakeConnection\n    url = \"https://myaccount.snowflakecomputing.com\"\n    warehouse = \"COMPUTE_WH\"\n    database = \"TEST_DB\"\n    role = \"SDLB_ROLE\"\n    authMode = {\n      type = BasicAuthMode\n      user = \"###ENV#SNOWFLAKE_USER###\"\n      password = \"###ENV#SNOWFLAKE_PASSWORD###\"\n    }\n  }\n}\n```\n"
      },
      "SparkClassicConnection" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "SparkClassicConnection"
          },
          "master" : {
            "type" : "string",
            "description" : "master URL for the Spark session. If not set, it will try to get an existing Spark session from the environment."
          },
          "deployMode" : {
            "type" : "string",
            "description" : "Spark deploy mode (\"client\" or \"cluster\"), passed on to the Spark session builder as \"deploy-mode\" configuration."
          },
          "sparkOptions" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string",
              "description" : "The value can be provided in plaintext or with the id of a secret provider and the\nname of the secret in the format\n```\n###<PROVIDERID>#<SECRETNAME>###\n```\n ,\ne.g.  \n```\n###ENV#<ENV_VARIABLE_NAME>###\n```\n\n\nto get a secret from an environment variable."
            },
            "description" : "spark options\n\nThe value can be provided in plaintext or with the id of a secret provider and the\nname of the secret in the format\n```\n###<PROVIDERID>#<SECRETNAME>###\n```\n ,\ne.g.  \n```\n###ENV#<ENV_VARIABLE_NAME>###\n```\n\n\nto get a secret from an environment variable."
          },
          "enableHive" : {
            "type" : "boolean",
            "description" : "enable hive for spark session"
          },
          "sparkUDFs" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "object",
              "properties" : {
                "className" : {
                  "type" : "string",
                  "description" : "fully qualified class name of class implementing SparkUDFCreator interface. The class needs a constructor without parameters."
                },
                "options" : {
                  "type" : "object",
                  "additionalProperties" : {
                    "type" : "string"
                  },
                  "description" : "Options are passed to SparkUDFCreator apply method."
                }
              },
              "title" : "SparkUDFCreatorConfig",
              "required" : [ "className" ],
              "additionalProperties" : false,
              "description" : "Configuration to register a UserDefinedFunction in the spark session of SmartDataLake."
            },
            "description" : "Define UDFs to be registered in spark session. The registered UDFs are available in Spark SQL\ntransformations and expression evaluation, e.g. configuration of ExecutionModes."
          },
          "pythonUDFs" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "object",
              "properties" : {
                "pythonFile" : {
                  "type" : "string",
                  "description" : "Optional pythonFile to use for python UDF."
                },
                "pythonCode" : {
                  "type" : "string",
                  "description" : "Optional pythonCode to use for python UDF."
                },
                "options" : {
                  "type" : "object",
                  "additionalProperties" : {
                    "type" : "string"
                  },
                  "description" : "Options are available in your python code as variable options."
                }
              },
              "title" : "PythonUDFCreatorConfig",
              "additionalProperties" : false,
              "description" : "Configuration to register a Python UDF in the spark session of SmartDataLake.  \nDefine a python function with type hints in python code and register it in global configuration.  \nThe name of the function must match the name you use to declare it in GlobalConf.  \nThe Python function can then be used in Spark SQL expressions."
            },
            "description" : "Define UDFs in python to be registered in spark session. The registered UDFs are available in\nSpark SQL transformations but not for expression evaluation."
          },
          "kryoClasses" : {
            "type" : "array",
            "items" : {
              "type" : "string"
            },
            "description" : "classes to register for spark kryo serialization"
          },
          "metadata" : {
            "$ref" : "#/definitions/Others/ConnectionMetadata",
            "description" : "Additional metadata for the Connection"
          }
        },
        "title" : "SparkClassicConnection",
        "required" : [ "type" ],
        "additionalProperties" : false,
        "description" : "Connection information for a classic Spark session.\n\nThis is the `EngineConnection` for the Spark DataFrame engine: it creates and configures the SparkSession\nshared by all Actions using it, including spark options, Kryo classes, Hive support and user defined functions.  \nActions select an engine connection with their `engineConnectionId` attribute; if none is given the connection\nwith id `default-engine` is used (configurable through the SDLB parameter `defaultEngineConnectionId` ).  \nLeave `master` unset to attach to an existing Spark session provided by the environment (e.g. Databricks,\nEMR, spark-submit); set it (e.g. `local` , `yarn` ) to let SDLB create the session itself. Example:\n```\nconnections {\n  default-engine {\n    type = SparkClassicConnection\n    master = \"local[*]\"\n    enableHive = false\n    sparkOptions {\n      \"spark.sql.shuffle.partitions\" = \"8\"\n    }\n  }\n}\n```\n\n\nNOTE: with `master = \"yarn\"` the environment variable SPARK_HOME must be set."
      },
      "SparkConnectConnection" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "SparkConnectConnection"
          },
          "url" : {
            "type" : "string",
            "description" : "remote URL of the Spark Connect server, e.g. \"sc://localhost:15002\".  \nAdditional parameters like tokens can be appended according to the Spark Connect connection string spec,\ne.g. \"sc://host:port/;token=ABCDEFG;user_id=user\"."
          },
          "sparkOptions" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string",
              "description" : "The value can be provided in plaintext or with the id of a secret provider and the\nname of the secret in the format\n```\n###<PROVIDERID>#<SECRETNAME>###\n```\n ,\ne.g.  \n```\n###ENV#<ENV_VARIABLE_NAME>###\n```\n\n\nto get a secret from an environment variable."
            },
            "description" : "spark options to set on the session builder\n\nThe value can be provided in plaintext or with the id of a secret provider and the\nname of the secret in the format\n```\n###<PROVIDERID>#<SECRETNAME>###\n```\n ,\ne.g.  \n```\n###ENV#<ENV_VARIABLE_NAME>###\n```\n\n\nto get a secret from an environment variable."
          },
          "metadata" : {
            "$ref" : "#/definitions/Others/ConnectionMetadata",
            "description" : "Additional metadata for the Connection"
          }
        },
        "title" : "SparkConnectConnection",
        "required" : [ "type", "url" ],
        "additionalProperties" : false,
        "description" : "Connection information for a Spark Connect session.\n\nNote that a Spark Connect session is a thin client connected to a remote Spark Connect server.  \nThere is no Hadoop FileSystem API available to access remote data - all data access happens through\nspark.read/write on the server side."
      }
    },
    "SaveModeOptions" : {
      "SaveModeGenericOptions" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "SaveModeGenericOptions"
          },
          "saveMode" : {
            "type" : "string",
            "enum" : [ "Merge", "OverwriteOptimized", "OverwritePreserveDirectories", "Ignore", "ErrorIfExists", "Append", "Overwrite" ]
          }
        },
        "title" : "SaveModeGenericOptions",
        "required" : [ "type", "saveMode" ],
        "additionalProperties" : false,
        "description" : "This class can be used to override save mode without further special parameters."
      },
      "SaveModeMergeOptions" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "SaveModeMergeOptions"
          },
          "deleteCondition" : {
            "type" : "string",
            "description" : "A condition to control if matched records are deleted. If no condition is given, *no* records are delete."
          },
          "updateCondition" : {
            "type" : "string",
            "description" : "A condition to control if matched records are updated. If no condition is given all matched records are updated (default).  \nNote that delete is applied before update. Records selected for deletion are automatically excluded from the updates."
          },
          "updateColumns" : {
            "type" : "array",
            "items" : {
              "type" : "string"
            },
            "description" : "List of column names to update in update clause. If empty all columns (except primary keys) are updated (default)"
          },
          "updateExistingCondition" : {
            "type" : "string"
          },
          "insertCondition" : {
            "type" : "string",
            "description" : "A condition to control if unmatched records are inserted. If no condition is given all unmatched records are inserted (default)."
          },
          "insertColumnsToIgnore" : {
            "type" : "array",
            "items" : {
              "type" : "string"
            },
            "description" : "List of column names to ignore in insert clause. If empty all columns are inserted (default)."
          },
          "insertValuesOverride" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "Optional Map of column name and value expression to override value on insert. Value expressions have to be a sql expression string, e.g. true or 'abc'."
          },
          "additionalMergePredicate" : {
            "type" : "string",
            "description" : "To optimize performance for SDLSaveMode.Merge it might be interesting to limit the records read from the existing table data, e.g. merge operation might use only the last 7 days."
          }
        },
        "title" : "SaveModeMergeOptions",
        "required" : [ "type" ],
        "additionalProperties" : false,
        "description" : "Options to control detailed behaviour of SaveMode.Merge.  \nIn Spark expressions use table alias 'existing' to reference columns of the existing table data, and table alias 'new' to reference columns of new data set."
      }
    },
    "ParsableScriptDef" : {
      "CmdScript" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "CmdScript"
          },
          "name" : {
            "type" : "string",
            "description" : "name of the transformer"
          },
          "description" : {
            "type" : "string",
            "description" : "Optional description of the transformer"
          },
          "winCmd" : {
            "type" : "string",
            "description" : "Cmd to execute on Windows operating systems - note that it is executed with \"cmd /C\" prefixed"
          },
          "linuxCmd" : {
            "type" : "string",
            "description" : "Cmd to execute on Linux operating systems - note that it is executed directly and not through a\nshell, so an interpreter must be part of the command if needed, e.g. \"sh ./scripts/export.sh\"."
          }
        },
        "title" : "CmdScript",
        "required" : [ "type" ],
        "additionalProperties" : false,
        "description" : "Execute a command on the command line and get its std output\nCommand can be different for windows and linux operating systems, but it must be defined for at least one of them.\n\nIf return value is not zero an exception is thrown.\n\nNote about internal implementation: on execution value of parameter map entries where key starts with\n- 'param' will be added as parameter after the command, sorted by key.  \nThis allows to customize execution behaviour through Actions or DataObjects using CmdScript. Example:\n```\nactions = {\n  export-airports {\n    type = CustomScriptAction\n    inputIds = [int-airports]\n    outputIds = [ext-airports]\n    scripts = [{\n      type = CmdScript\n      linuxCmd = \"sh ./scripts/export_airports.sh\"\n      winCmd = \"wsl sh ./scripts/export_airports.sh\"\n    }]\n  }\n}\n```\n\n\nNOTE: Only the command for the current operating system needs to be defined, but the job will fail on startup\nif the command for the operating system it is running on is missing.\n\nSEE: `DockerRunScript` to run the command inside a docker container instead."
      },
      "DockerRunScript" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "DockerRunScript"
          },
          "name" : {
            "type" : "string",
            "description" : "name of the transformer"
          },
          "description" : {
            "type" : "string",
            "description" : "Optional description of the transformer"
          },
          "image" : {
            "type" : "string",
            "description" : "Docker image to run"
          },
          "winDockerCmd" : {
            "type" : "string",
            "description" : "Cmd to execute docker on windows operating systems. Default is 'docker'."
          },
          "linuxDockerCmd" : {
            "type" : "string",
            "description" : "Cmd to execute docker on linux operating systems. Default is 'docker'."
          },
          "localDataDirToMount" : {
            "type" : "string",
            "description" : "Optional directory that will be mounted as /mnt/data in the container. This is needed if your container wants to access files available in your local filesystem."
          }
        },
        "title" : "DockerRunScript",
        "required" : [ "type", "image" ],
        "additionalProperties" : false,
        "description" : "Run a docker image and get its std output.\n\nIf return value is not zero an exception is thrown.\n\nNote about internal implementation: on execution value of parameter map entries where key starts with\n- 'runParam' will be added as parameter after the docker run command, sorted by their key.  \n- 'dockerParam' will be added as parameter for the docker command, e.g. before the image name in the docker run command, sorted by their key.  \nThis allows to customize execution behaviour through Actions or DataObjects using CmdScript.\n\nA typical use case is launching an Airbyte connector image with AirbyteDataObject, but it can be used wherever a\nscript definition is expected, e.g. in the `scripts` attribute of a CustomScriptAction. Example:\n```\ndataObjects = {\n  ext-postgres {\n    type = AirbyteDataObject\n    streamName = \"airports\"\n    cmd = {\n      type = DockerRunScript\n      image = \"airbyte/source-postgres:1.0.17\"\n      localDataDirToMount = \"data\"\n    }\n    # the entries of config are connector specific, see the documentation of the connector used\n    config = { host = \"localhost\", port = 5432, database = \"airports\", username = \"sdlb\", password = \"###\" }\n  }\n}\n```\n\n\nNOTE: Docker must be installed and callable on the machine running the SDLB job (the driver). The container runs\nlocally, it is not distributed to Spark executors.\n\nSEE: `CmdScript` to execute a plain command line instead."
      }
    },
    "Others" : {
      "ActionMetadata" : {
        "type" : "object",
        "properties" : {
          "name" : {
            "type" : "string",
            "description" : "Readable name of the Action"
          },
          "description" : {
            "type" : "string",
            "description" : "Description of the content of the Action"
          },
          "feed" : {
            "type" : "string",
            "description" : "Name of the feed this Action belongs to"
          },
          "tags" : {
            "type" : "array",
            "items" : {
              "type" : "string"
            },
            "description" : "Optional custom tags for this object"
          }
        },
        "title" : "ActionMetadata",
        "additionalProperties" : false,
        "description" : "Additional metadata for an Action"
      },
      "ConnectionMetadata" : {
        "type" : "object",
        "properties" : {
          "name" : {
            "type" : "string",
            "description" : "Readable name of the Connection"
          },
          "description" : {
            "type" : "string",
            "description" : "Description of the content of the Connection"
          },
          "layer" : {
            "type" : "string",
            "description" : "Name of the layer this Connection belongs to"
          },
          "subjectArea" : {
            "type" : "string",
            "description" : "Name of the subject area this Connection belongs to"
          },
          "tags" : {
            "type" : "array",
            "items" : {
              "type" : "string"
            },
            "description" : "Optional custom tags for this object"
          }
        },
        "title" : "ConnectionMetadata",
        "additionalProperties" : false,
        "description" : "Additional metadata for a Connection"
      },
      "DataObjectMetadata" : {
        "type" : "object",
        "properties" : {
          "name" : {
            "type" : "string",
            "description" : "Readable name of the DataObject"
          },
          "description" : {
            "type" : "string",
            "description" : "Description of the content of the DataObject"
          },
          "layer" : {
            "type" : "string",
            "description" : "Name of the layer this DataObject belongs to"
          },
          "subjectArea" : {
            "type" : "string",
            "description" : "Name of the subject area this DataObject belongs to"
          },
          "tags" : {
            "type" : "array",
            "items" : {
              "type" : "string"
            },
            "description" : "Optional custom tags for this object"
          }
        },
        "title" : "DataObjectMetadata",
        "additionalProperties" : false,
        "description" : "Additional metadata for a DataObject"
      },
      "Table" : {
        "type" : "object",
        "properties" : {
          "db" : {
            "type" : "string",
            "description" : "database-schema to be used for this table.  \nIf there exists a connection for the DataObject and this field is not defined, it will be set to the connections database value .  \nCalled db for backwards-compatibility because for hive tables, db and schema mean the same thing."
          },
          "name" : {
            "type" : "string",
            "description" : "table name"
          },
          "query" : {
            "type" : "string",
            "description" : "optional select query"
          },
          "primaryKey" : {
            "type" : "array",
            "items" : {
              "type" : "string"
            },
            "description" : "optional sequence of primary key columns"
          },
          "createAndReplacePrimaryKey" : {
            "type" : "boolean",
            "description" : "Parameter to define if the primary key should be created and updated\naccording to the SDLB configuration (=TRUE), or if they are configured just\nfor information purposes (=FALSE). It defaults to false. For the creation / replacement to work,\nat least one primary Key column must be defined.  \nPrimary keys are created at deployment time by DataObjectSchemaExporter,\nsee[[io.smartdatalake.workflow.dataobject.generic.CanHandleConstraints]]for the DataObjects supporting it. Using it in other DataObjects will have no effect.  \nIt also makes the primary key columns not null when creating or evolving\nthe table schema, see`io.smartdatalake.workflow.dataobject.generic.CanHandleTableSchema`."
          },
          "primaryKeyConstraintName" : {
            "type" : "string",
            "description" : "This parameter is used in case that createAndReplacePrimaryKey is set to TRUE.  \nIn case a constraint name is not given, the default value sdlb_\"tableName\"_pk will be used\nwhen updating the primary key."
          },
          "foreignKeys" : {
            "type" : "array",
            "items" : {
              "type" : "object",
              "properties" : {
                "db" : {
                  "type" : "string",
                  "description" : "target database, if not defined it is assumed to be the same as the table owning the foreign key"
                },
                "table" : {
                  "type" : "string",
                  "description" : "referenced target table name"
                },
                "columns" : {
                  "type" : "object",
                  "additionalProperties" : {
                    "type" : "string"
                  },
                  "description" : "mapping of source column(s) to referenced target table column(s). The map is given\nas a list of objects with the following syntax: {\"local_column_name\" : \"external_column_name\"}"
                },
                "name" : {
                  "type" : "string",
                  "description" : "optional name for foreign key, e.g. to depict its role.  \nForeign keys in .conf files are to be defined like the following example\n(here two foreign key objects):\nforeignKeys = [\n{\ndb = \"OPTIONAL_DB_name\"\ntable = \"table_id\"\ncolumns = {\n\"local_column_name\": \"external_column_name\"\n}\nname = \"OPTIONAL_key_name\"\n},\n{\ntable = \"another_table_id\"\ncolumns = {\n\"another_local_column_name\": \"another_external_column_name\"\n}\nname = \"another_OPTIONAL_key_name\"\n}\n]"
                }
              },
              "title" : "ForeignKey",
              "required" : [ "table", "columns" ],
              "additionalProperties" : false,
              "description" : "Foreign key definition."
            },
            "description" : "optional sequence of foreign key definitions.  \nThis is used as metadata for a data catalog, and to create foreign key\nconstraints if`createAndReplaceForeignKeys` is set to true."
          },
          "createAndReplaceForeignKeys" : {
            "type" : "boolean",
            "description" : "Parameter to define if the foreign keys should be created and updated\naccording to the SDLB configuration (=TRUE), or if they are configured just\nfor information purposes (=FALSE). It defaults to false.  \nForeign keys are created at deployment time by DataObjectSchemaExporter,\nsee`io.smartdatalake.workflow.dataobject.generic.CanHandleForeignKeys`.  \nNote that the referenced table must exist and have a primary key on the\nreferenced columns.  \nEach foreign key in the .conf files is an object with the following properties:\n{db: string, table: string , name: string map: Map[String]}, whereas a Map[String] is simply\na further object of the type {<local_column_name>:string, <external_column_name>:string}. For example:\nforeignKeys = [\n{\ndb = \"OPTIONAL_DB_name\"\ntable = \"table_id\"\ncolumns = {\n\"local_column_name\": \"external_column_name\"\n}\nname = \"OPTIONAL_key_name\"\n}\n]"
          },
          "catalog" : {
            "type" : "string",
            "description" : "Optional catalog to be used for this table. If null default catalog is used.  \nIf there exists a connection with catalog value for the DataObject and this field is not defined, it will be set to the connections catalog value."
          }
        },
        "title" : "Table",
        "required" : [ "name" ],
        "additionalProperties" : false,
        "description" : "Table attributes"
      }
    },
    "GenericDfTransformer" : {
      "BlacklistTransformer" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "BlacklistTransformer"
          },
          "name" : {
            "type" : "string",
            "description" : "name of the transformer"
          },
          "description" : {
            "type" : "string",
            "description" : "Optional description of the transformer"
          },
          "columnBlacklist" : {
            "type" : "array",
            "items" : {
              "type" : "string"
            },
            "description" : "List of columns to exclude from DataFrame"
          }
        },
        "title" : "BlacklistTransformer",
        "required" : [ "type", "columnBlacklist" ],
        "additionalProperties" : false,
        "description" : "Apply a column blacklist to a DataFrame: every column listed in `columnBlacklist` is dropped, all\nremaining columns are passed through unchanged.  \nUse this if a source delivers many columns and only a few need to be removed, e.g. to strip technical\nor sensitive attributes before writing them to the target DataObject.  \nUse `WhitelistTransformer` instead if it is easier to enumerate the columns to keep.\n\nColumn names are matched case-insensitively, unless case sensitivity is enabled globally by setting `global.environment.caseSensitive = true` . Blacklisted columns that do not exist in the DataFrame do not\nfail the Action, they are logged as a warning and ignored.  \nNote that the original column order is only preserved in the default case-insensitive mode; with case\nsensitivity enabled the order of the remaining columns is undefined. Example:\n```\nactions = {\n  load-orders {\n    type = CopyAction\n    inputId = stg-orders\n    outputId = int-orders\n    transformers = [{\n      type = BlacklistTransformer\n      columnBlacklist = [dl_load_ts, source_system, customer_email]\n    }]\n  }\n}\n```\n"
      },
      "ColumnsTransformer" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "ColumnsTransformer"
          },
          "name" : {
            "type" : "string",
            "description" : "name of the transformer"
          },
          "description" : {
            "type" : "string",
            "description" : "Optional description of the transformer"
          },
          "additionalColumns" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "optional tuples of [column name, spark sql expression] to be added as additional columns to the dataframe. This allows you to include commonly used context information available at runtime.  \nThe spark sql expressions are evaluated against an instance of`DefaultExpressionData]]and added to the DataFrame as literal columns.[[DefaultExpressionData` contains informations from the context of the SDLB job, like runId or feed name."
          },
          "additionalDerivedColumns" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "optional tuples of [column name, spark sql expression] to be added as additional columns to the dataframe. This allows you to run custom sql code on the input DataFrame and save the result into a new column for which you define the name."
          },
          "renamedColumns" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "optional tuples of [old column name, new column name]. For each tuple, a column is renamed. A RuntimeError will occur if a column to be renamed does not exist after applying all previous operations."
          },
          "droppedColumns" : {
            "type" : "array",
            "items" : {
              "type" : "string"
            },
            "description" : "optional list of column names to be dropped. A RuntimeError will occur if a column does not exist after applying all previous operations."
          }
        },
        "title" : "ColumnsTransformer",
        "required" : [ "type" ],
        "additionalProperties" : false,
        "description" : "Add, Rename or Drop columns from the Input DataFrame. The order of execution of the operations is the same as the parameters of the transformer: (1. add context info, 2. add custom sql, 3. rename, 4. drop).  \nNote that you can mix and match these operations to your liking and reference the same column from a previous operation if needed. Example:\n```\nactions = {\n  multiply {\n    type = CopyAction\n    inputId = stg-ratings\n    outputId = int-ratings\n    transformers = [{\n      type = ColumnsTransformer\n      additionalColumns = {dl_run_id = \"runId\"}\n      additionalDerivedColumns = {rating_doubled = \"rating * 2\"}\n      renamedColumns = {rating_doubled = rating_doubled_renamed}\n      droppedColumns = [name]\n    }]\n  }\n}\n```\n"
      },
      "ConvertNullValuesTransformer" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "ConvertNullValuesTransformer"
          },
          "name" : {
            "type" : "string",
            "description" : "Name of the transformer"
          },
          "description" : {
            "type" : "string",
            "description" : "Optional description of the transformer"
          },
          "includeColumns" : {
            "type" : "array",
            "items" : {
              "type" : "string"
            },
            "description" : "Optional list of columns to include into the transformation"
          },
          "excludeColumns" : {
            "type" : "array",
            "items" : {
              "type" : "string"
            },
            "description" : "Optional list of columns to exclude from the transformation"
          },
          "valueForString" : {
            "type" : "string",
            "description" : "Value to add for string values, default value is \"na\""
          },
          "valueForNumber" : {
            "type" : "integer",
            "description" : "Value to add for number values, default value is -1"
          }
        },
        "title" : "ConvertNullValuesTransformer",
        "required" : [ "type" ],
        "additionalProperties" : false,
        "description" : "Replace null values in a DataFrame by a configurable placeholder value, so that downstream joins,\ncomparisons and primary keys do not have to deal with nulls.  \nString columns are filled with `valueForString` and numeric columns with `valueForNumber` ; in both cases\nthe placeholder is cast to the data type of the column. Columns of any other data type (date, timestamp,\nboolean, struct, array, ...) are left untouched.\n\nBy default all columns of the DataFrame are converted. Limit the scope with either `includeColumns` or `excludeColumns` - the two are mutually exclusive and configuring both fails the transformation.  \nAll column names listed must exist in the DataFrame, otherwise the transformation fails.  \nColumn names are matched case-insensitively, unless case sensitivity is enabled globally by setting `global.environment.caseSensitive = true` . Example:\n```\nactions = {\n  load-ratings {\n    type = CopyAction\n    inputId = stg-ratings\n    outputId = int-ratings\n    transformers = [{\n      type = ConvertNullValuesTransformer\n      excludeColumns = [comment]\n      valueForString = \"n/a\"\n      valueForNumber = 0\n    }]\n  }\n}\n```\n"
      },
      "DataValidationTransformer" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "DataValidationTransformer"
          },
          "name" : {
            "type" : "string",
            "description" : "name of the transformer"
          },
          "description" : {
            "type" : "string",
            "description" : "Optional description of the transformer"
          },
          "rules" : {
            "type" : "array",
            "items" : {
              "$ref" : "#/definitions/ValidationRule/RowLevelValidationRule"
            },
            "description" : "list of validation rules to apply to the DataFrame"
          },
          "errorsColumn" : {
            "type" : "string",
            "description" : "Optional column name for the list of error messages. Default is \"errors\"."
          },
          "subFeedTypeForValidation" : {
            "type" : "string",
            "description" : "For validating the rule expression, the runtime subFeedType is not yet known.  \nBy default SparkSubFeed langauge is used, but you can configure a different one if needed."
          }
        },
        "title" : "DataValidationTransformer",
        "required" : [ "type", "rules" ],
        "additionalProperties" : false,
        "description" : "Apply a list of validation rules to a DataFrame and collect the error messages of all violated rules in\na new array column, instead of failing the Action.  \nNo rows are removed: the errors column stays empty for records that satisfy all rules. This allows to\nkeep bad records for reporting, or to split them off with a subsequent `FilterTransformer` on the\nerrors column. If the job should rather fail on bad data, use `io.smartdatalake.workflow.dataobject.generic.Constraint` or an Expectation like `io.smartdatalake.workflow.dataobject.expectation.SQLExpectation` on the output DataObject.\n\nThe rule expressions are already parsed when the configuration is read, so syntax errors are detected\nbefore the pipeline is started. Example:\n```\nactions = {\n  validate-ratings {\n    type = CopyAction\n    inputId = stg-ratings\n    outputId = int-ratings\n    transformers = [{\n      type = DataValidationTransformer\n      errorsColumn = validation_errors\n      rules = [\n        { type = RowLevelValidationRule, condition = \"rating is not null\", errorMsg = \"rating should not be empty\" }\n        { type = RowLevelValidationRule, condition = \"rating between 1 and 5\" }\n      ]\n    }]\n  }\n}\n```\n"
      },
      "DebugTransformer" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "DebugTransformer"
          },
          "name" : {
            "type" : "string",
            "description" : "name of the transformer"
          },
          "description" : {
            "type" : "string",
            "description" : "Optional description of the transformer"
          },
          "printSchema" : {
            "type" : "boolean",
            "description" : "true if the schema of the DataFrame should be logged. Default is true."
          },
          "printSchemaOptions" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "SubType specific options to pass to the printSchema statement.  \nFor Spark this is level=(int). Default is level=Int.MaxValue."
          },
          "show" : {
            "type" : "boolean",
            "description" : "true if data should be logged using show method. Default is false."
          },
          "showOptions" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "SubType specific options to pass to the show statement.  \nFor Spark this can be numRows=(int), truncate=(int) and vertical=(true/false). Default is numRows=10, truncate=20, vertical=false."
          },
          "explain" : {
            "type" : "boolean",
            "description" : "true if plan should be logged using explain method. Default is false."
          },
          "explainOptions" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "SubType specific options to pass to explain statement.  \nFor Spark this can be mode=(simple/extended/codegen/cost/formatted), see`ExplainMode` for details. Default is mode=simple.  \nDefault is to use the default of the underlying SubFeed type, e.g. 'simple' for Spark."
          },
          "logLevel" : {
            "type" : "string",
            "description" : "log level to use for logging. Possible values are 'debug', 'info', 'warn', 'error'. Default is 'info'."
          },
          "titleMarker" : {
            "type" : "string",
            "description" : "a string to prefix before and after the DataFrame for finding the debug output easier. Default is '###'."
          }
        },
        "title" : "DebugTransformer",
        "required" : [ "type" ],
        "additionalProperties" : false,
        "description" : "Log schema, sample data and plan of DataFrame to transform.  \nThis transformer can be used to log debug output between different transformers.  \nIt's not intended for production use.  \nThe DataFrame is returned unchanged, so the transformer can be inserted at any position in the `transformers` list of an Action without influencing its result. Example:\n```\nactions = {\n  load-ratings {\n    type = CopyAction\n    inputId = stg-ratings\n    outputId = int-ratings\n    transformers = [{\n      type = DebugTransformer\n      show = true\n      showOptions = { numRows = \"20\", vertical = \"true\" }\n      explain = true\n      explainOptions = { mode = \"extended\" }\n      logLevel = debug\n    }]\n  }\n}\n```\n"
      },
      "DecryptColumnsTransformer" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "DecryptColumnsTransformer"
          },
          "name" : {
            "type" : "string",
            "description" : "name of the transformer"
          },
          "description" : {
            "type" : "string",
            "description" : "Optional description of the transformer"
          },
          "decryptColumns" : {
            "type" : "array",
            "items" : {
              "type" : "string"
            },
            "description" : "List of columns [columnA, columnB] to be encrypted"
          },
          "key" : {
            "type" : "string",
            "description" : "contains the id of the provider and the name of the secret with format ###<PROVIDERID>#<SECRETNAME>###,\ne.g. ###ENV#<ENV_VARIABLE_NAME>### to get a secret from an environment variable OR ###CLEAR#mYsEcReTkeY###\n\nThe value can be provided in plaintext or with the id of a secret provider and the\nname of the secret in the format\n```\n###<PROVIDERID>#<SECRETNAME>###\n```\n ,\ne.g.  \n```\n###ENV#<ENV_VARIABLE_NAME>###\n```\n\n\nto get a secret from an environment variable."
          },
          "algorithm" : {
            "type" : "string",
            "description" : "Specify: \"GCM\" (AES/GCM/NoPadding), \"ECB\" (AES/ECB/PKCS5Padding),\nalternatively a class name extending trait EncryptDecrypt can be provided. DEFAULT: GCM"
          }
        },
        "title" : "DecryptColumnsTransformer",
        "required" : [ "type", "decryptColumns", "key" ],
        "additionalProperties" : false,
        "description" : "Decryption of specified columns using AES/GCM algorithm.\n\nUse this transformer to read back data that was written with `EncryptColumnsTransformer`. The listed columns are\ndecrypted in place, keeping name and position; all other columns are passed through unchanged. The columns must be of\ntype string and contain the cipher text produced by the same algorithm and the same key, otherwise decryption fails\nor returns garbage.\n\nNote that the key should not be written into the configuration in clear text, but referenced as a secret, e.g. `###ENV#CRYPT_KEY###` . See `EncryptColumnsTransformer` for the write side. Example:\n```\nactions = {\n  dec-customers {\n    type = CopyAction\n    inputId = enc-customers\n    outputId = int-customers\n    transformers = [{\n      type = DecryptColumnsTransformer\n      decryptColumns = [\"email\", \"phone\"]\n      key = \"###ENV#CRYPT_KEY###\"\n      algorithm = \"GCM\"\n    }]\n  }\n}\n```\n"
      },
      "DeduplicateTransformer" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "DeduplicateTransformer"
          },
          "name" : {
            "type" : "string",
            "description" : "Name of the transformer"
          },
          "description" : {
            "type" : "string",
            "description" : "Optional description of the transformer"
          },
          "rankingExpression" : {
            "type" : "string",
            "description" : "Optional ranking expression to choose the record to keep if there are duplicates.  \nIf not defined this might be not deterministic. Spark will use df.dropDuplicates(cols) function."
          },
          "primaryKeyColumns" : {
            "type" : "array",
            "items" : {
              "type" : "string"
            },
            "description" : "Optional list of primary key columns.  \nIf left empty the primary key of the Actions output DataObject is used."
          }
        },
        "title" : "DeduplicateTransformer",
        "required" : [ "type" ],
        "additionalProperties" : false,
        "description" : "Returns dataframe with only unique columns based on a primary key.\n\nIf primaryKeyColumns is left empty, the primaryKey of the Actions output DataObject must be defined.\n\nWith a rankingExpression the record with the largest value of that expression is kept per primary key\n(the ranking is applied in descending order), which is the usual way to keep the most recent version of\na record. Without it an arbitrary record per primary key is kept. Example:\n```\nactions = {\n  dedup-ratings {\n    type = CopyAction\n    inputId = stg-ratings\n    outputId = int-ratings\n    transformers = [{\n      type = DeduplicateTransformer\n      primaryKeyColumns = [id]\n      rankingExpression = \"coalesce(updated_at, created_at)\"\n    }]\n  }\n}\n```\n"
      },
      "EncryptColumnsTransformer" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "EncryptColumnsTransformer"
          },
          "name" : {
            "type" : "string",
            "description" : "name of the transformer"
          },
          "description" : {
            "type" : "string",
            "description" : "Optional description of the transformer"
          },
          "encryptColumns" : {
            "type" : "array",
            "items" : {
              "type" : "string"
            },
            "description" : "List of columns [columnA, columnB] to be encrypted"
          },
          "key" : {
            "type" : "string",
            "description" : "contains the id of the provider and the name of the secret with format ###<PROVIDERID>#<SECRETNAME>###,\ne.g. ###ENV#<ENV_VARIABLE_NAME>### to get a secret from an environment variable OR ###CLEAR#mYsEcReTkeY###\n\nThe value can be provided in plaintext or with the id of a secret provider and the\nname of the secret in the format\n```\n###<PROVIDERID>#<SECRETNAME>###\n```\n ,\ne.g.  \n```\n###ENV#<ENV_VARIABLE_NAME>###\n```\n\n\nto get a secret from an environment variable."
          },
          "algorithm" : {
            "type" : "string",
            "description" : "Specify: \"GCM\" (AES/GCM/NoPadding), \"ECB\" (AES/ECB/PKCS5Padding),\nalternatively a class name extending trait EncryptDecrypt can be provided. DEFAULT: GCM"
          }
        },
        "title" : "EncryptColumnsTransformer",
        "required" : [ "type", "encryptColumns", "key" ],
        "additionalProperties" : false,
        "description" : "Encryption of specified columns using AES/GCM algorithm.\n\nUse this transformer to pseudonymize sensitive attributes before they are persisted. The listed columns are replaced\nby their cipher text at the same position and get data type String, all other columns are passed through unchanged.  \nThe original data type is remembered in the column metadata, so that `DecryptColumnsTransformer` can restore it on\nread. Note that this only works if the output format preserves column metadata (Parquet, Hive, Delta, Iceberg); with\nformats like CSV the original data type is lost and the decrypted columns stay String.  \nChoose \"GCM\" (randomized, recommended) unless you need deterministic cipher text for joins or deduplication, in which\ncase use \"ECB\".\n\nNote that the key should not be written into the configuration in clear text, but referenced as a secret, e.g. `###ENV#CRYPT_KEY###` . The same key and algorithm are needed to read the data back, see `DecryptColumnsTransformer`. Example:\n```\nactions = {\n  enc-customers {\n    type = CopyAction\n    inputId = stg-customers\n    outputId = enc-customers\n    transformers = [{\n      type = EncryptColumnsTransformer\n      encryptColumns = [\"email\", \"phone\"]\n      key = \"###ENV#CRYPT_KEY###\"\n      algorithm = \"GCM\"\n    }]\n  }\n}\n```\n"
      },
      "FilterTransformer" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "FilterTransformer"
          },
          "name" : {
            "type" : "string",
            "description" : "name of the transformer"
          },
          "description" : {
            "type" : "string",
            "description" : "Optional description of the transformer"
          },
          "filterClause" : {
            "type" : "string",
            "description" : "Spark SQL expression to filter the DataFrame"
          },
          "subFeedTypeForValidation" : {
            "type" : "string",
            "description" : "When parsing the configuration the runtime subFeedType for validating the filter expression is not yet known.  \nBy default SparkSubFeed langauge is used, but you can configure a different one if needed."
          }
        },
        "title" : "FilterTransformer",
        "required" : [ "type", "filterClause" ],
        "additionalProperties" : false,
        "description" : "Apply a filter condition to a DataFrame, keeping only the rows for which `filterClause` evaluates to true.  \nThis is the simplest way to reduce data inside an Action, e.g. to drop technically invalid or irrelevant records\nbefore writing them to the output DataObject.\n\nThe filter expression is parsed already when the configuration is read, so a syntax error is reported as\nConfigurationException at startup and not only when the Action is executed. Example:\n```\nactions = {\n  copy-airports {\n    type = CopyAction\n    inputId = stg-airports\n    outputId = int-airports\n    transformers = [{\n      type = FilterTransformer\n      filterClause = \"type in ('large_airport','medium_airport') and ident is not null\"\n    }]\n  }\n}\n```\n\n\nNOTE: Filtering rows does not remove columns. Use `WhitelistTransformer` or `BlacklistTransformer` to\nselect columns, and `ColumnsTransformer` to add or rename them."
      },
      "PythonCodeDfTransformer" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "PythonCodeDfTransformer"
          },
          "name" : {
            "type" : "string",
            "description" : "name of the transformer"
          },
          "description" : {
            "type" : "string",
            "description" : "Optional description of the transformer"
          },
          "code" : {
            "type" : "string",
            "description" : "Optional python code to user for python transformation. The python code can use variables inputDf, dataObjectId and options. The transformed DataFrame has to be set with setOutputDf."
          },
          "file" : {
            "type" : "string",
            "description" : "Optional file with python code to use for python transformation. The python code can use variables inputDf, dataObjectId and options. The transformed DataFrame has to be set with setOutputDf."
          },
          "options" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "Options to pass to the transformation"
          },
          "runtimeOptions" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "optional tuples of [key, spark sql expression] to be added as additional options when executing transformation.  \nThe spark sql expressions are evaluated against an instance of`DefaultExpressionData`."
          }
        },
        "title" : "PythonCodeDfTransformer",
        "required" : [ "type" ],
        "additionalProperties" : false,
        "description" : "Configuration of a custom Spark-DataFrame transformation between one input and one output (1:1) as Python/PySpark code.  \nNote that this transformer needs a Python and PySpark environment installed.  \nPySpark session is initialize and available under variables `sc` , `session` , `sqlContext` .  \nOther variables available are\n- `inputDf` : Input DataFrame\n- `options` : Transformation options as Map[String,String]\n- `dataObjectId` : Id of input dataObject as String\nOutput DataFrame must be set with `setOutputDf(df)` . Alternatively the Python code can define a function named `transform` . It is then called dynamically by looking\nfor its parameter values in the input DataFrame and options, and its return value is used as output DataFrame.  \nA parameter named `options` gets all options as dict, a parameter with the name of an option gets its value\nconverted according to its type annotation or the type of its default value. As there is exactly one input\nDataFrame, a parameter named `df` / `inputDf` , a parameter matching the DataObjectId, or otherwise the first\nparameter without default value gets this DataFrame. Example:\n```\ncode = \"\"\"\n  from pyspark.sql.functions import col\n  def transform(inputDf, country: str = 'CH'):\n      return inputDf.where(col('country') == country)\n\"\"\"\n```\n\n\nEither `code` (inline PySpark code, best for a few lines) or `file` (a path to a `.py` file, best for longer logic\nand unit-testable code) must be defined. Prefer `ScalaClassSparkDfTransformer` or an SQL transformer if you do not\nspecifically need Python libraries, as starting the Python gateway adds overhead per Action. Example:\n```\nactions = {\n  select-airports {\n    type = CopyAction\n    inputId = stg-airports\n    outputId = int-airports\n    transformers = [{\n      type = PythonCodeDfTransformer\n      code = \"\"\"\n        from pyspark.sql.functions import col\n        setOutputDf(inputDf.where(col('country') == options['country']))\n      \"\"\"\n      options = { country = \"CH\" }\n    }]\n  }\n}\n```\n"
      },
      "SQLDfTransformer" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "SQLDfTransformer"
          },
          "name" : {
            "type" : "string",
            "description" : "name of the transformer"
          },
          "description" : {
            "type" : "string",
            "description" : "Optional description of the transformer"
          },
          "file" : {
            "type" : "string",
            "description" : "File where SQL code for transformation is loaded from.  \nUse tokens`%{<key>}`to replace with runtimeOptions in SQL code.  \nExample:`select * from test where run = %{runId}`The special token`%{inputViewName}`or`%{inputViewName_<input_name>}` can be used to insert the temporary input view names.  \nThe input name is either the name of the DataObject, or the name of the previous transformation if this is not the first transformation of the chain.  \nMake sure to set the name of the previous transformation in that case."
          },
          "code" : {
            "type" : "string",
            "description" : "SQL code for transformation.  \nUse tokens`%{<key>}`to replace with runtimeOptions in SQL code.  \nExample:`select * from test where run = %{runId}`The special token`%{inputViewName}`or`%{inputViewName_<input_name>}` can be used to insert the temporary input view names.  \nThe input name is either the name of the DataObject, or the name of the previous transformation if this is not the first transformation of the chain.  \nMake sure to set the name of the previous transformation in that case."
          },
          "options" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "Options to pass to the transformation"
          },
          "runtimeOptions" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "optional tuples of [key, spark sql expression] to be added as additional options when executing transformation.  \nThe spark sql expressions are evaluated against an instance of`DefaultExpressionData`."
          }
        },
        "title" : "SQLDfTransformer",
        "required" : [ "type" ],
        "additionalProperties" : false,
        "description" : "Configuration of a custom GenericDataFrame transformation between one input and one output (1:1) as SQL code or file.  \nThe input data is available as temporary view in SQL. The inputs name is either the name of the DataObject,\nor the name of the previous transformation, if this is not the first transformation of the chain.  \nAlso note that to create the name of temporary view, special characters are replaced by underscores and a postfix `_sdltemp` is added.  \nIt is therefore recommended to use special token `%{inputViewName}` or `%{inputViewName_<input name>}` that will be\nreplaced with the name of the temporary view at runtime.\n\nUse SQLDfTransformer for 1:1 transformations that are easiest expressed in SQL. Exactly one of `code` or `file` must be defined. For transformations with multiple inputs or outputs use `SQLDfsTransformer` in a `io.smartdatalake.workflow.action.CustomDataFrameAction` instead. Example:\n```\nactions = {\n  select-airports {\n    type = CopyAction\n    inputId = stg-airports\n    outputId = int-airports\n    transformers = [{\n      type = SQLDfTransformer\n      code = \"select ident, name, latitude_deg, longitude_deg from %{inputViewName} where type = '%{airportType}'\"\n      options = { airportType = \"large_airport\" }\n    }]\n  }\n}\n```\n"
      },
      "ScalaClassGenericDfTransformer" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "ScalaClassGenericDfTransformer"
          },
          "name" : {
            "type" : "string",
            "description" : "name of the transformer"
          },
          "description" : {
            "type" : "string",
            "description" : "Optional description of the transformer"
          },
          "className" : {
            "type" : "string",
            "description" : "class name implementing trait`CustomGenericDfTransformer`"
          },
          "options" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "Options to pass to the transformation"
          },
          "runtimeOptions" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "optional tuples of [key, spark sql expression] to be added as additional options when executing transformation.  \nThe spark sql expressions are evaluated against an instance of`DefaultExpressionData`."
          }
        },
        "title" : "ScalaClassGenericDfTransformer",
        "required" : [ "type", "className" ],
        "additionalProperties" : false,
        "description" : "Configuration of a custom Spark-DataFrame transformation between one input and one output (1:1) as Java/Scala Class.  \nDefine a transform function which receives a DataObjectId, a DataFrame and a map of options and has to return a\nDataFrame. The Java/Scala class has to implement interface `CustomGenericDfTransformer`.\n\nThe class is instantiated by reflection through its no-argument constructor, so it must be on the classpath of the\nSDLB job. Because it works on `GenericDataFrame` and receives the matching `io.smartdatalake.workflow.dataframe.DataFrameFunctions`, the transformation stays engine independent.  \nUse `options` / `runtimeOptions` to parameterize the code instead of hard-coding values.\n\nInstead of overwriting the standard transform function, the class can also implement any transform method using\nparameters of type DataFrameFunctions, Map[String,String], GenericDataFrame and any primitive data type. It is then\ncalled dynamically by looking for the parameter values in the Options, see `CustomGenericDfTransformer`.  \nThe id of the input DataObject is passed as option `dataObjectId` . Example:\n```\nactions = {\n  copy-airports {\n    type = CopyAction\n    inputId = stg-airports\n    outputId = int-airports\n    transformers = [{\n      type = ScalaClassGenericDfTransformer\n      className = com.sample.MyAirportsTransformer\n      options = { maxLatitude = \"60\" }\n      runtimeOptions = { runId = \"runId\" }\n    }]\n  }\n}\n```\n\n\nSEE: `ScalaClassGenericDfsTransformer` for the n:m variant, and `SQLDfTransformer` if the transformation can\nbe expressed in SQL."
      },
      "ScalaClassSnowparkDfTransformer" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "ScalaClassSnowparkDfTransformer"
          },
          "name" : {
            "type" : "string",
            "description" : "name of the transformer"
          },
          "description" : {
            "type" : "string",
            "description" : "Optional description of the transformer"
          },
          "className" : {
            "type" : "string",
            "description" : "class name implementing trait`CustomSnowparkDfTransformer`"
          },
          "options" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "Options to pass to the transformation"
          },
          "runtimeOptions" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "optional tuples of [key, spark sql expression] to be added as additional options when executing transformation.  \nThe spark sql expressions are evaluated against an instance of`DefaultExpressionData`."
          }
        },
        "title" : "ScalaClassSnowparkDfTransformer",
        "required" : [ "type", "className" ],
        "additionalProperties" : false,
        "description" : "Configuration of a custom Snowpark-DataFrame transformation between one input and one output (1:1) as Java/Scala Class.  \nDefine a transform function which receives a DataObjectId, a DataFrame and a map of options and has to return a\nDataFrame. The Java/Scala class has to implement interface `CustomSnowparkDfTransformer`.\n\nThe transformation is pushed down to Snowflake and executed by Snowpark, so no data is transferred to the Spark\ndriver or executors. It can be combined in the same `transformers` list with generic transformers like\nSQLDfTransformer or ColumnsTransformer, as long as input and output DataObjects are Snowflake tables.\n\nInstead of overwriting the standard transform function, the class can also implement any transform method using\nparameters of type Session, Map[String,String], DataFrame and any primitive data type. It is then called\ndynamically by looking for the parameter values in the Options, see `CustomSnowparkDfTransformer`.  \nThe id of the input DataObject is passed as option `dataObjectId` . Example:\n```\nactions = {\n  copy-snowpark {\n    type = CopyAction\n    inputId = sf-airports-stg\n    outputId = sf-airports\n    transformers = [{\n      type = ScalaClassSnowparkDfTransformer\n      className = com.company.transformer.CleanAirportsSnowparkTransformer\n      options = { minRating = \"3\" }\n      runtimeOptions = { appName = \"application\" }\n    }]\n  }\n}\n```\n\n\nNOTE: Input and output DataObject must be of type SnowflakeTableDataObject, as the Snowpark session is taken from\nthe Action's first input. The class given in `className` must be on the classpath of the SDLB job."
      },
      "ScalaClassSparkDfTransformer" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "ScalaClassSparkDfTransformer"
          },
          "name" : {
            "type" : "string",
            "description" : "name of the transformer"
          },
          "description" : {
            "type" : "string",
            "description" : "Optional description of the transformer"
          },
          "className" : {
            "type" : "string",
            "description" : "class name implementing trait`CustomDfTransformer`"
          },
          "options" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "Options to pass to the transformation"
          },
          "runtimeOptions" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "optional tuples of [key, spark SQL expression] to be added as additional options when executing\ntransformation. The spark SQL expressions are evaluated against an instance of`DefaultExpressionData`."
          }
        },
        "title" : "ScalaClassSparkDfTransformer",
        "required" : [ "type", "className" ],
        "additionalProperties" : false,
        "description" : "Configuration of a custom Spark-DataFrame transformation between one input and one output (1:1)\nas Java/Scala Class. The Java/Scala class has to implement interface `CustomDfTransformer`.\n\nThere are two methods of defining the transformation:\n\n1) Overwrite the standard transform function of CustomDfTransformer, which receives a DataObjectId, a DataFrame\nand a map of options and has to return a DataFrame. The exact signature is `transform(session: SparkSession, options: Map[String,String], df: DataFrame, dataObjectId: String): DataFrame` .\n\n2) Implement any transform method using parameters of type SparkSession, Map[String,String], DataFrame,\nDataset[<Product>] and any primitive data type (String, Boolean, Int, ...). Primitive data types might also use\ndefault values or be enclosed in an Option[...] to mark it as non required. The transform method is then called\ndynamically by looking for the parameter values in the options. As there is exactly one input DataFrame, a\nDataFrame or Dataset parameter gets this DataFrame independent of the parameters name.\n\nNote that the following options are passed by default to the transformation:\n- isExec: defined as `context.isExecPhase` - dataObjectId: id of the DataObject of the input SubFeed\n\nPick this transformer when the transformation logic is too complex for SQL and should live in compiled, unit-testable\ncode. The class named by `className` must be on the classpath of the SDLB job and needs a constructor without\narguments. For transformations with multiple inputs or outputs use `ScalaClassSparkDfsTransformer` instead.  \nFor strongly typed code use a dynamic transform method with `Dataset[<Product>]` parameters and return type,\ne.g. `transform(ds: Dataset[Rating], factor: Int = 2): Dataset[Rating]` . Example:\n```\nactions = {\n  compute-departures {\n    type = CopyAction\n    inputId = stg-departures\n    outputId = int-departures\n    transformers = [{\n      type = ScalaClassSparkDfTransformer\n      className = com.sample.transformer.ComputeDistanceTransformer\n      options = { unit = \"km\" }\n      runtimeOptions = { runId = \"runId\" }\n    }]\n  }\n}\n```\n"
      },
      "ScalaCodeSparkDfTransformer" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "ScalaCodeSparkDfTransformer"
          },
          "name" : {
            "type" : "string",
            "description" : "name of the transformer"
          },
          "description" : {
            "type" : "string",
            "description" : "Optional description of the transformer"
          },
          "code" : {
            "type" : "string",
            "description" : "Scala code for transformation. The Scala code needs to be a function of type`fnTransformType`."
          },
          "file" : {
            "type" : "string",
            "description" : "File where Scala code for transformation is loaded from. The Scala code in the file needs to be\na function of type`fnTransformType`."
          },
          "options" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "Options to pass to the transformation"
          },
          "runtimeOptions" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "optional tuples of [key, spark SQL expression] to be added as additional options when executing\ntransformation. The spark SQL expressions are evaluated against an instance of`DefaultExpressionData`."
          }
        },
        "title" : "ScalaCodeSparkDfTransformer",
        "required" : [ "type" ],
        "additionalProperties" : false,
        "description" : "Configuration of a custom Spark-DataFrame transformation between one input and one output (1:1)\nas Scala code which is compiled at runtime. Define a transform function which receives a\nDataObjectId, a DataFrame and a map of options and has to return a DataFrame. The Scala code has\nto implement a function of type `fnTransformType`.\n\nUse this transformer for ad-hoc DataFrame logic that should be kept next to the pipeline definition\ninstead of in a separate jar. Exactly one of `code` or `file` must be defined. The code is compiled\nwith the Scala compiler on job startup (or lazily, see `Environment.compileScalaCodeLazy` ), so syntax\nerrors surface in the prepare phase and not only when data is processed. Alternatively the code can create an implementation of `CustomDfTransformer` defining any transform method.  \nThe transform method is then called dynamically by looking for the parameter values in the Options, see `CustomDfTransformer`. Example:\n```\ncode = \"\"\"\n  import io.smartdatalake.workflow.action.spark.customlogic.CustomDfTransformer\n  import org.apache.spark.sql.DataFrame\n  new CustomDfTransformer {\n    def transform(df: DataFrame, increment: Int = 1): DataFrame = df.withColumn(\"rating\", df(\"rating\") + increment)\n  }\n\"\"\"\n```\n Example:\n```\nactions = {\n  increment-rating {\n    type = CopyAction\n    inputId = src1DO\n    outputId = tgt1DO\n    transformers = [{\n      type = ScalaCodeSparkDfTransformer\n      code = \"\"\"\n        import org.apache.spark.sql.{DataFrame, SparkSession}\n        (session: SparkSession, options: Map[String,String], df: DataFrame, dataObjectId: String) => {\n          df.withColumn(\"rating\", df(\"rating\") + 1)\n        }\n      \"\"\"\n    }]\n  }\n}\n```\n\n\nNOTE: Runtime compilation needs the Scala compiler on the classpath; for production pipelines a\ncompiled transformer ( `ScalaClassSparkDfTransformer` ) is usually preferable."
      },
      "ScalaNotebookSparkDfTransformer" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "ScalaNotebookSparkDfTransformer"
          },
          "name" : {
            "type" : "string",
            "description" : "name of the transformer"
          },
          "description" : {
            "type" : "string",
            "description" : "Optional description of the transformer"
          },
          "url" : {
            "type" : "string",
            "description" : "Url to download notebook in IPYNB-format, which defines transformation."
          },
          "functionName" : {
            "type" : "string",
            "description" : "The notebook needs to contain a Scala-function with this name and type`fnTransformType`."
          },
          "authMode" : {
            "oneOf" : [ {
              "$ref" : "#/definitions/AuthMode/GCPCredentialsKeyAuth"
            }, {
              "$ref" : "#/definitions/AuthMode/PublicKeyAuthMode"
            }, {
              "$ref" : "#/definitions/AuthMode/SASLSCRAMAuthMode"
            }, {
              "$ref" : "#/definitions/AuthMode/SSLCertsAuthMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/AWSUserPwdAuthMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/AuthHeaderMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/AzureADClientGrantAuthMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/BasicAuthMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/CustomHttpAuthMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/OAuthMode"
            }, {
              "$ref" : "#/definitions/HttpAuthMode/TokenAuthMode"
            } ],
            "description" : "optional authentication information for webservice, e.g. BasicAuthMode for user/pw\nauthentication"
          },
          "options" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "Options to pass to the transformation"
          },
          "runtimeOptions" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "optional tuples of [key, spark SQL expression] to be added as additional options when executing\ntransformation. The spark SQL expressions are evaluated against an instance of`DefaultExpressionData`."
          }
        },
        "title" : "ScalaNotebookSparkDfTransformer",
        "required" : [ "type", "url", "functionName" ],
        "additionalProperties" : false,
        "description" : "Configuration of a custom Spark-DataFrame transformation between one input and one output (1:1)\nas Scala code which is compiled at runtime. The code is loaded from a Notebook. It should define\na transform function with a configurable name, which receives a DataObjectId, a DataFrame and a\nmap of options and has to return a DataFrame, see also ( `fnTransformType` ). Notebook-cells\nstarting with \"//!IGNORE\" will be ignored.\n\nUse this transformer to move a transformation prototyped interactively in a notebook into a pipeline\nwithout copying the code: SDLB downloads the notebook from `url` , concatenates all Scala code cells\nand compiles the function named `functionName` . Cells containing only exploratory code (test data, `show()` calls) should be marked with a leading \"//!IGNORE\" comment so they are skipped.\n\nThe function named `functionName` can also define any other parameters of type SparkSession, Map[String,String],\nDataFrame, Dataset[<Product>] and primitive data types. It is then called dynamically by looking for the parameter\nvalues in the Options, see `CustomDfTransformer`. As there is exactly one input DataFrame, a DataFrame or\nDataset parameter gets this DataFrame independent of the parameters name. Example:\n```\nactions = {\n  increment-rating {\n    type = CopyAction\n    inputId = src1DO\n    outputId = tgt1DO\n    transformers = [{\n      type = ScalaNotebookSparkDfTransformer\n      url = \"http://my-notebook-server:8192/notebook/Test.ipynb?download=true\"\n      functionName = testTransform\n      authMode = { type = BasicAuthMode, user = \"notebook-user\", password = \"###ENV#NOTEBOOK_PWD###\" }\n    }]\n  }\n}\n```\n\n\nNOTE: The notebook is downloaded on every job start, so the pipeline result depends on the current\nnotebook content. Prefer `ScalaCodeSparkDfTransformer` or `ScalaClassSparkDfTransformer` for reproducible production pipelines."
      },
      "SparkFlattenDfTransformer" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "SparkFlattenDfTransformer"
          },
          "name" : {
            "type" : "string",
            "description" : "name of the transformer"
          },
          "description" : {
            "type" : "string",
            "description" : "Optional description of the transformer"
          },
          "enableExplode" : {
            "type" : "boolean",
            "description" : "Enables exploding Arrays in the Dataframe, therefore potentially increasing the number of rows. Default is set to \"false\""
          }
        },
        "title" : "SparkFlattenDfTransformer",
        "required" : [ "type" ],
        "additionalProperties" : false,
        "description" : "Returns a flattened Dataframe from another DataFrame\nwith a nested schema. Schemas in the form:\n\n|---parent\n\n|------|child1\n\n|------|child2\n\nare transformed to the following flat columns:\n\n|---parent_child1\n\n|---parent_child2\n\nNesting is resolved recursively, so deeply nested structs end up as a single flat level of columns\nnamed by their full path joined with \"_\". This is typically used right after reading semi-structured\nsources (JSON, XML, webservice payloads) to obtain a table-like schema that can be written to a\nrelational or file DataObject. With `enableExplode = true` array columns are exploded as well, which\nmultiplies rows instead of columns; the transformer needs no column list and adapts to schema changes. Example:\n```\nactions = {\n  flatten-departures {\n    type = CopyAction\n    inputId = stg-departures\n    outputId = int-departures\n    transformers = [{\n      type = SparkFlattenDfTransformer\n      enableExplode = true\n    }]\n  }\n}\n```\n\n\nNOTE: Exploding arrays changes the cardinality of the DataFrame - primary keys of the input are no\nlonger unique in the output."
      },
      "SparkRepartitionTransformer" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "SparkRepartitionTransformer"
          },
          "name" : {
            "type" : "string",
            "description" : "name of the transformer"
          },
          "description" : {
            "type" : "string",
            "description" : "Optional description of the transformer"
          },
          "numberOfTasksPerPartition" : {
            "type" : "integer",
            "description" : "Number of Spark tasks to create per partition value by repartitioning the DataFrame."
          },
          "keyCols" : {
            "type" : "array",
            "items" : {
              "type" : "string"
            },
            "description" : "Optional key columns to distribute records over Spark tasks inside a partition value."
          }
        },
        "title" : "SparkRepartitionTransformer",
        "required" : [ "type", "numberOfTasksPerPartition" ],
        "additionalProperties" : false,
        "description" : "Repartition a Spark DataFrame in the middle of a transformation chain, controlling how many Spark\ntasks - and therefore how many output files per partition value - the following steps work with.  \nUse it to avoid the small-files problem when writing many small partitions, or to increase\nparallelism before an expensive transformation. Repartitioning triggers a shuffle, so apply it\ndeliberately.\n\nFor a detailled description about repartitioning DataFrames see also `SparkRepartitionDef`. Example:\n```\nactions = {\n  copy-departures {\n    type = CopyAction\n    inputId = stg-departures\n    outputId = int-departures\n    transformers = [{\n      type = SparkRepartitionTransformer\n      numberOfTasksPerPartition = 10\n      keyCols = [icao24]\n    }]\n  }\n}\n```\n\n\nNOTE: If the output DataObject already defines `sparkRepartition` , prefer configuring it there;\nthis transformer is for repartitioning between transformation steps."
      },
      "StandardizeColNamesTransformer" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "StandardizeColNamesTransformer"
          },
          "name" : {
            "type" : "string",
            "description" : "name of the transformer"
          },
          "description" : {
            "type" : "string",
            "description" : "Optional description of the transformer"
          },
          "camelCaseToLower" : {
            "type" : "boolean",
            "description" : "If selected, converts Camel case names to lower case  with underscores, i.e. TestString → test_string, testABCtest → test_ABCtest\nOtherwise converts just to lower case. Default=true"
          },
          "normalizeToAscii" : {
            "type" : "boolean",
            "description" : "If selected, converts UTF-8 special characters (e.g. diacritics, umlauts) to ASCII chars (best effort), i.e. Öffi_émily → Oeffi_emily. Default: true"
          },
          "removeNonStandardSQLNameChars" : {
            "type" : "boolean",
            "description" : "Remove all chars from a string which don't belong to lowercase SQL standard naming characters, i.e abc$!-& → abc. Default=true"
          },
          "replaceNonStandardSQLNameCharsWithUnderscores" : {
            "type" : "boolean",
            "description" : "If selected, replaces all chars from a string which don't belong to lowercase SQL standard naming characters with underscores (_), i.e. \"value of property!in$$\" → \"value_of_property_in_\". Note: removeNonStandardSQLNameChars needs to be disabled. Default=false"
          }
        },
        "title" : "StandardizeColNamesTransformer",
        "required" : [ "type" ],
        "additionalProperties" : false,
        "description" : "Standardizes column names to be used without quoting by using camelCase to lower_case_with_underscore rule (default), and further cleanup rules for special characters (default).  \nParameters below can be used to disable specific rules if needed.\n\nAdd it as first transformer when reading from sources with inconsistent or unsafe column names (Excel sheets, CSV\nexports, JSON APIs), so that all downstream SQL can reference columns without quoting.  \nWith the default settings `testColumn` becomes `test_column` and `c?olumnN[ä]me` becomes `column_naeme` . Example:\n```\nactions = {\n  copy-airports {\n    type = CopyAction\n    inputId = stg-airports\n    outputId = int-airports\n    transformers = [{\n      type = StandardizeColNamesTransformer\n      camelCaseToLower = true\n      normalizeToAscii = true\n    }]\n  }\n}\n```\n\n\nNOTE: `removeNonStandardSQLNameChars` and `replaceNonStandardSQLNameCharsWithUnderscores` are mutually exclusive;\nenabling both fails at runtime. To get underscores instead of removed characters, disable the former and\nenable the latter."
      },
      "StandardizeSparkDatatypesTransformer" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "StandardizeSparkDatatypesTransformer"
          },
          "name" : {
            "type" : "string",
            "description" : "name of the transformer"
          },
          "description" : {
            "type" : "string",
            "description" : "Optional description of the transformer"
          }
        },
        "title" : "StandardizeSparkDatatypesTransformer",
        "required" : [ "type" ],
        "additionalProperties" : false,
        "description" : "Standardize datatypes of a Spark-DataFrame.  \nCurrent implementation converts all decimal datatypes to a corresponding integral or float datatype.\n\nDecimal columns are common when reading from JDBC sources or Parquet files written by other tools, but\nthey are inconvenient downstream: they compare and serialize differently and often carry an unnecessarily\nwide precision. Adding this transformer as the first entry of a transformation chain gives all Actions a\nuniform, narrow numeric type per column without listing columns explicitly. The transformer takes no\nconfiguration attributes beyond the common `name` / `description` . Example:\n```\nactions = {\n  copy-departures {\n    type = CopyAction\n    inputId = stg-departures\n    outputId = int-departures\n    transformers = [{ type = StandardizeSparkDatatypesTransformer }]\n  }\n}\n```\n\n\nNOTE: The conversion is lossy if a decimal column holds values that do not fit the derived integral or\nfloat type; check precision and scale of the source schema before enabling it."
      },
      "WhitelistTransformer" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "WhitelistTransformer"
          },
          "name" : {
            "type" : "string",
            "description" : "name of the transformer"
          },
          "description" : {
            "type" : "string",
            "description" : "Optional description of the transformer"
          },
          "columnWhitelist" : {
            "type" : "array",
            "items" : {
              "type" : "string"
            },
            "description" : "List of columns to keep from DataFrame"
          }
        },
        "title" : "WhitelistTransformer",
        "required" : [ "type", "columnWhitelist" ],
        "additionalProperties" : false,
        "description" : "Apply a column whitelist to a DataFrame: only the columns listed in `columnWhitelist` are kept, all others are\ndropped. Use it to project a wide source down to the attributes a pipeline really needs, e.g. to exclude\nconfidential columns, without writing a SQL select statement.\n\nColumn names are matched case-insensitively unless case sensitivity is enabled globally\n( `global.environment.caseSensitive` ). In the default case-insensitive mode the output keeps the column order\nof the input DataFrame; with case sensitivity enabled the order of the selected columns is undefined. Example:\n```\nactions = {\n  copy-airports {\n    type = CopyAction\n    inputId = stg-airports\n    outputId = int-airports\n    transformers = [{\n      type = WhitelistTransformer\n      columnWhitelist = [ident, name, latitude_deg, longitude_deg]\n    }]\n  }\n}\n```\n\n\nNOTE: Names in `columnWhitelist` which do not exist in the input DataFrame are logged as warning and ignored,\nthey do not fail the Action.\n\nSEE: `BlacklistTransformer` to instead drop a list of columns and keep the rest."
      }
    },
    "AuthMode" : {
      "GCPCredentialsKeyAuth" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "GCPCredentialsKeyAuth"
          },
          "serviceAccountKey" : {
            "type" : "string",
            "description" : "A String that represents the Service Account Key encoded in Base64.\n\nThe value can be provided in plaintext or with the id of a secret provider and the\nname of the secret in the format\n```\n###<PROVIDERID>#<SECRETNAME>###\n```\n ,\ne.g.  \n```\n###ENV#<ENV_VARIABLE_NAME>###\n```\n\n\nto get a secret from an environment variable."
          },
          "serviceAccountKeyFile" : {
            "type" : "string",
            "description" : "A path that represents the location of the JSON-file with the service account key.\n\nThe value can be provided in plaintext or with the id of a secret provider and the\nname of the secret in the format\n```\n###<PROVIDERID>#<SECRETNAME>###\n```\n ,\ne.g.  \n```\n###ENV#<ENV_VARIABLE_NAME>###\n```\n\n\nto get a secret from an environment variable."
          }
        },
        "title" : "GCPCredentialsKeyAuth",
        "required" : [ "type" ],
        "additionalProperties" : false,
        "description" : "Authorization parameters to access a GCP-ressource using Service Account Keys.  \nA Service Account Key can be either read as Base64-encoded JSON file (String),\nor as a path containing the JSON-File with the key, as described by Google. Only one of the options should be provided.\n\nExactly one of `serviceAccountKey` and `serviceAccountKeyFile` must be set. Configuring both is rejected with a\nConfigurationException in the prepare phase, configuring none already fails while BigQueryTableConnection is\ninstantiated during config parsing. Currently supported by BigQueryTableConnection only. Example:\n```\nconnections = {\n  google-playground {\n    type = BigQueryTableConnection\n    parentProject = \"playground-223717\"\n    authMode = {\n      type = GCPCredentialsKeyAuth\n      serviceAccountKeyFile = \"/etc/secrets/playground-223717-7baadcb4cfef.json\"\n    }\n  }\n}\n```\n"
      },
      "PublicKeyAuthMode" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "PublicKeyAuthMode"
          },
          "user" : {
            "type" : "string",
            "description" : "user to login with (supports secret providers). Although declared optional for configuration\nparsing, it must be defined, otherwise a ConfigurationException is thrown.\n\nThe value can be provided in plaintext or with the id of a secret provider and the\nname of the secret in the format\n```\n###<PROVIDERID>#<SECRETNAME>###\n```\n ,\ne.g.  \n```\n###ENV#<ENV_VARIABLE_NAME>###\n```\n\n\nto get a secret from an environment variable."
          }
        },
        "title" : "PublicKeyAuthMode",
        "required" : [ "type" ],
        "additionalProperties" : false,
        "description" : "Validate by user and private/public key Private key is read from .ssh\n\nOnly the user name is configured; the private key is taken from the default ssh location (~/.ssh) of the\nprocess running SDLB, and the corresponding public key must be registered on the server. Choose this over `BasicAuthMode` to avoid storing a password in the configuration. Currently supported by\nSFtpFileRefConnection only. Example:\n```\nconnections = {\n  sftp-src {\n    type = SFtpFileRefConnection\n    host = \"sftp.example.com\"\n    authMode = {\n      type = PublicKeyAuthMode\n      user = \"###ENV#SFTP_USER###\"\n    }\n  }\n}\n```\n"
      },
      "SASLSCRAMAuthMode" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "SASLSCRAMAuthMode"
          },
          "username" : {
            "type" : "string",
            "description" : "user name to authenticate with (supports secret providers)\n\nThe value can be provided in plaintext or with the id of a secret provider and the\nname of the secret in the format\n```\n###<PROVIDERID>#<SECRETNAME>###\n```\n ,\ne.g.  \n```\n###ENV#<ENV_VARIABLE_NAME>###\n```\n\n\nto get a secret from an environment variable."
          },
          "password" : {
            "type" : "string",
            "description" : "password to authenticate with (supports secret providers). Although declared\noptional for configuration parsing, it must be defined, otherwise a\nConfigurationException is thrown.\n\nThe value can be provided in plaintext or with the id of a secret provider and the\nname of the secret in the format\n```\n###<PROVIDERID>#<SECRETNAME>###\n```\n ,\ne.g.  \n```\n###ENV#<ENV_VARIABLE_NAME>###\n```\n\n\nto get a secret from an environment variable."
          },
          "sslMechanism" : {
            "type" : "string",
            "description" : "SASL mechanism to use, e.g. \"SCRAM-SHA-512\", \"SCRAM-SHA-256\" or \"PLAIN\". It is\npassed as Kafka`sasl.mechanism` and selects the JAAS login module\n(PlainLoginModule for \"plain\", ScramLoginModule otherwise)."
          },
          "truststorePath" : {
            "type" : "string",
            "description" : "optional path to the Java truststore holding the brokers CA certificate. Only\nneeded if the broker certificate is not trusted by the default JVM truststore."
          },
          "truststoreType" : {
            "type" : "string",
            "description" : "type of the truststore, e.g. \"JKS\" or \"PKCS12\" (default: \"JKS\")"
          },
          "truststorePassSecret" : {
            "type" : "string",
            "description" : "password of the truststore (supports secret providers). Mandatory if`truststorePath` is set.\n\nThe value can be provided in plaintext or with the id of a secret provider and the\nname of the secret in the format\n```\n###<PROVIDERID>#<SECRETNAME>###\n```\n ,\ne.g.  \n```\n###ENV#<ENV_VARIABLE_NAME>###\n```\n\n\nto get a secret from an environment variable."
          }
        },
        "title" : "SASLSCRAMAuthMode",
        "required" : [ "type", "username", "sslMechanism" ],
        "additionalProperties" : false,
        "description" : "Authenticate using SASL_SSL Authentication.\n\nConfiguration needed are username and password, plus a Java truststore if the brokers certificate is not trusted\nby the default JVM truststore.\n\nSets Kafka `security.protocol=SASL_SSL` and builds the corresponding `sasl.jaas.config` . Pick this over `SSLCertsAuthMode` if the broker authenticates users by name/password (SCRAM or PLAIN) rather than by\nclient certificate. Currently supported by KafkaConnection only. Example:\n```\nconnections = {\n  kafka-con {\n    type = KafkaConnection\n    brokers = \"kafka-broker-1:9093,kafka-broker-2:9093\"\n    authMode = {\n      type = SASLSCRAMAuthMode\n      username = \"###ENV#KAFKA_USER###\"\n      password = \"###ENV#KAFKA_PWD###\"\n      sslMechanism = \"SCRAM-SHA-512\"\n      truststorePath = \"/etc/kafka/secrets/kafka.truststore.jks\"\n      truststorePassSecret = \"###ENV#KAFKA_TRUSTSTORE_PWD###\"\n    }\n  }\n}\n```\n"
      },
      "SSLCertsAuthMode" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "SSLCertsAuthMode"
          },
          "keystorePath" : {
            "type" : "string",
            "description" : "path to the Java keystore containing the client certificate and private key"
          },
          "keystoreType" : {
            "type" : "string",
            "description" : "type of the keystore, e.g. \"JKS\" or \"PKCS12\" (default: \"JKS\")"
          },
          "keystorePass" : {
            "type" : "string",
            "description" : "password of the keystore (supports secret providers). Although declared optional for\nconfiguration parsing, it must be defined, otherwise a ConfigurationException is thrown.\n\nThe value can be provided in plaintext or with the id of a secret provider and the\nname of the secret in the format\n```\n###<PROVIDERID>#<SECRETNAME>###\n```\n ,\ne.g.  \n```\n###ENV#<ENV_VARIABLE_NAME>###\n```\n\n\nto get a secret from an environment variable."
          },
          "truststorePath" : {
            "type" : "string",
            "description" : "path to the Java truststore containing the CA certificate of the server"
          },
          "truststoreType" : {
            "type" : "string",
            "description" : "type of the truststore, e.g. \"JKS\" or \"PKCS12\" (default: \"JKS\")"
          },
          "truststorePass" : {
            "type" : "string",
            "description" : "password of the truststore (supports secret providers). Although declared optional for\nconfiguration parsing, it must be defined, otherwise a ConfigurationException is thrown.\n\nThe value can be provided in plaintext or with the id of a secret provider and the\nname of the secret in the format\n```\n###<PROVIDERID>#<SECRETNAME>###\n```\n ,\ne.g.  \n```\n###ENV#<ENV_VARIABLE_NAME>###\n```\n\n\nto get a secret from an environment variable."
          }
        },
        "title" : "SSLCertsAuthMode",
        "required" : [ "type", "keystorePath", "truststorePath" ],
        "additionalProperties" : false,
        "description" : "Authenticate using SSL Certificates.\n\nConfiguration needed are a Java keystore and truststore.\n\nSets Kafka `security.protocol=SSL` and configures mutual TLS: the keystore holds the client certificate and\nprivate key presented to the broker, the truststore holds the CA certificate used to verify the broker.  \nPick this over `SASLSCRAMAuthMode` if the broker authenticates clients by certificate instead of\nuser/password. Currently supported by KafkaConnection only. Example:\n```\nconnections = {\n  kafka-con {\n    type = KafkaConnection\n    brokers = \"kafka-broker-1:9093,kafka-broker-2:9093\"\n    authMode = {\n      type = SSLCertsAuthMode\n      keystorePath = \"/etc/kafka/secrets/kafka.keystore.jks\"\n      keystorePass = \"###ENV#KAFKA_KEYSTORE_PWD###\"\n      truststorePath = \"/etc/kafka/secrets/kafka.truststore.jks\"\n      truststorePass = \"###ENV#KAFKA_TRUSTSTORE_PWD###\"\n    }\n  }\n}\n```\n"
      }
    },
    "GenericDfsTransformer" : {
      "DfTransformerWrapperDfsTransformer" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "DfTransformerWrapperDfsTransformer"
          },
          "transformer" : {
            "oneOf" : [ {
              "$ref" : "#/definitions/GenericDfTransformer/BlacklistTransformer"
            }, {
              "$ref" : "#/definitions/GenericDfTransformer/ColumnsTransformer"
            }, {
              "$ref" : "#/definitions/GenericDfTransformer/ConvertNullValuesTransformer"
            }, {
              "$ref" : "#/definitions/GenericDfTransformer/DataValidationTransformer"
            }, {
              "$ref" : "#/definitions/GenericDfTransformer/DebugTransformer"
            }, {
              "$ref" : "#/definitions/GenericDfTransformer/DecryptColumnsTransformer"
            }, {
              "$ref" : "#/definitions/GenericDfTransformer/DeduplicateTransformer"
            }, {
              "$ref" : "#/definitions/GenericDfTransformer/EncryptColumnsTransformer"
            }, {
              "$ref" : "#/definitions/GenericDfTransformer/FilterTransformer"
            }, {
              "$ref" : "#/definitions/GenericDfTransformer/PythonCodeDfTransformer"
            }, {
              "$ref" : "#/definitions/GenericDfTransformer/SQLDfTransformer"
            }, {
              "$ref" : "#/definitions/GenericDfTransformer/ScalaClassGenericDfTransformer"
            }, {
              "$ref" : "#/definitions/GenericDfTransformer/ScalaClassSnowparkDfTransformer"
            }, {
              "$ref" : "#/definitions/GenericDfTransformer/ScalaClassSparkDfTransformer"
            }, {
              "$ref" : "#/definitions/GenericDfTransformer/ScalaCodeSparkDfTransformer"
            }, {
              "$ref" : "#/definitions/GenericDfTransformer/ScalaNotebookSparkDfTransformer"
            }, {
              "$ref" : "#/definitions/GenericDfTransformer/SparkFlattenDfTransformer"
            }, {
              "$ref" : "#/definitions/GenericDfTransformer/SparkRepartitionTransformer"
            }, {
              "$ref" : "#/definitions/GenericDfTransformer/StandardizeColNamesTransformer"
            }, {
              "$ref" : "#/definitions/GenericDfTransformer/StandardizeSparkDatatypesTransformer"
            }, {
              "$ref" : "#/definitions/GenericDfTransformer/WhitelistTransformer"
            } ],
            "description" : "Configuration for a GenericDfTransformerDef to be applied"
          },
          "subFeedsToApply" : {
            "type" : "array",
            "items" : {
              "type" : "string"
            },
            "description" : "Names of SubFeeds the transformation should be applied to. Default is an empty list,\nwhich will apply the transformation to all subfeeds."
          }
        },
        "title" : "DfTransformerWrapperDfsTransformer",
        "required" : [ "type", "transformer" ],
        "additionalProperties" : false,
        "description" : "A Transformer to use single DataFrame Transformers as multiple DataFrame Transformers.  \nThis works by selecting the SubFeeds (DataFrames) the single DataFrame Transformer should be applied to.  \nAll other SubFeeds will be passed through without transformation.  \nUse it in Actions working on many DataFrames at once, like CustomDataFrameAction, to reuse a\none-DataFrame transformer such as `FilterTransformer`, `BlacklistTransformer` or `DebugTransformer` there. The transformation is applied to each selected SubFeed separately.  \nThe name and description of this transformer are taken over from the wrapped transformer.  \nThe Action fails if a name in `subFeedsToApply` does not match one of the SubFeeds handed to this transformer.  \nThese are the input DataObject ids if this is the first transformer of the chain, otherwise the output names\nof the preceding transformer. Example: filter one of the input SubFeeds before joining them with a SQL transformation.  \n```\nactions = {\n  join-ratings {\n    type = CustomDataFrameAction\n    inputIds = [stg-ratings, stg-airports]\n    outputIds = [int-ratings]\n    transformers = [{\n      type = DfTransformerWrapperDfsTransformer\n      subFeedsToApply = [stg-ratings]\n      transformer = { type = FilterTransformer, filterClause = \"rating is not null\" }\n    },{\n      type = SQLDfsTransformer\n      code = { int-ratings = \"select * from %{inputViewName_stg-ratings} join %{inputViewName_stg-airports} using (airport_id)\" }\n    }]\n  }\n}\n```\n"
      },
      "PythonCodeDfsTransformer" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "PythonCodeDfsTransformer"
          },
          "name" : {
            "type" : "string",
            "description" : "name of the transformer"
          },
          "description" : {
            "type" : "string",
            "description" : "Optional description of the transformer"
          },
          "code" : {
            "type" : "string",
            "description" : "Optional python code to user for python transformation. The python code can use variables inputDfs and options. The transformed DataFrame has to be set with setOutputDfs."
          },
          "file" : {
            "type" : "string",
            "description" : "Optional file with python code to use for python transformation. The python code can use variables inputDfs and options. The transformed DataFrames has to be set with setOutputDfs."
          },
          "options" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "Options to pass to the transformation"
          },
          "runtimeOptions" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "optional tuples of [key, spark sql expression] to be added as additional options when executing transformation.  \nThe spark sql expressions are evaluated against an instance of`DefaultExpressionData`."
          }
        },
        "title" : "PythonCodeDfsTransformer",
        "required" : [ "type" ],
        "additionalProperties" : false,
        "description" : "Configuration of a custom Spark-DataFrame transformation between many inputs and many outputs (n:m) as Python/PySpark code.  \nNote that this transformer needs a Python and PySpark environment installed.  \nPySpark session is initialize and available under variables `sc` , `session` , `sqlContext` .  \nOther variables available are\n- `inputDfs` : Input DataFrames\n- `options` : Transformation options as Map[String,String]\nOutput DataFrames must be set with `setOutputDfs(dict)` .\n\n`inputDfs` is a dict keyed by input DataObjectId, and the dict passed to `setOutputDfs` must be keyed by output\nDataObjectId. Either `code` (inline PySpark code) or `file` (a path to a `.py` file) must be defined. Use this\ntransformer with `io.smartdatalake.workflow.action.CustomDataFrameAction` ; for a single input and output prefer `PythonCodeDfTransformer`. Alternatively the Python code can define a function named `transform` . It is then called dynamically by looking\nfor its parameter values in the input DataFrames and options, and its return value is used as output DataFrames.  \nA parameter with the name of an input DataFrame gets this DataFrame, a `df` prefix is stripped from the parameter\nname before the lookup and the lookup ignores case, `-` and `_` . A parameter named `dfs` gets all input\nDataFrames as dict, a parameter named `options` gets all options as dict, and a parameter with the name of an\noption gets its value converted according to its type annotation or the type of its default value. Example:\n```\ncode = \"\"\"\n  def transform(dfStgDepartures, dfStgAirports):\n      return {'int-departures': dfStgDepartures.join(dfStgAirports, 'ident')}\n\"\"\"\n```\n Example:\n```\nactions = {\n  join-flights {\n    type = CustomDataFrameAction\n    inputIds = [stg-airports, stg-departures]\n    outputIds = [int-departures]\n    transformers = [{\n      type = PythonCodeDfsTransformer\n      code = \"\"\"\n        df = inputDfs['stg-departures'].join(inputDfs['stg-airports'], 'ident')\n        setOutputDfs({'int-departures': df})\n      \"\"\"\n    }]\n  }\n}\n```\n"
      },
      "SQLDfsTransformer" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "SQLDfsTransformer"
          },
          "name" : {
            "type" : "string",
            "description" : "name of the transformer"
          },
          "description" : {
            "type" : "string",
            "description" : "Optional description of the transformer"
          },
          "files" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "Map of output names to corresponding Files where SQL code for transformation is loaded from for transformation.  \nIf this is the last transformation in the chain, the output name has to match an output DataObject id,\notherwise it can be any name which will then be available in the next transformation.  \nUse tokens`%{<key>}`to replace with runtimeOptions in SQL code.  \nExample:`select * from test where run = %{runId}`The special token`%{inputViewName_<input_name>}` can be used to insert the name of temporary views.  \nThe input name is either the id of an input DataObject, or the name of an output of the previous transformation if this is not the first transformation of the chain.  \nMake sure to set the name of the previous transformation in that case."
          },
          "code" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "Map of output names to corresponding SQL code for transformation.  \nIf this is the last transformation in the chain, the output name has to match an output DataObject id,\notherwise it can be any name which will then be available in the next transformation.  \nUse tokens`%{<key>}`to replace with runtimeOptions in SQL code.  \nExample:`select * from test where run = %{runId}`.  \nThe special token`%{inputViewName_<input_name>}` can be used to insert the name of temporary views.  \nThe input name is either the id of an input DataObject, or the name of an output of the previous transformation if this is not the first transformation of the chain.  \nMake sure to set the name of the previous transformation in that case."
          },
          "options" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "Options to pass to the transformation"
          },
          "runtimeOptions" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "optional tuples of [key, spark sql expression] to be added as additional options when executing transformation.  \nThe spark sql expressions are evaluated against an instance of`DefaultExpressionData`."
          }
        },
        "title" : "SQLDfsTransformer",
        "required" : [ "type" ],
        "additionalProperties" : false,
        "description" : "Configuration of a custom GenericDataFrame transformation between many inputs and many outputs (n:m) as SQL code.  \nThe input data is available as temporary views in SQL. As name for the temporary views the input DataObjectId is used\n(special characters are replaces by underscores).  \nThe input data is available as temporary view in SQL. The input name is either an id of the input DataObject,\nor the name of an output of the previous transformation if this is not the first transformation of the chain.  \nAlso note that to create the name of temporary view, special characters are replaced by underscores and a postfix `_sdltemp` is added.  \nIt is therefore recommended to use the special token `%{inputViewName_<input name>}` , that will be replaced with the name\nof the temporary view at runtime.\n\nNote that you can access arbitrary tables from the metastore in the SQL code, but this is against the principle of SDLB\nto access data through DataObjects. Accessing tables directly in SQL code has a negative impact on the maintainability of the project.\n\nUse SQLDfsTransformer inside a `io.smartdatalake.workflow.action.CustomDataFrameAction` for joins, unions and\nfan-outs expressed in SQL. Either `code` or `files` must be non empty, and per output name only one of them may\nbe defined. If this is the last transformer of the chain, its output names must match the Action's outputIds. Example:\n```\nactions = {\n  join-departures-airports {\n    type = CustomDataFrameAction\n    inputIds = [stg-departures, int-airports]\n    outputIds = [btl-departures-airports]\n    transformers = [{\n      type = SQLDfsTransformer\n      code = {\n        btl-departures-airports = \"select d.*, a.name from %{inputViewName_stg-departures} d join %{inputViewName_int-airports} a on d.estdepartureairport = a.ident\"\n      }\n    }]\n  }\n}\n```\n"
      },
      "ScalaClassGenericDfsTransformer" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "ScalaClassGenericDfsTransformer"
          },
          "name" : {
            "type" : "string",
            "description" : "name of the transformer"
          },
          "description" : {
            "type" : "string",
            "description" : "Optional description of the transformer"
          },
          "className" : {
            "type" : "string",
            "description" : "class name implementing trait`CustomGenericDfsTransformer`"
          },
          "options" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "Options to pass to the transformation"
          },
          "runtimeOptions" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "optional tuples of [key, spark sql expression] to be added as additional options when executing transformation.  \nThe spark sql expressions are evaluated against an instance of`DefaultExpressionData`."
          },
          "renamedInputIds" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "optional map of [input DataFrame name, renamed input DataFrame name]. Adapt names of input\nDataFrames to the expected names in the transformation. This is useful if the transformation\nexpects specific input names, or if you want to use more generic names in the transformation\nthan the actual input DataObjectIds."
          },
          "renamedOutputIds" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "optional map of [output DataFrame name, renamed output DataFrame name]. Adapt names of output\nDataFrames of the transformation to the expected names of the output DataObjects or the next\ntransformation."
          },
          "overrideOutputId" : {
            "type" : "string",
            "description" : "override name of output DataFrame, if the transformer returns a single DataFrame, and not a\nMap of type String → GenericDataFrame. By default, a single DataFrame is named after the\noutput DataObjectId of the Action if the action has only one output DataObject. This\nparameter is ignored if the transformation returns multiple DataFrames."
          }
        },
        "title" : "ScalaClassGenericDfsTransformer",
        "required" : [ "type", "className" ],
        "additionalProperties" : false,
        "description" : "Configuration of a custom Spark-DataFrame transformation between many inputs and many outputs (n:m)\nDefine a transform function which receives a map of input DataObjectIds with DataFrames and a map of options and as\nto return a map of output DataObjectIds with DataFrames, see also trait `CustomGenericDfsTransformer`.\n\nUse this transformer inside a `io.smartdatalake.workflow.action.CustomDataFrameAction` whenever a join, union or\nfan-out is easier to implement in Scala/Java than in SQL. The class is instantiated by reflection through its\nno-argument constructor and must be on the classpath of the SDLB job. If this is the last transformer of the chain,\nthe returned map must contain an entry for every outputId of the Action.\n\nInstead of overwriting the standard transform function, the class can also implement any transform method using\nparameters of type DataFrameFunctions, Map[String,String], GenericDataFrame and any primitive data type. It is then\ncalled dynamically by looking for the parameter values in the input DataFrames and Options, see `CustomGenericDfsTransformer`. Example:\n```\nactions = {\n  join-departures-airports {\n    type = CustomDataFrameAction\n    inputIds = [stg-departures, int-airports]\n    outputIds = [btl-departures-airports]\n    transformers = [{\n      type = ScalaClassGenericDfsTransformer\n      className = com.sample.MyJoinTransformer\n      options = { joinType = \"left\" }\n    }]\n  }\n}\n```\n"
      },
      "ScalaClassSnowparkDfsTransformer" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "ScalaClassSnowparkDfsTransformer"
          },
          "name" : {
            "type" : "string",
            "description" : "name of the transformer"
          },
          "description" : {
            "type" : "string",
            "description" : "Optional description of the transformer"
          },
          "className" : {
            "type" : "string",
            "description" : "class name implementing trait`CustomSnowparkDfsTransformer`"
          },
          "options" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "Options to pass to the transformation"
          },
          "runtimeOptions" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "optional tuples of [key, spark sql expression] to be added as additional options when executing transformation.  \nThe spark sql expressions are evaluated against an instance of`DefaultExpressionData`."
          },
          "renamedInputIds" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "optional map of [input DataFrame name, renamed input DataFrame name]. Adapt names of input\nDataFrames to the expected names in the transformation."
          },
          "renamedOutputIds" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "optional map of [output DataFrame name, renamed output DataFrame name]. Adapt names of output\nDataFrames of the transformation to the expected names of the output DataObjects or the next\ntransformation."
          },
          "overrideOutputId" : {
            "type" : "string",
            "description" : "override name of output DataFrame, if the transformer returns a single DataFrame, and not a\nMap of type String → DataFrame. By default, a single DataFrame is named after the output\nDataObjectId of the Action if the action has only one output DataObject."
          }
        },
        "title" : "ScalaClassSnowparkDfsTransformer",
        "required" : [ "type", "className" ],
        "additionalProperties" : false,
        "description" : "Configuration of a custom Snowpark-DataFrame transformation between many inputs and many outputs (n:m) as Java/Scala Class.  \nDefine a transform function which receives a map of input DataObjectIds with DataFrames and a map of options and as\nto return a map of output DataObjectIds with DataFrames, see also trait `CustomSnowparkDfsTransformer`.\n\nUse this transformer to implement joins or splits between several Snowflake tables in Scala/Java code, which is\npushed down to Snowflake and executed by Snowpark. If a single input and output is sufficient, prefer the simpler `ScalaClassSnowparkDfTransformer`.\n\nInstead of overwriting the standard transform function, the class can also implement any transform method using\nparameters of type Session, Map[String,String], DataFrame and any primitive data type. It is then called\ndynamically by looking for the parameter values in the input DataFrames and Options, see `CustomSnowparkDfsTransformer`. Example:\n```\nactions = {\n  join-snowpark {\n    type = CustomDataFrameAction\n    inputIds = [sf-airports, sf-departures]\n    outputIds = [sf-departures-enriched]\n    transformers = [{\n      type = ScalaClassSnowparkDfsTransformer\n      className = com.company.transformer.JoinDeparturesSnowparkTransformer\n      options = { joinType = \"left\" }\n    }]\n  }\n}\n```\n\n\nNOTE: All input and output DataObjects must be of type SnowflakeTableDataObject, as the Snowpark session is taken\nfrom the Action's first input. The returned map must be keyed by the output DataObject ids."
      },
      "ScalaClassSparkDfsTransformer" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "ScalaClassSparkDfsTransformer"
          },
          "name" : {
            "type" : "string",
            "description" : "name of the transformer"
          },
          "description" : {
            "type" : "string",
            "description" : "Optional description of the transformer"
          },
          "className" : {
            "type" : "string",
            "description" : "class name implementing trait`CustomDfsTransformer`"
          },
          "options" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "Options to pass to the transformation"
          },
          "runtimeOptions" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "optional tuples of [key, spark SQL expression] to be added as additional options when executing\ntransformation. The spark SQL expressions are evaluated against an instance of`DefaultExpressionData`."
          },
          "renamedInputIds" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "optional map of [input DataFrame name, renamed input DataFrame name]. Adapt names of input\nDataFrames to the expected names in the transformation. This is useful if the transformation\nexpects specific input names, or if you want to use more generic names in the transformation\nthan the actual input DataObjectIds."
          },
          "renamedOutputIds" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "optional map of [output DataFrame name, renamed output DataFrame name]. Adapt names of output\nDataFrames of the transformation to the expected names of the output DataObjects or the next\ntransformation. This is useful if the transformation returns DataFrames with specific names, or\nif you want to use more generic names in the transformation than the actual output\nDataObjectIds."
          },
          "overrideOutputId" : {
            "type" : "string",
            "description" : "override name of output DataFrame, if the transformer returns a single DataFrame/Dataset, and\nnot a Map of type String → DataFrame. By default, a single DataFrame/Dataset is named after\nthe output DataObjectId of the Action if the action has only one output DataObject. With this\nparameter you can explicitly define the name of the output DataFrame/Dataset independent of the\noutput DataObjectId of the Action. This parameter is ignored if the transformation returns\nmultiple DataFrames."
          }
        },
        "title" : "ScalaClassSparkDfsTransformer",
        "required" : [ "type", "className" ],
        "additionalProperties" : false,
        "description" : "Configuration of a custom Spark-DataFrame transformation between many inputs and many outputs\n(n:m)\n\nTo define the transformation a class implementing the trait `CustomDfsTransformer` has to be\ncreated. There are two methods to define of defining transformation:\n\n1) Overwrite the generic transform function of CustomDfsTransformer: Define a transform function\nwhich receives a map of input DataObjectIds with DataFrames and a map of options and has to\nreturn a map of output DataObjectIds with DataFrames. The exact signature is `transform(session: SparkSession, options: Map[String,String], dfs: Map[String,DataFrame]): Map[String,DataFrame]` .\n\n2) Implement any transform method using parameters of type SparkSession, Map[String,String],\nDataFrame, Dataset[<Product>] and any primitive data type (String, Boolean, Int, ...). Primitive\ndata types might also use default values or be enclosed in an Option[...] to mark it as non\nrequired. The transform method is then called dynamically by looking for the parameter values in\nthe input DataFrames and Options. Using this method you can avoid writing code to prepare\nDataFrames and Options from the corresponding Map parameters. It also allows the UI to display\ninput parameter details of your transformation.\n\nNote that the following options are passed by default to the transformation:\n- isExec: defined as `context.isExecPhase` - outputDataObjectId: defined as outputDataObject.id if transformation has only one output\nDataObject configured.\n\nThe class named by `className` must be on the classpath of the SDLB job and needs a constructor without arguments.  \nInput and output DataFrames are matched by DataObjectId; use `renamedInputIds` / `renamedOutputIds` to write generic\ntransformers whose names are independent of the configured DataObjectIds. For a single input and output, `ScalaClassSparkDfTransformer` is simpler. Example:\n```\nactions = {\n  join-departures {\n    type = CustomDataFrameAction\n    inputIds = [stg-airports, stg-departures]\n    outputIds = [int-departures]\n    transformers = [{\n      type = ScalaClassSparkDfsTransformer\n      className = com.sample.transformer.JoinDeparturesTransformer\n      options = { country = \"CH\" }\n      renamedInputIds = { stg-airports = airports }\n    }]\n  }\n}\n```\n"
      },
      "ScalaCodeSparkDfsTransformer" : {
        "type" : "object",
        "properties" : {
          "type" : {
            "const" : "ScalaCodeSparkDfsTransformer"
          },
          "name" : {
            "type" : "string",
            "description" : "name of the transformer"
          },
          "description" : {
            "type" : "string",
            "description" : "Optional description of the transformer"
          },
          "code" : {
            "type" : "string",
            "description" : "Scala code for transformation. The Scala code needs to be a function of type`fnTransformType`."
          },
          "file" : {
            "type" : "string",
            "description" : "File where Scala code for transformation is loaded from. The Scala code in the file needs to be\na function of type`fnTransformType`."
          },
          "options" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "Options to pass to the transformation"
          },
          "runtimeOptions" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string"
            },
            "description" : "optional tuples of [key, spark SQL expression] to be added as additional options when executing\ntransformation. The spark SQL expressions are evaluated against an instance of`DefaultExpressionData`."
          }
        },
        "title" : "ScalaCodeSparkDfsTransformer",
        "required" : [ "type" ],
        "additionalProperties" : false,
        "description" : "Configuration of a custom Spark-DataFrame transformation between many inputs and many outputs\n(n:m) as Scala code which is compiled at runtime. Define a transform function which receives a\nmap of input DataObjectIds with DataFrames and a map of options and has to return a map of output\nDataObjectIds with DataFrames. The Scala code has to implement a function of type `fnTransformType`.\n\nUse this transformer inside a `CustomDataFrameAction` whenever a transformation needs more than one\ninput DataFrame (joins, unions) or produces more than one output DataFrame. The returned map must\ncontain an entry for every output DataObject id of the action. Exactly one of `code` or `file` must\nbe defined; the code is compiled at job startup so errors surface in the prepare phase. Alternatively the code can create an implementation of `CustomDfsTransformer` defining any transform method.  \nThe transform method is then called dynamically by looking for the parameter values in the input DataFrames and\nOptions, see `CustomDfsTransformer`. Example:\n```\ncode = \"\"\"\n  import io.smartdatalake.workflow.action.spark.customlogic.CustomDfsTransformer\n  import org.apache.spark.sql.DataFrame\n  new CustomDfsTransformer {\n    def transform(dfStgDepartures: DataFrame, dfIntAirports: DataFrame): DataFrame = {\n      dfStgDepartures.join(dfIntAirports, Seq(\"ident\"))\n    }\n  }\n\"\"\"\n```\n Example:\n```\nactions = {\n  join-departures-airports {\n    type = CustomDataFrameAction\n    inputIds = [stg-departures, int-airports]\n    outputIds = [btl-departures-arrivals-airports]\n    transformers = [{\n      type = ScalaCodeSparkDfsTransformer\n      code = \"\"\"\n        import org.apache.spark.sql.{DataFrame, SparkSession}\n        (session: SparkSession, options: Map[String,String], dfs: Map[String,DataFrame]) => {\n          val df = dfs(\"stg-departures\").join(dfs(\"int-airports\"), Seq(\"ident\"))\n          Map(\"btl-departures-arrivals-airports\" → df)\n        }\n      \"\"\"\n    }]\n  }\n}\n```\n\n\nSEE: `ScalaCodeSparkDfTransformer` for the 1:1 case"
      }
    }
  },
  "properties" : {
    "global" : {
      "type" : "object",
      "properties" : {
        "hadoopOptions" : {
          "type" : "object",
          "additionalProperties" : {
            "type" : "string",
            "description" : "The value can be provided in plaintext or with the id of a secret provider and the\nname of the secret in the format\n```\n###<PROVIDERID>#<SECRETNAME>###\n```\n ,\ne.g.  \n```\n###ENV#<ENV_VARIABLE_NAME>###\n```\n\n\nto get a secret from an environment variable."
          },
          "description" : "The value can be provided in plaintext or with the id of a secret provider and the\nname of the secret in the format\n```\n###<PROVIDERID>#<SECRETNAME>###\n```\n ,\ne.g.  \n```\n###ENV#<ENV_VARIABLE_NAME>###\n```\n\n\nto get a secret from an environment variable."
        },
        "statusInfo" : {
          "type" : "object",
          "properties" : {
            "port" : {
              "type" : "integer",
              "description" : ": port with which the first connection attempt is made"
            },
            "maxPortRetries" : {
              "type" : "integer",
              "description" : ": If port is already in use, we will increment port by one and try with that new port.  \nmaxPortRetries describes how many times this should be attempted. If set to 0 it will not be attempted.  \nValues below 0 are not allowed."
            },
            "stopOnEnd" : {
              "type" : "boolean",
              "description" : ": Set to false if the Server should remain online even after SDL has finished its execution.  \nIn that case, the Application needs to be stopped manually. Useful for debugging."
            }
          },
          "title" : "StatusInfoConfig",
          "additionalProperties" : false,
          "description" : "enable a REST API providing live status info, see detailed configuration`StatusInfoConfig`"
        },
        "memoryLogTimer" : {
          "type" : "object",
          "properties" : {
            "intervalSec" : {
              "type" : "integer",
              "description" : "interval in seconds between memory usage logs"
            },
            "logLinuxMem" : {
              "type" : "boolean",
              "description" : "enable logging linux memory"
            },
            "logLinuxCGroupMem" : {
              "type" : "boolean",
              "description" : "enable logging details about linux cgroup memory"
            },
            "logBuffers" : {
              "type" : "boolean",
              "description" : "enable logging details about different jvm buffers"
            }
          },
          "title" : "MemoryLogTimerConfig",
          "required" : [ "intervalSec" ],
          "additionalProperties" : false,
          "description" : "enable periodic memory usage logging, see detailed configuration`MemoryLogTimerConfig`"
        },
        "shutdownHookLogger" : {
          "type" : "boolean",
          "description" : "enable shutdown hook logger to trace shutdown cause"
        },
        "stateListeners" : {
          "type" : "array",
          "items" : {
            "type" : "object",
            "properties" : {
              "className" : {
                "type" : "string",
                "description" : "fully qualified class name of class implementing StateListener interface. The class needs a constructor with one parameter`options: Map[String,String]` ."
              },
              "options" : {
                "type" : "object",
                "additionalProperties" : {
                  "type" : "string",
                  "description" : "The value can be provided in plaintext or with the id of a secret provider and the\nname of the secret in the format\n```\n###<PROVIDERID>#<SECRETNAME>###\n```\n ,\ne.g.  \n```\n###ENV#<ENV_VARIABLE_NAME>###\n```\n\n\nto get a secret from an environment variable."
                },
                "description" : "Options are passed to StateListener constructor.\n\nThe value can be provided in plaintext or with the id of a secret provider and the\nname of the secret in the format\n```\n###<PROVIDERID>#<SECRETNAME>###\n```\n ,\ne.g.  \n```\n###ENV#<ENV_VARIABLE_NAME>###\n```\n\n\nto get a secret from an environment variable."
              }
            },
            "title" : "StateListenerConfig",
            "required" : [ "className" ],
            "additionalProperties" : false,
            "description" : "Configuration to notify interested parties about action results & metric"
          },
          "description" : "Define state listeners to be registered for receiving events of the execution of SmartDataLake\njob"
        },
        "secretProviders" : {
          "type" : "object",
          "additionalProperties" : {
            "type" : "object",
            "properties" : {
              "className" : {
                "type" : "string",
                "description" : "fully qualified class name of class implementing SecretProvider interface. The class needs a constructor with parameter \"options: Map[String,String]\"."
              },
              "options" : {
                "type" : "object",
                "additionalProperties" : {
                  "type" : "string"
                },
                "description" : "Options are passed to SecretProvider apply method."
              }
            },
            "title" : "SecretProviderConfig",
            "required" : [ "className" ],
            "additionalProperties" : false,
            "description" : "Configuration to register a SecretProvider."
          },
          "description" : "Define SecretProvider's to be registered."
        },
        "allowOverwriteAllPartitionsWithoutPartitionValues" : {
          "type" : "array",
          "items" : {
            "type" : "string"
          },
          "description" : "Configure a list of exceptions for partitioned DataObject id's, which are allowed to overwrite\nthe all partitions of a table if no partition values are set. This is used to override/avoid a\nprotective error when using SDLSaveMode.OverwriteOptimized|OverwritePreserveDirectories. Define\nit as a list of DataObject id's."
        },
        "allowAsRecursiveInput" : {
          "type" : "array",
          "items" : {
            "type" : "string"
          },
          "description" : "List of DataObjects for which the validation rules for Action.recursiveInputIds are *not*\nchecked. The validation rules are 1) that recursive input DataObjects must also be listed in\noutput DataObjects of the same action 2) the DataObject must implement\nTransactionalSparkTableDataObject interface Listing a DataObject in allowAsRecursiveInput can\nbe used for well thought exceptions, but should be avoided in general. Note that if 1) is true,\nalso 2) must be fulfilled for Spark to work properly (because Spark can't read/write the same\nstorage location in the same job), but there might be cases with recursions with different\nActions involved, that do not need to fulfill 2)."
        },
        "synchronousStreamingTriggerIntervalSec" : {
          "type" : "integer",
          "description" : "Trigger interval for synchronous actions in streaming mode in seconds (default = 60 seconds)\nThe synchronous actions of the DAG will be executed with this interval if possible. Note that\nfor asynchronous actions there are separate settings, e.g. SparkStreamingMode.triggerInterval."
        },
        "environment" : {
          "type" : "object",
          "additionalProperties" : {
            "type" : "string"
          },
          "description" : "Override environment settings defined in Environment object by setting the corresponding key to\nthe desired value (key in camelcase notation with the first letter in lowercase)"
        },
        "pluginOptions" : {
          "type" : "object",
          "additionalProperties" : {
            "type" : "string",
            "description" : "The value can be provided in plaintext or with the id of a secret provider and the\nname of the secret in the format\n```\n###<PROVIDERID>#<SECRETNAME>###\n```\n ,\ne.g.  \n```\n###ENV#<ENV_VARIABLE_NAME>###\n```\n\n\nto get a secret from an environment variable."
          },
          "description" : "Options for SDLPlugin initialization. Note that SDLPlugin.startup is executed before SDLB\nparses the config, and pluginOptions are only available later when calling SDLPlugin.configure\nmethod. An SDLPlugin is set through Environment.plugin, normally this is configured through the\njava system property \"sdl.pluginClassName\".\n\nThe value can be provided in plaintext or with the id of a secret provider and the\nname of the secret in the format\n```\n###<PROVIDERID>#<SECRETNAME>###\n```\n ,\ne.g.  \n```\n###ENV#<ENV_VARIABLE_NAME>###\n```\n\n\nto get a secret from an environment variable."
        },
        "uiBackend" : {
          "type" : "object",
          "properties" : {
            "baseUrl" : {
              "type" : "string",
              "description" : "API base URL for upload. Add tenant, repo (required) and env as query parameters, e.g. https://<host>?tenant=<tenant>&repo=<repository>&env=<environment>\""
            },
            "tenant" : {
              "type" : "string",
              "description" : "Optional unique name for your organization within the SDLB UI. The userId set through authMode needs to have access to this tenant.  \nDefault for tenant is the special \"PrivateTenant\". In this case the API will use the userId of the authenticated user as tenant."
            },
            "repo" : {
              "type" : "string",
              "description" : "Repository under which this upload should be stored in the UI backend.  \nThe repository name normally has the meaning of the project or application."
            },
            "env" : {
              "type" : "string",
              "description" : "Environment under which this upload should be stored in the UI backend, e.g. dev or prd.  \nDefault is 'std'."
            },
            "authMode" : {
              "oneOf" : [ {
                "$ref" : "#/definitions/HttpAuthMode/AWSUserPwdAuthMode"
              }, {
                "$ref" : "#/definitions/HttpAuthMode/AuthHeaderMode"
              }, {
                "$ref" : "#/definitions/HttpAuthMode/AzureADClientGrantAuthMode"
              }, {
                "$ref" : "#/definitions/HttpAuthMode/BasicAuthMode"
              }, {
                "$ref" : "#/definitions/HttpAuthMode/CustomHttpAuthMode"
              }, {
                "$ref" : "#/definitions/HttpAuthMode/OAuthMode"
              }, {
                "$ref" : "#/definitions/HttpAuthMode/TokenAuthMode"
              } ],
              "description" : "optional configuration of authentication to access UI backend API through baseUrl."
            },
            "stagePath" : {
              "type" : "string",
              "description" : "optional (but recommended) hadoop directory to save state if upload fails temporarily.  \nUpload of these files is retried on initialization of next SDLB run."
            },
            "processUpdates" : {
              "type" : "boolean",
              "description" : "optional; if false, only initial & final state is uploaded, otherwise all state updates are uploaded as partial updates.  \nDefault is processUpdates=true."
            },
            "timeouts" : {
              "type" : "object",
              "properties" : {
                "connectionTimeoutMs" : {
                  "type" : "integer"
                },
                "readTimeoutMs" : {
                  "type" : "integer"
                }
              },
              "title" : "HttpTimeoutConfig",
              "required" : [ "connectionTimeoutMs", "readTimeoutMs" ],
              "additionalProperties" : false,
              "description" : "configuration of HTTP timeouts. Default is connectionTimeout=500ms, readTimeout=5s."
            },
            "proxy" : {
              "type" : "object",
              "properties" : {
                "host" : {
                  "type" : "string",
                  "description" : "proxy host"
                },
                "port" : {
                  "type" : "integer",
                  "description" : "proxy port"
                },
                "user" : {
                  "type" : "string",
                  "description" : "The value can be provided in plaintext or with the id of a secret provider and the\nname of the secret in the format\n```\n###<PROVIDERID>#<SECRETNAME>###\n```\n ,\ne.g.  \n```\n###ENV#<ENV_VARIABLE_NAME>###\n```\n\n\nto get a secret from an environment variable."
                },
                "password" : {
                  "type" : "string",
                  "description" : "The value can be provided in plaintext or with the id of a secret provider and the\nname of the secret in the format\n```\n###<PROVIDERID>#<SECRETNAME>###\n```\n ,\ne.g.  \n```\n###ENV#<ENV_VARIABLE_NAME>###\n```\n\n\nto get a secret from an environment variable."
                }
              },
              "title" : "HttpProxyConfig",
              "required" : [ "host", "port" ],
              "additionalProperties" : false,
              "description" : "optional configuration of HTTP proxy to access UI backend."
            }
          },
          "title" : "UIBackendConfig",
          "required" : [ "baseUrl", "repo" ],
          "additionalProperties" : false,
          "description" : "Configuration of the UI backend to upload state updates of the Job runs."
        },
        "dataObjectsSchemaSource" : {
          "type" : "string",
          "description" : "Optional source URI for DataObjects schemas for development.* This is used for development on\nlocal environment without access to data. Schemas can be exported on dev/prod environment using\nthe DataObjectSchemaExporter application, and used on local environment to define DataObject\nschemas for executing dry-run's. Source must be a path like`file:./schema`or`uiBackend`.  \nuiBackend will use global.uiBackend configuration to query UI backend for schemas. Default:`file:./schema` ."
        },
        "defaultSparkConnectionId" : {
          "type" : "string",
          "description" : "Optional default ConnectionId for Spark connections. This is used to avoid having to specify\nthe Spark connection for every Spark Action. Note that the default connection can still be\noverwritten for each Spark Action by specifying the sparkConnectionId in the action's\nconfiguration."
        }
      },
      "title" : "GlobalConfig",
      "additionalProperties" : false,
      "description" : "Global configuration options\n\nNote that global configuration is responsible to hold SparkSession, so that its created once and\nonly once per SDLB job. This is especially important if JVM is shared between different SDL jobs\n(e.g. Databricks cluster), because sharing SparkSession in object Environment survives the\ncurrent SDLB job."
    },
    "connections" : {
      "type" : "object",
      "additionalProperties" : {
        "oneOf" : [ {
          "$ref" : "#/definitions/Connection/BigQueryTableConnection"
        }, {
          "$ref" : "#/definitions/Connection/DebeziumConnection"
        }, {
          "$ref" : "#/definitions/Connection/DeltaLakeTableConnection"
        }, {
          "$ref" : "#/definitions/Connection/HadoopFileConnection"
        }, {
          "$ref" : "#/definitions/Connection/IcebergTableConnection"
        }, {
          "$ref" : "#/definitions/Connection/JdbcTableConnection"
        }, {
          "$ref" : "#/definitions/Connection/KafkaConnection"
        }, {
          "$ref" : "#/definitions/Connection/SFtpFileRefConnection"
        }, {
          "$ref" : "#/definitions/Connection/ScalaConnection"
        }, {
          "$ref" : "#/definitions/Connection/SnowflakeConnection"
        }, {
          "$ref" : "#/definitions/Connection/SparkClassicConnection"
        }, {
          "$ref" : "#/definitions/Connection/SparkConnectConnection"
        } ],
        "description" : "Map of Connection name and definition"
      }
    },
    "dataObjects" : {
      "type" : "object",
      "additionalProperties" : {
        "oneOf" : [ {
          "$ref" : "#/definitions/DataObject/AccessTableDataObject"
        }, {
          "$ref" : "#/definitions/DataObject/ActionsExporterDataObject"
        }, {
          "$ref" : "#/definitions/DataObject/AirbyteDataObject"
        }, {
          "$ref" : "#/definitions/DataObject/AvroFileDataObject"
        }, {
          "$ref" : "#/definitions/DataObject/BigQueryTableDataObject"
        }, {
          "$ref" : "#/definitions/DataObject/CsvFileDataObject"
        }, {
          "$ref" : "#/definitions/DataObject/DataObjectsExporterDataObject"
        }, {
          "$ref" : "#/definitions/DataObject/DebeziumCdcDataObject"
        }, {
          "$ref" : "#/definitions/DataObject/DeltaLakeTableDataObject"
        }, {
          "$ref" : "#/definitions/DataObject/ExcelFileDataObject"
        }, {
          "$ref" : "#/definitions/DataObject/IcebergTableDataObject"
        }, {
          "$ref" : "#/definitions/DataObject/JdbcTableDataObject"
        }, {
          "$ref" : "#/definitions/DataObject/JsonFileDataObject"
        }, {
          "$ref" : "#/definitions/DataObject/KafkaTopicDataObject"
        }, {
          "$ref" : "#/definitions/DataObject/ODataDataObject"
        }, {
          "$ref" : "#/definitions/DataObject/OpenApiDataObject"
        }, {
          "$ref" : "#/definitions/DataObject/PKViolatorsDataObject"
        }, {
          "$ref" : "#/definitions/DataObject/ParquetFileDataObject"
        }, {
          "$ref" : "#/definitions/DataObject/RawFileDataObject"
        }, {
          "$ref" : "#/definitions/DataObject/RelaxedCsvFileDataObject"
        }, {
          "$ref" : "#/definitions/DataObject/SFtpFileRefDataObject"
        }, {
          "$ref" : "#/definitions/DataObject/SnowflakeTableDataObject"
        }, {
          "$ref" : "#/definitions/DataObject/SparkConnectTableDataObject"
        }, {
          "$ref" : "#/definitions/DataObject/WebserviceFileDataObject"
        }, {
          "$ref" : "#/definitions/DataObject/XmlFileDataObject"
        } ],
        "description" : "Map of DataObject name and definition"
      }
    },
    "actions" : {
      "type" : "object",
      "additionalProperties" : {
        "oneOf" : [ {
          "$ref" : "#/definitions/Action/CopyAction"
        }, {
          "$ref" : "#/definitions/Action/CustomDataFrameAction"
        }, {
          "$ref" : "#/definitions/Action/CustomFileAction"
        }, {
          "$ref" : "#/definitions/Action/CustomScriptAction"
        }, {
          "$ref" : "#/definitions/Action/DeduplicateAction"
        }, {
          "$ref" : "#/definitions/Action/FileTransferAction"
        }, {
          "$ref" : "#/definitions/Action/HistorizeAction"
        } ],
        "description" : "Map of Action name and definition"
      }
    },
    "agents" : {
      "type" : "object",
      "additionalProperties" : {
        "oneOf" : [ {
          "$ref" : "#/definitions/Agent/AzureRelayAgent"
        }, {
          "$ref" : "#/definitions/Agent/JettyAgent"
        }, {
          "$ref" : "#/definitions/Agent/StorageAgent"
        } ],
        "description" : "Map of Agent name and definition"
      }
    }
  },
  "required" : [ "dataObjects", "actions" ],
  "additionalProperties" : true
}